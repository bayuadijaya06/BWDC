# 51-UX — UX/UI Specification

**Proyek:** BWDCS  
**Versi:** 1.0.0  
**Tanggal:** 2026-09-17  
**Status:** Draf Desain

---

## 1. Design Principles

1. **Purpose-Driven** — Setiap elemen UI melayani fungsi yang jelas
2. **Progressive Disclosure** — Tampilkan informasi secukupnya, detail saat dibutuhkan
3. **Consistency** — Pola yang sama untuk action yang sama
4. **Feedback** — User selalu tahu status sistem
5. **Accessibility** — WCAG 2.1 AA compliance

---

## 2. Layout Structure

```
┌─────────────────────────────────────────────────────────────┐
│  HEADER                                                      │
│  [Logo] [Search]                              [Notif] [User] │
├────────┬────────────────────────────────────────────────────┤
│        │                                                    │
│ SIDEBAR│  MAIN CONTENT AREA                               │
│        │                                                    │
│ • Dash │  ┌──────────────────────────────────────────┐     │
│ • Proj │  │ Page Header                             │     │
│ • Docs │  │ Breadcrumb | Title | Actions            │     │
│ • Task │  ├──────────────────────────────────────────┤     │
│ • Appr │  │                                          │     │
│ • Repo │  │  Content (Table/Form/Card/etc)          │     │
│ • Admin│  │                                          │     │
│        │  └──────────────────────────────────────────┘     │
│        │                                                    │
├────────┴────────────────────────────────────────────────────┤
│  FOOTER (optional: version, copyright)                     │
└─────────────────────────────────────────────────────────────┘
```

### 2.1 Sidebar Navigation

| Menu | Sub-items | Access |
|---|---|---|
| Dashboard | — | All |
| Projects | List, Create | Manager+ |
| Documents | All, My, Pending Review, Revision Required, Approved | All (filtered) |
| Tasks | My, Team, Overdue, Completed | All |
| Approvals | Pending, Approved, Rejected | Reviewer+ |
| Reports | Projects, Documents, Tasks | Admin/Manager |
| Reports > Audit | Audit log | Admin |
| Administration | Users, Roles, Org, Categories, Workflows, Settings | Admin |

Label sub-menu mengikuti tabel pemetaan status kanonik vs label di `50-FSD.md` §11 (ADR-0012). "Overdue" pada menu Tasks adalah **filter turunan**, bukan nilai status.

Kolom Access mengikuti matriks permission `44-SECURITY.md` §3.1 (ADR-0014): "Admin/Manager" berarti `report:read`, sedangkan Audit hanya `audit:read` (Administrator saja).

**"Reviewer+" pada menu Approvals bukan role kelima** (menutup temuan **C-006**). Label itu memisahkan dua hal yang berbeda:

- **Melihat** antrean/halaman Approvals memakai `workflow_instance:read` — semua role memilikinya (`44-SECURITY.md` §3.1.2), jadi menunya boleh tampil untuk semua.
- **Bertindak** (Approve/Reject/Request Revision) memakai `workflow_instance:approve`/`reject`/`request_revision` — hanya Administrator dan Manager, dan hanya bila user itu benar-benar penanggung jawab step berjalan (`44-SECURITY.md` §3.3).

Jadi "Reviewer+" = **role sistem yang berwenang bertindak pada step itu**, bukan himpunan role baru; tombol aksinya disembunyikan bila izinnya tidak ada. Kolom Access di tabel nav sebaiknya dibaca sebagai "role yang melihat aksi di halaman itu", bukan "role yang boleh membuka halaman".

> Spesifikasi halaman yang sebelumnya tanpa FSD sudah ada: Approvals → `50-FSD.md` §5.4, Reports → `50-FSD.md` §10.6, Administration > Workflows → `50-FSD.md` §10.7 (temuan C-018 ditutup pada P-015).

### 2.2 Header

- Logo + App name (left)
- Global search (center, optional)
- Notification bell dengan badge (right)
- User menu: Profile, Settings, Logout (right)

---

## 3. Color System

**Primary:** `#1D4ED8` (Blue 700) — Action buttons, links, active states  
**Secondary:** `#475569` (Slate 600) — Secondary actions, text  
**Success:** `#16A34A` (Green 600) — Approved, completed  
**Warning:** `#D97706` (Amber 600) — Revision required, pending  
**Danger:** `#DC2626` (Red 600) — Rejected, errors, overdue  
**Neutral:** `#F8FAFC` (Slate 50) background, `#E2E8F0` (Slate 200) borders

---

## 4. Typography

| Level | Size | Weight | Use |
|---|---|---|---|
| H1 | 28px | 600 | Page titles |
| H2 | 22px | 600 | Section titles |
| H3 | 18px | 600 | Card titles |
| Body | 14px | 400 | Default text |
| Small | 12px | 400 | Captions, metadata |
| Mono | 13px | 400 | Code, IDs, checksums |

Font: System font stack (`Inter, -apple-system, BlinkMacSystemFont, sans-serif`)

---

## 5. Component Library

### 5.1 Data Table

```tsx
interface DataTableProps {
  columns: Column[];
  data: RowData[];
  pagination?: Pagination;
  loading?: boolean;
  emptyState?: React.ReactNode;
  onRowClick?: (row: RowData) => void;
  actions?: Action[];
}
```

Features:
- Sortable columns
- Filterable (column-level)
- Pagination (server-side)
- Row actions (dropdown)
- Checkbox selection (bulk actions)

### 5.2 Status Badge

```tsx
<Badge status="approved">Approved</Badge>
<Badge status="in_review">In Review</Badge>
<Badge status="revision_required">Revision Required</Badge>
<Badge status="rejected">Rejected</Badge>
<Badge status="draft">Draft</Badge>
```

### 5.3 File Upload

```tsx
<FileUpload
  maxFileSize={100 * 1024 * 1024}
  accept=".pdf,.txt,.csv,.xlsx,.jpg,.png"
  onUpload={(file) => {...}}
/>
```

Features:
- Drag & drop zone
- Progress indicator
- File validation feedback
- Multiple file support (future)

### 5.4 Modal

```tsx
<Modal
  title="Submit for Review"
  isOpen={isOpen}
  onClose={() => setIsOpen(false)}
  onConfirm={handleSubmit}
  confirmText="Submit"
  cancelText="Cancel"
>
  {/* Form content */}
</Modal>
```

### 5.5 Timeline

```tsx
<Timeline items={[
  { date: '2026-09-17', title: 'Document Created', actor: 'Bayu' },
  { date: '2026-09-17', title: 'Submitted for Review', actor: 'Bayu' },
  { date: '2026-09-18', title: 'Technical Review Approved', actor: 'Siti' },
]} />
```

### 5.6 Approval Panel

```tsx
<ApprovalPanel
  workflow={workflowInstance}
  canAct={true}
  onApprove={(comment) => {...}}
  onReject={(comment) => {...}}
  onRequestRevision={(comment) => {...}}
/>
```

---

## 6. Page Specifications

### 6.1 Dashboard

**Layout:** Grid of stat cards + recent activity

```
┌─────────────┬─────────────┬─────────────┬─────────────┐
│ Active Proj │ Pending     │ Docs Under  │ Open Tasks  │
│     12      │   Reviews   │  Review     │     8       │
├─────────────┼─────────────┼─────────────┼─────────────┤
│ Revision    │ Overdue     │ Recent      │ Quick       │
│  Required   │  Tasks      │ Activity    │ Actions     │
│     3       │     2       │ (list)      │ (buttons)   │
└─────────────┴─────────────┴─────────────┴─────────────┘
```

### 6.2 Project List

**Layout:** Data table dengan filter bar

```
┌─────────────────────────────────────────────────────────────┐
│ Filters: [Status ▼] [Search...]          [+ Create Project] │
├─────────────────────────────────────────────────────────────┤
│ Code     │ Name          │ Status  │ Owner │ Members │ ...  │
│ WEB      │ Website Redesign│ Active │ Bayu  │    5    │ ...  │
│ API      │ API Migration  │ Active │ Siti  │    3    │ ...  │
└─────────────────────────────────────────────────────────────┘
 Pagination: 1 2 3 ... 10 →
```

### 6.3 Document Detail

**Layout:** Two-column (main content + sidebar)

```
┌─────────────────────────┬─────────────────────────────┐
│ Title: BRD v1.2         │ Status: In Review           │
│ WEB-001                 │ Project: Website Redesign   │
│                         │ Category: Requirements      │
│ [Download] [Edit] [     │ Owner: Bayu                 │
│  Submit]                │                             │
├─────────────────────────┼─────────────────────────────┤
│ Versions Timeline       │ Workflow                    │
│ ○ v1.2 (current) ● Bayu │ Step 1: Technical Review ✓  │
│ ○ v1.1    ● Bayu        │ Step 2: Manager Review ●    │
│ ○ v1.0    ● Bayu        │ Step 3: Final Approval      │
│                         │                             │
│ Comments                │ Activity                    │
│ [Avatar] Bayu: Great    │ Sep 17 Bayu uploaded v1.2   │
│ [Input field     [Send]] │ Sep 17 Siti approved step 1 │
└─────────────────────────┴─────────────────────────────┘
```

### 6.4 Approval Page

**Layout:** Card-based queue

```
┌─────────────────────────────────────────────────────────────┐
│ My Approvals (3 pending)                                    │
├─────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────┐ ┌─────────────────────────┐    │
│ │ BRD for Website         │ │ API Specs v2.1          │    │
│ │ WEB-001                 │ │ API-001                 │    │
│ │ Project: Website        │ │ Project: API Migration  │    │
│ │ Step: Manager Review    │ │ Step: Technical Review  │    │
│ │ Due: Sep 20             │ │ Due: Sep 18             │    │
│ │ [Preview] [Approve]     │ │ [Preview] [Approve]     │    │
│ │         [Revise] [Reject]│ │         [Revise] [Reject]│   │
│ └─────────────────────────┘ └─────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

---

## 7. Interaction Patterns

### 7.1 Loading States
- Skeleton screen untuk list pages
- Spinner untuk single entity load
- Optimistic update untuk mutations

### 7.2 Error States
- Inline validation errors (red border + message)
- Toast notifications untuk global errors
- Error boundary untuk component crashes

### 7.3 Empty States
- Illustration + message + CTA
- Contoh: "No documents yet. Upload your first document."

### 7.4 Confirmation Patterns
- Modal untuk destructive actions (delete, reject)
- Toast untuk informational actions (save, create)

---

## 8. Responsive Behavior

| Breakpoint | Behavior |
|---|---|
| ≥ 1024px | Full sidebar + two-column layouts |
| 768-1023px | Collapsed sidebar (icon-only) |
| < 768px | Hamburger menu, single column, stacked cards |

---

## 9. Accessibility Checklist

- [ ] All interactive elements have focus states
- [ ] Color is not the only means of conveying information
- [ ] Form inputs have associated labels
- [ ] ARIA landmarks for main regions
- [ ] Keyboard navigation supported
- [ ] Screen reader test for key flows
- [ ] Minimum touch target: 44x44px
