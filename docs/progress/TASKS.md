# TASKS — Backlog & Papan Kerja

**Protokol:** `docs/design/02-AGENT-PROGRESS-PROTOCOL.md`
**Aturan:** task diberi ID `T-###` dan tidak pernah dihapus. Jika batal, ubah status menjadi `CANCELLED` beserta alasannya.
**Status yang dipakai:** `TODO`, `IN PROGRESS`, `BLOCKED`, `REVIEW`, `DONE`, `CANCELLED`.

Setiap task minimal mencantumkan: fase roadmap, dokumen referensi, requirement terkait, bukti selesai.

---

## 1. Papan Status

### TODO

| ID | Task | Fase | Dokumen | Requirement | Bukti selesai |
|---|---|---|---|---|---|
| T-014 | Nyalakan Docker daemon — **opsional**, hanya bila memilih jalur kontainer. Bukan blocker karena PostgreSQL 16 sudah tersedia | 0 | `60-DEPLOYMENT.md` §2 | NFR-PORT-01 | `docker info` menampilkan ServerVersion |
| T-017 | Perbaiki **sisa temuan** `AUDIT-001`. **Per P-030 kesembilan temuan lama sudah `FIXED`**: C-006/C-007/C-010/C-028 lewat penyelarasan dokumen + ADR-0020 (P-026), dan C-004/C-009/C-033/C-035 lewat implementasi ADR-0019/0021/0022 (`T-039`/`T-040`/`T-041`, P-029/P-030) — **tidak ada lagi temuan berstatus `APPROVED`**. Yang masih `OPEN` per **P-037** tinggal **C-050** (Q-019, keputusan produk); **C-048** ditutup `T-043` pada P-032, dan **C-015** ditutup pada **P-037** sesudah user menjawab Q-001/Q-002 — `DESIGN.md` terisi (ADR-0006/0007 `ACCEPTED`) dan temuan **C-060** yang ikut ketahuan (`51-UX.md` §3/§4 menyimpang dari arah desain) ditutup di sesi yang sama. Sejak P-037 **tidak ada lagi temuan teknis yang terbuka** pada modul backend; **P-041** menambahkan satu temuan yang sifatnya berbeda — **C-063** (FSD §3.2 menuntut dropdown `Owner` yang wajib padahal tidak ada endpoint daftar pengguna), ditemukan saat **membangun halaman bisnis pertama**, dan pilihannya (endpoint baru dengan izin baru, atau menurunkan FSD) butuh ADR sehingga statusnya `OPEN` (Q-024). Sudah selesai lewat `T-018`-`T-032`: C-001/C-002/C-003 (T-018..T-020), C-017 (T-021), C-014 (T-022), C-011/C-013 (T-023), C-012 (T-025), C-005 (T-026), C-022 (T-027), C-018 (T-029), C-016 + C-024 (T-030), C-025 (T-028), C-026 + C-027 (T-031), C-020 (T-032). Empat temuan baru yang muncul saat mengerjakan `T-004` (C-029, C-030, C-031 SQL/migrasi; C-032 struktur package) ditutup di sesi yang sama — C-032 lewat **ADR-0018**. **P-027** menemukan tiga lagi saat mengerjakan modul Comment: **C-048** (`meta.total` pada halaman di luar rentang, ditambal di modul komentar — sisanya `T-043`), **C-049** (bentuk rute daftar komentar yang tidak dapat dipasang — `FIXED` di sesi itu), dan **C-050** (threading dijanjikan `50-FSD.md` §7 tanpa kolomnya — `OPEN`, Q-019). Sejak P-024 **tidak ada lagi temuan yang bisa dikerjakan tanpa keputusan Anda**: sisanya butuh keputusan (C-004/C-007/C-009/C-010 butuh ADR; C-006 cukup diselaraskan; C-015 dials desain; C-028 retensi audit; C-033 dan C-035 menunggu Q-013/Q-014; C-045 dan C-046 yang ditemukan P-025 menunggu **Q-017**). C-038 ditutup P-024 lewat `T-036`, dan dua temuan baru yang muncul di sesi itu (C-043 salinan Makefile di `60-DEPLOYMENT.md` §3.1; C-044 hitungan audit 31 vs 32) ditutup di sesi yang sama. **P-025** menemukan C-045 (pesan `422` yang menyalahkan JSON untuk UUID tidak sah di body) dan C-046 (penyaring rentang tanggal di FSD tanpa kontrak); keduanya **ditutup P-026** bersama C-047 (tabel status `80-ROADMAP.md` tertinggal dan label fase Task tidak sesuai). T-017 sendiri tidak ditutup sampai kesembilannya berstatus `FIXED`/`REJECTED` | 0 | `docs/progress/audits/AUDIT-001-2026-09-17-kontradiksi-dokumen.md` | — | Temuan yang dipilih berstatus FIXED di tabel tindak lanjut audit, dengan bukti perbaikan |

| T-046 | **Kewajiban berulang** (kelanjutan `T-008`, yang menyerahkan berkasnya pada 2026-09-17): jaga `CONTINUE.md` tetap selaras dengan `STATE.md` setiap akhir sesi | semua | `CONTINUE.md` §0, `02-AGENT-PROGRESS-PROTOCOL.md` | — | Blok §0 cocok dengan isi `STATE.md` |
| T-084 | **Project Members CRUD** — tambah/hapus anggota + pindah owner di `ProjectDetail` (butuh `GET /users`/`GET /admin/users` untuk memilih user — menunggu Q-024) | 4 | `50-FSD.md` §3.3, `42-API.md` §3 | FR-PROJ-04/05 | `POST /projects/:id/members` + `DELETE /:id/members/:userId` sudah hidup, UI `C-063` | **DONE** |
| T-085 | **Documents — Category filter frontend** (dropdown `?category_id=` di `Documents` page, `51-UX.md` §2.1) | 4 | `50-FSD.md` §4.1, `42-API.md` §4 | Q-016 | `DocumentListQuery.category_id` sudah hidup di backend/frontend service | **DONE** |
| T-086 | **Approvals — Resubmit** (`POST /workflows/instances/:id/resubmit` di `ApprovalDetail` saat `revision_required`) | 4 | `42-API.md` §5, `43-WORKFLOW.md` §4.6, `50-FSD.md` §4.3 | FR-WF-09 | Tombol Resubmit + validasi versi | **DONE** |
| T-091 | **Document detail completion** (permintaan user P-073): kategori di dialog buat + tautan kategori di detail, panel Workflow hidup (submit/resubmit), perbaiki alasan basi Workflow/Activity | 4 | `50-FSD.md` §4.3, `42-API.md` §4/§5 | FR-DOC-07, FR-WF-09 | `CreateDocumentInput.category_id`, panel Workflow di `DocumentDetail` | **DONE** |
| T-087 | **Dashboard finalization** — saat `T-074`/`T-082`/`T-083` selesai, perbarui KPI/chart (`Department`/`SLA`/`Published`) dan filter global `Department`/`SLA Status` | 5 | `52-DASHBOARD-ANALYTICS.md` §4, `50-FSD.md` §9 | FR-DASH-03/04 | Q-DASH-01..04 terjawab, migrasi `012` | **IN PROGRESS** |
| T-088 | **Notification Center frontend** — bell badge + dropdown `is_read` + `read-all` (`50-FSD.md` §8.2, `51-UX.md` §2.2) | 4 | `42-API.md` §8, `44-SECURITY.md` §3.1.3 | FR-NOTIF-* | `GET /notifications` + `PATCH /:id/read` sudah hidup | **DONE** |
| T-074 | **Backlog analitik penuh** — department (`projects.department_id`), SLA (`deadline`→ definisi), `review_due_at`/`expiry_at`/`published_at`, `workflow_stage_history` presisi, kosakata `published`/`obsolete` | 5 | `52-DASHBOARD-ANALYTICS.md` §4, `41-DATABASE.md` §2.7, `80-ROADMAP.md` Phase 5 | — | Migrasi `012` + ADR, `Dashboard.md` §1.6/§3 dapat dihidupkan |

### IN PROGRESS

| ID | Task | Mulai | Agen | Catatan |
|---|---|---|---|---|
| — | — | — | — | — |

### BLOCKED

| ID | Task | Blocker | Aksi yang dibutuhkan |
|---|---|---|---|
| **T-050** | Tetapkan **lisensi proyek** dan buat berkas `LICENSE` (jenisnya belum diputuskan; pemegang hak cipta sudah dijawab pemilik: **BSA**) | Menunggu keputusan pemilik proyek — **Q-023** butir (a) dan (c) | Pilih jenis lisensi (proprietary *all rights reserved*, MIT, atau Apache-2.0) dan tentukan apakah kontribusi pihak ketiga diterima. Sesudahnya: buat `LICENSE`, isi field `license` di `frontend/package.json`, perbarui `README.md` §13 + Q-023 + ledger |

Catatan: papan ini terisi **satu** task pada P-038 — `T-050` — dan itu **bukan** blocker teknis melainkan keputusan pemilik proyek (lisensi, Q-023). Dua task yang berakhir di P-037 (`T-006` isi `DESIGN.md`, `T-007` gate UI) tetap selesai di sana sesudah **Q-001** (mode antislop `during`) dan **Q-002** (jalur 2 ADR-0007) dijawab, sehingga pekerjaan UI tidak lagi terhalang; `T-045` selesai pada sesi yang sama sesudah **Q-020** (ADR-0023) dijawab. Blocker tersisa yang lain juga keputusan produk, bukan teknis: Q-019/C-050 (threading komentar) — dan keduanya tidak menghalangi modul mana pun. `T-016` (Docker) tetap opsional.

### DONE

| ID | Task | Selesai | Bukti |
|---|---|---|---|
| **T-059** | Bangun **halaman bisnis kedua** di frontend: **Documents** (`50-FSD.md` §4, `42-API.md` §4) — daftar ber-penyaring dan ber-paginasi, dialog unggah **dua langkah**, halaman detail dengan metadata + versi + unduh + unggah versi baru + arsip, memakai lapisan data yang sama polanya dengan Projects. Bagian dari **Phase 4** | 2026-09-22 | **DONE (P-044).** Lapisan data: `services/documents.ts` (satu tempat yang tahu bentuk §4 — `listDocuments`, `createDocument`, `uploadDocumentVersion` multipart **tanpa** menyetel `Content-Type` agar boundary tetap ditulis peramban, `downloadDocumentVersion` sebagai blob ber-`Authorization`, `archiveDocument` membuka `data.document`, `listDocumentVersions`), `queries/documents.ts` (kunci kueri terpusat + invalidasi), `utils/download.ts` (penyimpan blob + nama berkas dari `Content-Disposition`), util format ukuran berkas dan nilai kosong yang **menerangkan keadaan** (`EMPTY_VALUE`, `EMPTY_DATE`) alih-alih tanda pisah. Halaman: `pages/Documents/index.tsx` (penyaring `?status=`/`?project_id=`/`?search=` hidup di URL sehingga dapat dibagikan, keadaan memuat/kosong/gagal dibedakan, kolom mengikuti tabel `50-FSD.md` §4.1), `CreateDocumentDialog.tsx` + `FilePicker.tsx` + `UploadVersionDialog.tsx` (**dua langkah**: metadata lebih dulu — nomor dokumen ditampilkan `read-only` **setelah** server membangkitkannya — lalu berkas), `DocumentDetail.tsx` (metadata, daftar versi terbaru lebih dulu + unduh per versi, unggah versi baru, arsip, dan bagian Workflow/Comments/Activity/Related Tasks yang menyebut **alasan** masing-masing belum ada). Navigasi `Documents` menjadi `ready` (`navigation.ts`, task `T-059`) dan rute `/documents` + `/documents/:id` terpasang. **Bukti test:** `services/documents.test.ts`, `utils/download.test.ts`, `pages/Documents/Documents.test.tsx`, `pages/Documents/DocumentDetail.test.tsx` — total frontend **188 test / 21 berkas** hijau, `tsc --noEmit` + `eslint .` bersih, `vite build` 426,03 kB js / 22,79 kB css. **Bukti server nyata:** halaman dibuka di dev server pada sesi itu (daftar, penyaring `?status=archived`, detail, dan bagian yang menyebut dirinya belum dibangun), dan bukti HTTP-nya diperbarui serta dijalankan ulang pada P-045 (unggah → unduh byte-identik → arsip → `409` → cakupan `404`/`200`). Menemukan **C-070** (kontrak arsip tanpa bentuk amplop) dan **C-071** (pesan penjaga append-only menyebut tabel yang salah) — keduanya ditutup `T-060` di sesi yang sama. Log `prompts/P-044-2026-09-22-halaman-documents-dan-lapisan-data.md` |
| **T-060** | Perbaiki dua temuan yang ketahuan saat halaman Documents dibangun: **C-070** (kontrak arsip dokumen tidak menyebut bentuk amplopnya) dan **C-071** (satu fungsi penjaga append-only menyebut `audit_logs` secara tetap untuk tiga tabel yang berbeda) | 2026-09-22 | **DONE (P-044).** **C-070:** `42-API.md` §4 menulis `POST /documents/:id/archive` sebagai "Response 200: dokumen terarsip" **tanpa bentuk amplop**, sehingga klien yang dibangun sesi itu mengetik `ApiSuccess<DocumentRecord>` dan `archiveDocument()` mengembalikan `undefined` pada panggilan nyata (halaman detail akan meledak pada render berikutnya) — dan seluruh test tetap hijau karena mock-nya menebak bentuk yang sama. Kontrak kini menyebut `data.document` (+ `data.current_version`) beserta contoh JSON, membandingkannya dengan `POST /documents` (amplop sama) dan `POST /documents/:id/upload` (objek versi **telanjang** di `data`), dan `services/documents.ts` membuka `data.document` dengan komentar yang menunjuk C-070. **C-071:** `prevent_audit_modification()` dipasang pada `audit_logs` (migrasi `007`) **dan** `document_versions` serta `documents` (migrasi `010`), tetapi pesannya tetap `'audit_logs bersifat append-only'` — `DELETE FROM document_versions` menjawab tentang tabel yang tidak tersentuh. Migrasi **`011`** menulis ulang fungsi sebagai `CREATE OR REPLACE` memakai `TG_TABLE_NAME` (nama tabel sasaran sudah ada di trigger, tanpa argumen tambahan, jadi tidak ada jendela waktu tanpa penjaga), dan `TestAppendOnlyMessageNamesTheOffendingTable` mengunci pesannya. **Gigi dibuktikan:** versi lama dipasang langsung di `bwdcs_test` → test **gagal** dengan pesan yang salah; dipulihkan lalu hijau. **Bukti server nyata:** arsip `200` dengan `keys(data)=['current_version','document']`, `status=archived`, `archived_at` terisi, versi tetap ada dan tetap dapat diunduh, `409` untuk unggahan sesudah arsip; `versi_skema 11` di `bwdcs` **dan** `bwdcs_test`. Log `prompts/P-044-2026-09-22-halaman-documents-dan-lapisan-data.md` |
| **T-061** | Perbaiki temuan **C-072**: unggahan `.txt` dan `.csv` — dua dari delapan jenis berkas yang dijanjikan `50-FSD.md` §4.2 — **selalu** dibalas `422`, karena MIME hasil `http.DetectContentType` (`text/plain; charset=utf-8`) dibandingkan utuh terhadap daftar tertutup `44-SECURITY.md` §4.2 yang menulis `text/plain` | 2026-09-22 | **DONE (P-045).** Ditemukan dari **satu unggahan berkas sungguhan** ke server yang sedang berjalan, bukan dari `make test`: `.txt` → `422 VALIDATION_ERROR file`, `.csv` → `422`, sementara `.pdf` → `201`. Perbaikan: `normalizeMimeType` (`internal/service/document_service_upload.go`) membuang parameter header sebelum pencocokan — hanya tipe media yang dibandingkan, sesuai RFC 7231 — dan daftar ekstensi tetap tertutup. **Test baru:** `TestDocumentUploadAcceptsDetectedMimeWithParameters` (lima golongan berkas, MIME **dihitung dari byte** lewat `http.DetectContentType`) dan `TestUploadAcceptsDocumentedTextTypesHTTP` (`.txt` berbaris, `.txt` satu baris, `.csv` sebagai multipart sungguhan → `201`, `mime_type` tersimpan sama dengan yang dideteksi, `Content-Type` unduhan `text/plain`, isi utuh). **Kasus lama diganti, bukan dilonggarkan:** `palsu.pdf` berisi teks yang dahulu dituntut `422` lulus **hanya** karena cacat ini; desain memakai dua penjaga yang berdiri sendiri tanpa aturan pasangan, jadi kasusnya kini `.pdf` berisi arsip ZIP plus `.sh`. **Gigi dibuktikan:** dengan `normalizeMimeType` dikembalikan ke bentuk lama, **enam subtest gagal** tepat pada `text/plain; charset=utf-8`; dipulihkan lalu hijau. **Bukti server nyata:** unggah `.txt` 39 byte → `201` dengan `mime_type` `text/plain; charset=utf-8`, unduh **byte-identik** (`sha256 c25e7b6e…`, `cmp IDENTIK`, `Content-Length: 39`). Dokumen desain diselaraskan: `44-SECURITY.md` §4.2 (menyatakan parameter dibuang dan kedua penjaga tidak berpasangan), `42-API.md` §4 tabel validasi, `40-TSD.md` §2.x. Log `prompts/P-045-2026-09-22-mime-berkas-dan-ledger-p044.md` |
| **T-062** | Bangun **halaman bisnis ketiga** di frontend: **Tasks** (`50-FSD.md` §6, `42-API.md` §6) — daftar ber-penyaring dengan penyaring **tri-state overdue**, transisi status Start/Complete/Reopen sesuai tabel transisi, memakai pola lapisan data yang sudah terbukti di Projects dan Documents. Bagian dari **Phase 4** | 2026-09-22 | **DONE (P-046).** Lapisan data: `services/tasks.ts` (satu tempat yang tahu bentuk §6: `listTasks`, `fetchTask`, `createTask`, `updateTask` (PATCH), `completeTask` (endpoint sendiri), kosakata prioritas + label, `validateDueRange` dengan batas **inklusif** dan penolakan rentang terbalik pada klien), `queries/tasks.ts` (kunci kueri terpusat; mutasi detail **diambil ulang**, bukan ditambal, karena `is_overdue` dan `updated_at` dihitung server). Halaman: `pages/Tasks/index.tsx` (sub-halaman §6.1 sebagai penyaring yang dapat dibagikan — `?view=mine` → `?assignee_id=<diri>`, `?view=team`, `?overdue=true`, `?status=completed`, dengan bentuk lama `?view=overdue`/`?view=completed` tetap diterima; penyaring **tri-state overdue** `""|true|false` dengan tiga pilihan yang menerangkan ketiga keadaannya; rentang tenggat RFC 3339 ber-offset eksplisit; penanggung jawab dipilih dari **anggota project** karena kontrak belum punya endpoint daftar pengguna (C-063) dan batas itu dinyatakan di layar), `TaskDetail.tsx` (aksi dipilih menurut status berjalan: Start `open→in_progress` via PATCH, Complete `in_progress→completed` via `POST /tasks/:id/complete`, Reopen `completed→open` via PATCH — bukan tiga tombol yang dibiarkan gagal; penjelasan `409` di luar tabel transisi dan `404` cakupan tulis ditampilkan apa adanya; tabel transisi endpoint+izin dirender di halaman), `CreateTaskDialog.tsx` (project → anggota + dokumen project sebagai sumber pilihan). Navigasi `Tasks` menjadi `ready` dan rute `/tasks` + `/tasks/:id` terpasang. **Bukti test:** `services/tasks.test.ts`, `pages/Tasks/Tasks.test.tsx` (termasuk test eksplisit tiga keadaan penyaring overdue), `TaskDetail.test.tsx` (ketiga transisi + `409`), `AppShell.test.tsx` (laci menu) — total frontend **234 test / 24 berkas** hijau, `tsc --noEmit` + `eslint .` bersih, `vite build` 455,8 kB js / 23,5 kB css. **Bukti server nyata (probe 41/41 PASS, `scripts/probe-task-module.py`):** tri-state `?overdue=` terbukti di server (`false` memuat task tanpa tenggat, berbeda dari tidak dikirim; nilai lain `422`), rentang inklusif + `due_to==due_from` sah + terbalik `422` pada `due_to`, `meta.total` utuh di halaman luar rentang, ketiga transisi + `409` untuk Complete pada Open dan PATCH `status=completed`, Complete idempoten, penanda overdue hilang saat selesai, viewer non-anggota `404`/`total 0` dan `403` menulis, database dev kembali ke baseline (users 1, audit_logs 43, skema 11). Log `prompts/P-046-2026-09-22-halaman-tasks-dan-probe-41-pass.md` |
| **T-056** | Terapkan aturan dan skill antislop pada frontend yang sudah berdiri: tiga state lebar (`51-UX.md` §8) dengan **laci menu** yang sungguhan, target sentuh 44px di layar sentuh, keadaan memuat/kosong/gagal utuh, dan penyelarasan dokumen ke perilaku nyata (menutup **C-067** dan **C-069**) | 2026-09-22 | **DONE (P-043).** Yang berdiri: `hooks/useMediaQuery.ts` (tiga state lebar dibaca `useSyncExternalStore`, bukan state yang disalin di effect) + `hooks/useModalLayer.ts` (perilaku lapisan modal dipakai **bersama** `Dialog` dan laci: fokus awal `[data-autofocus]`, jebakan Tab, Escape, kunci gulir, fokus kembali), `AppShell.tsx` (laci < 768px dengan latar penutup ber-penanda `data-drawer-backdrop`, `inert` pada konten, kolom kompak 12rem 768-1023px, kolom penuh 15rem ≥ 1024px), `Sidebar.tsx`/`Header.tsx` (target sentuh, label tema pendek, nama akun dipotong), dan `tap-target` di `Button`/`Field`/`DataTable`/`States`. **C-067** ditutup: `51-UX.md` §8 menulis state tengah "icon-only" padahal implementasinya kompak **berlabel** — tabelnya diselaraskan beserta alasannya (ikon generik dilarang `DESIGN.md` §1/R-04) dan perilaku laci ditulis di sana karena kini diukur mesin. **C-069** ditutup: lima teks pengguna yang memuat em dash diganti kalimat yang menerangkan keadaan (`EMPTY_VALUE`/`EMPTY_DATE`), dan keputusan R-02 di `01-AGENT-WORKFRAME.md` §3.2 dipersempit ke apa yang benar-benar diperiksa mesin (prosa/komentar lama → **Q-025**). Bukti: `tsc --noEmit` + `eslint .` bersih (**dua error lint dan satu error `tsc` dari pekerjaan yang belum selesai diperbaiki, bukan diabaikan**), **137 test / 16 berkas** hijau, `vite build` 397 kB js / 21,9 kB css, dan pengukuran peramban sungguhan (T-057) di 375/768/1024/1440px + kedua tema. Log `prompts/P-043-2026-09-22-antislop-frontend-dan-laci-menu.md` |
| **T-057** | Buat pengukur tata letak **di peramban sungguhan** (`scripts/responsive-evidence.mjs`) supaya klaim R-03/R-34/R-35 (tanpa gulir mendatar, target sentuh, laci menutupi konten, kedua tema) tidak lagi diklaim dari kelas CSS; gigi pemeriksanya wajib dibuktikan (menutup **C-068**) | 2026-09-22 | **DONE (P-043).** Skrip Node tanpa dependensi baru: menjalankan Chrome yang sudah terpasang lewat protokol DevTools (`WebSocket` bawaan Node), login lewat **form yang sama dengan pengguna**, lalu mengukur per lebar gulir mendatar halaman beserta elemen penyebabnya (elemen di dalam wadah bergulir tidak ikut dituduh), ukuran setiap kontrol terhadap ambang lebarnya sendiri (44px < 1024px, 36px ≥ 1024px), state sidebar, dan perilaku laci 375px (dibuka, lalu jendela dilebarkan **saat masih terbuka** dan dipersempit lagi: laci harus berhenti menutupi konten, permintaannya dilupakan, gulir bebas lagi) plus kedua tema. Hasil P-043 pada halaman Projects **dengan satu project nyata**: 0px gulir mendatar di keempat lebar, 0 kontrol di bawah ambang (10/38/38/38 diperiksa), sidebar laci/192px/240px/240px, dan 6 pengukuran tema semuanya tanpa gulir mendatar. **C-068** ditutup: dua test baru tetap hijau sesudah perilaku yang diklaimnya dihapus, dan dua pemeriksaan di skrip ini pun lulus hampa (mengukur tetangga panel sebagai "konten", menutup laci sebelum melebarkan jendela) — keduanya diperbaiki, lalu **enam cacat** disuntikkan ulang dan keenamnya tertangkap. Skrip **tidak** dijalankan di CI (butuh Chrome + server hidup) dan tercatat di `README.md` §4/§12.3, `AGENTS.md`, `51-UX.md` §8, `70-TESTING.md` §3.14b, `.freebuff/run.md` §4; `check-readme-facts.sh` diperluas ke **semua** berkas `scripts/` (bukan hanya `*.sh`) supaya kelas C-066 tidak terulang untuk pemeriksa berbasis Node |
| **T-058** | Tegakkan R-02 secara mesin pada teks yang dibaca pengguna: butir 8 `scripts/check-antislop-refs.sh` + pengganti nilai kosong yang menerangkan keadaan alih-alih tanda pisah | 2026-09-22 | **DONE (P-043).** `check-antislop-refs.sh` kini memindai `frontend/src` (komentar dibuang lebih dulu: blok `/* */` mode slurp, lalu `//` yang berdiri sebagai awal komentar supaya `https://` tidak memotong baris) dan menolak em dash — `antislop-refs OK … 8 pemeriksaan`. Tiga teks pengguna diganti kalimat yang menjelaskan (`EMPTY_VALUE = "Belum diisi"`, `EMPTY_DATE = "Belum ditetapkan"` di `src/utils/format.ts`), satu kalimat penjelas dialog dan satu pemisah role anggota ditulis ulang. **Gigi dibuktikan tiga kali:** em dash di string UI → `FAIL`; em dash hanya di komentar → lolos (memang dikecualikan); em dash di string sesudah `https://` → tetap tertangkap (strip komentar tidak membutakan pemeriksa). Log `prompts/P-043-2026-09-22-antislop-frontend-dan-laci-menu.md` |
| **T-053** | Bangun **halaman bisnis pertama** di frontend: **Projects** (`50-FSD.md` §3, `42-API.md` §3) — daftar ber-penyaring dan ber-paginasi, dialog buat project, halaman detail dengan tab yang jujur menyebut apa yang belum dibangun, memakai pustaka server-state **TanStack Query** yang `30-ARCHITECTURE.md` §2.1 janjikan untuk "halaman data pertama" dan cakupan data server apa adanya (project di luar keanggotaan tidak pernah dikirim server). Bagian dari **Phase 4** | 2026-09-21 | **DONE (P-041).** Lapisan data: `services/projects.ts` (satu tempat yang tahu bentuk `42-API.md` §3), `queries/client.ts` + `queries/projects.ts` (kunci kueri terpusat, `staleTime`, invalidasi setelah mutasi). Halaman: `pages/Projects/index.tsx` (daftar, penyaring `?search=`/`?status=`, paginasi dari `meta`, keadaan memuat/kosong/gagal yang dibedakan), `CreateProjectDialog.tsx` (validasi klien mengikuti §3.2 termasuk pola kode `^[A-Z0-9]+(-[A-Z0-9]+)*$`, lalu pemetaan galat server `422 fieldErrors`/`409` ke field-nya), `ProjectDetail.tsx` (metadata langsung dari `GET /projects/:id`, daftar anggota, tab Documents/Tasks/Workflow/Activity yang menandai dirinya **belum dibangun** alih-alih menampilkan tabel kosong). Primitives baru: `Dialog.tsx` (fokus awal, `aria-modal`, Escape, jebakan fokus) dan pemakaian `DataTable` yang sudah ada. **Bukti test:** 41 test baru (`services/projects.test.ts` 9, `pages/Projects/Projects.test.tsx` 15, `CreateProjectDialog.test.tsx` 8, `ProjectDetail.test.tsx` 9) — total frontend **129 test / 14 berkas** hijau, `tsc --noEmit` + `eslint .` bersih, `vite build` 395 kB js / 21,1 kB css. **Bukti server nyata:** halaman dibuka di dev server pada sesi yang sama — daftar memuat keadaan kosong yang benar, dialog membuat project sungguhan (`PREVIEW-041`), peramban berpindah ke `/projects/<uuid>` hasil server, metadata + anggota terbaca, dan tab yang belum dibangun menyebut dirinya. **Menemukan C-063** (field `Owner` wajib di `50-FSD.md` §3.2 tanpa endpoint daftar pengguna; Q-024) — dicatat **OPEN**, bukan dikarang pemilih pengguna palsu, dan batas itu dituliskan di layar serta dikunci test. Log `prompts/P-041-2026-09-21-halaman-projects-dan-lapisan-data.md` |
| **T-054** | Pasang **skill antislop** dari rilis resminya dan hentikan penyimpangan salinan: penuhi blok penunjuk `AGENTS.md` (lima skill yang didaftarkan tetapi tidak ada di disk — C-064), selaraskan `antislop.md` di root dengan rilis upstream, hapus salinan daftar aturan/Gate dari dokumen desain (C-065), catat provenans `sha256`, dan buat pemeriksa yang menahannya di CI | 2026-09-22 | **DONE (P-042).** Atas **izin eksplisit user** (Q-003), agen mengunduh dari repo `miqdadbadjuber/anti-slop` pada **tag rilis `v3.2.12`** (bukan `main`): core `antislop.md` (root), lima skill (`antislop`, `antislop-ui`, `antislop-copywriting`, `antislop-human` + `contrast-check.py`, `antislop-layoutmobile`, `antislop-code`), dan salinan `LICENSE` (MIT) — semuanya byte-identik, `sha256` dicatat di `skills/README.md` §1 bersama cara memperbaruinya (§2). Sebelumnya `AGENTS.md` mendaftarkan lima skill yang **tidak ada** (C-064) dan core di root adalah **varian lama** yang berbeda dari salinan Gate di `01-AGENT-WORKFRAME.md` §5.3 (C-065) — dua versi core di satu repo. Perbaikan dokumen: tabel 23 aturan (§3.2) dan blok empat blok Gate (§5.3) **dihapus** dan digantikan penunjuk + keputusan proyek + bentuk laporan Gate, karena salinan yang tidak diperiksa mesin selalu kalah dari sumbernya (pola C-014); pohon §6 diselaraskan dengan `skills/` dan `scripts/` yang sebenarnya. Pemeriksa baru `scripts/check-antislop-refs.sh` menegakkan tujuh hal tanpa jaringan: daftar aturan terbaca & berurutan, setiap `R-XX` yang dirujuk ada di core, path skill dua arah (disebut ↔ ada di disk), `sha256` cocok tabel provenans, salinan core di `skills/antislop/SKILL.md` identik dengan `antislop.md`, klaim rentang aturan cocok dengan daftar, dan kalimat khas aturan/Gate upstream tidak tersalin ke dokumen proyek. Hasil `antislop-refs OK — 38 aturan (R-01..R-38), 93 rujukan, 7 berkas skill, 7 pemeriksaan`; **gigi dibuktikan** dengan enam cacat disuntikkan sementara (nomor aturan yang tidak ada, path `skills/<nama>/SKILL.md` hantu, `sha256` skill UI diubah, salinan core diubah, kalimat Gate disalin, dan klaim rentang aturan yang ujungnya berhenti sebelum aturan terakhir) — keenamnya tertangkap — lalu dipulihkan. Bukti tambahan: pemeriksa kontras **upstream** dijalankan atas 10 pasangan token proyek dan angkanya sama dengan komentar di `tokens.css` (5,09 / 7,15 / 15,46 / 6,93 / 10,37 / 4,65). Keputusan dicatat **ADR-0025** (pin, lisensi MIT, dan amandemen butir 2 ADR-0006: izin unduh itu satu kali, bukan aturan tetap); pemeriksa masuk CI + `README.md` §12.3, `12-DEVELOPMENT-WORKFLOW.md` §8, `90-AGENT-GUIDE.md`, `02-AGENT-PROGRESS-PROTOCOL.md` §6.4/§8, `AGENTS.md`. Log `prompts/P-042-2026-09-22-skill-antislop-terpasang-dan-dipin.md` |
| **T-055** | Rapikan klaim **himpunan** di `README.md` §4: pohon folder hanya menyebut dua dari lima skrip yang sudah berjalan di CI (C-066), dan `check-readme-facts.sh` memperoleh pembanding dua arah antara daftar di README dengan isi `scripts/` dan `skills/` | 2026-09-22 | **DONE (P-042).** Ketahuan ketika pohon §4 dibuka untuk keperluan lain (menambahkan `skills/`): blok `scripts/` masih berbunyi dua skrip padahal `check-readme-facts.sh` (P-039), `check-api-contract.sh` (P-040), dan `check-antislop-refs.sh` (P-042) sudah dijalankan CI setiap push — README menyembunyikan tiga perintah verifikasi yang justru wajib dijalankan kontributor. `check-readme-facts.sh` tidak menangkapnya karena ia membandingkan **angka & versi**, bukan **daftar**: kelasnya sama dengan C-061/C-062 (klaim tentang keadaan repo), tetapi jenis klaimnya **himpunan**, bukan angka. **Perbaikan:** pohon §4 memuat kelima skrip beserta fungsinya dan direktori `skills/`; `check-readme-facts.sh` mendapat **§6** yang memeriksa dua arah — setiap `scripts/*.sh` wajib disebut di README, dan setiap path `scripts/...`/`skills/...` yang ditulis README wajib ada di disk (25 → **37 fakta**). **Gigi dibuktikan** dengan dua cacat disuntikkan sementara: menghapus satu baris skrip dari pohon → `FAIL … tidak pernah disebut`; menambah path `skills/...` hantu → `FAIL … menunjuk path yang tidak ada`; keduanya dipulihkan dan hijau kembali. Sebagai efek samping, `check-doc-links.sh` diberi dua klasifikasi yang ditemukan sesi ini: `guide.md` masuk daftar `ABSENT_DOCS` (panduan upstream yang sengaja tidak disalin), dan isi `skills/` selain `README.md` dilewati karena berkas pihak ketiga yang rujukannya menunjuk tata letak repo upstream (varian Claude/Gemini) dan keasliannya dikunci `sha256` — bukan ditambal dengan menyunting salinannya |
| **T-052** | Buat pemeriksa **anotasi izin endpoint** terhadap matriks RBAC dan router (`scripts/check-api-contract.sh`) supaya `T-024` tidak lagi dirawat sebagai hitungan manual dan pasangan izin karangan tidak lolos | 2026-09-21 | **DONE (P-040).** Skrip membaca matriks `44-SECURITY.md` §3.1.2 sebagai sumber kebenaran (**44 pasangan**), lalu menegakkan tiga aturan: setiap endpoint di `42-API.md` punya izin yang terbaca (baris `Izin:` di bloknya **atau** baris di tabel izin babnya), setiap pasangan `resource:action` pada anotasi/tabel **ada di matriks**, dan setiap `RequirePermission(deps.Permission, ...)` di `internal/handler/router.go` (**18 pasangan**) memakai pasangan dari matriks yang sama. Hasil: `api-contract OK — 110 pemeriksaan, 55 endpoint` (48 beranotasi di bloknya + 7 lewat tabel izin bab §4). **Gigi dibuktikan** dengan tiga cacat sementara: menghapus satu baris `Izin:` → tertangkap; menambah `document_version:read` pada anotasi (pasangan yang tidak ada di matriks) → tertangkap; mengubah `task:complete` → `task:finish` di route → tertangkap. Skrip ini **tidak** mengesahkan pasangan baru — menambah pasangan izin tetap menuntut ADR (ADR-0014); log `prompts/P-040-2026-09-21-anotasi-izin-endpoint-dan-pemeriksanya.md` |
| **T-024** | Anotasi `Izin:` (pasangan `resource`, `action`) pada setiap endpoint di `42-API.md` supaya pemetaan endpoint → izin dapat dibaca langsung dari satu tempat (fase 1; dokumen `42-API.md`, `44-SECURITY.md` §3.1 — ADR-0014; requirement FR-ROLE-03) | 2026-09-21 | **DONE (P-040).** Ke-10 endpoint yang tersisa diberi izinnya langsung dari matriks §3.1.2 — bukan dikarang: `GET|POST /workflows/definitions` (`workflow_definition:read` semua role / `workflow_definition:manage` Administrator), tiga endpoint notifications (`notification:read` untuk daftar, `notification:update` untuk menandai dibaca, keduanya **semua role** dengan cakupan `user_id = user`), `GET|POST /admin/users` (`user:read`/`user:create`), `PATCH /admin/users/:id` (izinnya tadinya hanya menempel di dalam butir prosa — kini baris `Izin:` sendiri), `GET /admin/roles` (`role:read`), dan `PATCH /admin/settings/:key` (`setting:manage`). Hasil: **55 endpoint — 48 punya baris `Izin:` di bloknya, 7 lewat tabel izin bab §4**; tidak ada lagi endpoint tanpa keterangan, dan sejak sesi ini angka itu **dihitung mesin** (`T-052`) alih-alih ditulis manual. Satu anotasi lama diperbaiki agar dapat diperiksa: kalimat "matriks tidak punya `comment:update`" ditulis ulang tanpa token pasangan, karena pemeriksa membacanya sebagai klaim pasangan |
| **T-051** | Buat pemeriksa **kesegaran angka & versi di `README.md`** terhadap repo (jumlah route + rincian per modul, versi dependensi backend/frontend, rentang migrasi, klaim "belum ada") supaya kelas cacat C-061/C-062 tidak terulang; jalankan juga di CI | 2026-09-21 | **DONE (P-039).** `scripts/check-readme-facts.sh` menghitung setiap fakta dari **sumbernya**, bukan dari dokumen lain: jumlah route + rincian per modul + jumlah endpoint tiap modul dari `backend/internal/handler/router.go`; versi Go/Gin/pgx/viper/goose dari `backend/go.mod`; versi React/Vite/Tailwind/TypeScript dari `frontend/package.json`; rentang migrasi dari berkas di `backend/internal/migration/`; dan klaim "belum ada/belum diisi" dari keberadaan path. Keluarannya `readme-facts OK — N fakta diperiksa` (25 fakta) atau `FAIL` beserta nomor baris. Dua pagar yang menjaga pemeriksa ini tidak bokek diam-diam: **setiap group route wajib punya ember** (group baru yang belum terklasifikasi menggagalkan pemeriksaan, dan rincian yang tidak berjumlah sama dengan total juga menggagalkan), dan **klaim yang polanya hilang dari README dianggap gagal** — pemeriksa yang berhenti memeriksa tanpa suara lebih berbahaya daripada pemeriksa yang berisik. **Gigi dibuktikan:** lima cacat disuntikkan sementara ke README (route 32→31, Project 8→9 endpoint, React 19→18, migrasi 010→009, satu kalimat "frontend/ masih kosong") — **kelimanya tertangkap**, lalu README dipulihkan (25 fakta OK lagi); ember route baru diuji terpisah dengan salinan sementara `router.go` (+1 group `notifications`) dan tertangkap oleh dua pagar sekaligus. Pemeriksa itu **gagal pada percobaan pertama** terhadap README yang sudah ada, dan kegagalan itu sendiri menemukan **C-062** (baris "Frontend (rencana)" masih menulis React 18 + "Belum diinisialisasi") yang lalu diperbaiki di sesi yang sama. Tersambung ke CI (`.github/workflows/ci.yml`) dan ke perintah verifikasi di `README.md` §12.3, `12-DEVELOPMENT-WORKFLOW.md` §8, `02-AGENT-PROGRESS-PROTOCOL.md` §6.2/§8, `90-AGENT-GUIDE.md`, dan `AGENTS.md`; log `prompts/P-039-2026-09-21-pemeriksa-kesegaran-angka-readme.md` |
| **T-049** | Tambahkan bagian **Kontribusi** pada `README.md`: urutan alur perubahan, verifikasi wajib, aturan yang tidak boleh dilanggar, konvensi commit, dan kanal usulan (dokumen `README.md` §12; sumber aturan `AGENTS.md`, `01-AGENT-WORKFRAME`, `02-AGENT-PROGRESS-PROTOCOL`, `12-DEVELOPMENT-WORKFLOW`) | 2026-09-21 | **DONE (P-038).** `README.md` §12 memuat enam subbagian: **12.1** sebelum menyentuh berkas (menunjuk §11 + `CONTINUE.md`, bukan menyalin urutannya), **12.2** lima langkah satu perubahan (rencana di log prompt → perubahan terkecil → verifikasi → ledger → keputusan/pertanyaan) dengan aturan "satu prompt = satu unit yang dapat diverifikasi", **12.3** perintah verifikasi yang wajib hijau — termasuk alasan `make test` **bukan** `go test` telanjang (test integrasi di-skip) dan kedua skrip dokumen yang ada saat itu beserta keluaran yang diharapkan (**P-039** menambahkan yang ketiga, `check-readme-facts.sh`), **12.4** tabel sepuluh aturan yang tidak boleh dilanggar **beserta sumbernya masing-masing** (bukan salinan teksnya, supaya tidak lahir sumber kebenaran kedua — pola C-014), **12.5** konvensi commit (ID requirement + ID task, ledger di perubahan yang sama, bahasa dokumen) dan penegasan bahwa agen tidak `commit`/`push` tanpa permintaan, **12.6** kanal usulan — ditulis jujur bahwa **belum ada pelacak isu**, sehingga usulan masuk lewat `TASKS.md`/`OPEN-QUESTIONS.md`. Berkas `LICENSE` **tidak** dibuat karena pemilik memilih menunda (Q-023), dan ketiadaannya ditampilkan di pohon `README.md` §4 dengan penanda "belum ada". Sesi ini juga menemukan & menutup **C-061**: enam klaim basi di README, termasuk jumlah route **30 → 32** yang diverifikasi dari `internal/handler/router.go`. Bukti: `check-ledger.sh` → `ledger OK`, `check-doc-links.sh` → `BROKEN: 0`; log `prompts/P-038-2026-09-21-bagian-kontribusi-dan-status-lisensi.md` |
| **T-048** | Bangun kerangka frontend di atas arah desain yang baru ditetapkan: Vite + React + TypeScript + Tailwind, token dari `DESIGN.md`, shell + primitives + halaman awal, dengan test yang memeriksa token dan aksesibilitas. Bagian dari **Phase 4**; dokumen `DESIGN.md`, `51-UX.md` §2, `30-ARCHITECTURE.md` §2.1/§2.2, `60-DEPLOYMENT.md` §3.2; requirement R-37 + NFR terkait (R-21 tema, R-32 keyboard, WCAG AA) | 2026-09-21 | **DONE (P-037).** `frontend/` berdiri: Vite **8** + React **19** + TypeScript 5.9 (`strict`) + Tailwind **v4** lewat plugin `@tailwindcss/vite` — **tanpa `tailwind.config.js`** karena v4 CSS-first (ADR-0024) — plus Zustand 5, axios, React Router 7, dan Vitest + Testing Library + `axe-core`. `src/styles/tokens.css` memuat token `DESIGN.md` (Paper/Ink/Signal/status, skala teks, radius, bayangan, gerak) dan lapisan semantik `@theme inline` sehingga satu blok membalik seluruh tema; komponen **tidak pernah** menulis warna langsung. Yang berdiri: `AppShell`/`Header`/`Sidebar`/`PageHeader`, primitives (`Button`, `Field`, `Panel`, `DataTable`, `StatusBadge`, `States`, `tones`), `services/http.ts` (interceptor + pemetaan galat `42-API.md` §12 termasuk `fieldErrors` dan `retryAfterSeconds`) + `auth.ts` + `session.ts`, `store/auth.ts`, `store/theme.ts`, halaman Login (benar-benar memanggil `/auth/login` → `/auth/me`), Dashboard yang **hanya** menyebut modul yang sudah/belum ada tanpa angka karangan (R-18), `ModulePending`, `NotFound`. **Bukti:** `tsc --noEmit` bersih, `eslint .` bersih, **88 test / 10 berkas** hijau, `vite build` → `dist/` (104 modul; 336 kB js / 19,5 kB css, gzip 108 kB / 5,0 kB); halaman dibuka di dev server dan rutenya ditelusuri. **Test kontras punya gigi:** `src/styles/tokens.contrast.test.ts` menghitung sendiri rasio WCAG 2.2 AA dari `tokens.css` (31 test); mengubah `--color-ink-500` menjadi `#a8a8a8` membuat **3 test gagal** (`--text-muted di atas --surface = 2,18:1`), lalu pulih setelah nilainya dikembalikan. Sesi ini juga memperbaiki parser blok CSS di test itu (prelude `@theme` terbawa `@import`/`@custom-variant`, membuat 15 test gagal padahal tokennya benar). Menutup **C-015** lewat `T-006` dan menutup **C-060** (`51-UX.md` §3/§4 diselaraskan); log `prompts/P-037-2026-09-21-design-md-dan-kerangka-frontend.md` |
| **T-007** | Gate UI Phase 4: mode antislop dipilih **dan** `DESIGN.md` terisi sebelum UI dibangun | 2026-09-21 | **DONE (P-037).** Kedua syaratnya terpenuhi pada sesi yang sama: user menjawab **Q-001** dengan mode **`during`** (ADR-0006 `ACCEPTED`, jadi filter antislop dipakai selama pengerjaan, bukan sesudahnya) dan **Q-002** dengan **jalur 2 ADR-0007** (`ACCEPTED`, agen menyusun arah desain atas instruksi eksplisit) sehingga `DESIGN.md` berstatus `TERISI`. Bukti bahwa gate itu benar-benar terbuka: pekerjaan UI pertama (`T-048`) dikerjakan **sesudah** keduanya selesai, dan `DESIGN.md` §9 memuat Design Read yang mengikatnya. Temuan **C-015** ditutup oleh pasangan keputusan ini |
| **T-006** | Isi `DESIGN.md` **dan** catat dials (fase pra-4; dokumen `DESIGN.md`, `11-DESIGN-DIRECTION.md`; requirement R-37) | 2026-09-21 | **DONE (P-037).** User memilih **jalur 2 ADR-0007** dan memberi izin eksplisit kepada agen, jadi statusnya jujur ditulis sebagai arah yang **disusun agen**, bukan identitas resmi pemilik produk. `DESIGN.md` `TERISI` lengkap: identitas & kepribadian (§1), palet §2.1-§2.4 **dengan alasan per token dan angka kontras** (diambil dari perhitungan, bukan dikira), tipografi §3 (system stack — tanpa berkas font, tanpa CDN; plafon **26px**; aturan mono untuk nomor), mood & kepadatan §4 (baris tabel 36px, angka rata kanan), dials §5 (**ENERGY 1 / RHYTHM 2 / MOTION 1** + alasan per dial), motif "punggung rekam" §6, tema terang/gelap §7, referensi & anti-referensi §8, Design Read §9. Sumber mekanisnya `frontend/src/styles/tokens.css`, dan test kontras menuntut keduanya tetap cocok. Dial 1/2/1 di `01-AGENT-WORKFRAME.md` §3.3 dinyatakan **resmi**; `11-DESIGN-DIRECTION.md` berstatus `SUPERSEDED`. Menutup **C-015**; log `prompts/P-037-2026-09-21-design-md-dan-kerangka-frontend.md` |
| **T-047** | Buat pemeriksa konsistensi ledger otomatis (hitungan audit, status task, rujukan test yang sudah tidak ada) dan jalankan di CI supaya kelas cacat C-044/C-055/C-057 tidak terulang | 2026-09-20 | **DONE (P-036).** `scripts/check-ledger.sh` memeriksa: (1) marker `<!-- audit-summary total= fixed= approved= open= rejected= rinci= -->` cocok dengan tabel tindak lanjutnya dan salinannya seragam di `audits/README.md`, `AGENTS.md`, `STATE.md`, `CONTINUE.md`, termasuk angka `N FIXED`/`N OPEN` dan penjumlahan di prosa lima berkas itu; (2) papan `TASKS.md`: satu ID hanya di satu kolom status, baris `DONE` bertanggal, kolom lain tidak boleh mengklaim selesai; (3) rujukan `TestXxx`/`path::TestXxx` pada dokumen status dan ADR wajib ada di `backend/`; (4) hitungan test per berkas dan `**total suite N test**` di `STATE.md` §3 wajib sama dengan `grep -c '^func Test'` (236); (5) setiap `T-###`/`C-###` yang dirujuk wajib ada di papan/tabel audit. **Bukti punya gigi:** dua cacat disuntikkan sementara (suite `236 → 213`, marker `fixed=55 → 54`) → dua `FAIL` spesifik + `exit=1`; cacat ketiga di prosa (`56 + 0 + 2 = 59`) → `FAIL AGENTS.md:57: penjumlahan audit di prosa tidak berjumlah`. Dijalankan pada ledger yang sudah bersih → `ledger OK — 0 peringatan, 236 test`. CI `.github/workflows/ci.yml` menjalankan skrip ini bersama `check-doc-links.sh` (job `ledger`) dan `make test` (job `backend`, PostgreSQL 16 `bwdcs_test`). Menutup **C-058** dan **C-059**; aturan ditulis di `02-AGENT-PROGRESS-PROTOCOL.md` §6.1 |
| **T-044** | Terapkan semantik rentang `?due_from=&due_to=` sebagai interval **tertutup** `[due_from, due_to]` (kedua batas inklusif), menggantikan setengah terbuka yang diusulkan agen di Q-017 butir (10) — fase 1; `42-API.md` §6, `50-FSD.md` §6.1, `70-TESTING.md` §3.11/§3.11a, FR-TASK-06 | 2026-09-19 | **DONE (P-028).** Kueri `<=`, validasi `due_to < due_from` → `422` (kedua batas sama sah), test `TestTaskListDueRangeFilterIsInclusiveBothEnds` (7 kasus) + kasus pembeda di test handler, dan semantiknya kini **dikunci di level HTTP** oleh `TestTaskListDueRangeContractAtHTTP` (16 subtest: hanya `due_from`, hanya `due_to`, kedua batas sama, rentang terbalik — P-033). Bukti server nyata di `prompts/P-028-*.md` §6 |
| **T-045** | `POST /auth/refresh`: daftarkan route + tukar refresh token dengan sepasang token baru, mengikuti keputusan user pada **Q-020** (ADR-0023) | 2026-09-20 | **DONE (P-034, lanjutan).** Bentuk tokennya adalah **JWT bertanda `typ: refresh`** berumur 7 hari (`jwt.RefreshExpiry`), **tanpa penyimpanan di server** dan tanpa migrasi — Opsi A yang dipilih user. Kode: `jwt.Service.GenerateRefresh`/`ValidateRefresh` + klaim `Type` yang wajib (token tanpa `typ` ditolak), `jwt.RefreshExpiry`, `AuthService.Refresh` (tipe → pencabutan lewat jalur yang sama → akun aktif → terbitkan pasangan baru), `dto.RefreshRequest`/`RefreshResponse`, `AuthHandler.Refresh`, route `POST /api/v1/auth/refresh` **tanpa** `AuthMiddleware`; `POST /auth/login` kini juga mengembalikan `refresh_token` + `refresh_expires_at` (tanpa itu endpointnya tidak punya modal untuk ditukar). **Bukti test:** enam test `internal/pkg/jwt` (termasuk refresh-ditolak-sebagai-bearer dan token-tanpa-`typ`), lima test `auth_service_test.go` (penukaran pasangan, sesi tercabut, access token ditolak, akun nonaktif, dan batasan stateless yang dikunci terbuka), tiga test handler (end-to-end, refresh-bukan-bearer, dan pemetaan `422`/`401`/`401 TOKEN_REVOKED`) — semuanya `ok`. **Test punya gigi:** dengan pemeriksaan tipe dinonaktifkan sementara, **enam test gagal di tiga paket**, termasuk `TestRefreshTokenIsNotABearerToken` (`status 200`, padahal harus `401`). **Bukti server nyata:** login menyerahkan kedua token; dekode klaim → access 24 jam, refresh 7 hari; refresh token sebagai bearer → `401 UNAUTHORIZED`; access token di endpoint refresh → `401`; `{}` → `422 field=refresh_token`; penukaran sah → `200` dengan pasangan baru yang keduanya berbeda, dan token penggantinya dipakai ke `/auth/me` → `200`; `logout_all` → `200` lalu refresh → `401 TOKEN_REVOKED`; `audit_logs` tetap hanya `LOGIN=1`. Database dev dikembalikan persis seperti semula. Ringkasan: `70-TESTING.md` §3.12d; ADR: `docs/adr/0023-bentuk-token-refresh.md` |
| **T-034** | Daftarkan `POST /auth/change-password` dengan aturan "cabut seluruh sesi lain" memakai `users.tokens_invalid_before` **plus** penerbitan token baru (FR-AUTH-09). Endpoint `POST /auth/refresh` **dipisah** ke `T-045` karena bentuk tokennya belum diputuskan (**Q-020**) — **dan `T-045` selesai pada sesi yang sama** sesudah user memilih Opsi A (ADR-0023), jadi `T-034` tertutup seluruhnya | 2026-09-20 | **DONE, sebagian langsung dan sisanya pada sesi yang sama (P-034).** `POST /api/v1/auth/change-password` hidup: verifikasi `old_password` **lebih dulu** (gagal di sini tidak mengubah apa pun), lalu hash baru + `RevokeAllForUser` + audit `PASSWORD_CHANGED` dalam **satu** transaksi (ADR-0011), dan token pengganti diterbitkan **sesudah commit** — kalau di dalam transaksi, `iat`-nya bisa jatuh di detik yang sama dengan titik pencabutan (C-053). Response `200` memuat `token` + `expires_at`. **Bukti test:** `auth_service_test.go::TestChangePasswordKeepsCurrentSession` (lima hal sekaligus: `jti` pengganti baru dan tidak dianggap tercabut; token sesi ini **dan** perangkat lain sama-sama mati; user lain tidak tersentuh; `PASSWORD_CHANGED` tepat satu; password lama gagal sedangkan yang baru berhasil), `TestChangePasswordRejectsWrongCurrentPassword` (nol audit, sesi tidak dicabut, password lama masih berlaku), `TestChangePasswordEnforcesNewPasswordRules` (batas bawah, **tepat** pada ambang, dan mengulang password lama), `auth_handler_test.go::TestChangePasswordEndToEnd` (HTTP: `200` + token baru; `/auth/me` dengan token pengganti `200`; token lama dan token perangkat lain `401 TOKEN_REVOKED`; login password lama `401 INVALID_CREDENTIALS`, baru `200`), `TestChangePasswordErrorMapping` (lima subtest: `400 INVALID_CURRENT_PASSWORD`, `422` + `field`, `401` tanpa token). Test E2E menunggu 1,1 detik lebih dulu supaya yang diuji benar-benar pencabutannya, bukan token yang selamat karena terbit di detik yang sama. **Test punya gigi:** dengan `RevokeAllForUser` dinonaktifkan sementara, dua test gagal tepat pada asersi "token sesi ini/perangkat lain harus ditolak". `gofmt`/`vet` bersih, `make test` hijau (sembilan paket). Ringkasan: `70-TESTING.md` §3.12c; log `prompts/P-034-2026-09-20-change-password-dan-status-refresh.md` |
| **T-043** | Seragamkan `meta.total` pada **tiga** endpoint daftar (project, document, task) dengan tambalan yang sudah dipakai modul komentar (temuan **C-048**) | 2026-09-20 | **DONE (P-032).** Predikat daftar diangkat jadi variabel bersama di tiap repository (`projectListWhere`, `documentListWhere`, `taskListWhere`), sehingga `List` (jalur cepat `COUNT(*) OVER()`) dan `count` (kueri hitung) membangun kueri dari sumber yang **sama** dan tidak dapat menyimpang. `count` hanya dipanggil saat `len(rows) == 0 && offset > 0`, jadi halaman normal tetap satu perjalanan ke database. Kueri hitung menghormati penyaring yang sama, termasuk aturan default dokumen menyembunyikan `archived` (ADR-0019) dan cakupan baca task. **Bukti test:** `TestProjectListOutOfRangePageKeepsTotal`, `TestDocumentListOutOfRangePageKeepsTotal`, `TestTaskListOutOfRangePageKeepsTotal` (masing-masing memeriksa halaman 1 (2 baris, total 3), halaman di luar rentang (0 baris, total 3), penyaring yang ikut dihitung, dan untuk document aturan arsip; untuk task cakupan organisasi lain tetap 0). Ketiga test **gagal dengan `total 0`** saat tambalan dinonaktifkan sementara, jadi test menangkap cacatnya, bukan lulus kosong. `gofmt`/`vet` bersih, `make test` hijau (sembilan paket), database test kembali kosong. Ringkasan: `70-TESTING.md` §3.13a; log `prompts/P-032-2026-09-20-meta-total-seragam.md` |
| **T-040** | Implementasi **ADR-0021** (temuan C-033): kolom `users.tokens_invalid_before` diperiksa middleware bersama `jti`, dan `logout_all: true` benar-benar mencabut seluruh sesi; entri `NOT_IMPLEMENTED` dihapus dari `42-API.md` §12 | 2026-09-19 | **DONE (P-030).** `RevocationRepository.SessionRevoked(jti, userID, issuedAt)` menjawab **kedua** sebab dalam **satu** kueri (cache per `jti`, TTL 30 detik; user yang barisnya hilang dianggap tercabut), `RevokeAllForUser` menulis `users.tokens_invalid_before` **dan** membuang seluruh entri cache user itu, dan `AuthService.Logout` mencabut `jti` request **plus** seluruh token lama + audit `LOGOUT_ALL` dalam satu transaksi. Middleware: `AuthConfig.Sessions` (interface `SessionChecker`) + `jwt.Validate` kini **menolak token tanpa `iat`** (tanpa itu pencabutan massal tidak dapat ditegakkan). `logout_all` → `200`; `501 NOT_IMPLEMENTED` dihapus dari kontrak **dan** dari `internal/pkg/response`. Test: `TestLogoutAllInvalidatesOtherTokens`, `TestLoginRightAfterLogoutAllStillWorks`, `TestLogoutRevokesToken`, `TestAuthMiddlewareChecksSessionAgainstTokenIssueTime`, `TestAuthMiddlewareRejectsTokenWithoutIssuedAt`, `TestValidateRejectsTokenWithoutIssuedAt`, `TestLogoutAllEndToEnd`, `TestLoginTelemetrySchemaMatchesAdr0022` — semua `ok`. **Bukti server nyata** (binari dibangun ulang, `versi_skema 10`): tiga token (dua "perangkat lain"), `logout_all` → `200`, ketiganya → `401 TOKEN_REVOKED`, dan token hasil **login ulang** tepat sesudahnya → `200`; `audit LOGOUT_ALL=1`. Dua temuan lahir dari menjalankannya: **C-053** (`NOW()` mentah menolak token yang lahir di detik yang sama — nilai ditulis `date_trunc('second', NOW())`) dan **C-052** (contoh `423` di §12 bertentangan dengan prosanya). Ringkasan: `70-TESTING.md` §3.12/§3.12b; log `prompts/P-030-2026-09-19-pencabutan-sesi-dan-auto-lock.md`. **Bukti dijalankan ulang pada P-035** terhadap binari yang memuat `change-password` + `refresh`: ketiga token user itu `401 TOKEN_REVOKED` sementara **token user lain tetap `200`**, refresh token lama sesudah `logout_all` juga `401 TOKEN_REVOKED`, dan login ulang tepat sesudahnya sah — `70-TESTING.md` §3.12b; log `prompts/P-035-2026-09-20-probe-ulang-alur-sesi-dan-lock.md` |
| **T-041** | Implementasi **ADR-0022** (temuan C-009 + C-035): `login_attempts`, `users.locked_until`, auto-lock `423 LOCKED` dengan ambang dari `system_settings`, penggantian penghitung di memori, dan `POST /admin/users/:id/unlock` | 2026-09-19 | **DONE (P-030).** `LoginAttemptRepository` (tulis setiap percobaan + hitung kegagalan menurut **jam database**), `AuthService.Login`/`applyLockout` (jendela 15 menit = `service.LoginAttemptWindow`; percobaan yang **melewati** ambang yang dibalas `423`; lock aktif **tidak** diperpanjang), `UserRepository.LockUntil`/`ClearLock`, `service.UserService.Unlock` + `handler.UserHandler` + route `/admin/users/:id/unlock` (izin `user:update`), `dto.LockedDetails`; **`internal/service/login_guard.go` dan testnya dihapus** (ADR-0022 butir 6). Test: `TestLoginLockoutAfterThreshold`, `TestLockExpiresWithoutIntervention`, `TestSuccessfulLoginWritesAuditAndAttempt`, `TestFailedLoginRecordedForUnknownUsername`, `TestLoginWrongPassword`, empat test `user_service_test.go` (unlock + idempotensi + telemetri tidak dihapus + backoff), tiga test handler unlock, `TestLoginLockedReturns423`, dan `TestLoginTelemetrySchemaMatchesAdr0022` (kolom + FK `ON DELETE SET NULL` + indeks) — semua `ok`, `make test` hijau untuk sembilan paket. **Bukti server nyata:** lima percobaan gagal → `401`×4 lalu **`423`** + `details.retry_after_seconds=900`; password benar saat terkunci → `423`; token sesi lama tetap `200` (lock hanya menghalangi login); `unlock` → `200` (panggilan kedua `200`, audit tetap `1`); percobaan atas username **tidak ada** → dua baris `login_attempts` `user_id NULL`; `login_attempts` admin `succeeded=true`=4/`false`=6; `audit LOGOUT_ALL=1`. Temuan C-035 (`audit_logs` tetap tidak memuat percobaan gagal), **C-054** (pemicu `429` di dokumen tidak lagi cocok) dan **C-055** (hitungan audit tidak dapat diperiksa silang) ditutup di sesi yang sama; database dev dikembalikan (`audit_logs 43`, `login_attempts 0`, `users 1`). Ringkasan: `70-TESTING.md` §3.12/§3.12b. **Bukti dijalankan ulang pada P-035:** `401`×4 lalu `423` pada percobaan kelima (ambang dibaca dari `system_settings`, bukan dikarang), `Retry-After: 900` + `details.retry_after_seconds=900` + `locked_until` benar-benar terkirim, `423` juga untuk password benar saat terkunci, akun terkunci masih dapat memperpanjang sesinya lewat `POST /auth/refresh` (`200`), `unlock` idempoten (`USER_UNLOCKED=1`), dan `audit_logs LOGIN=6` tepat sama dengan jumlah login berhasil sementara sembilan percobaan gagal tidak menghasilkan satu baris pun — log `prompts/P-035-2026-09-20-probe-ulang-alur-sesi-dan-lock.md` |
| **T-039** | Implementasi **ADR-0019** (temuan C-004): ganti `DELETE /documents/:id` dengan `POST /documents/:id/archive`; `documents.archived_at` + nilai kanonik `archived`; dokumen terarsip menolak unggahan versi baru; pasang dua trigger append-only pada `document_versions`; `409` saat workflow `running` atau sudah terarsip | 2026-09-19 | **DONE (P-029).** Migrasi `010` (`010_session_revocation_login_attempts_document_archive.sql`) memuat seluruh isi yang dijadwalkan `41-DATABASE.md` §4: `documents.archived_at`, `CHECK` enam nilai, dan dua trigger `document_versions` —plus skema ADR-0021/0022 (`users.tokens_invalid_before`, `users.locked_until`, `login_attempts`) karena berkas migrasi **tidak boleh disunting setelah diterapkan**; kodenya tetap milik `T-040`/`T-041`. Kode: `model.DocumentStatusArchived` + `ArchivedAt`, `DocumentRepository.Archive` (menggantikan `Delete`, penjaga `status <> 'archived'` di `WHERE`), daftar default tanpa arsip (`?status=archived` menampilkannya), `DocumentService.Archive` (audit `DOCUMENT_ARCHIVED` di transaksi yang sama), penolakan unggahan versi pada dokumen terarsip **sebelum** berkas ditulis, `DocumentHandler.Archive`, route `POST /documents/:id/archive` ber-izin `document:update`; route `DELETE` **dihapus**. Test: `TestArchiveDocumentKeepsVersionsAndFiles`, `TestArchivedDocumentLeavesDefaultListButStaysInFilter`, `TestArchiveRejectedWhileWorkflowRunning`, `TestArchivedDocumentRejectsNewVersion`, `TestArchiveSecondTimeIsRejected` (service), `TestArchiveDocumentEndToEnd` + `TestDocumentPermissionsFollowMatrix` (HTTP), `TestDocumentStatusVocabularyIncludesArchived` + tujuh `TestDocumentVersions_*` (migrasi) — semuanya `ok` di `make test`. **Bukti server nyata** (13 probe + 4 query `psql`, binari dibangun ulang, `versi_skema 10`): arsip `200` `status=archived` `archived_at` terisi, baris+versi+berkas tetap, unduhan tetap `200` identik, daftar default `total 0` / `?status=archived` `total 1`, unggahan ulang & arsip ulang `409`, `DELETE` lama `404`, Viewer `403` "tidak memiliki izin document:update". Data uji dibersihkan sampai database dev kembali (`projects 0`, `documents 0`, `document_versions 0`, `users 1`, `audit_logs 43`). Ditemukan & ditutup di sesi yang sama: **C-051** (FK kaskade `document_versions` tetap ada di skema tetapi kaskadenya ditahan trigger — pembersihan dokumen berversi wajib lewat jalur pemeliharaan; dicatat di `41-DATABASE.md` §2.3 dan diuji `TestDocumentVersions_DeleteFromDocumentsIsRejected`). Ringkasan bukti: `70-TESTING.md` §3.12a; log `prompts/P-029-2026-09-19-arsip-dokumen-dan-trigger-versi.md` |
| T-042 | Modul Comment (**Phase 3 `80-ROADMAP.md`**): lima endpoint `42-API.md` §7 (`GET|POST /comments`, `GET|PATCH|DELETE /comments/:id`) dengan **cakupan yang diturunkan dari entitas** (tabel `comments` tidak menyimpan `project_id`) dan **edit/hapus hanya milik sendiri** — kepemilikan, bukan izin (`44-SECURITY.md` §3.1.3) | 2026-09-19 | Kelima endpoint §7 hidup; `42-API.md` §7 ditulis ulang menjadi kontrak penuh (pemetaan `entity_type` → project, aturan tiap field, bentuk kueri daftar beserta alasan C-049, batas isi 2000 karakter, kepemilikan, kode error, dan catatan `meta.total` C-048). Cakupan `44-SECURITY.md` §3.1.3 sekarang memuat rujukan implementasi keempat: pemetaan entitas → project tinggal **satu** (`commentEntityProjectCase`, `internal/repository/comment_repository.go`), predikat cakupannya bentuk yang sama dengan `projectScopePredicate` dengan penyusun `systemScope`, dan edit/hapus memakai `WHERE created_by_id = actor` (kueri tanpa JOIN project, sehingga penulis tetap dapat menyunting komentarnya walau sudah bukan anggota project). **Bukti test:** `internal/model/comment_test.go` (3), `internal/service/comment_service_test.go` (11 — termasuk empat jenis entitas, `workflow` lewat instance→dokumen→project, dan test yang mengunci ketiadaan threading), `internal/handler/comment_handler_test.go` (7 lewat HTTP), semuanya `ok` di `make test` (delapan paket hijau). **Bukti server nyata** (binari dibangun ulang, server dijalankan dalam satu perintah bersama probe-nya): `POST /comments` → **201** (spasi tepi isi dibuang, `created_by_username` terisi); daftar `total 1`; `?page=5&limit=2` → `data: []` dengan **`total: 2`**; `422` ber-field untuk `entity_type` asing (`workflow_instance` — pesannya menyebut empat nilai sah), `entity_id` bukan UUID, `content` kosong/spasi, `limit=101`, `page=0`; entitas hantu → **404 entity not found**; `PATCH {}` → `422 field=body`; aktor kedua ber-role **viewer**: daftar `total 0` dan `GET`/`POST`/`PATCH`/`DELETE` **404**, lalu **sesudah** ditambahkan sebagai anggota project daftar `total 2` tetapi `PATCH` komentar orang lain tetap **404** sementara komentarnya sendiri **201/200/200** — `403` hanya muncul saat Viewer mencoba `POST /projects`; `audit_logs` ber-`entity = comment`: `COMMENT_CREATED` **3**, `COMMENT_UPDATED` **2**, `COMMENT_DELETED` **2**. Data bukti dibersihkan sampai database dev kembali seperti semula (`users 1`, `projects 0`, `comments 0`, `audit 43`), tanpa proses server tertinggal. Tiga temuan: **C-048** (`meta.total` 0 pada halaman di luar rentang — ditambal di modul ini; tiga modul lain `T-043`), **C-049** (`GET /comments/:entityType/:entityId` tidak dapat dipasang bersama `/comments/:id` — Gin panik saat registrasi; kontrak memakai bentuk kueri), **C-050** (threading dijanjikan `50-FSD.md` §7 tanpa kolom induk — Q-019, rekomendasi ditunda). Log `prompts/P-027-2026-09-19-modul-comment-dan-kepemilikan.md` |
| T-038 | Modul Task (**Phase 3 `80-ROADMAP.md`, dikerjakan lebih awal dari urutan** — lihat Q-018): `POST`/`GET`/`GET :id`/`PATCH`/`complete` dengan cakupan **dua baris** `44-SECURITY.md` §3.1.3 (baca: keanggotaan project untuk Contributor/Viewer, seluruh organisasi untuk Manager/Administrator; tulis: Contributor hanya task miliknya) dan penanda overdue turunan ADR-0012 | 2026-09-19 | Kelima endpoint `42-API.md` §6 hidup; `42-API.md` §6 ditulis ulang menjadi kontrak penuh (izin per endpoint, aturan tiap field, tabel transisi `50-FSD.md` §6.3 → endpoint+izin, penyaring, kode error). Cakupan `44-SECURITY.md` §3.1.3 sekarang memuat rujukan implementasi ketiga: `taskScope` (`internal/service/scope.go`) menyalurkan role ke `taskReadPredicate`/`taskWritePredicate` di dalam `WHERE`, dengan pembeda role sebagai **parameter** (`internal/repository/task_repository.go`). **Bukti test:** `internal/model/task_test.go` (3), `internal/service/task_service_test.go` (12 — termasuk `TestTaskListOverdueFilterMatchesDerivedFlag` yang mengikat penyaring `?overdue=` dengan penanda turunan), `internal/handler/task_handler_test.go` (10 lewat HTTP), semuanya `ok` di `make test` (delapan paket, dua kali). **Bukti server nyata:** `POST /tasks` → **201** (`High` → `high`, `status` `open`); Contributor/Viewer `POST` → **403**; `422` ber-`details.field` untuk title/assignee/due_date/status/priority/overdue/document_id; project di luar cakupan → **404**; cakupan baca (Contributor anggota melihat 2 task, Viewer non-anggota hanya 1) dan cakupan tulis (Contributor mengubah task orang lain → **404**, bukan **403**); `in_progress` → `completed` lewat `PATCH` → **409**, `complete` ulang **200** tanpa audit ganda, Reopen **200**; `assignee_id` tanpa `task:assign` → **403**; pindah project → **409**; penyaring `?overdue=true` (2 baris) / `?overdue=false` (1) / `?priority=urgent` (1) diverifikasi pada binari yang **dibangun ulang**; tujuh entri `audit_logs` ber-`entity = task`. Dua temuan baru ditemukan & dicatat: **C-045** (pesan `422` menyesatkan untuk UUID tidak sah di body) dan **C-046** (penyaring rentang tanggal di FSD tanpa kontrak) — keduanya **ditutup pada P-026**: `bindJSON` kini menamai field yang benar untuk UUID/waktu/tipe/JSON rusak (berlaku lintas modul, terbukti pada `PATCH /tasks/:id` **dan** `POST /projects`), dan `?due_from=`/`?due_to=` ditambahkan sebagai interval setengah terbuka `[due_from, due_to)` ber-batas RFC 3339 (test dahulu bernama `TestTaskListDueRangeFilterIsHalfOpen` + 7 kasus HTTP) — **semantik ini dikoreksi user pada P-028 menjadi kedua batas inklusif** <!-- ledger-check: skip: nama test lama yang memang sudah diganti, bukan rujukan test hidup --> (`T-044`; test kini `TestTaskListDueRangeFilterIsInclusiveBothEnds`). Sebelas kontrak yang diputuskan agen dicatat di `OPEN-QUESTIONS.md` **Q-017**; log `prompts/P-025-2026-09-19-modul-task-dan-cakupan-baris-kedua.md` dan `P-026-2026-09-19-perbaikan-c045-c046-dan-riset-best-practice.md` |
| T-036 | Perbaiki temuan **C-038**: test memakai database dev yang sama dengan server berjalan, sehingga data nyata membuat suite gagal (`23503` pada `projects_owner_id_fkey`) | 2026-09-19 | Database test terpisah **`bwdcs_test`** dibuat (owner role `bwdcs`; role aplikasi tidak diberi `CREATEDB`, jadi pembuatannya lewat peran superuser lokal). `backend/Makefile` kini **menurunkan** `TEST_DATABASE_URL` dari kredensial `.env` (`postgres://$DB_USER:$DB_PASSWORD@$DB_HOST:$DB_PORT/${TEST_DB_NAME:-bwdcs_test}`), memakai `-count=1`, mencetak nama database yang dipakai sambil menyamarkan sandi (`make test-dsn`), dan **berhenti dengan pesan** bila DSN menunjuk nama database dev — jadi kesalahan lama tidak dapat terulang tanpa disadari. Prosedur dicatat di `70-TESTING.md` §8.1; perintah di `12-DEVELOPMENT-WORKFLOW.md` §8 dan `90-AGENT-GUIDE.md` §7 dialihkan ke `make test`. **Bukti:** dengan project nyata `LIVE-DEV` hidup di database dev (`POST /api/v1/projects` → 201, server berjalan) `make test` hijau untuk delapan paket dua kali berturut-turut, dan database dev tidak tersentuh (`projects=1` tetap); perintah lama yang menunjuk database dev gagal `SQLSTATE 23503` pada empat test `internal/bootstrap`; `bwdcs_test` termigrasi otomatis (22 tabel, `role_permissions` 104, versi skema 9) dan bersih dari data uji. Temuan baru yang ditutup sesi ini: **C-043** (`60-DEPLOYMENT.md` §3.1 menyalin Makefile yang menyimpang), **C-044** (hitungan audit 31 vs 32 FIXED). Log `prompts/P-024-2026-09-19-database-test-terpisah-bwdcs-test.md` |
| T-037 | Modul Document: unggah dokumen + versi, generator `document_number` `{PROJECT_CODE}-{NNN}` **di dalam transaksi**, unduh dengan audit, cakupan data yang sama dengan project | 2026-09-19 | Ketujuh endpoint `42-API.md` §4 hidup dan diuji dua lapis: `internal/service/document_number_test.go` (5 test penomoran: tiga test `70-TESTING.md` §3.5 + nomor per project + project di luar cakupan), `internal/service/document_service_test.go` (9 test) + `document_upload_limit_internal_test.go` (unit batas ukuran), `internal/handler/document_handler_test.go` (11 test HTTP). Bukti pada server nyata: `POST /documents` → **201** `DOC-UJI-001` lalu `DOC-UJI-002`; unggah multipart `BRD.pdf` → versi **1.0** (50 byte, `application/pdf`, SHA-256 64 heksadesimal, `file_key` sesuai ADR-0005), unggahan kedua → **1.1**; `GET /documents/:id/versions` terbaru dulu dengan penulisnya; unduh → **200** + `Content-Disposition` + `Content-Length: 50` dan isi **identik** (`cmp`); cakupan: user ber-izin `document:read` tetapi bukan anggota → daftar `total 0`, detail/versi/unduhan **404** (bukan 403), lalu terlihat sesudah menjadi anggota; Viewer tetap **403** untuk unggah/hapus (izin ≠ cakupan); `DELETE` → **200**, baris versi `0`, berkas hilang dari storage. Enam entri `audit_logs` (`DOCUMENT_CREATED` ×2, `DOCUMENT_VERSION_CREATED` ×2, `DOCUMENT_DOWNLOADED`, `DOCUMENT_DELETED`) ber-`entity_id` **nomor dokumen**. Ditemukan & ditutup di sesi yang sama: **C-039** (test JWT gagal acak), **C-040** (`checksum: "sha256:..."` tidak muat di `VARCHAR(64)`), **C-041** (normalisasi `projects.code` hanya di handler), **C-042** (komentar menjanjikan aturan tanpa sumber). Delapan kontrak yang diputuskan agen dicatat di `OPEN-QUESTIONS.md` **Q-016**; log `prompts/P-023-2026-09-19-modul-document-unggah-versi-dan-penomoran.md` |
| T-035 | Modul Project: `POST`/`GET`/`PATCH`/`archive` project, anggota project, dan **cakupan data anggota di kueri** (`44-SECURITY.md` §3.1.3) | 2026-09-19 | Kedelapan endpoint `42-API.md` §3 hidup dan diuji dua lapis: `internal/service/project_service_test.go` (14 test) + `internal/handler/project_handler_test.go` (10 test, termasuk 8 endpoint tanpa token → 401). Bukti pada server nyata: `POST /api/v1/projects` → **201** (`demo-prj` → `DEMO-PRJ`); user ber-izin `project:read` **tetapi bukan anggota** → daftar `meta.total=0` dan detail **404** (bukan 403); setelah dijadikan anggota → daftar memuat project itu; Administrator organisasi lain → **404** (isolasi tenant); kode duplikat → **409**; `PATCH` dengan `code` → **409**; `DELETE` owner dari anggota → **409**; role di luar himpunan tertutup → **422**. Cakupan diterapkan di `WHERE` (`ProjectScope`: organisasi + keanggotaan, atau seluruh organisasi bila role sistem `administrator`) — bukan di middleware. Audit `PROJECT_CREATED`, `PROJECT_UPDATED`, `PROJECT_ARCHIVED`, `PROJECT_MEMBER_ADDED`, `PROJECT_MEMBER_REMOVED` ditulis di transaksi yang sama (ADR-0011) dan diverifikasi di `audit_logs`. Dokumen: `42-API.md` §3 (+`Izin:` per endpoint, cakupan, 404 vs 403, 409) dan §1/§12, `40-TSD.md` §2.3-§2.6/§6, `44-SECURITY.md` §3.1.3, `50-FSD.md` §3.1-§3.3, `70-TESTING.md` §3.7/§4.1, `TRACEABILITY` FR-PROJ-01..07. Tiga kontrak yang saya putuskan sendiri dicatat di `OPEN-QUESTIONS.md` Q-015; log `prompts/P-022-...md` |
| T-005 | Auth module: login, JWT (dengan `jti`), middleware RBAC, rate limit, logout + daftar revokasi (ADR-0009) | 2026-09-18 | Login admin pertama **benar-benar terbukti**: `POST /api/v1/auth/login` → **200** + token (panjang 421, `expires_at` +24 jam), `GET /api/v1/auth/me` → 200 dengan **44 izin** (Administrator) dan `audit:read` benar, `POST /api/v1/auth/logout` → 200, lalu token yang sama → **401 `TOKEN_REVOKED`**; baris `token_revocations` (`reason=logout`) dan entri audit `LOGIN`/`LOGOUT` terverifikasi lewat `psql`. Kode: `internal/pkg/jwt` (HS256 + `jti` wajib, alg none/issuer lain/tanpa exp ditolak), `internal/pkg/response`, `internal/model`, `internal/repository` (user, `token_revocations` + cache TTL 30 detik + cleanup berkala, `system_settings`), `internal/service` (auth, `PermissionChecker` tanpa bypass, `AuditService` di transaksi pemanggil, `LoginGuard`), `internal/middleware` (Auth, RequirePermission, RateLimit, CorrelationID, Logger, CORS), `internal/dto`, `internal/handler` (auth + `router.go`), `cmd/server/main.go`. Test: jwt (9), middleware (10), service auth + RBAC (19 termasuk test `70-TESTING.md` §4.1), handler end-to-end (9) — semua lulus; cakupan `internal/service` 70.9%, `internal/jwt` 83.9%, `internal/middleware` 63.1%, `internal/handler` 57.4%. Batasan jujur: `logout_all` → `501 NOT_IMPLEMENTED` (temuan **C-033**, Q-013); login gagal tidak dapat masuk `audit_logs` (temuan **C-035**, Q-014). Temuan baru C-033..C-037; log `prompts/P-021-...md` |
| T-033 | Buat test `internal/config` hermetis terhadap environment pemanggil (viper `AutomaticEnv` selalu menang atas `.env`) | 2026-09-18 | Helper `clearEnv(t)` menambah/menghapus seluruh variabel yang dibaca `Load` sebelum tiap test (`APP_*`, `DB_*`, `JWT_*`, `STORAGE_*`, `REDIS_URL`, `ADMIN_*`), dipakai `setEnvBaseline` + tiga test yang tidak memakainya. Bukti: `go test ./... -p 1` hijau **baik** dengan `.env` ter-export (sebelumnya dua test gagal) maupun tanpa variabel itu |
| T-032 | Perbaiki temuan C-020: ganti cuplikan SQL trigger immutable yang tidak valid dengan pola PostgreSQL yang benar-benar jalan, lalu selaraskan testnya | 2026-09-18 | `44-SECURITY.md` §6 ditulis ulang: fungsi PL/pgSQL `prevent_audit_modification()` + `trg_audit_logs_append_only` (`BEFORE UPDATE OR DELETE`, row-level) + `trg_audit_logs_no_truncate` (`BEFORE TRUNCATE`, statement-level), tolak SQLSTATE `23001`; jalur pemeliharaan eksplisit `SET LOCAL bwdcs.audit_maintenance = 'on'`; `REVOKE` ditolak sebagai pengaman utama (aplikasi = owner tabel). Trigger **diikat ke migrasi `007`** (`41-DATABASE.md` §2.5/§4 — sebelumnya tidak ada migrasi yang memasangnya, jadi janji FR-AUDIT-03 tidak dapat dijalankan apa adanya); test baru `70-TESTING.md` §4.3 (6 test: UPDATE/DELETE/TRUNCATE ditolak, INSERT lolos, dua trigger terpasang, GUC harus opt-in) + catatan teardown §8; checklist `44-SECURITY.md` §8. Pola dijalankan & diuji pada PostgreSQL 16.10 sebelum ditulis ke dokumen. Memunculkan temuan baru **C-028**; log `prompts/P-019-...md` |
| T-004 | Migration `001`-`009` + seed role (`008`) + bootstrap admin (ADR-0010) | 2026-09-18 | `goose up` sukses (versi skema 9) dan `down-to 0` lalu `up` kembali bersih; `\dt` → **22 tabel** (21 tabel `41-DATABASE.md` §2 + `goose_db_version`), 104 baris `role_permissions` (44/30/18/12), 2 trigger append-only, FK `fk_documents_workflow_instance`, 5 `system_settings`; startup nyata: log `migrasi selesai` → `bootstrap admin pertama selesai` (org `MYORG`, user `admin`) → `GET /health` **HTTP 200**; admin pertama tersimpan sebagai bcrypt cost 12 dengan role `administrator` (login penuh menunggu `T-005`); test: `internal/migration` (13 test) + `internal/bootstrap` (10 test) lulus; temuan baru **C-029**, **C-030**, **C-031** ditutup dan **C-032** ditutup lewat **ADR-0018**; log `prompts/P-020-...md` |
| T-031 | Perbaiki temuan C-026 & C-027: dua kontrak infrastruktur yang dipakai kode skeleton tidak dapat dijalankan apa adanya | 2026-09-18 | `40-TSD.md` §2.4 `FileStorage.Save` memuat parameter `originalName` + key opaque + wajib sanitasi + larangan menimpa; `60-DEPLOYMENT.md` §5 memakai `storage.Ping`; implementasi `internal/pkg/filestorage/local.go` (sanitasi nama, tolak segmen `..`, `O_EXCL`) dan `internal/handler/health_handler.go`; test `local_test.go` (9 test, termasuk anti-traversal dan imutabilitas versi); temuan ditemukan saat menulis skeleton `T-003`; log `prompts/P-018-...md` |
| T-013 | Buat role `bwdcs` + database `bwdcs` di instance PostgreSQL 16.10 yang sudah berjalan (Postgres.app, port 5432) | 2026-09-18 | `PGPASSWORD=... psql -h localhost -p 5432 -U bwdcs -d bwdcs -Atc "select current_user || ' | ' || current_database()"` → `bwdcs | bwdcs`; database lain pada instance yang sama tidak disentuh; kredensial di `.env` lokal (tidak di-commit) |
| T-012 | Pasang `goose` (`go install github.com/pressly/goose/v3/cmd/goose@latest`) | 2026-09-18 | `goose version: v3.28.0` di `~/go/bin/goose`; dipanggil Makefile backend lewat target `migrate-up`/`migrate-down`/`migrate-status`. **Dipin ulang ke v3.24.1 pada P-020** agar sama dengan library yang dipakai saat startup (`ADR-0018` butir 3): v3.24.2+ menuntut Go ≥ 1.23 sedangkan toolchain mesin 1.22.5 |
| T-011 | Tambahkan Go, `$HOME/go/bin`, dan client psql 16 ke `PATH` profil shell | 2026-09-18 | Blok `BWDCS toolchain` idempoten di `~/.zshrc`; `go version` = 1.22.5 dan `psql --version` = 16.x dari login shell; PostgreSQL 14.6 Homebrew tidak diubah |
| T-003 | Backend skeleton: `go.mod`, `cmd/server/main.go`, `internal/config`, health endpoint `GET /health` | 2026-09-18 | `go build ./...`, `go vet ./...`, `gofmt -l .` bersih, `go test ./...` lulus (config 84.1%, filestorage 77.8%); server jalan dan `curl /health` → `{"status":"healthy"}` HTTP 200 (database + storage); module `bwdcs/backend`; `Makefile` backend (build/run/test/vet/migrate-*) |
| T-002a | Buat `.gitignore` (memuat `.env`, `storage/`, `bin/`, `node_modules/`) saat `git init` | 2026-09-18 | `git check-ignore -v .env` → `.gitignore:2:.env`; `git status` tidak menampilkan `.env` walau berkasnya ada; `backend/storage` dan `backend/bin` juga terabaikan |
| T-002 | Inisialisasi git repo + struktur folder `backend/`, `frontend/`, `scripts/` | 2026-09-18 | `git init -b main` (branch `main`); struktur backend persis `40-TSD.md` §2.0 (14 direktori + `.gitkeep` untuk yang masih kosong); `.editorconfig` baru; `frontend/README.md` menandai blokir `DESIGN.md`; `backend/` terisi skeleton (T-003) |
| T-028 | Tetapkan kontrak endpoint re-submit setelah revisi — lanjutkan instance yang sama (ADR-0016 butir 2) | 2026-09-18 | `42-API.md` §5 `POST /workflows/instances/:id/resubmit` (5 prasyarat, guard status dokumen + guard ADR-0015, efek, `document_status` pada endpoint daftar, jeda revisi pada `/actions`); `43-WORKFLOW.md` §4.6 baru + §4.2 langkah 4 (satu aksi per step = per siklus) + §4.5 butir 3; `40-TSD.md` §2.4 `Resubmit` + §6 route `workflow_instance:submit`; `50-FSD.md` §4.3/§5.2/§5.4; `70-TESTING.md` §3.6 (7 test); temuan baru **C-025** FIXED; tanpa perubahan skema; log `prompts/P-017-...md` |
| T-030 | Tetapkan format & pemberian `document_number` lewat ADR agar validasi migrasi `T-004` tidak dikarang agen (temuan C-016; sekaligus C-024) | 2026-09-18 | ADR-0017 (ACCEPTED): `{PROJECT_CODE}-{NNN}`, dibangkitkan server lewat `document_sequences` (`INSERT ... ON CONFLICT ... RETURNING`, satu transaksi), immutable, tanpa penomoran manual, `projects.code` permanen. `41-DATABASE.md` §2.2/§2.3/§3/§4, `42-API.md` §3/§4/§12, `50-FSD.md` §3.2/§4.2, `20-SRS.md` FR-DOC-02, `40-TSD.md` §2.1/§5.4, `44-SECURITY.md` §4.1, `90-AGENT-GUIDE.md` §3.1, `70-TESTING.md` §3.1/§3.5/§5.1, `51-UX.md` §6.2-§6.4, `IDEA.md`; ID hantu `FR-DOC-08` → `[FR-VER-01, FR-VER-04]` (C-024); log `prompts/P-016-...md` |
| T-029 | Lengkapi spec FSD untuk tiga halaman nav tanpa spec — Approvals, Reports, Administration > Workflows (temuan C-018) | 2026-09-18 | `50-FSD.md` §5.4 (Approvals = view workflow instance), §10.6 (Reports), §10.7 (Workflow Definition Management), penunjuk §5.1; `42-API.md` §5 endpoint daftar `GET /workflows/instances` + rujukan T-027→T-028 + catatan §11 → §10.7; `51-UX.md` §2.1 penunjuk; `AGENTS.md` baris routing; log `prompts/P-015-...md`; link check BROKEN 0 |
| T-027 | Selaraskan perilaku `request_revision` — kembali ke step sebelumnya, sesuai FR-WF-09 (temuan C-022) | 2026-09-18 | ADR-0016 (ACCEPTED); `43-WORKFLOW.md` §4.5 ditulis ulang (rollback satu langkah, batas bawah step 1, instance tetap `running`, re-submit lanjut instance yang sama), catatan §7 diperbaiki; `20-SRS.md` FR-WF-09 + penunjuk ADR; `42-API.md` §5 (`/workflows/submit` hanya untuk draft, aksi `request_revision`); `41-DATABASE.md` §2.4; `50-FSD.md` §8.1; `70-TESTING.md` §3.4; log `prompts/P-014-...md` |
| T-026 | Tetapkan pola optimistic locking transisi workflow instance + selaraskan skema dan dokumen (temuan C-005, sekaligus C-021 dan C-023) | 2026-09-18 | ADR-0015 (ACCEPTED); `41-DATABASE.md` §2.4 (`version`, `current_step_deadline`, indeks parsial, blok aturan transisi), `43-WORKFLOW.md` §4.1/§4.2/§4.5/§6/§7, `42-API.md` §5/§12 (`409 WORKFLOW_CONFLICT`), `40-TSD.md` §2.3/§2.4/§6, `70-TESTING.md` §3.3/§5.1/§8.1; log `prompts/P-013-...md` §6 (fence penutup ganda ditemukan & diperbaiki, link check BROKEN 0) |
| T-025 | Lengkapi kontrak endpoint untuk enam requirement yang belum punya endpoint (temuan C-012) | 2026-09-18 | Log `prompts/P-012-...md` §6: `42-API.md` §2 (`change-password`), §9 (filter audit), §10 baru (Reports/export), §11 (`reset-password`, `roles`, `organizations`, PATCH user dipersempit); `50-FSD.md` §2.2/§10.1/§10.3 diberi penunjuk endpoint; link check BROKEN 0 |
| T-022 | Hapus ringkasan environment variable usang dan tetapkan sumber tunggal konfigurasi runtime (temuan C-014) | 2026-09-18 | Log `prompts/P-010-...md` §6: `90-AGENT-GUIDE.md` §7 diganti penunjuk ke `60-DEPLOYMENT.md` §2.1 + `.env.example`; blok YAML compose kedua di `30-ARCHITECTURE.md` §5.1 dan kredensial `admin123` di `70-TESTING.md` §5.1 dihapus; `grep admin123 docs/design` kosong, `docker compose config -q` exit 0, link check BROKEN 0 |
| T-021 | Tetapkan matriks permission RBAC lengkap sebagai sumber migrasi `008_seed_default_roles.sql` (temuan C-017, sekaligus C-008) | 2026-09-18 | ADR-0014 (ACCEPTED); `44-SECURITY.md` §3.1 (17 resource × 15 action, 44 baris matriks, scoping §3.1.3), `40-TSD.md` §5.3/§2.2/§6, `41-DATABASE.md` §2.1/§4/§4.1, `70-TESTING.md` §4.1, `51-UX.md` §2.1, `50-FSD.md` §10.2, `20-SRS.md` FR-ROLE-03; verifikasi hitung matriks -> 44/30/18/12 = 104 baris |
| T-023 | Rekonsiliasi daftar endpoint API: satukan endpoint definisi workflow, lengkapi endpoint yang hilang, tetapkan `42-API.md` sebagai sumber tunggal (temuan C-011 & C-013) | 2026-09-18 | Log `prompts/P-011-...md` §6: `42-API.md` (2 endpoint ditambah, 1 dihapus, header sumber tunggal), `40-TSD.md` §6 jadi contoh wiring, `44-SECURITY.md` §3.3 diperbaiki; 12 fence markdown liar dibersihkan; link check BROKEN 0 |
| T-018 | Tetapkan lapisan audit log (temuan C-001): audit hanya di service, di dalam transaksi yang sama; handler tidak memanggil audit | 2026-09-18 | ADR-0011 (ACCEPTED); `40-TSD.md` §2.4/§2.6, `90-AGENT-GUIDE.md` §3.1, `12-DEVELOPMENT-WORKFLOW.md` §5, `30-ARCHITECTURE.md` §3.3 diselaraskan; log `prompts/P-008-...md` §6 |
| T-019 | Satukan struktur folder backend ke satu sumber (temuan C-002) | 2026-09-18 | ADR-0013 (ACCEPTED); `40-TSD.md` §2.0 pohon kanonik, pohon duplikat di `30-ARCHITECTURE.md` §3.2, `01-AGENT-WORKFRAME.md` §6, `90-AGENT-GUIDE.md` §2.1 diganti tautan; `70-TESTING.md` path test disesuaikan; log `prompts/P-008-...md` §6 |
| T-020 | Buat pemetaan status kanonik vs label + aturan "overdue" sebagai turunan (temuan C-003, sekaligus C-019) | 2026-09-18 | ADR-0012 (ACCEPTED); `50-FSD.md` §11 (4 tabel), `51-UX.md` §2.1, `43-WORKFLOW.md` §7, `20-SRS.md` FR-TASK-06, `41-DATABASE.md` §2.3/§2.5, `IDEA.md` 2.G, `80-ROADMAP.md` Phase 3; log `prompts/P-008-...md` §6 |
| T-000 | Foundation dokumentasi agen: ledger progress, protokol, ADR, workflow, skrip verifikasi dokumen | 2026-09-17 | Log `prompts/P-001-...md`; `bash scripts/check-doc-links.sh` -> BROKEN 0, exit 0 |
| T-016 | Audit kontradiksi seluruh dokumen desain (`AUDIT-001`) | 2026-09-17 | Log `prompts/P-007-...md`; 20 temuan berlabel severitas dengan bukti `file:line` di `docs/progress/audits/AUDIT-001-...md` |
| T-015 | Siapkan konfigurasi runtime: `.env.example` + `docker-compose.yml` sesuai `60-DEPLOYMENT.md` §2 | 2026-09-17 | `docker compose --env-file .env.example -f docker-compose.yml config -q` -> exit 0; render: app 8081->8080, postgres 5433->5432, `DB_HOST=postgres` |
| T-001 | Verifikasi tooling mesin kerja | 2026-09-17 | Log `prompts/P-004-...md`; hasil lengkap di `STATE.md` §2: Node 26.7.0, npm 11.19.0 OK; Go 1.22.5 ada di `/usr/local/go/bin` (tidak di PATH); PostgreSQL 14.6 < 16 dan instalasinya rusak; goose belum ada; Docker daemon mati; port 8080 dipakai `wms-backend` |
| T-008 | File handoff lintas agen `CONTINUE.md` (instruksi resume + snapshot) | 2026-09-17 | Log `prompts/P-002-...md`; terpasang di `AGENTS.md`, `01`, `02`, `12`, `90`, `docs/progress/README.md`. Kewajiban **menjaga** berkas itu selaras dicatat terpisah sebagai `T-046` |

---
| **T-063** | Rapikan dua keluhan nyata pada halaman yang sudah berdiri: **(1)** penyaring **Penanggung jawab** di Tasks tingginya berbeda dari penyaring lain, dan **(2)** **sidebar** memuat sub-item yang isinya hanya tab atau penyaring halaman, sehingga daftar menu panjang tanpa menambah kemampuan | 2026-09-23 | **DONE (P-047).** **(1) Sebabnya bukan warna atau ukuran, melainkan kolom.** Kolom Penanggung jawab memuat `<p>` catatan di bawah `<select>`-nya, sehingga kolom itu lebih tinggi daripada kolom lain dan kontrolnya terangkat sendiri dari baris ber-`items-end`; ketujuh kontrol lain berbaris di `top 220 → bottom 256`, sedangkan yang ini `top 197`. Catatannya **dipindah** ke blok catatan di bawah baris penyaring (bukan dihapus: sumber pilihan penanggung jawab tetap dinyatakan, termasuk batas C-063), sehingga seluruh kontrol pada satu garis berbagi satu tepi bawah. **(2) Sebabnya model navigasi.** `config/navigation.ts` mendaftarkan `subItems` untuk hampir setiap modul — delapan entri menu menghasilkan **lima belas** tautan, dan semuanya penyaring yang sudah ada di halamannya (`?view=mine`, `?status=in_review`, `?overdue=true`, …). `subItems` **dibuang** dari model dan `Sidebar.tsx` kembali merender satu tautan per modul; sub-navigasi dipindah ke halaman yang memiliki daftarnya: **Documents** mendapat baris tab `Semua / Milik saya / Pending Review / Revision Required / Approved` (sehingga penyaring `?view=mine` tetap dapat dibuka dan alasan Q-016 tetap terbaca di layar, bukan hanya lewat URL yang harus dihafal), sedangkan **Projects** tidak butuh tab karena "List" adalah halaman itu sendiri dan "Create" adalah tombol di header. `51-UX.md` §2.1 ditulis ulang mengikuti keputusan itu (kolom `Sub-items` → kolom halaman + sub-navigasinya). Penanda menu aktif kini memakai `requiresExactMatch`, yang **diturunkan dari daftar path** — sebelumnya `/reports` ikut menyala saat yang dibuka `/reports/audit` karena `NavLink` mencocokkan awalan. **Bukti test:** frontend **240 test / 24 berkas** hijau (`tsc --noEmit` + `eslint .` bersih, `vite build` 455,3 kB js / 23,3 kB css) dengan test baru yang mengunci bentuk menu (`navigation.test.ts`: tanpa `subItems`, aturan pencocokan persis, tidak ada dua menu yang saling menutupi), perilaku sidebar (`AppShell.test.tsx`: satu tautan per modul, tepat satu menu aktif, menu induk tidak menyala pada path bersarang), dan tab Documents (`Documents.test.tsx`: tab memetakan ke status kanonik + `Milik saya` dapat dibuka dari tab). **Bukti peramban nyata (`scripts/responsive-evidence.mjs`, bagian `navigation` yang baru):** sidebar `[Dashboard, Projects, Documents, Tasks, Approvals, Reports, Reports > Audit, Administration]`, **0** tautan menu berkueri, **0** tautan menu ke sub-halaman, **1** menu bertanda aktif; baris penyaring Task terbagi **2 garis** dengan **0** kontrol terangkat dan tinggi seragam (36px); tab Documents lima dengan satu penanda, dan klik tab `Milik saya` memindahkan penanda sekaligus memunculkan alasan Q-016; gulir mendatar `0` di 375/768/1024/1440px, 6 pengukuran tema, laci 375px membereskan dirinya. **Gigi dibuktikan dua kali:** cacat (1) dipasang kembali → skrip bukti **FAIL** tepat pada `kolom "Penanggung jawab" melanjutkan 23px di bawah kontrolnya`; aturan awalan lama dipasang kembali → test AppShell **gagal** pada `Reports` yang ikut `aria-current="page"`. Log `prompts/P-047-2026-09-23-sidebar-modul-dan-penyaring-sebaris.md` |
| **T-064** | Bangun **modul Workflow** di backend (`43-WORKFLOW.md`, `42-API.md` §5, ADR-0014/0015/0016) — **sembilan endpoint**: definisi + step, submit, aksi `approve`/`reject`/`request_revision`, re-submit setelah revisi, dan daftar/detail instance ber-cakupan. Modul terakhir yang tersisa di Phase 2 | 2026-09-23 | **DONE (P-048).** Sembilan endpoint hidup (`/workflows/definitions`, `/definitions/:id`, `/definitions/:id/steps`, `/submit`, `/instances`, `/instances/:id`, `/instances/:id/resubmit`, `/instances/:id/actions`), las lengkap `model`/`dto`/`repository`/`service`/`handler` + wiring `main.go`/`router.go`. **Aturan yang ditegakkan, bukan disalin:** instance lahir `version 0` di step 1 dan setiap transisinya lewat conditional `UPDATE` ber-guard **empat** kondisi — `rowsAffected = 0` → rollback + `409 WORKFLOW_CONFLICT` ber-`details` objek (ADR-0015); aksi step menuntut **dua** syarat sekaligus (role penanggung jawab step **dan** izin `workflow_instance:<action>`, temuan **C-073**); `request_revision` mundur **satu** step (batas bawah step 1) dan instance **tetap `running`**, sehingga service wajib membaca `documents.status = 'revision_required'` untuk menolak seluruh aksi selama jeda revisi (ADR-0016); re-submit melanjutkan instance yang sama tanpa menulis `workflow_actions` dan tanpa memindahkan step; keterlambatan step tetap **turunan** (`is_overdue`, ADR-0012). **Bukti test:** 29 fungsi (model 6, service 18, handler 5) — termasuk konkurensi §3.3, rollback §3.4, dan siklus re-submit; suite backend **268 test**, sembilan paket hijau, `gofmt` bersih. **Gigi dibuktikan mutasi:** kondisi `version` di `ApplyTransition` diganti sementara menjadi selalu-benar → `TestWorkflowTransitionGuardIsOptimistic` **gagal** tepat pada `menyentuh 1 baris, diharapkan 0 (ADR-0015 §6)`; dipulihkan lalu hijau. **Bukti server nyata:** `scripts/probe-workflow-module.py` **46/46 asersi PASS** dengan lima aktor login sungguhan (admin, manager, contributor, viewer, non-anggota) — definisi & `order` duplikat, submit + notifikasi `APPROVAL_REQUIRED` + audit, izin dari isi body, penunjukan step sebagai syarat tambahan (**administrator → `403`**), `version` basi → `409` ber-`details`, approve tengah/akhir, rollback satu step, jeda revisi, re-submit tanpa baris aksi baru, `is_overdue` turunan, cakupan non-anggota `total 0`/`404`, dan baseline kembali (audit 49/49, skema 11). **Tiga temuan ditutup di sesi yang sama:** **C-073** (pseudokode `43-WORKFLOW.md` §4.1 memuat `OR be admin` yang tidak ada di dokumen pengikat), **C-074** (`42-API.md` §5 menyebut "satu-satunya route" untuk himpunan yang `42-API.md` §6 dan `40-TSD.md` §6 sebut **dua**), dan **C-075** (`check-ledger.sh` melewatkan dua klaim hitungan test §3 karena polanya tidak mengenali bentuk `(N test: …)`, sementara baris yang sama masih menulis versi skema 10 padahal 11 — polanya diperluas, angkanya dikoreksi, gigi dibuktikan dengan memasang kembali nilai lama). **Tanpa perubahan skema** (versi goose tetap 11): tabel dan `notifications.type` bebas sudah cukup. Log `prompts/P-048-2026-09-23-modul-workflow-dan-probe-46-pass.md` |
| **T-065** | Rapikan penyaring rentang tenggat di halaman Tasks supaya kedua batasnya tidak terpisah baris — kekurangan yang dinyatakan sendiri `70-TESTING.md` §3.14e | 2026-09-23 | **DONE (P-049).** Sebabnya **bukan** warna atau ukuran, melainkan **susunan**: baris penyaring melipat (`flex-wrap`), sehingga dua kolom terpisah dapat jatuh ke garis berbeda — dan itu kini **terukur**, bukan diduga: bentuk lama mencatat batas awal di `top 220px` dan batas akhir di `289px` pada 1440px. Kedua isian berpindah ke **satu kelompok ber-peran `group` berlabel "Rentang tenggat"** (`flex flex-col gap-1.5 sm:flex-row sm:items-center sm:gap-2`): berdampingan pada lebar lebar, menumpuk **di dalam kelompok yang sama** pada lebar sempit. Pemisah "sampai" dibiarkan `aria-hidden` karena kedua isian sudah bernama sendiri lewat `aria-label`. **Ukuran menemukan cacat yang baru saja dibuat:** pasangan berdampingan pada 375px menuntut ~400px dan mendorong halaman menggulir **71px**; diperbaiki dengan menumpuknya di dalam kelompok, **bukan** dengan memotong lebar isian atau mengembalikannya ke dua kolom. Hasil terukur: 1440px `fromTop == toTop == 289`, `sameLine = true`, `sameGroup = true`; 375px `sameGroup = true`, `overflowX = 0`, tinggi kontrol 44px seragam; `contained = 0` di kedua lebar. **Test yang mengunci strukturnya:** `Tasks.test.tsx` — "menempatkan kedua batas rentang di satu kolom yang tidak melipat" (keduanya di dalam `role="group"` bernama "Rentang tenggat", barisnya `flex` **tanpa** `flex-wrap`, tiga anak). **Gigi dibuktikan** dengan memasang kembali bentuk lamanya → skrip bukti **FAIL tiga butir sekaligus** (`tidak berada di satu kelompok ber-label` pada 1440px **dan** 375px, plus `terpisah baris (atas 220px vs 289px)`). **Satu cacat pada alat ukurnya sendiri ikut ketahuan** dan ditutup: butir itu semula mencari isian lewat `aria-label` sehingga berbunyi "tidak ditemukan" — berhenti **mengukur** tepat pada bentuk yang harus ditangkapnya (kelas C-075); pencariannya dipindah ke **`id`** yang stabil dan pesannya dikoreksi. Verifikasi: `tsc --noEmit` + `eslint .` bersih, frontend **241 test / 24 berkas** hijau, `responsive-evidence OK`. **Tanpa perubahan backend, kontrak, izin, atau skema.** Tidak ada temuan audit baru. Log `prompts/P-049-2026-09-23-rentang-tenggat-satu-kelompok.md` |
| **T-066** | Jalankan pengukuran tata letak per lebar untuk halaman **Tasks** dan **Documents**, bukan hanya **Projects** | 2026-09-23 | **DONE (P-050).** Sapuan per lebar dan per tema di `scripts/responsive-evidence.mjs` kini berjalan untuk **setiap** halaman yang berdiri — `projects`/`tasks`/`documents` × `375/640/768/1024/1440` = **15 pengukuran tata letak** dan **18 pengukuran tema** — dan pada **setiap** lebar ikut diperiksa sidebar, bilah tab Documents, dan baris penyaring Task (ambang kesebarisannya kini **diturunkan** dari kelas `sm:` elemennya, 640px, bukan dari daftar lebar yang dirawat tangan). Perluasan cakupan itu langsung menemukan **tiga** kelas cacat: **(C-076)** kolom penyaring tidak dapat menyusut, sehingga `#penyaring-project-dokumen` selebar 403px pada viewport 375px membuat halaman menggulir mendatar 44px — sebabnya min-width otomatis item flex = min-content anaknya, dan min-content sebuah `<select>` ditentukan **teks pilihan terpanjang**, yaitu data pengguna, sehingga cacatnya muncul dan hilang tergantung isi database; **(C-077)** `@utility tap-target` hanya menetapkan `min-height`, sehingga tab sub-halaman **"Tim"** terukur **43x44px** — satu piksel di bawah ambangnya sendiri; dan **(C-078)** lima cacat pada alat ukurnya sendiri (cakupan yang tidak pernah meluas; argumen berspasi `--widths 375,768` yang **diam-diam diabaikan** oleh parser padahal bentuk itulah yang tertulis di komentar pemakaiannya; jeda tetap yang kalah balapan sehingga pernah melaporkan positif palsu; cacat "kolom melanjutkan di bawah kontrolnya" yang menandai kendali majemuk yang sah; dan probe stres yang menuduh kontrol yang salah karena melaporkan luas halaman, bukan pertambahannya). Perbaikannya: `min-w-0` pada **setiap** kolom ber-`select` di ketiga halaman (kolom rentang tenggat sengaja **tidak**, karena dua `datetime-local` memang tidak dapat menyusut), `tap-target` menetapkan **kedua sisi**, penantian "tata letak berhenti berubah", dan probe **pilihan sengaja panjang** pada setiap `select` penyaring — sehingga aturannya tidak lagi bergantung pada data yang kebetulan ada (probe itu menemukan cacat yang sama di halaman **Projects**, yang sudah disapu penuh sejak P-043: 65px). **Bukti:** `tsc --noEmit` + `eslint .` bersih, frontend **247 test / 25 berkas** hijau (naik dari 241/24; tiga test per halaman mengunci `min-w-0` dengan penjaga hampa, plus `src/styles/tap-target.test.ts` yang membaca `tokens.css`), `vite build` 455,5 kB js, `responsive-evidence OK` — **0px gulir mendatar** di kelimabelas pengukuran, **0** kontrol di bawah ambang, **0** pertambahan dari stres pilihan panjang. **Gigi dibuktikan** dengan memasang kembali dua cacat sekaligus → **FAIL enam butir** termasuk `Tim=43x44` dan `gulir mendatar 44px (#penyaring-project-dokumen)`. Lima pemeriksa hijau; audit **78/76/0/2**. **Tanpa perubahan backend, kontrak, izin, atau skema.** Log `prompts/P-050-2026-09-23-sapuan-per-lebar-setiap-halaman.md` |
| **T-069** | Terapkan pola kelompok ber-label P-049 pada penyaring tanggal Documents, lengkap dengan kontrak backend yang belum ada (`updated_from`/`updated_to` pada `GET /documents` — sisi tanggal Q-016), dan buktikan dengan pengukuran peramban | 2026-09-23 | **DONE (P-052).** Backend: interval **tertutup** `[updated_from, updated_to]`, instan RFC 3339 ber-offset, rentang terbalik `422 field=updated_to` — semantik sama persis dengan `due_from`/`due_to` tasks; terpasang di tiga lapis (handler/service/repository) dengan parameter yang **sama** mengalir ke tambalan `COUNT(*) OVER()` milik C-048 sehingga `meta.total` tidak dapat menyimpang. Halaman: kelompok `role="group"` berlabel "Rentang pembaruan", berdampingan dari `sm:` dan menumpuk dalam kelompok yang sama di bawahnya; **satu aksi terapkan** untuk pencarian dan rentang karena Enter pada form memicu submit tombol pertama (dua aksi terpisah membuat Enter di isian tanggal membuang draft rentangnya); konverter dipinjam dari modul task, validator terpisah karena kunci kontraknya `updated_*`. Pemeriksa baris digeneralisasi `measureFilterRow(formLabel)` untuk **ketiga** halaman pada setiap lebar, kelompok dicari lewat **struktur** + `expectedRanges` per halaman (Projects 0, Tasks 1, Documents 1) — tanpa itu menghapus `role="group"` tetap hijau. **Bukti:** service + HTTP test (11 kasus + 3 kasus 422 + terbalik), server nyata 422 dua bentuk pada binari terkini (probe pertama memakai binari lama dan membalas 200 — pelajaran run.md §3), peramban `sameGroup` + `sameLine` di 768/1024/1440px dan menumpuk dalam kelompok di 375px dengan `overflowX 0`; gigi dibuktikan (hapus `role="group"` → `FAIL` di keempat lebar). Dua cacat alat ukur lahir dan ditutup di sesi yang sama: tombol bersebelahan kelompok ber-label dituduh melanjutkan kolomnya (ekornya bahasa tata letak `items-end`, kini dibedakan dengan alasan tertulis), dan test batas gagal lagi pada sapuan penuh karena fixture memotong `created_at` ke detik (diperbaiki `RFC3339Nano` + kasus `2027`→`2020`). Verifikasi: backend **270 test** / 9 paket hijau, frontend **257 test / 25 berkas**, `tsc`/`eslint`/`vite build` bersih, `responsive-evidence OK`, enam pemeriksa hijau. **Tanpa perubahan skema, izin, atau migrasi** — versi goose tetap **11**. Log `prompts/P-052-2026-09-23-rentang-tanggal-dokumen-satu-kelompok.md` |
| **T-067** | Beri **halaman anak yang bukan modul** jalan masuk tanpa mengembalikannya ke sidebar: `Audit` (di bawah Reports) perlu rute dan tempat di baris sub-navigasi modul induknya, sementara `51-UX.md` §2.1 menetapkan sidebar muat **modul saja** | 2026-09-23 | **DONE (P-051).** `frontend/src/config/navigation.ts` memisahkan dua daftar: `navigation` (sidebar — modul saja, satu segmen, tanpa kueri) dan **`subPages`** (halaman bernavigasi yang **bukan** menu, dengan kolom `parent`), plus `visibleSubPages(parent, granted)` dan `subNavFamily(path, granted)` yang mengembalikan modul induk beserta halaman anak yang boleh dilihat aktor. `App.tsx` membangun rute pending dari **kedua** daftar, sehingga `/reports/audit` hidup walau modul induknya masih halaman penjelasan (R-24), dan `ModulePending.tsx` merender baris sub-navigasi keluarga itu — bentuk yang sama dengan tab Documents/Tasks, hanya tautannya menunjuk **path lain**, bukan kueri penyaring. **Bukti test:** `navigation.test.ts` (kedua daftar terpisah, setiap halaman anak punya induk yang ada dan berada di bawahnya, menu sidebar satu segmen tanpa kueri) dan `AppShell.test.tsx` (satu tautan per modul, tepat satu menu aktif, `Reports` **tidak** menyala saat `/reports/audit` dibuka, sub-halaman tetap punya jalan masuk). Verifikasi: `tsc --noEmit` + `eslint .` bersih, frontend **254 test / 25 berkas** hijau, `vite build` 456,31 kB js / 23,43 kB css. Aturan bentuknya kini **dijaga mesin** oleh `T-068`, bukan oleh test klien yang tidak berjalan di CI. Log `prompts/P-051-2026-09-23-pemeriksa-batas-sidebar.md` |
| **T-068** | Buat pemeriksa yang menolak item sidebar baru yang membawa kueri penyaring atau menunjuk sub-halaman, supaya aturan `51-UX.md` §2.1 tidak dapat dilanggar diam-diam (menutup **C-079**) | 2026-09-23 | **DONE (P-051).** Skrip baru `scripts/check-navigation.sh` — **tanpa Node, tanpa runner TypeScript** — membaca teks model navigasi dan tabel §2.1, lalu menolak: entri sidebar yang membawa kueri/karakter fragmen, entri sidebar **lebih dari satu segmen** (menunjuk sub-halaman), label menu bergaya remah (`X > Y`), halaman anak tanpa induk/berinduk hantu/di luar induknya, path atau label ganda, dan himpunan menu yang menyimpang dari tabel §2.1 — **dua arah**, ditambah tuntutan bahwa teks §2.1 masih menyatakan "modul saja". Keluarannya `navigation OK — 7 menu sidebar (tanpa kueri, satu segmen), 1 halaman anak ber-induk, 8 baris §2.1 cocok`. **Dua cacat pada skripnya sendiri ditutup di sesi yang sama:** penjaga hampa yang hanya menghitung jumlah menu tidak menyala saat kunci `label:` diganti nama sehingga parser memancarkan tujuh objek berisi kosong, dan skrip melaporkan **25 kegagalan yang semuanya menyesatkan** (**C-080**) — kini penjaga **bentuk** menolak objek tanpa label/path/induk dan berhenti dengan satu sebab yang benar. **Gigi dibuktikan dengan tujuh cacat disuntikkan sementara:** entri sidebar menunjuk sub-halaman, entri sidebar berkueri `?view=mine`, halaman anak berinduk hantu, menu baru di kode tanpa baris dokumen, halaman anak dipindah modulnya di dokumen, teks §2.1 dilunakkan, dan kunci `label:` diganti nama — ketujuhnya tertangkap, dan kedua berkas dipulihkan **byte-identik** (`diff -q`) sebelum sesi ditutup. Tersambung ke CI (`.github/workflows/ci.yml`, job `ledger`), `README.md` §4/§12.3, `02-AGENT-PROGRESS-PROTOCOL.md` §6.5/§8, `12-DEVELOPMENT-WORKFLOW.md` §8, `51-UX.md` §2.1, dan `AGENTS.md`. Log `prompts/P-051-2026-09-23-pemeriksa-batas-sidebar.md` |
| **T-070** | Bangun **halaman Approvals** di frontend (`50-FSD.md` §5.4, `42-API.md` §5) — daftar ber-tab (Pending/Approved/Rejected) dan detail `/approvals/:id` dengan aksi `approve`/`reject`/`request_revision` + penanganan `409 WORKFLOW_CONFLICT` | 2026-09-23 | **DONE (P-053).** Lapisan data `services/workflows.ts` + `queries/workflows.ts` mengikuti `42-API.md` §5 apa adanya (`listWorkflowInstances` dengan `status`+`scope`, `fetchWorkflowInstance`, `actWorkflowInstance` dengan `version`+`comment`, `resubmitWorkflowInstance`); halaman daftar `pages/Approvals/index.tsx` — tiga tab memetakan ke `status` kanonik (`running`→Pending + `scope=assigned_to_me`, `completed`→Approved, `rejected`→Rejected), `document_status=revision_required` dikecualikan **di klien** pada Pending (jeda revisi — tidak ada yang dapat bertindak sampai re-submit), kolom `document_number/title/project/step/deadline/status` dari `dto.WorkflowInstanceListItemResponse`, pagination dari `meta`; detail `pages/Approvals/Detail.tsx` — metadata instance, penanganan jeda revisi (`revision_required`→ tidak ada aksi), aksi `approve`/`reject`/`request_revision` dengan `version` dari instance + `comment` terpangkas, `409`→`alert`+`refetch`, riwayat `actions`. **Bukti test:** `services/workflows.test.ts` (8), `Approvals.test.tsx` (9), `ApprovalDetail.test.tsx` (7) — total frontend **282 test / 28 berkas** hijau, `tsc`/`eslint`/`vite build` bersih. Sidebar tetap 7 modul (tanpa kueri) dan `responsive-evidence` mencakup `/approvals` (`Pages: projects/tasks/documents/approvals`, `expectedRanges 0` untuk Approvals). Log `prompts/P-053-2026-09-23-halaman-approvals.md` |
| **T-071** | **Dashboard Analytics MVP — ADR-0026 + metric dictionary** (analisis P-054, desain tanpa migrasi): tetapkan KPI 6 + chart 8 yang hidup dari `documents`/`workflow_instances`/`workflow_actions`/`document_versions`/`tasks`/`audit_logs` tanpa `012`, dan backlog penuh (department, SLA, review_due/expiry/published, stage history) | 2026-09-23 | **DONE (P-055).** `52-DASHBOARD-ANALYTICS.md` v0.2.0 (9 bab, metric dictionary 16, pemetaan READY/BUTUH+ tabel kosakata 7→6) diikat **ADR-0026 ACCEPTED** — KPI 6 (total, active, pending, overdue, avg time, revised) + chart 8 (Status Dist, Volume Trend, Approval Trend, Funnel BWDCS 4 tahap, Pending Aging, Avg Time per Stage estimasi, By Category, Activity Trend) hidup tanpa migrasi; 2 KPI (Due for Review, SLA) + 5 chart (by Department, by Type khusus, SLA, Expiry Calendar, Obsolete) ditahan di `52-*` §4 / Q-DASH-01..04. Kontrak `GET /analytics/dashboard` satu endpoint agregat (`42-API.md` §13, `report:read`, interval tertutup, cakupan di kueri) — alternatif 8 endpoint ditolak. Papan +3 TODO tersisa (`T-072` API, `T-073` frontend, `T-074` backlog Phase 5). Log `prompts/P-055-2026-09-23-adr-0026-dashboard-mvp.md` |
| **T-072** | **Backend Analytics API** `GET /analytics/dashboard` (`42-API.md` §13, `52-DASHBOARD-ANALYTICS.md` §5 + ADR-0026) — satu endpoint agregat dengan `from`/`to` + `project_id`, izin `report:read` (`44-SECURITY.md` §3.1.2 Admin/Manager), cakupan `44-SECURITY.md` §3.1.3 di kueri (non-Admin hanya project yang diikutinya) — KPI 6 + chart 8 MVP tanpa tabel baru | 2026-09-23 | **DONE (P-056).** `internal/{dto/analytics,repository/analytics_repository,service/analytics_service,handler/analytics_handler}` + wiring `router.go:analytics`/`main.go`. KPI: `COUNT documents` + `active/pending/overdue workflows` + `AVG completed` + `revised month`; chart: `statusDist` `GROUP BY status`, `volumeTrend` per day, `approvalTrend` per week, `funnel` 5, `pendingAging` 5 bucket, `avgTimePerStage` estimasi, `byCategory`, `activityTrend` dari `audit_logs`. Query `from`/`to` RFC3339 interval tertutup, `project_id` UUID — `422` untuk bentuk salah, `403` untuk `viewer` tanpa `report:read`, `401` tanpa token. Cakupan via `projectScopePredicate` (JOIN `projects p`), `revised` via `document_versions.created_at >= monthStart`. **Bukti test:** `analytics_handler_test.go` (5 validasi + 1 sukses) — manager `200` dengan `kpis`+`charts` array (tidak nil), viewer `403`, tanpa token `401`, `from`/`project_id` salah `422`; `make test` 9 paket **272 test** (naik 2). Log `prompts/P-056-2026-09-23-analytics-api.md` |
| **T-073** | **Frontend Dashboard MVP** — KPI 6 + chart 8 + filter global `?from=&to=&project_id=` + drill-down ke daftar, memakai `recharts` ringan (ADR-0026) | 2026-09-23 | **DONE (P-057).** `services/analytics.ts` (`fetchDashboard` `from`/`to`/`project_id` + `validateDashboardRange` interval tertutup) + `queries/analytics.ts` + `pages/Dashboard/index.tsx` (KPI grid `Panel` mono, 8 chart `recharts` — Status Dist donut `Pie`, Volume Trend `Line`, Approval Trend stacked `Bar`, Funnel `Bar` 5, Pending Aging `Bar`, Avg Time per Stage horizontal `Bar`, By Category horizontal `Bar`, Activity Trend 4 `Line` — filter global `?from=&to=&project_id=` hidup di URL, `project_id` dari `useProjectList`, `report:read` guard, `recharts` `3.10.1`, tanpa `#hex` — token `var(--color-status-*)`); links drill-down ke `/documents`/`/approvals` dll. **Bukti test:** `services/analytics.test.ts` (5), `pages/Dashboard/Dashboard.test.tsx` (5) — KPI tampil, akses terbatas tanpa `report:read`, error `500` + muat ulang, filter `from`/`to` → `fetchDashboard`, empty `0`+`Belum ada dokumen`; total frontend **292 test / 30 berkas** hijau, `tsc`/`lint`/`vite build` bersih. Log `prompts/P-057-2026-09-23-dashboard-frontend.md` |
| **T-075** | **Kategori dokumen pada GET /documents** `?category_id=` (`50-FSD.md` §4.1, `42-API.md` §4 — Q-016, tanpa migrasi baru karena `document_categories` sudah ada `41-DATABASE.md` §2.3) | 2026-09-23 | **DONE (P-058).** `repository/document_repository.go` (`DocumentListFilter.CategoryID`, `documentListWhere` `$9`, `LIMIT $10 OFFSET $11`, `count` `$9`), `service/document_service.go` (`CategoryID`), `handler/document_handler.go` (`parseDocumentListQuery` `category_id` UUID → `422 field=category_id`), `dto/analyticsDto` tidak berubah; `frontend/src/services/documents.ts` (`category_id`), `frontend/src/services/documents.test.ts` (+1). **Bukti test:** `TestDocumentListCategoryFilter` (buat 2 kategori, 2 dokumen beda kategori → filter `cat1` `1`, fake `0`, bukan UUID `422 field=category_id`) — handler `200`; `make test` 9 paket **273 test** (naik 1), frontend **293 test / 30 berkas**. Docs `42-API.md` §4 (`?category_id=`) dan `50-FSD.md` §4.1 (Category kini hidup) diselaraskan. Sisa Q-016 hanya `owner` (menunggu Q-024). Log `prompts/P-058-2026-09-23-category-filter.md` |
| **T-076** | **Notifikasi in-app** `GET /notifications` + `PATCH /:id/read` + `POST /read-all` (`42-API.md` §8, cakupan `user_id = user`, tanpa migrasi baru) | 2026-09-23 | **DONE (P-059).** `model/notification.go` + `repository/notification_repository.go` (`List` `COUNT(*) OVER()` + `is_read` filter, `MarkRead`/`MarkAllRead` `user_id = $1`), `service/notification_service.go` (`List`/`MarkRead`/`MarkAllRead`), `handler/notification_handler.go` (`List` `is_read`/`page`/`limit` `422`, `MarkRead` `id` UUID `422`/`404`, `MarkAllRead`), wiring `router.go` (`/notifications` `notification:read`/`update`) + `main.go` + `main_test.go` `engineParts.notification`. **Bukti test:** `notification_handler_test.go` ( `is_read` bukan boolean `422`, tanpa token `401`, `is_read=true` `200`, `id` bukan UUID `422`, bukan milik user `404`, milik user `200`, `read-all` `200`) — 2 `func Test` (7 subtest); `make test` 9 paket **275 test** (naik 2). Log `prompts/P-059-2026-09-23-notification-api.md` |
| **T-077** | **Audit read** `GET /audit` (`42-API.md` §9, `audit:read` Admin, filter `actor_id`/`action`/`entity`/`date_from`/`date_to`, terbaru dulu) | 2026-09-23 | **DONE (P-060).** `model/audit.go` + `repository/audit_repository.go` (`List` `COUNT(*) OVER()` + filter `actor_id`/`action`/`entity`/`entity_id`/`date_from`/`date_to`, `LEFT JOIN users`), `service/audit_read_service.go` (`List`), `handler/audit_handler.go` (`List` `page`/`limit` `422`, `actor_id` UUID `422`, `date_from`/`date_to` `422`), wiring `router.go` (`/audit` `audit:read`) + `main.go` + `main_test.go` `engineParts.audit`. **Bukti test:** `audit_handler_test.go` (`page` bukan angka `422`, `actor_id` bukan UUID `422`, tanpa token `401`, viewer `403`, admin `200` dengan `data` array) — 2 `func Test` (6 subtest); `make test` 9 paket **277 test** (naik 2). Log `prompts/P-060-2026-09-23-audit-read.md` |
| **T-078** | **Project Detail — Documents tab inline** (daftar dokumen `?project_id=` di dalam tab Project, `Panel` + `DataTable`) | 2026-09-23 | **DONE (P-062).** `pages/Projects/ProjectDetail.tsx` — `builtTabs` `+documents`, `pendingTabs` `4→3` (documents keluar, ` - `), `useDocumentList({ project_id: id }, { enabled: tab==="documents" })` + `documentColumns` (`document_number`, `title`→`/documents/:id`, `status` `StatusBadge`, `latest_version`, `owner`) + `Panel` `GET /documents?project_id=` + `DataTable` `loading`/`error`/`meta`/`emptyState` `Belum ada dokumen`. Nav `Documents` tanpa `belum`. **Bukti test:** `ProjectDetail.test.tsx` — `menampilkan tab Documents sebagai built` (`Belum ada dokumen` + `listDocuments` `project_id: p1`, `menyebut bagian yang belum dibangun untuk tab yang masih pending` `T-079`); total frontend **294 test / 30 berkas** hijau, `typecheck`/`lint`/`build` OK, `antislop-refs OK`. Log `prompts/P-062-2026-09-23-project-documents-tab.md` |
| **T-079** | **Project Detail — Tasks tab inline** (daftar task `?project_id=` di dalam tab Project, `Panel` + `DataTable`) | 2026-09-23 | **DONE (P-063).** `pages/Projects/ProjectDetail.tsx` — `builtTabs` `+tasks`, `pendingTabs` `3→2` (tasks keluar), `useTaskList({ project_id: id }, { enabled: tab==="tasks" })` + `taskColumns` (`title`→`/tasks/:id`, `status` `StatusBadge`, `due_date` `OverdueFlag`, `assignee`) + `Panel` `DataTable` `loading`/`error`/`meta`/`emptyState` `Belum ada tugas`. Nav `Tasks` tanpa `belum`. **Bukti test:** `ProjectDetail.test.tsx` — `menampilkan tab Documents sebagai built` + `menyebut bagian yang belum dibangun untuk tab yang masih pending` (`T-080`), `listTasks` mock; total frontend **294 test / 30 berkas** hijau, `typecheck`/`lint`/`build` OK. Log `prompts/P-063-2026-09-23-project-tasks-tab.md` |
| **T-080** | **Project Detail — Workflow tab inline** (daftar workflow instances per project `?project_id=`) | 2026-09-23 | **DONE (P-064).** `repository/workflow_repository.go` (`WorkflowInstanceFilter.ProjectID`, `ListInstances`/`countInstances` `AND ($8::uuid IS NULL OR p.id = $8)`), `service/workflow_service.go` (`ProjectID`), `handler/workflow_handler.go` (`parseWorkflowInstanceQuery` `project_id` UUID `422`), `42-API.md` §5 Query `?project_id=`, `frontend/src/services/workflows.ts` (`project_id`), `frontend/src/queries/workflows.ts` (`enabled`), `pages/Projects/ProjectDetail.tsx` (`builtTabs` `+workflow`, `pendingTabs` `3→1` (workflow keluar), `useWorkflowInstances({ project_id: id })` + `workflowColumns` 4 kolom + `Panel` `DataTable`). **Bukti test:** `workflow_handler_test.go` `TestWorkflowListProjectFilter` (`project_id` bukan UUID `422 field=project_id`, valid `200`), `ProjectDetail.test.tsx` (`workflow` built `Panel`/`DataTable`); total backend **278 test** (naik 1), frontend **294 test / 30 berkas** hijau, `typecheck`/`lint`/`build` OK. Log `prompts/P-064-2026-09-23-project-workflow-tab.md` |
| **T-081** | **Project Detail — Activity tab inline** (audit per project `?project_id=`) | 2026-09-23 | **DONE (P-065).** `repository/audit_repository.go` (`AuditListFilter.ProjectID`, `WHERE metadata->>'project_id' = $7::text OR (entity='project' AND entity_id=$7::text)`), `handler/audit_handler.go` (`project_id` UUID `422`), `42-API.md` §9 Query `?project_id=`, `frontend/src/services/audit.ts` (`project_id`), `frontend/src/queries/audit.ts` (`useAuditList`), `pages/Projects/ProjectDetail.tsx` (`builtTabs` `+activity`, `pendingTabs` `1→0`, `useAuditList({ project_id: id })` + `activityColumns` 4 kolom + `Panel` `DataTable`). **Bukti test:** `audit_handler_test.go` `TestAuditListProjectFilter` (`project_id` bukan UUID `422`, valid `200`), `ProjectDetail.test.tsx` (`activity` built `Belum ada aktivitas` + `listAudit` `project_id: p1`); total backend **279 test** (naik 1), frontend **294 test / 30 berkas** hijau. Log `prompts/P-065-2026-09-23-project-activity-tab.md` |
| **T-089** | **Polish filter & tabel & tombol Create** — filter kontras (`bg-surface-raised` `border` `p-3` `shadow-sm`, lebar konsisten `flex-1 min-w-[140px] max-w-[200px]`), tabel Tasks `density comfortable` + kolom sempit, tombol Create `primary` kontras (`bg-accent` `shadow-sm`) | 2026-09-23 | **DONE (P-066).** `Button.tsx` `primary` `bg-accent text-paper-000 border-accent-strong shadow-sm font-semibold`, filter `flex flex-wrap gap-3 rounded-panel border bg-surface-raised p-3 shadow-sm` + kolom `flex-1 min-w-[140px] max-w-[200px]`, Tasks `due_date 190→140` `assignee 150→120` `project 190→130` + `density comfortable` (`h-11`). **Bukti:** `typecheck`/`lint` OK, `test:run` 294/30 PASS, `build` 893kB, `responsive-evidence` 16×4 OK, `antislop-refs OK`. Log `prompts/P-066-2026-09-23-polish-filter-tabel-tombol.md` |
| **T-090** | **Perbaikan create button dark contrast + dashboard tabs + chart dark contrast + audit antislop** — create button `primary` dari `bg-accent text-paper-000` (dark 1.74:1 FAIL) → `bg-text text-surface-raised border-text` (light 17.8:1 dark 14.1:1 setara Daftar Project); Dashboard 8 chart grid → 3 tab Dokumen (Sebaran,Funnel,Kategori) / Workflow (Volume,Approval,Activity) / Antrian (Aging,Avg Time) + per chart `stroke var(--line)` `tick var(--text-muted)` `Tooltip var(--surface-raised)`; audit 38 aturan R-01..R-38 | 2026-09-24 | **DONE (P-067).** `Button.tsx` primary `bg-text text-surface-raised border-text hover:opacity-90 shadow-sm font-semibold` (dark 14.1:1), `Dashboard/index.tsx` state `chartTab` + 3 tab `role=tablist aria-selected` + konstanta `chartTick/chartGridStroke/tooltipStyle` + 8 chart dibagi 3/3/2 + dark contrast per chart (`CartesianGrid var(--line)`, `X/Y tick var(--text-muted)`, `Tooltip bg var(--surface-raised)`); `Dashboard.test.tsx` tab grouping (Dokumen→Funnel, Workflow→Volume+Activity, Antrian→Aging) + axe; `Tasks.test.tsx` `WEB` title. **Bukti:** `typecheck` OK, `lint` OK, `test:run` 294/30 PASS, `build` 895kB, `make test` 279, `check-ledger OK (279)`, `BROKEN 0`, `readme-facts 46`, `api-contract 115/55`, `antislop-refs OK (38 aturan,212 rujukan)`, `navigation OK (7 menu,1 anak)`; kontras terhitung `dark accent/white 1.74 FAIL → text/surface-raised 14.1 PASS`. Log `prompts/P-067-2026-09-24-perbaikan-create-button-dashboard-tabs-dan-kontras.md` |
| **T-082** | **Reports Export** `GET /reports/export?type=projects|documents|tasks&format=csv` (`report:export`, cakupan `44-SECURITY.md` §3.1.3, audit `REPORT_EXPORTED`) | 2026-09-24 | **DONE (P-068).** `dto/report_dto.go`, `service/report_service.go` (`REPORT_EXPORTED`, `Export` CSV header=kolom tabel 50-FSD, `systemScope`/`taskScope` Limit 10000, audit `pool.Begin` tx), `handler/report_handler.go` (validasi type `projects/documents/tasks` + format `csv` + project_id UUID → 422, 401/403, 200 `text/csv` `Content-Disposition: attachment; filename="bwdcs-<type>-<YYYYMMDD>.csv"`), `router.go` 1 reports route (47 total, 1 reports), `main.go` wiring, `main_test.go` report engineParts, `report_handler_test.go` (401/403/422 + 200 csv header + Disposition + audit). **Bukti:** `go vet/build` OK, `make test` 9 paket **281 test** (naik 2), `check-ledger OK 281`, `readme-facts OK 47 (1 reports)`, `api-contract OK 122/56`, `BROKEN 0`, `antislop-refs OK`, `navigation OK`. Log `prompts/P-068-2026-09-24-reports-export.md` |
| **T-083** | **Administration Users/Roles/Organizations** `GET /admin/users`, `POST /admin/users`, `GET /admin/roles`, `GET /admin/organizations` (`user:read`/`create`, `role:read`, `organization:read` Admin saja) | 2026-09-24 | **DONE (P-069).** `dto/user_dto.go` DTO admin; `service/user_service.go` `ListUsers` (search ILIKE, page/limit, total, roles per user), `CreateUser` (trim, password 8+, role_ids UUID exists, bcrypt 12, tx users+user_roles+audit USER_CREATED), `ListRoles`/`ListOrganizations`; `handler/user_handler.go` 4 handlers (ListUsers 200 meta, CreateUser 201/409/422, ListRoles/Organizations 200) + `router.go` 5 admin routes (51 total, 1→5 admin) + `user_handler_test.go` (401/403, list 200 meta, create 201+search, roles 200 contains administrator). **Bukti:** `go vet/build` OK, `make test` 9 paket **283 test** (naik 2), `ledger OK 283`, `readme-facts OK 51 (5 admin)`, `api-contract OK 126/56`, `BROKEN 0`. Log `prompts/P-069-2026-09-24-administration-users-roles-orgs.md` |

## 2. Backlog Fase Berikutnya (belum dipecah jadi task)

| Fase | Cakupan | Sumber |
|---|---|---|
| 1 | Project, Document, DocumentVersion, storage abstraction, download, search | `80-ROADMAP.md` Phase 1 |
| 2 | Workflow definition/instance/action, notifikasi, audit | `43-WORKFLOW.md` |
| 3 | Task, overdue detection, comment | `50-FSD.md` §6, §7 |
| 4 | Frontend SPA seluruh halaman | `50-FSD.md`, `51-UX.md`, `DESIGN.md` |
| 5 | Export CSV, audit UI, performance, deployment produksi | `60-DEPLOYMENT.md` |
