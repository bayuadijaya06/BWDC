# TRACEABILITY — Requirement → Desain → Kode → Test

**Protokol:** `docs/design/02-AGENT-PROGRESS-PROTOCOL.md`
**Aturan:** setiap kali sebuah requirement mulai dikerjakan, tambahkan/ubah barisnya di sini. Satu requirement boleh punya banyak baris (per lapisan). Status akhir sebuah requirement hanya `DONE` bila kolom Bukti terisi.

**Status:** `TODO` (belum disentuh), `IN PROGRESS`, `PARTIAL` (kode ada, test belum), `DONE` (kode + test + bukti).

---

## 1. Functional Requirements

Format kolom: ID requirement dari `20-SRS.md` §3, bagian desain yang mengatur, file implementasi, test yang menutup, status, bukti.

| Requirement | Desain | Implementasi | Test | Status | Bukti |
|---|---|---|---|---|---|
| FR-AUTH-01 | `40-TSD.md` §5.2, `44-SECURITY.md` §2.1, `42-API.md` §2 (`POST /auth/login`) | `internal/service/auth_service.go` (bcrypt verify, akun nonaktif ditolak, audit `LOGIN`), `internal/handler/auth_handler.go`, `internal/repository/user_repository.go` | `internal/service/auth_service_test.go::TestLoginSuccess`, `TestLoginWrongPassword`, `TestLoginUnknownUsernameUsesSameError`, `internal/handler/auth_handler_test.go::TestAuthFlowEndToEnd` | DONE | P-021: admin pertama login via HTTP `POST /api/v1/auth/login` → **200** + token; `GET /api/v1/auth/me` → 200 dengan 44 izin (`docs/progress/prompts/P-021-*.md`) |
| FR-AUTH-02 | `40-TSD.md` §5.2 (`JWTClaims` + `jti`), `44-SECURITY.md` §2.2 — ADR-0009 | `internal/pkg/jwt/jwt.go` (HS256, `iss=bwdcs`, `exp` wajib, `jti` UUID per token) | `internal/pkg/jwt/jwt_test.go` (roundtrip, jti unik, tanpa jti ditolak, alg none ditolak, issuer lain ditolak) + `TestLoginSuccess` | DONE | P-021: token hasil login divalidasi ulang oleh penerbitnya; `jti` tampil di metadata audit `LOGIN` |
| FR-AUTH-03 | `60-DEPLOYMENT.md` §2.1 (`JWT_EXPIRY`, default 24h), `44-SECURITY.md` §2.2 | `internal/config/config.go` (JWT.Expiry), `internal/pkg/jwt/jwt.go` (`exp` = iat + Expiry) | `internal/pkg/jwt/jwt_test.go::TestGenerateAndValidate`, `TestValidateRejectsExpired`, `TestValidateRejectsExpiryMissing` | DONE | P-021: `expires_at` pada response login = +24 jam; token tanpa `exp` ditolak |
| FR-AUTH-04 | `44-SECURITY.md` §2.2, `42-API.md` §2 (`POST /auth/logout`) — ADR-0009 + **ADR-0021** | `internal/service/auth_service.go` (Logout: revokasi `jti` + seluruh token lama + audit satu transaksi), `internal/repository/token_revocation_repository.go` (kedua sebab dalam satu kueri; cache TTL 30 detik; `RevokeAllForUser`), `internal/middleware/auth.go` (`401 TOKEN_REVOKED`) | `internal/service/auth_service_test.go::TestLogoutRevokesToken`, `TestLogoutAllInvalidatesOtherTokens`, `TestLoginRightAfterLogoutAllStillWorks`, `internal/middleware/auth_test.go::TestAuthMiddlewareRejectsRevokedToken`, `TestAuthMiddlewareChecksSessionAgainstTokenIssueTime`, `internal/handler/auth_handler_test.go::TestAuthFlowEndToEnd`, `TestLogoutAllEndToEnd` | **DONE** | P-021: setelah logout, token yang sama → **401 TOKEN_REVOKED**; baris `token_revocations` (`reason=logout`) dan audit `LOGOUT` terverifikasi di database. **P-030 (`T-040`) menutup batasannya:** `logout_all: true` → **200** dan mencabut seluruh sesi user itu lewat `users.tokens_invalid_before` (ditulis `date_trunc('second', NOW())`, C-053) **plus** `jti` request; token hasil login ulang tepat sesudahnya tetap `200`; `501 NOT_IMPLEMENTED` tidak ada lagi di `42-API.md` §12 maupun `internal/pkg/response` |
| FR-AUTH-05 | `44-SECURITY.md` §2.1, `docs/adr/0010-first-run-bootstrap.md` butir 7 (bcrypt cost 12) | `backend/internal/bootstrap/bootstrap.go` (hash admin pertama), `internal/service/auth_service.go` (verifikasi + perbandingan boneka untuk username tak dikenal) | `internal/bootstrap/bootstrap_test.go::TestEnsureAdminFirstRun_CreatesOrgAdminAndRole` (assert `bcrypt.Cost == 12`), `internal/service/auth_service_test.go::TestLoginSuccess` | DONE | P-021: hash cost 12 diuji dari dua arah — dibuat bootstrap dan diverifikasi login; tidak ada plaintext di database/log |
| FR-AUTH-06 | `44-SECURITY.md` §2.3, `42-API.md` §12 (`429`, `423`) — **ADR-0022** | `internal/repository/login_attempt_repository.go` (menulis setiap percobaan + menghitung kegagalan menurut jam database), `internal/service/auth_service.go` (`applyLockout`: ambang dari `system_settings`, jendela 15 menit konstanta kode), `internal/repository/user_repository.go` (`LockUntil`/`ClearLock`), `internal/middleware/rate_limit.go` (batas **per alamat klien** → `429`) | `internal/service/auth_service_test.go::TestLoginLockoutAfterThreshold`, `TestLockExpiresWithoutIntervention`, `internal/handler/auth_handler_test.go::TestLoginLockedReturns423`, `internal/middleware/auth_test.go::TestRateLimitMiddleware` | **DONE** | P-021: dulu batas percobaan per username di memori. **P-030 (`T-041`) menggantinya dengan auto-lock akun:** percobaan yang **melewati** ambang dibalas `423 LOCKED` + header `Retry-After` + `details.retry_after_seconds`, password benar saat terkunci **juga** `423` (status akun dinilai sebelum password), dan `429` kini murni batas **laju per alamat klien** (C-054). `login_guard.go` **dihapus** — tidak ada lagi keadaan pembatas di proses |
| FR-AUTH-07 | `44-SECURITY.md` §2.3 (akun dinonaktifkan), `42-API.md` §11 (`PATCH /admin/users/:id`) | `internal/service/auth_service.go` (`is_active=false` ditolak `403 ACCOUNT_INACTIVE` sebelum password dinilai) | `internal/service/auth_service_test.go::TestLoginInactiveAccountRejected`, `internal/handler/auth_handler_test.go::TestLoginInactiveAccountForbidden` | PARTIAL | P-021: sisi login tertutup. Endpoint Administrator untuk mengaktifkan/menonaktifkan akun **belum ada** (modul admin menyusul); auto-lock yang dapat dibuka Administrator **sudah berjalan** (`POST /admin/users/:id/unlock`, ADR-0022, `T-041`, P-030 — C-009 `FIXED`) |
| FR-ROLE-01 | `41-DATABASE.md` §2, `44-SECURITY.md` §3.1.2 (4 role dasar) — ADR-0014 | `backend/internal/migration/008_seed_default_roles.sql` (4 baris `roles`), `internal/repository/user_repository.go` (`Roles`) | `internal/migration/migration_test.go::TestSeedRolePermissions_RowCounts`, `internal/service/auth_service_test.go::TestLoginSuccess` (role terbaca saat login), `internal/handler/auth_handler_test.go::TestAuthFlowEndToEnd` | PARTIAL | P-021: role user terbaca di jalur nyata (login + `/auth/me`); halaman/endpoint pengelolaan role (`FR-ROLE-04`) belum |
| FR-ROLE-02 | `41-DATABASE.md` §2 (`user_roles`) — ADR-0014 | `backend/internal/migration/002_create_users_and_roles.sql` (tabel `user_roles`) + `backend/internal/bootstrap/bootstrap.go` (penetapan role admin pertama) | `backend/internal/bootstrap/bootstrap_test.go::TestEnsureAdminFirstRun_CreatesOrgAdminAndRole` | PARTIAL | P-020: relasi user→role terbukti lewat bootstrap; pengelolaan role lewat API menunggu `T-005`/`FR-ROLE-04` |
| FR-ROLE-03 | `44-SECURITY.md` §3.1 (matriks 44 baris) + §3.2 (tanpa bypass Administrator), `41-DATABASE.md` §4 (seed `008`, 104 baris) — ADR-0014 | `backend/internal/migration/008_seed_default_roles.sql` (104 baris `role_permissions`), `internal/repository/user_repository.go` (`HasPermission`), `internal/service/permission_checker.go`, `internal/middleware/permission.go` (`RequirePermission`) | `internal/migration/migration_test.go` (4 test matriks), `internal/service/permission_checker_test.go` (test `70-TESTING.md` §4.1: 10 kasus + hitungan 44/30/18/12 + tanpa bypass + pasangan di luar kosakata), `internal/middleware/auth_test.go::TestRequirePermissionAllows/Denies` | DONE | P-021: izin diperiksa langsung dari tabel `role_permissions`; tidak ada cabang khusus Administrator di kode (temuan C-037 ditutup). **P-040:** `scripts/check-api-contract.sh` menambah pemeriksaan mesin atas pemetaan endpoint → pasangan izin — **55 endpoint** di `42-API.md` (48 baris `Izin:` di bloknya + 7 lewat tabel izin bab §4) terhadap **44 pasangan** matriks, termasuk **18 pasangan** di `RequirePermission` `router.go`; `T-024` ditutup dan tidak lagi berupa hitungan manual |
| FR-ROLE-04 | `44-SECURITY.md` §3.1.2 (`user_role:manage`), `42-API.md` §11 (`PUT /admin/users/:id/roles`) — ADR-0014 | — | — | TODO | — |
| FR-AUTH-08 | `42-API.md` §11 (`POST /admin/users/:id/reset-password`), `50-FSD.md` §2.2 — ADR-0009 + **ADR-0021** | `internal/repository/user_repository.go` (`UpdatePasswordHash`); mekanisme pencabutan seluruh sesi **sudah ada** (`users.tokens_invalid_before` + `RevokeAllForUser`, P-030) | — (endpoint reset-password Administrator belum terdaftar; menunggu modul admin) | **PARTIAL** | P-026: Q-013 `RESOLVED` (ADR-0021). P-030 (`T-040`): kolom dan mekanisme pencabutannya **sudah terpasang**; yang tersisa hanya route + handler |
| FR-AUTH-09 | `42-API.md` §2 (`POST /auth/change-password`), `50-FSD.md` §2.2 — ADR-0009 + **ADR-0021** | `internal/service/auth_service.go` (`ChangePassword`: verifikasi `old_password` lebih dulu, lalu hash + `RevokeAllForUser` + audit `PASSWORD_CHANGED` dalam satu transaksi, token pengganti diterbitkan **sesudah commit**), `internal/handler/auth_handler.go` (`ChangePassword` + `writeChangePasswordError`), `internal/dto/auth_dto.go` (`ChangePasswordRequest`/`Response`), `internal/handler/router.go` (`POST /auth/change-password`, hanya autentikasi) | `internal/service/auth_service_test.go::TestChangePasswordKeepsCurrentSession`, `TestChangePasswordRejectsWrongCurrentPassword`, `TestChangePasswordEnforcesNewPasswordRules`, `internal/handler/auth_handler_test.go::TestChangePasswordEndToEnd`, `TestChangePasswordErrorMapping` | **DONE** | **P-034 (`T-034`) menutup baris ini.** Aturan "seluruh sesi lain dicabut, sesi yang dipakai tetap hidup" dinyatakan lewat `users.tokens_invalid_before` **plus** token pengganti yang `iat`-nya jatuh setelah titik pencabutan — bukan `keepJTI`, yang memang tidak dapat dinyatakan oleh penanda per user (Q-013). Bukti HTTP: token pengganti → `/auth/me` `200`; token yang dipakai memanggil endpoint ini dan token perangkat lain → `401 TOKEN_REVOKED`; sesi user **lain** tidak tersentuh. `POST /auth/refresh` dari `T-034` dipisah ke `T-045` (Q-020) |
| FR-ORG-02 | `docs/adr/0010-first-run-bootstrap.md` (bootstrap organisasi + admin pertama), `41-DATABASE.md` §2.1 (`users.organization_id NOT NULL`) | `backend/internal/migration/001_create_organizations.sql`, `backend/internal/migration/002_create_users_and_roles.sql`, `backend/internal/bootstrap/bootstrap.go` | `backend/internal/bootstrap/bootstrap_test.go::TestEnsureAdminFirstRun_CreatesOrgAdminAndRole` | PARTIAL | P-020: setiap user wajib punya organisasi (FK NOT NULL) dan organisasi pertama dibuat bootstrap; dibuat sebagai baris baru karena FR-ORG-02 sebelumnya belum punya baris traceability |
| FR-ORG-03 | `42-API.md` §11 (`POST`/`PATCH /admin/organizations`) | — | — | TODO | — |
| FR-PROJ-01 | `50-FSD.md` §3.2, `42-API.md` §3 (`POST /projects`), `44-SECURITY.md` §3.1.2 (`project:create` = Administrator + Manager) | `internal/service/project_service.go` (`Create`), `internal/handler/project_handler.go`, `internal/repository/project_repository.go`, `internal/dto/project_dto.go` | `internal/service/project_service_test.go::TestProjectCreateRecordsOwnerMembershipAndAudit`, `TestProjectCodeUniquePerOrganization`, `internal/handler/project_handler_test.go::TestCreateProjectEndToEnd`, `TestCreateProjectRequiresPermission` (viewer & contributor → 403) | DONE | P-022: `POST /api/v1/projects` pada server nyata → **201** (`demo-prj` dinormalisasi menjadi `DEMO-PRJ`), viewer → **403 FORBIDDEN**; audit `PROJECT_CREATED` terverifikasi di `audit_logs`. **Lapisan UI (P-041):** `src/pages/Projects/CreateProjectDialog.tsx` mengirim bentuk §3.2 yang sama (kode dinormalisasi UPPERCASE di klien sebelum dikirim untuk umpan balik cepat, server tetap menormalkan ulang), dan memetakan `422` ber-`fieldErrors` serta `409` kode duplikat ke field yang bersangkutan — diuji `CreateProjectDialog.test.tsx` (8 test, termasuk kode tidak sah dan galat server per-field) |
| FR-PROJ-02 | `50-FSD.md` §3.2, `42-API.md` §3 (`POST`/`PATCH /projects/:id`), ADR-0017 (code permanen) | `internal/service/project_service.go` (`Create`/`Update`, aturan tanggal), `internal/handler/project_handler.go` (validasi 422 berstruktur), `internal/repository/project_repository.go` (`Update` whitelist kolom) | `internal/service/project_service_test.go::TestProjectUpdateRecordsAudit`, `TestProjectUpdateRejectsCodeChange`, `TestProjectUpdateRejectsInvertedDateRange`, `internal/handler/project_handler_test.go::TestCreateProjectValidation` (7 kasus), `TestPatchProjectRejectsCodeChange` | DONE | P-022: seluruh field tersimpan dan terbaca ulang; `PATCH` dengan `code` → **409**, body kosong → **422**, `target_end_date < start_date` → **422** dengan `details.field` tepat |
| FR-PROJ-03 | `41-DATABASE.md` §2.2 (`status` kanonik `active`/`archived`), `42-API.md` §3, ADR-0012 (`status` hanya nilai kanonik) | `internal/model/project.go` (konstanta status, `IsArchived`), `internal/repository/project_repository.go` (`Archive`) | `internal/service/project_service_test.go::TestProjectArchiveIsStatusChangeAndIdempotent`, `TestProjectListFilterStatusAndSearch`, `internal/handler/project_handler_test.go::TestArchiveProjectEndpoint`, `TestProjectListQueryValidation` | DONE | P-022: nilai di luar `active`/`archived` ditolak `422` (`?status=arsip` → 422 yang menyebut dua nilai sah); response memuat `status` kanonik, bukan label UI. **Lapisan UI (P-041):** penyaring status di `src/pages/Projects/index.tsx` mengirim nilai **kanonik** (`active`/`archived`) dan menampilkan label dari `src/types/status.ts` — pemetaan kanonik→label tinggal satu (ADR-0012), dan `Projects.test.tsx` mengunci bahwa query yang dikirim berisi nilai kanonik |
| FR-PROJ-04 | `50-FSD.md` §3.1/§3.3, `42-API.md` §3 (`GET`/`POST /projects/:id/members`) | `internal/repository/project_repository.go` (`Members`, `AddMember`, `UpsertMember`), `internal/service/project_service.go` (`AddMember`/`RemoveMember`) | `internal/service/project_service_test.go::TestProjectMemberLifecycle`, `internal/handler/project_handler_test.go::TestProjectMemberEndpoints` | DONE | P-022: project dengan anggota >1 terbukti lewat HTTP (`POST members` → 201, `GET members` → 2 anggota); `member_count` pada daftar dihitung dari `project_members`. **Lapisan UI (P-041):** daftar anggota tampil di `src/pages/Projects/ProjectDetail.tsx` dari `GET /projects/:id` (tanpa panggilan tambahan), dan tombol tambah/cabut anggota **tidak** ditampilkan karena butuh pemilih pengguna yang belum ada — batas itu dinyatakan di layar dan dikunci `ProjectDetail.test.tsx` (temuan **C-063**, Q-024) |
| FR-PROJ-05 | `50-FSD.md` §3.3, `41-DATABASE.md` §2.2 (`project_members.role`), `42-API.md` §3 | `internal/model/project.go` (`ProjectRoles`, `IsProjectRole`), `internal/repository/project_repository.go`, `internal/service/project_service.go` | `internal/service/project_service_test.go::TestProjectMemberLifecycle`, `TestProjectOwnerCannotBeRemoved`, `TestProjectOwnerTransferKeepsNewOwnerInScope`, `internal/handler/project_handler_test.go::TestProjectMemberEndpoints` | DONE | P-022: role di luar `owner`/`manager`/`contributor`/`viewer` → **422** tanpa menyentuh database; owner otomatis beranggota (dibuat & saat kepemilikan dipindahkan) |
| FR-PROJ-06 | `44-SECURITY.md` §3.1.3 (cakupan anggota / seluruh organisasi untuk Administrator), `42-API.md` §3, ADR-0014 | `internal/service/project_service.go` (`Scope`), `internal/repository/project_repository.go` (`projectScopePredicate` di dalam `WHERE`) | `internal/service/project_service_test.go::TestProjectListScopeOnlyMembers`, `TestProjectListScopeAdministratorSeesOrganization`, `TestProjectScopeDoesNotCrossOrganizations`, `internal/handler/project_handler_test.go::TestProjectListScopeHidesOtherProjects` | DONE | P-022: di server nyata, user ber-izin `project:read` tetapi bukan anggota → daftar **0** dan detail **404**; setelah dijadikan anggota → daftar memuat project itu (200); Administrator organisasi lain tetap **404**. **Lapisan UI (P-041):** halaman tidak pernah menyaring cakupan sendiri — teks keadaan kosong menyatakan "Project di luar keanggotaan Anda tidak dikirim server" — sehingga tidak ada aturan cakupan kedua di klien yang dapat berbeda dari `projectScopePredicate` |
| FR-PROJ-07 | `50-FSD.md` §3.1, `42-API.md` §3 (`POST /projects/:id/archive`), ADR-0012 | `internal/repository/project_repository.go` (`Archive` mengubah `status`, tidak ada `DELETE` project di modul ini), `internal/service/project_service.go` | `internal/service/project_service_test.go::TestProjectArchiveIsStatusChangeAndIdempotent`, `internal/handler/project_handler_test.go::TestArchiveProjectEndpoint` | DONE | P-022: arsip dua kali sama-sama **200**; baris project tetap ada dan tetap dapat dibaca anggota (`GET /projects/:id` → 200 sesudah arsip); audit `PROJECT_ARCHIVED` tercatat per panggilan. **Lapisan UI (P-041):** tombol **Arsipkan** tampil pada baris daftar (hanya bila izin `project:archive` dimiliki dan statusnya masih `active`), membuka dialog konfirmasi yang menyatakan "tidak ada baris, versi dokumen, atau berkas yang dihapus", dan mengirim `POST /projects/:id/archive` — **bukan** `DELETE` (dikunci `src/services/projects.test.ts`). Terbukti pada server nyata: project `PREVIEW-041` → status berpindah ke `Archived` di daftar tanpa reload, dan `audit_logs` mencatat `PROJECT_ARCHIVED` |
| FR-DOC-01 | `50-FSD.md` §4.2 (dua langkah: metadata lalu berkas), `42-API.md` §4 (`POST /documents`, `POST /documents/:id/upload`), `44-SECURITY.md` §3.1.2 (`document:create` dan `document_version:upload` untuk Contributor+) | `internal/service/document_service.go` (`Create`), `document_service_upload.go` (`UploadVersion`), `internal/handler/document_handler.go` (multipart + magic bytes), `internal/repository/document_repository.go` | `internal/service/document_service_test.go::TestDocumentCreateStoresMetadataAndAudit`, `TestDocumentUploadCreatesVersionWithChecksum`, `internal/handler/document_handler_test.go::TestUploadAndDownloadDocumentEndToEnd`, `TestDocumentPermissionsFollowMatrix` | DONE | P-023: di server nyata, `POST /documents` → **201** dan `POST /documents/:id/upload` (multipart) → **201** versi `1.0` (50 byte, `application/pdf`); Viewer → **403** pada keduanya, Contributor boleh mengunggah. **P-044 (lapisan UI, `T-059`):** halaman Documents memakai alur dua langkah yang sama (`CreateDocumentDialog` → nomor `read-only` dari server → `FilePicker`/`UploadVersionDialog`), test klien `src/pages/Documents/Documents.test.tsx`. **P-045:** unggahan `.txt`/`.csv` yang sebelumnya selalu `422` kini benar-benar bekerja (`TestUploadAcceptsDocumentedTextTypesHTTP`, temuan **C-072**). **P-047:** penyaring `Milik saya` (`?view=mine`) yang belum dapat dijalankan kini punya jalan masuk yang jelas — baris tab di halaman Documents, bukan sub-item sidebar — sehingga alasan **Q-016** terbaca di layar saat tabnya dibuka |
| FR-DOC-02 | `41-DATABASE.md` §2.3 (`documents.document_number`, `document_sequences`), `42-API.md` §4 (`POST /documents` tidak menerima nomor dari klien), `50-FSD.md` §4.2 — ADR-0017 | `backend/internal/migration/004_create_documents.sql` (kolom nomor + tabel penghitung), `internal/repository/document_repository.go` (`NextNumber`: `INSERT … ON CONFLICT … RETURNING`), `internal/service/document_service.go` (`formatDocumentNumber`), `internal/service/project_service.go` (`normalizeProjectCode`, C-041) | `internal/service/document_number_test.go` (4 test), `internal/handler/document_handler_test.go::TestCreateDocument_RejectsClientSuppliedNumber`, `internal/migration/migration_test.go::TestSchemaTablesExist` | DONE | P-023: server nyata → `DOC-UJI-001` lalu `DOC-UJI-002`; rollback tidak menghabiskan nomor; dua pembuatan paralel dapat nomor berbeda; kiriman `document_number` → **422** tanpa menyisakan baris apa pun |
| FR-DOC-03 | `50-FSD.md` §4.1 (badge status), §11 (nilai kanonik vs label) — ADR-0012 + **ADR-0019** | `internal/model/document.go` (`DocumentStatuses`, `IsDocumentStatus`), `internal/handler/document_handler.go` (filter status → 422 di luar himpunan), `internal/migration/004_create_documents.sql` + `010_..._document_archive.sql` (CHECK enam nilai) | `internal/handler/document_handler_test.go::TestDocumentScopeHidesDocumentsFromOutsiders` (`?status=arsip` → **422**), `TestArchiveDocumentEndToEnd` (`?status=archived` → `200`), `internal/migration/migration_test.go::TestDocumentStatusVocabularyIncludesArchived` | **DONE** | P-023: dokumen baru selalu `draft`; label UI tidak pernah disimpan. **P-029 (`T-039`) menutup nilai keenam**: `archived` ada di `model.DocumentStatuses` **dan** di `CHECK` kolom (dibandingkan test), arsip hanya mengubah `status`+`archived_at`, dan terarsip keluar dari daftar default tetapi kembali lewat `?status=archived` |
| FR-DOC-04 | `41-DATABASE.md` §2.3 (`documents.project_id` NOT NULL + FK), `42-API.md` §4 — ADR-0017 | `internal/model/document.go`, `internal/repository/document_repository.go` (kueri selalu JOIN `projects p`), `internal/service/document_service.go` (`Create` menolak project di luar cakupan) | `internal/service/document_service_test.go::TestDocumentCreateStoresMetadataAndAudit`, `TestDocumentScopeFollowsProjectMembership`, `internal/service/document_number_test.go::TestCreateDocument_RejectsProjectOutsideScope` | DONE | P-023: dokumen selalu lahir di dalam project; project di luar cakupan → **404** dan penghitung nomor tidak maju |
| FR-DOC-05 | `50-FSD.md` §4.3 (Download untuk Viewer+), `42-API.md` §4 (`GET /documents/:id/download/:versionId`) — ADR-0005 | `internal/service/document_service_upload.go` (`Download`), `internal/handler/document_handler.go` (`DataFromReader` + `Content-Disposition`), `internal/pkg/filestorage/local.go` | `internal/service/document_service_test.go::TestDocumentDownloadReturnsContentAndAudit`, `internal/handler/document_handler_test.go::TestUploadAndDownloadDocumentEndToEnd`, `TestDocumentDownloadStreamsLargeContent` | DONE | P-023: unduhan server nyata → **200**, `Content-Disposition: attachment; filename="BRD.pdf"`, `Content-Length` 50, isi **identik** dengan berkas asli (`cmp`); non-anggota → **404** |
| FR-DOC-06 | `20-SRS.md` FR-DOC-06, `42-API.md` §4 (`?search=`), `44-SECURITY.md` §3.1.3 | `internal/repository/document_repository.go` (`title`/`document_number` ILIKE di dalam kueri yang sudah bercakupan) | `internal/service/document_service_test.go::TestDocumentScopeFollowsProjectMembership` (pencarian nomor dokumen) | DONE | P-023: pencarian nomor mengembalikan tepat satu baris; pencariannya berada di dalam kueri bercakupan, bukan penyaringan setelah baca |
| FR-DOC-07 | `42-API.md` §4 (`?project_id=&status=&updated_from=&updated_to=`), `50-FSD.md` §4.1 | `internal/handler/document_handler.go` (`parseDocumentListQuery`), `internal/repository/document_repository.go` (`DocumentListFilter`) | `internal/service/document_service_test.go::TestDocumentScopeFollowsProjectMembership` (filter status + project), `TestDocumentListUpdatedAtRangeInclusive` (rentang), `internal/handler/document_handler_test.go::TestDocumentScopeHidesDocumentsFromOutsiders` (`?project_id=`), `TestDocumentListUpdatedAtRangeContractAtHTTP` (11 kasus + 422) | PARTIAL | P-023: filter `project_id` dan `status` berjalan dan diuji. **P-052:** penyaring rentang tanggal §4.1 hidup sebagai `updated_from`/`updated_to` — interval **tertutup**, instan RFC 3339 ber-offset, rentang terbalik `422 field=updated_to` (semantik sama dengan `due_from`/`due_to` tasks); dibuktikan service + HTTP + server nyata (422 dua bentuk) + kelompok ber-label di peramban (`70-TESTING.md` §3.14i). **P-071:** filter `category` hidup sebagai `?category_id=` + dropdown di halaman. **P-073:** kategori lengkap di alur buat (select di `CreateDocumentDialog` langkah 1, dikirim bila dipilih) dan tautan kategori di detail. **Yang masih PARTIAL:** filter `owner` (Q-016, menunggu Q-024) |
| FR-VER-01 | `50-FSD.md` §4.2 (setiap unggahan menghasilkan versi), `42-API.md` §4 — ADR-0005 | `internal/service/document_service_upload.go` (`UploadVersion`), `internal/repository/document_repository.go` (`AddVersion`, `SetCurrentVersion`) | `internal/service/document_service_test.go::TestDocumentUploadCreatesVersionWithChecksum`, `internal/handler/document_handler_test.go::TestUploadAndDownloadDocumentEndToEnd` | DONE | P-023: dua unggahan → dua baris `document_versions`; `documents.current_version` naik 0 → 1 → 2 |
| FR-VER-02 | `50-FSD.md` §4.2 (major.minor), `42-API.md` §4 (tabel versi berikutnya) | `internal/model/document.go` (`NextVersion`, `ParseVersion`, `FormatVersion`), `internal/service/document_service_upload.go` (major bila status `revision_required`) | `internal/service/document_service_test.go::TestDocumentUploadIncrementsMinorThenMajorAfterRevision`, `internal/handler/document_handler_test.go::TestUploadAndDownloadDocumentEndToEnd` | DONE | P-023: `1.0` → `1.1` pada unggahan biasa, dan `2.0` sesudah status `revision_required` (ADR-0016); label versi selalu dari server |
| FR-VER-03 | `44-SECURITY.md` §6/§6.1 (trigger `document_versions` menyusul ADR-0019), `40-TSD.md` §2.4 (`Save` menolak menimpa) — ADR-0005 + **ADR-0019** | `internal/pkg/filestorage/local.go` (`O_EXCL`, tolak berkas yang sudah ada), service tidak menyediakan jalur ubah/hapus versi | `internal/pkg/filestorage/local_test.go` (tolak penimpaan), `internal/handler/document_handler_test.go` (tidak ada endpoint yang mengubah versi), `internal/migration/audit_append_only_test.go::TestDocumentVersions_*` (tujuh test: `UPDATE`/`DELETE`/`TRUNCATE` ditolak `23001`, `INSERT` lolos, dua trigger terpasang, kaskade `DELETE FROM documents` tertahan, jalur pemeliharaan opt-in) | **DONE** | P-023: tidak ada route yang menyentuh versi lama; memperbaiki isi berarti versi baru (`1.1` sebagai baris barU, bukan penimpaan). **Cacat yang dulu diakui — `DELETE /documents/:id` menghapus baris versi berkaskade — sudah hilang pada P-029 (`T-039`, ADR-0019):** arsip menggantikan endpoint itu, dan `document_versions` kini append-only di **tiga lapis** (service, storage, trigger `23001`) |
| FR-VER-04 | `50-FSD.md` §4.3 (timeline versi), `42-API.md` §4 (`GET /documents/:id/versions`) | `internal/repository/document_repository.go` (`Versions`, JOIN `users` untuk penulis), `internal/handler/document_handler.go` (`Versions`) | `internal/service/document_service_test.go::TestDocumentUploadIncrementsMinorThenMajorAfterRevision`, `internal/handler/document_handler_test.go::TestUploadAndDownloadDocumentEndToEnd` | DONE | P-023: daftar terbaru lebih dulu (`1.1` sebelum `1.0`) dengan `uploaded_by_username`, `revision_note`, `size`, dan `created_at`. **P-044 (lapisan UI):** timeline versi ditampilkan di `DocumentDetail.tsx` beserta unduh per versi (`utils/download.ts`), test klien `src/pages/Documents/DocumentDetail.test.tsx`; bukti server nyata P-045: unduhan versi byte-identik (`sha256` sama, `cmp IDENTIK`) |
| FR-VER-05 | `50-FSD.md` §4.3, `44-SECURITY.md` §3.1.2 (`document_version:download` juga untuk Viewer) | `internal/service/document_service_upload.go` (`FindVersion` dalam cakupan dokumen), `internal/handler/document_handler.go` (`Download`) | `internal/service/document_service_test.go::TestDocumentDownloadReturnsContentAndAudit`, `internal/handler/document_handler_test.go::TestUploadAndDownloadDocumentEndToEnd` (versi pertama diunduh setelah versi kedua ada) | DONE | P-023: versi lama tetap dapat diunduh; `versionId` milik dokumen lain → **404** |
| FR-VER-06 | `40-TSD.md` §2.4/§2.5, `41-DATABASE.md` §2.3 (`document_versions` menyimpan metadata) — ADR-0005 | `internal/pkg/filestorage/local.go` (berkas di filesystem), `internal/repository/document_repository.go` (metadata di PostgreSQL) | `internal/service/document_service_test.go::TestDocumentUploadCreatesVersionWithChecksum` (byte + checksum + `file_key` di storage), `TestArchiveDocumentKeepsVersionsAndFiles` (arsip: baris versi **dan** berkas tetap ada) | **DONE** | P-023: `file_key` ada di disk (`storage.Exists`) dan barisnya ada di `document_versions`. **Sejak ADR-0019 (`T-039`, P-029) tidak ada lagi jalur hapus dari API** — arsip menjaga keduanya, dan pembersihan dokumen berversi hanya lewat jalur pemeliharaan eksplisit (C-051) |
| FR-TASK-01 | `20-SRS.md` FR-TASK-01, `50-FSD.md` §6.2, `42-API.md` §6 (`POST /tasks`), `44-SECURITY.md` §3.1.2 (`task:create` = Administrator + Manager) | `internal/handler/task_handler.go` (`Create`, `validateCreateTask`), `internal/service/task_service.go` (`Create`), `internal/repository/task_repository.go` (`Create`), `internal/dto/task_dto.go` | `internal/handler/task_handler_test.go::TestCreateTaskEndToEnd`, `TestCreateTaskValidation`, `TestTaskPermissionsFollowMatrix`, `internal/service/task_service_test.go::TestTaskCreateStoresFieldsAndAudit` | DONE | P-025: server nyata → `POST /api/v1/tasks` **201** (prioritas `High` dinormalkan menjadi `high`, `status` `open`); Contributor & Viewer → **403 FORBIDDEN**; task tanpa `title`/`assignee_id`/`due_date` → **422** dengan tiga `details.field`; kiriman `status` dari klien → **422** |
| FR-TASK-02 | `20-SRS.md` FR-TASK-02, `50-FSD.md` §6.1/§6.2 (kolom + field form), `41-DATABASE.md` §2.5, `42-API.md` §6 | `internal/model/task.go`, `internal/dto/task_dto.go` (`TaskResponse` + kolom turunan), `backend/internal/migration/006_create_tasks.sql`, `internal/repository/task_repository.go` (`taskSelectColumns`, JOIN untuk `project_code`/`assignee_username`/`document_number`) | `internal/service/task_service_test.go::TestTaskCreateStoresFieldsAndAudit`, `TestTaskUpdateGuards`, `internal/handler/task_handler_test.go::TestCreateTaskEndToEnd` | DONE | P-025: response memuat seluruh field FR-TASK-02 **dan** kolom turunan `50-FSD.md` §6.1 (`project_code`, `assignee_username`, `created_by_username`, `document_number`, `is_overdue`, `project_archived`); `priority` kosong memakai default kolom `medium`, bukan `low` yang dikarang handler |
| FR-TASK-03 | `50-FSD.md` §11.2/§6.3 (nilai kanonik `open`/`in_progress`/`completed` dan tiga aksi), `42-API.md` §6 (tabel transisi) — ADR-0012 | `internal/model/task.go` (`TaskStatuses`, `IsTaskStatus`, `CanTransitionTaskStatus`), `backend/internal/migration/006_create_tasks.sql` (CHECK status + default `open`), `internal/service/task_service.go` (`Complete`, penjaga transisi di `Update`), `internal/handler/task_handler.go` | `internal/model/task_test.go::TestCanTransitionTaskStatus`, `TestTaskVocabularyNormalization`, `internal/service/task_service_test.go::TestTaskStatusTransitions`, `internal/handler/task_handler_test.go::TestTaskStatusTransitionsOverHTTP` | DONE | P-025: `Complete` hanya dari `in_progress` (task `open` → **409** dengan pesan bersyarat), `complete` ulang **200** idempoten **tanpa** audit ganda, Reopen `completed` → `open` **200**, dan `in_progress` → `completed` lewat `PATCH` → **409** (jalurnya endpoint ber-izin `task:complete`). **Lapisan UI (P-046):** halaman detail Tasks merender tabel transisi §6.3 dan menawarkan tombol menurut status berjalan — `Complete` tidak pernah muncul pada task `open`; ketiga transisi + penolakan `409` dikunci `TaskDetail.test.tsx` dan dibuktikan probe **41/41 PASS** (`70-TESTING.md` §3.14d) |
| FR-TASK-04 | `20-SRS.md` FR-TASK-04, `41-DATABASE.md` §2.5 (CHECK prioritas + default `medium`), `42-API.md` §6 | `internal/model/task.go` (`TaskPriorities`, `IsTaskPriority`, `NormalizeTaskPriority`), `internal/handler/task_handler.go` (validasi body **dan** query `?priority=`), `internal/repository/task_repository.go` (penyaring prioritas di `WHERE`) | `internal/model/task_test.go::TestTaskVocabularyNormalization`, `internal/service/task_service_test.go::TestTaskListFiltersAndPagination` (per prioritas), `internal/handler/task_handler_test.go::TestTaskListQueryValidation` | DONE | P-025: `?priority=urgent` pada server nyata menyaring tepat satu baris; `?priority=sedang` → **422** yang menyebut empat nilai sah; `?priority=urgent&overdue=true` menyaring keduanya sekaligus |
| FR-TASK-05 | `20-SRS.md` FR-TASK-05, `41-DATABASE.md` §2.5 (`tasks.document_id` → `documents`), `42-API.md` §6 | `internal/service/task_service.go` (`ensureDocumentInProject`), `internal/repository/task_repository.go` (`DocumentInProject`), `internal/dto/task_dto.go` (`document_id`), `internal/repository/task_repository.go` (JOIN `documents` untuk `document_number`) | `internal/service/task_service_test.go::TestTaskDocumentLinkMustStayInSameProject` | DONE | P-025: server nyata → `PATCH` dengan dokumen di project **lain** → **422** field `document_id`; dokumen di project yang sama → **200** dan response memuat `document_number` (`UJI-TASK-002`) |
| FR-TASK-06 | `50-FSD.md` §11.4 (turunan `due_date` + status), `43-WORKFLOW.md` §7, `42-API.md` §6 (`is_overdue` **dan** `?overdue=`) — ADR-0012 | `internal/model/task.go` (`IsTaskOverdue`), `internal/repository/task_repository.go` (`taskSelectColumns` + penyaring tri-state `Overdue *bool` di `WHERE`, sebelum `LIMIT`), `internal/dto/task_dto.go` (`is_overdue` read-only) | `internal/model/task_test.go::TestIsTaskOverdue`, `internal/service/task_service_test.go::TestTaskOverdueIsDerived`, `TestTaskListOverdueFilterMatchesDerivedFlag`, `internal/handler/task_handler_test.go::TestTaskDetailReturnsOverdueFlag`, `TestTaskListQueryValidation` (penyaring benar-benar menyaring) | DONE | P-025: server nyata → task lewat tenggat ber-`status` `open` bernilai `is_overdue: true` dan **tidak ada kolom** `is_overdue`/`overdue` di `information_schema`; setelah `complete` nilainya `false`; hasil daftar identik dengan rumus SQL yang dihitung manual di `psql`; `?overdue=true` → 2 baris, `?overdue=false` → 1 baris, `?overdue=iya` → **422**. **Lapisan UI (P-046):** daftar Tasks mengirim ketiga keadaan penyaring (`""`/`"true"`/`"false"`) dan menerangkan bahwa `false` memuat task tanpa tenggat; dikunci `Tasks.test.tsx` (nilai kueri `""` → `"false"` → `"true"`) dan dibuktikan probe (`total=4` → `true total=2` → `false total=2`). **Tata letaknya diukur (P-047):** penyaring ini dulu duduk di kolom yang lebih tinggi sehingga kontrolnya terangkat dari barisnya; kini **0** kontrol terangkat dan semua kontrol pada satu garis berbagi tepi bawah (`70-TESTING.md` §3.14e) |
| FR-TASK-07 | `20-SRS.md` FR-TASK-07, `44-SECURITY.md` §3.1.3 (dua baris cakupan task), `42-API.md` §6 | `internal/service/scope.go` (`taskScope`), `internal/repository/task_repository.go` (`taskReadPredicate`/`taskWritePredicate` di dalam `WHERE`), `internal/service/task_service.go` (`Scope`, `FindByIDForUpdate` untuk tulis) | `internal/service/task_service_test.go::TestTaskReadScopeFollowsRoles`, `TestTaskWriteScopeContributorOwnTasksOnly`, `internal/handler/task_handler_test.go::TestTaskScopeHidesTasksFromOutsiders`, `TestTaskPermissionsFollowMatrix` | DONE | P-025: server nyata → Manager/Administrator melihat seluruh organisasi; Contributor yang **anggota** project melihat dua task; Viewer yang belum anggota hanya melihat task miliknya; Contributor yang mengubah task orang lain (yang tetap boleh ia baca) → **404**, bukan `403` |
| FR-WF-01 | `42-API.md` §5 (`POST /workflows/definitions`, izin `workflow_definition:manage`) — ADR-0014 | `internal/model/workflow.go` (kosakata definisi + step), `internal/repository/workflow_repository.go` (`CreateDefinition`, `CreateStep`), `internal/service/workflow_service.go` (`CreateDefinition`, `ListDefinitions`), `internal/handler/workflow_handler.go` (+ `router.go` memasang `workflow_definition:manage`) | `internal/service/workflow_service_test.go::TestWorkflowDefinitionCreateListAndAddStep`, `internal/handler/workflow_handler_test.go::TestWorkflowDefinitionPermissions`, `TestWorkflowEndpointsRequireAuthentication`, `TestWorkflowValidation` | DONE | P-048: server nyata → definisi dua step `201` beserta step-nya, definisi tanpa step `422` pada field `steps`, `role` di luar empat role sistem `422` (`workflow_steps.responsible_role`), semua role boleh **membaca** daftar definisi (`200`) sementara contributor yang mengubahnya `403 FORBIDDEN`; definisi workflow **hanya** di `/workflows/definitions`, tanpa varian `/admin/...` (`40-TSD.md` §6) |
| FR-WF-02 | `42-API.md` §5 (`POST /workflows/definitions/:id/steps`), `41-DATABASE.md` §2.4 | `internal/repository/workflow_repository.go` (`CreateStep` + `UNIQUE (workflow_def_id, order)`), `internal/service/workflow_service.go` (`AddStep`, validasi `order` wajib > 0), `internal/dto/workflow_dto.go` (`Order *int` supaya "tidak dikirim" dapat dibedakan dari `0`) | `internal/service/workflow_service_test.go::TestWorkflowDefinitionCreateListAndAddStep`, `TestWorkflowDefinitionStepOrderUnique`, `internal/handler/workflow_handler_test.go::TestWorkflowValidation` | DONE | P-048: server nyata → step baru `201` (role kosong tersimpan `NULL` = siapa pun yang terautentikasi), `order` yang sudah dipakai `409 CONFLICT` (bukan `500` — pelanggaran UNIQUE dipetakan), `order` duplikat **di dalam satu permintaan** `422` pada `steps[1].order`, dan penambahan step **tidak** menomori ulang step yang sudah ada (`[1, 2]`) |
| FR-WF-03 | `20-SRS.md` §3.7, `43-WORKFLOW.md` §5 — **C-010** | `internal/service/workflow_service.go` (`canActOnStep`: role penanggung jawab step **dan** izin `workflow_instance:<action>`, keduanya syarat — **C-073**), `internal/model/workflow.go` (`IsWorkflowStepRole`), `internal/repository/workflow_repository.go` (`ResponsibleUsersForRole`) | `internal/model/workflow_test.go::TestWorkflowStepRoleVocabulary`, `internal/service/workflow_service_test.go::TestWorkflowActionPermissionComesFromBody`, `TestWorkflowActionRequiresStepResponsibility`, `internal/handler/workflow_handler_test.go::TestWorkflowDefinitionPermissions` | DONE | P-048: server nyata → contributor (punya `workflow_instance:submit`, **tanpa** `workflow_instance:approve`) `403` walau route-nya hanya menuntut `workflow_instance:read`; **administrator yang bukan penanggung jawab step juga `403`** — penunjukan step adalah syarat **tambahan**, bukan pengganti izin (`44-SECURITY.md` §3.3, temuan **C-073**). `responsible_user_id` per user tetap **tidak ada** (MVP role-based, keputusan P-026); yang dikirim `responsible_user_ids` adalah **turunan** role step saat submit, bukan kolom |
| FR-WF-06 | `43-WORKFLOW.md` §4.1/§6, `41-DATABASE.md` §2.4 (`current_step` + `current_step_deadline` + `version`) — ADR-0015 | `internal/repository/workflow_repository.go` (`CreateInstance`, `FindByIDForUpdate`, `ResponsibleUsersForRole`), `internal/service/workflow_service.go` (`Submit`, `ExecuteAction`), `internal/handler/workflow_handler.go` (`Submit`), `internal/service/document_service.go` (dokumen → `in_review` + `workflow_instance_id`) | `internal/service/workflow_service_test.go::TestWorkflowSubmitStartsInstanceAndNotifies`, `TestWorkflowSubmitRequiresScope`, `TestWorkflowApproveAdvancesThenCompletes`, `internal/model/workflow_test.go::TestStepDeadline`, `TestIsStepOverdue`, `internal/handler/workflow_handler_test.go::TestWorkflowLifecycleOverHTTP` | DONE | P-048: server nyata → submit `201` dengan `status running`, `current_step 1`, **`version 0`**, deadline terisi `NOW() + deadline_days`; dokumen berpindah `in_review` dan terikat ke instancenya; submit kedua atas dokumen yang sama `409`; approve step tengah memindahkan ke step 2 **dan** menaikkan `version`; approve step terakhir menyelesaikan instance (`completed`, `completed_at`) dan menyetujui dokumen, lalu `current_step_deadline` dikosongkan; aksi atas instance yang sudah selesai `409` (bukan `403`) |
| FR-WF-07 | `43-WORKFLOW.md` §4.3/§6 (guard `version` di database, konflik → `409`), `42-API.md` §5 (`POST /workflows/instances/:id/actions`, `409 WORKFLOW_CONFLICT`) — ADR-0015 | `internal/repository/workflow_repository.go` (`ApplyTransition` — conditional `UPDATE … WHERE id AND version AND status AND current_step`), `internal/service/workflow_service.go` (`rowsAffected = 0` → rollback + `409`), `internal/pkg/response/response.go` (amplop konflik ber-`details`) | `internal/service/workflow_service_test.go::TestWorkflowTransitionGuardIsOptimistic`, `TestWorkflowConcurrentApprovalAcceptsExactlyOne`, `TestWorkflowRejectedActionLeavesNoSideEffects`, `internal/handler/workflow_handler_test.go::TestWorkflowLifecycleOverHTTP` | DONE | P-048: server nyata → `version` basi `409 WORKFLOW_CONFLICT` dengan `details` **objek** keadaan terkini (`current_status`/`current_step`/`current_version`), bukan daftar. **Gigi dibuktikan mutasi:** kondisi `version` di `ApplyTransition` diganti sementara menjadi selalu-benar → `TestWorkflowTransitionGuardIsOptimistic` gagal tepat pada `menyentuh 1 baris, diharapkan 0 (ADR-0015 §6)`; dipulihkan lalu hijau (`70-TESTING.md` §3.15) |
| FR-WF-08 | `43-WORKFLOW.md` §4.4/§6, `42-API.md` §5 | `internal/service/workflow_service.go` (`ExecuteAction` — `reject` → `document.rejected` + `instance.rejected`), `internal/repository/workflow_repository.go` (`ApplyTransition` + `InsertAction`) | `internal/service/workflow_service_test.go::TestWorkflowRejectTerminatesInstance`, `TestWorkflowSingleActionPerCycle` | DONE | P-048: instance berakhir `rejected` bersama dokumennya, notifikasi `DOCUMENT_REJECTED` ke pemilik dokumen, dan audit entri aksi ditulis di transaksi yang sama. **Bukti pada server nyata masih dari test integrasi, bukan probe** — probe P-048 sengaja memakai `approve`/`request_revision` untuk siklus hidup dan tidak mengulang `reject` di HTTP; batas ini dinyatakan, bukan disamarkan |
| FR-WF-09 | `43-WORKFLOW.md` §4.5 (rollback ke step sebelumnya, batas bawah step 1) + §4.6 (re-submit melanjutkan instance yang sama, jeda revisi, siklus aksi), `42-API.md` §5 (`POST /workflows/instances/:id/resubmit`) — ADR-0016; guard transisi ADR-0015 | `internal/model/workflow.go` (`PreviousStep` — batas bawah step 1), `internal/service/workflow_service.go` (`ExecuteAction` untuk `request_revision`, `Resubmit` — jalur kedua masuk review), `internal/repository/workflow_repository.go` (`RefreshStepDeadline`, `HasDocumentVersion`) | `internal/service/workflow_service_test.go::TestWorkflowRequestRevisionRollsBackOneStepAndPauses`, `TestWorkflowRequestRevisionGoesToPreviousStepNotStepOne`, `TestWorkflowRequestRevisionOnFirstStepStaysOnStepOne`, `TestWorkflowResubmitContinuesSameInstance`, `TestWorkflowResubmitPrerequisites`, `internal/handler/workflow_handler_test.go::TestWorkflowLifecycleOverHTTP` | DONE | P-048: server nyata → `request_revision` dari step 3 mundur ke **step 2** (step sebelumnya, bukan step 1) dan dari step 1 tetap di step 1; instance **tetap `running`** dan `revision_required` hanya milik dokumen; selama jeda revisi **seluruh** aksi `409`; `resubmit` melanjutkan instance yang sama (`version` naik satu), mengembalikan dokumen ke `in_review` **tanpa** memindahkan step, **tidak** menambah baris `workflow_actions`, dan membuka siklus aksi kembali (reviewer yang sama memutuskan lagi — C-025); resubmit tanpa versi berkas baru `409`; audit membedakan `DOCUMENT_SUBMITTED` dari `DOCUMENT_RESUBMITTED`. **Lapisan UI (P-072):** tombol `Resubmit for Review` di `ApprovalDetail.tsx` saat `document_status = revision_required` (hanya bila punya `workflow_instance:submit`, versi instance dikirim, `409`→alert+refetch, `403`→pesan izin) — `ApprovalDetail.test.tsx` 3 test resubmit |
| UI-APPROVALS | `50-FSD.md` §5.4 (Halaman Approvals), `42-API.md` §5 (`GET /workflows/instances`, `GET /:id`, `POST /:id/actions`), `51-UX.md` §2.1 (sidebar modul saja) | `frontend/src/services/workflows.ts` (`listWorkflowInstances` dengan `status`+`scope`, `fetchWorkflowInstance`, `actWorkflowInstance`), `frontend/src/queries/workflows.ts`, `frontend/src/pages/Approvals/index.tsx` (tab Pending/Approved/Rejected → status kanonik, `revision_required` di klien pada Pending), `frontend/src/pages/Approvals/Detail.tsx` (aksi + `409` + jeda revisi), `frontend/src/config/navigation.ts` (`Approvals` ready `T-070`), `frontend/src/App.tsx` (`/approvals`/`/approvals/:id`) | `frontend/src/services/workflows.test.ts` (8), `frontend/src/pages/Approvals/Approvals.test.tsx` (9), `frontend/src/pages/Approvals/ApprovalDetail.test.tsx` (10) + `axe` | DONE | P-053: `GET /workflows/instances?status=running&scope=assigned_to_me` → Pending (kecuali `revision_required` di klien), `completed`→Approved, `rejected`→Rejected; link ke `/approvals/:id` + pagination dari `meta`; detail menampilkan `approve`/`reject`/`request_revision` dengan `version`+`comment` dan `409 WORKFLOW_CONFLICT`→alert+refetch; **P-072: tombol `Resubmit for Review` saat jeda revisi (`workflow_instance:submit`, `version` dari instance, `409`→alert+refetch)**; `responsive-evidence` mencakup `/approvals` (4 halaman × widths) |
| FR-DASH-02 | `20-SRS.md` FR-DASH-02, `52-DASHBOARD-ANALYTICS.md` §3/§5, `42-API.md` §13 (`GET /analytics/dashboard`, `from`/`to` interval tertutup, `report:read`, cakupan di kueri) — ADR-0026 | `internal/dto/analytics_dto.go` (`AnalyticsQuery` + `DashboardResponse`), `internal/repository/analytics_repository.go` (10 agregat scoped `projectScopePredicate` + `from`/`to`/`project_id`), `internal/service/analytics_service.go` (`Dashboard` via `systemScope`), `internal/handler/analytics_handler.go` (`Dashboard` + `parseAnalyticsQuery`), `internal/handler/router.go` (`/analytics`), `cmd/server/main.go` wiring | `internal/handler/analytics_handler_test.go` (6 test: `from`/`project_id`/`to<from` → `422`, tanpa token `401`, viewer `403`, manager `200` dengan `kpis`+`charts`) | DONE | P-056: `GET /analytics/dashboard` `200` dengan `kpis` 6 + `charts` 8 — interval tertutup, `report:read` Admin/Manager |
| FR-DASH-03 | `52-DASHBOARD-ANALYTICS.md` §7 metric dictionary 16, `50-FSD.md` §9 — ADR-0026 | `52-DASHBOARD-ANALYTICS.md` §7 (16 metrik, KPI 6 + chart 8 tanpa `012`) | — | DONE | P-055: ADR-0026 `ACCEPTED` — 6 KPI + 8 chart hidup tanpa `012`, ditahan di `52-*` §4 / Q-DASH |
| FR-AUDIT-04 | `42-API.md` §9 (`GET /audit` `actor_id`/`action`/`entity`/`date_from`/`date_to`, `audit:read` Admin, terbaru dulu), `44-SECURITY.md` §3.1.2, `41-DATABASE.md` §2.5 | `model/audit.go` + `repository/audit_repository.go` (`List` `COUNT(*) OVER()` + filter, `LEFT JOIN users`), `service/audit_read_service.go`, `handler/audit_handler.go` (`List` `422`/`404`/`403`), `handler/router.go` (`/audit`), `cmd/server/main.go` | `handler/audit_handler_test.go` (6 subtest: `page`/`actor_id` `422`, tanpa token `401`, viewer `403`, admin `200`) | DONE | P-060: `GET /audit` `200` dengan `data` array + `meta`, `page`/`limit` `422`, `actor_id` UUID `422`, `date_from`/`date_to` `422`, `401`/`403` |
| FR-AUDIT-01 | `44-SECURITY.md` §6, `40-TSD.md` §2.4 (audit ditulis di service, dalam transaksi yang sama) — ADR-0011 | `internal/service/audit_service.go` (`AuditService.Log` menerima `pgx.Tx`), `internal/service/auth_service.go` (`LOGIN`/`LOGOUT`), `internal/service/project_service.go` (`PROJECT_CREATED`/`_UPDATED`/`_ARCHIVED`/`PROJECT_MEMBER_ADDED`/`_REMOVED`), `internal/service/document_service.go` + `document_service_upload.go` (`DOCUMENT_CREATED`/`DOCUMENT_VERSION_CREATED`/`DOCUMENT_DOWNLOADED`/**`DOCUMENT_ARCHIVED`** — sejak P-029; `DOCUMENT_DELETED` tidak ada lagi karena tidak ada operasi hapus dokumen), `internal/service/task_service.go` (`TASK_CREATED`/`TASK_UPDATED`/`TASK_ASSIGNED`/`TASK_COMPLETED`), `internal/service/comment_service.go` (`COMMENT_CREATED`/`COMMENT_UPDATED`/`COMMENT_DELETED`) | `internal/service/auth_service_test.go::TestLoginSuccess` (audit `LOGIN`), `internal/service/project_service_test.go` (7 test menghitung entri audit per aksi), `internal/service/document_service_test.go` (6 test per aksi dokumen, termasuk `TestArchiveDocumentKeepsVersionsAndFiles`), `internal/handler/document_handler_test.go::TestDocumentAuditEntityUsesDocumentNumber`, `internal/service/task_service_test.go::TestTaskCreateStoresFieldsAndAudit`, `TestTaskAssignWritesSeparateAudit`, `TestTaskStatusTransitions`, `internal/service/comment_service_test.go::TestCommentCreateStoresFieldsAndAudit`, `TestCommentEditDeleteAreOwnershipOnly`, `internal/handler/comment_handler_test.go::TestCommentCreateAndListEndToEnd` | PARTIAL | P-023: aksi "login", "create project", "change permission", "upload doc", "create version", "download doc", dan (tambahan) "archive document" — sejak P-029, menggantikan "delete document" yang tidak ada lagi — tercatat di transaksi yang sama dengan perubahannya. Server nyata: enam entri `audit_logs` ber-`entity = document` dan `entity_id` **nomor dokumen** (`DOC-UJI-001`). Sejak **P-025** aksi task juga tercatat: `TASK_CREATED` (penugasan awal ada di metadata, bukan entri `TASK_ASSIGNED` terpisah), `TASK_UPDATED`, `TASK_ASSIGNED` (hanya bila `assignee_id` benar-benar berubah), dan `TASK_COMPLETED` — tujuh baris `audit_logs` ber-`entity = task` pada server nyata. Sejak **P-027** aksi komentar juga tercatat (`COMMENT_CREATED`/`COMMENT_UPDATED`/`COMMENT_DELETED`, ber-`entity = comment`, metadata memuat `entity_type`/`entity_id` dan `content_size` — **tanpa** menyalin isi komentar): bukti server nyata menunjukkan 3/2/2 baris, dan tidak ada satu pun entri untuk permintaan yang ditolak. Sisa aksi FR-AUDIT-01 (submit/approve/reject/request revision) menyusul bersama modul workflow. **Keputusan P-026 (ADR-0022):** login **gagal** memang **tidak** masuk `audit_logs` — `actor_id` tetap `NOT NULL` karena tabel itu bermakna "tindakan aktor yang terautentikasi", dan jejaknya ada di tabel `login_attempts` (`T-041`); jadi "login" pada requirement ini = login **berhasil**, dan itu keputusan, bukan kekurangan. `DOCUMENT_DOWNLOADED` ditulis di transaksi singkat tersendiri karena aksinya read-only (ADR-0011 butir 4). **P-029 (`T-039`):** aksi arsip juga tercatat — `DOCUMENT_ARCHIVED` dengan `entity_id` = nomor dokumen, ditulis di transaksi yang sama, dan **tidak ada** entri untuk permintaan yang ditolak (unggahan versi baru pada dokumen terarsip dan arsip ulang sama-sama `409`) |
| FR-AUDIT-03 | `41-DATABASE.md` §2/§4 (trigger dipasang migrasi `007`), `44-SECURITY.md` §6 (`prevent_audit_modification()` + `trg_audit_logs_append_only`/`trg_audit_logs_no_truncate`, tolak SQLSTATE `23001`), `40-TSD.md` §2.4 (append-only: entri audit ikut rollback, tidak pernah lahir untuk perubahan yang batal) — ADR-0011 | — | `70-TESTING.md` §4.3 | TODO | — |
| FR-AUDIT-04 | `42-API.md` §9 (`GET /audit` dengan filter), `44-SECURITY.md` §3.1.2 (`audit:read` = Administrator) | — | — | TODO | — |
| FR-REP-01 | `42-API.md` §10 (`GET /reports/export?type=projects|documents|tasks&format=csv`, `report:export` Admin/Manager, cakupan 44-SECURITY §3.1.3, audit `REPORT_EXPORTED`) , `50-FSD.md` §10.6 (Projects/Documents/Tasks export) | `dto/report_dto.go`, `service/report_service.go` (`REPORT_EXPORTED`, `Export` CSV header=kolom tabel 50-FSD, `systemScope`/`taskScope`), `handler/report_handler.go` (`Export` + `parseReportExportQuery`), `handler/router.go` (`GET /reports/export`), `cmd/server/main.go` | `handler/report_handler_test.go::TestReportExportValidation`, `TestReportExportSuccess` (401/403/422 + 200 `text/csv` `Content-Disposition: attachment` + header `code`/`document_number`/`title`) | DONE | P-068: `GET /reports/export?type=projects&format=csv` → 200 `text/csv` `bwdcs-projects-<YYYYMMDD>.csv` + header `code,name,status...`; `documents` → `document_number`; `tasks` → `title`; type/format/project_id salah → 422; tanpa token 401; viewer 403; audit `REPORT_EXPORTED` (prompts/P-068) |
| FR-CMT-01 | `20-SRS.md` FR-CMT-01, `50-FSD.md` §7 (empat jenis entitas), `42-API.md` §7 (`POST /comments`) — ADR-0014 (`comment:create` untuk semua role) | `internal/model/comment.go` (kosakata `entity_type` + normalisasi), `internal/service/comment_service.go` (`Create`, `validateCommentContent`), `internal/repository/comment_repository.go` (`EntityProject` + `commentEntityProjectCase`), `internal/handler/comment_handler.go` | `internal/model/comment_test.go` (3 test kosakata), `internal/service/comment_service_test.go::TestCommentCreateStoresFieldsAndAudit`, `TestCommentWorksOnEveryEntityType` (project/document/task/workflow), `TestCommentCreateValidatesEntityAndContent`, `TestCommentRejectsEntityOutsideScope`, `internal/handler/comment_handler_test.go::TestCommentCreateAndListEndToEnd`, `TestCommentViewerCanComment`, `TestCommentCreateValidationOverHTTP`, `TestCommentOnTaskAndDocumentOverHTTP` | DONE | P-027: server nyata → `POST /comments` `201` pada project (spasi tepi isi dibuang, `created_by_username` terisi); `422` ber-field untuk `entity_type` asing (`workflow_instance`), `entity_id` bukan UUID, `content` kosong/spasi/melebihi batas; entitas di luar cakupan → `404 entity not found`; role **viewer** dapat berkomentar (`201`) walau `POST /projects` baginya `403`; audit `COMMENT_CREATED` 3 baris ber-`entity = comment` |
| FR-CMT-02 | `20-SRS.md` FR-CMT-02, `41-DATABASE.md` §2.5 (`created_by_id` + `created_at` `NOT NULL`), `42-API.md` §7 (`GET /comments`) | `internal/repository/comment_repository.go` (`commentSelectColumns` + JOIN `users`, urutan `created_at, id`), `internal/dto/comment_dto.go` (`created_by_id`, `created_by_username`, `created_at`), `internal/handler/comment_handler.go` (`List`, `Get`) | `internal/service/comment_service_test.go::TestCommentCreateStoresFieldsAndAudit` (penulis + cap waktu), `TestCommentReadScopeFollowsEntityProject`, `internal/handler/comment_handler_test.go::TestCommentCreateAndListEndToEnd` (meta paginasi + kronologis), `TestCommentScopeAndOwnershipOverHTTP` | DONE | P-027: server nyata → entri `comments` menyimpan `created_by_id`/`created_at` dari database (bukan dari klien); `created_by_username` ikut di response; `audit_logs` mencatat penulis tiap aksi; daftar `?page=5` melaporkan `meta.total = 2` walau halamannya kosong (tambalan C-048) |
| FR-CMT-03 | `20-SRS.md` FR-CMT-03, `50-FSD.md` §7 (timeline), `42-API.md` §7 (urutan kronologis + paginasi; entitas selalu satu) | `internal/repository/comment_repository.go` (`ORDER BY c.created_at ASC, c.id ASC`; hitung ulang total saat halaman kosong), `internal/handler/comment_handler.go` (`parseCommentListQuery`: `entity_type`/`entity_id` wajib) | `internal/service/comment_service_test.go::TestCommentListIsChronologicalAndPaginated`, `internal/handler/comment_handler_test.go::TestCommentCreateAndListEndToEnd`, `TestCommentListQueryAndPatchValidation` | DONE | P-027: server nyata → dua komentar kembali dalam urutan penulisan (terlama lebih dulu) dengan `meta` `{page, limit, total, total_page}`; permintaan tanpa `entity_type`/`entity_id` → `422`; `?entity_type=workflow_instance` → `422`; `limit=101`/`page=0` → `422` |

> Tabel di atas hanya memuat requirement prioritas High yang pertama akan disentuh (Phase 0-2). Tambahkan baris lain saat dikerjakan, jangan biarkan requirement yang sudah dikerjakan tidak tercatat.
>
> Baris `FR-TASK-03`, `FR-TASK-06`, dan perubahan kolom Desain pada `FR-AUDIT-01`/`FR-AUDIT-03` ditambahkan pada sesi **P-008**, sebagai bagian dari perbaikan temuan audit C-001 dan C-003 (ADR-0011, ADR-0012).
>
> Baris `FR-ROLE-01`..`FR-ROLE-04` ditambahkan pada sesi **P-009**, bagian dari perbaikan temuan audit C-017 dan C-008 (ADR-0014). Test yang menutupnya: `70-TESTING.md` §4.1 (test matriks per pasangan resource/action, jumlah baris seed, dan scoping).
>
> Baris `FR-TASK-01`..`FR-TASK-07` diisi pada sesi **P-025** bersama modul Task (`T-038`), dan baris `FR-AUDIT-01` diperbarui untuk empat aksi task. Sebelumnya hanya `FR-TASK-03` dan `FR-TASK-06` yang ada di tabel ini, keduanya bertanda `TODO`; kini ketujuh requirement task berstatus `DONE` dengan bukti pada server nyata (`70-TESTING.md` §3.10).
>
> Baris `FR-CMT-01`..`FR-CMT-03` ditambahkan pada sesi **P-027** bersama modul Comment (`T-042`), ketiganya `DONE` dengan bukti pada server nyata (`70-TESTING.md` §3.13). Baris `FR-AUDIT-01` diperluas untuk tiga aksi komentar. Yang **belum** dicakup: threading (`Reply`) yang tidak punya kolom induk — temuan **C-050**/Q-018, dan `@mention`/`COMMENT_MENTION` yang milik modul Notification yang belum dibangun.
>
> Baris `FR-WF-01` dan `FR-WF-02` ditambahkan pada sesi **P-011**, saat dua endpoint definisi workflow dilengkapi di `42-API.md` §5 (perbaikan temuan audit C-011 dan C-013). Belum ada implementasi maupun test; statusnya `TODO`.
>
> Baris `FR-WF-06`, `FR-WF-07`, dan `FR-WF-08` ditambahkan pada sesi **P-013**, saat pola optimistic locking transisi workflow instance ditetapkan (ADR-0015, perbaikan temuan audit C-005). Test yang menutupnya: `70-TESTING.md` §3.3 (dua aksi bersamaan → tepat satu berhasil, transaksi yang kalah tidak meninggalkan action/audit) dan §5.1 (konflik terlihat sebagai pemberitahuan, bukan sukses). Baris `FR-WF-09` diberi catatan bahwa arah rollback-nya belum diputuskan (C-022).
>
> Baris `FR-WF-09` diperbarui pada sesi **P-014**: arah rollback `request_revision` ditetapkan lewat ADR-0016 (kembali ke step sebelumnya, sesuai FR-WF-09 apa adanya; batas bawah step 1; instance tetap `running`; re-submit melanjutkan instance yang sama). Test yang menutupnya: `70-TESTING.md` §3.4. Status implementasi tetap `TODO`.

> Kontrak re-submit setelah revisi ditetapkan pada sesi **P-017** (`42-API.md` §5, `T-028`): `POST /workflows/instances/:id/resubmit` melanjutkan instance yang sama dengan izin `workflow_instance:submit`. Kolom Desain `FR-WF-09` dilengkapi penunjuk §4.6 (jeda revisi + siklus aksi), dan temuan **C-025** ditutup di sesi yang sama. Test yang menutupnya: `70-TESTING.md` §3.6. Status implementasi tetap `TODO`.
>
> **Seluruh baris `FR-WF-*` berubah menjadi `DONE` pada sesi P-048** bersama modul Workflow (`T-064`): sembilan endpoint `42-API.md` §5 hidup, 29 test menguncinya (`70-TESTING.md` §3.15), dan perilakunya dibuktikan pada server nyata oleh `scripts/probe-workflow-module.py` (**46/46 asersi PASS**, lima aktor login sungguhan). Dua requirement diperbarui **isi**-nya, bukan hanya statusnya: **FR-WF-03** sekarang menyatakan syaratnya **dua** (`role` penanggung jawab step **dan** izin `workflow_instance:<action>`, bukan salah satu) mengikuti `44-SECURITY.md` §3.1/§3.3 dan `50-FSD.md` §5.4 — perbedaan itu ditemukan saat implementasi dan dicatat sebagai **C-073**; dan **FR-WF-08** diberi catatan jujur bahwa bukti `reject` masih dari test integrasi, bukan probe HTTP di server nyata. `responsible_user_ids` pada response `POST /workflows/submit` adalah **turunan** role step, bukan kolom per user (`responsible_user_id` tetap ditunda, keputusan P-026).

> Penomoran dokumen ditetapkan pada sesi **P-016** lewat ADR-0017 (`{PROJECT_CODE}-{NNN}`, dibangkitkan server, immutable, tanpa penomoran manual) — perbaikan temuan C-016. Baris `FR-DOC-02` ditambahkan karena kebijakan penomoran adalah bagian desain metadata dokumen; test yang menutupnya: `70-TESTING.md` §3.1 dan §3.5 (nomor berurutan, rollback tidak menghabiskan nomor, dua pembuatan bersamaan tidak bertabrakan), serta §5.1 (E2E tidak mengisi nomor).

> Sesi **P-023** mengerjakan `T-037` (modul dokumen). Tujuh baris `FR-DOC-01`..`FR-DOC-07` berpindah dari `TODO`/`PARTIAL` ke `DONE` (kecuali `FR-DOC-07` → `PARTIAL`, karena `50-FSD.md` §4.1 menyebut filter kategori/owner/tanggal yang belum ada di kontrak `42-API.md` §4 — Q-016), dan **enam baris `FR-VER-01`..`FR-VER-06` ditambahkan**: sebelumnya requirement versi dokumen sama sekali belum punya baris meski modulnya sekarang dikerjakan — tanpa ini, "setiap upload menghasilkan version baru" dan "version lama immutable" tidak dapat ditelusuri ke kode maupun test. Kolom Bukti tiap baris memuat hasil yang **dijalankan** (server nyata: `DOC-UJI-001`/`DOC-UJI-002`, versi `1.0` → `1.1`, unduhan identik dengan `cmp`, kaskade hapus), bukan pembacaan dokumen. Test penutupnya `70-TESTING.md` §3.8.

> Halaman Approvals (`50-FSD.md` §5.4), Reports (§10.6), dan Administration > Workflows (§10.7) dispesifikasikan pada sesi **P-015** (perbaikan temuan audit C-018). Ketiganya lapisan UI untuk requirement yang sudah dilacak di atas — tidak ada requirement baru. Kolom Desain `FR-REP-01` dilengkapi penunjuk §10.6; antrean Approvals memakai endpoint daftar `GET /workflows/instances` yang ditambahkan ke `42-API.md` §5 pada sesi yang sama.
>
> Baris `FR-AUTH-08`, `FR-AUTH-09`, `FR-ORG-03`, `FR-REP-01`, `FR-AUDIT-04` ditambahkan pada sesi **P-012**, dan kolom Desain `FR-ROLE-04` dilengkapi, saat enam requirement tanpa kontrak endpoint dilengkapi di `42-API.md` (perbaikan temuan audit C-012). Belum ada implementasi maupun test; statusnya `TODO`.
>
> Kolom Desain dan Test `FR-AUDIT-03` dilengkapi pada sesi **P-019** (perbaikan temuan audit C-020): penegakan append-only ternyata **tidak pernah dipasang** — cuplikan di `44-SECURITY.md` §6 memanggil `raise_exception()` yang tidak ada di PostgreSQL, dan tidak ada migrasi yang menyebutnya. Trigger kini didefinisikan di `44-SECURITY.md` §6 dan dipasang migrasi `007` (`41-DATABASE.md` §4); test penutupnya `70-TESTING.md` §4.3. Status implementasi tetap `TODO` karena migrasi `007` sendiri belum dikerjakan (`T-004`).
>
> Sesi **P-021** mengerjakan `T-005` (modul auth). Enam baris berpindah ke `DONE` — `FR-AUTH-01`, `FR-AUTH-02`, `FR-AUTH-03`, `FR-AUTH-04`, `FR-AUTH-05`, `FR-AUTH-06`, dan `FR-ROLE-03` — karena jalur login/logout/RBAC kini nyata dan teruji di HTTP, bukan hanya di skema. Tiga baris yang sebelumnya belum ada (`FR-AUTH-03`, `FR-AUTH-04`, `FR-AUTH-07`) ditambahkan. `FR-AUTH-04` diberi batasan eksplisit: `logout_all` belum dapat dijalankan (temuan **C-033**, Q-013). `FR-AUTH-07` tetap `PARTIAL` karena hanya sisi login yang tertutup.

> Sesi **P-020** mengerjakan `T-004` (migrasi `001`-`009` + bootstrap ADR-0010). Lima baris requirement di atas berpindah dari `TODO` ke `PARTIAL` karena **lapisan skema dan data awalnya sudah nyata dan teruji** — `FR-AUTH-05` (bcrypt cost 12 di bootstrap), `FR-ROLE-01`/`FR-ROLE-02`/`FR-ROLE-03` (4 role, `user_roles`, 104 baris izin), `FR-DOC-02` (kolom nomor + `document_sequences`), dan `FR-ORG-02` yang **barisnya belum pernah ada** meski bootstrap ADR-0010 sudah diputuskan sejak P-004. `FR-AUDIT-03` tetap `TODO` di lapisan ini karena implementasi lengkapnya menuntut service yang menulis audit (ADR-0011) — trigger + test-nya sendiri sudah terpasang dan lulus.

> Sesi **P-051** juga tidak menambah requirement baru, tetapi ia menegakkan **aturan desain** yang sampai sesi itu tidak dijaga mesin: `51-UX.md` §2.1 (sidebar memuat **modul saja**) hanya ditegakkan test klien, dan test klien **tidak berjalan di CI** — sementara satu-satunya pemeriksa yang menyentuh bentuk menu (`scripts/responsive-evidence.mjs`) hanya mencari item menu **berkueri**, sehingga path bersarang luput (**C-079**). Karena tidak ada requirement yang berubah, baris `FR-*` tetap seperti semula; yang berubah adalah **tempat** halaman anak bernavigasi: `Audit` (di bawah Reports) kini terdaftar di daftar `subPages` (`parent`) dan hidup di baris sub-navigasi modul induknya (`T-067`), bukan sebagai entri sidebar. Buktinya mesin, bukan klaim: `bash scripts/check-navigation.sh` → `navigation OK — 7 menu sidebar (tanpa kueri, satu segmen), 1 halaman anak ber-induk, 8 baris §2.1 cocok`, dengan gigi yang dibuktikan tujuh cacat (`70-TESTING.md` §3.14h). Sesi yang sama juga menaikkan anggaran waktu suite frontend karena flakiness yang terukur (**C-082**), dan menambahkan aturan ledger yang membandingkan total temuan di dalam laporan audit dengan markernya (**C-081**).
>
> Sesi **P-049** juga tidak menambah requirement baru: menyatukan kedua batas rentang tenggat ke satu kelompok berlabel adalah **bentuk** dari penyaring `?due_from=&due_to=` yang sudah `DONE` (`FR-TASK-02` mencatat field form dan penyaringnya, `FR-TASK-06` tri-state overdue), sehingga baris `FR-TASK-*` tetap `DONE` tanpa perubahan status — semantik rentangnya pun tidak disentuh dan tetap **tertutup** seperti diputuskan Q-017. Kekurangan yang ditutup berasal dari catatan sesi P-047 sendiri (`70-TESTING.md` §3.14e), bukan dari requirement baru, dan buktinya di peramban: `fromTop == toTop == 289` dengan `sameLine` **dan** `sameGroup` pada 1440px, `sameGroup` dengan `overflowX = 0` pada 375px, plus tiga butir kegagalan yang terbukti bergigi (`70-TESTING.md` §3.14f).
>
> Sesi **P-047** juga tidak menambah requirement baru: ia memindahkan **tempat** sub-navigasi (tab dan penyaring) dari sidebar ke halaman yang memilikinya, dan menyamakan kesebarisan baris penyaring Task (`T-063`). Karena itu baris `FR-DOC-*` dan `FR-TASK-*` tetap `DONE` tanpa perubahan status; bukti UI tambahan dicatat di `FR-DOC-01` (baris tab Documents dan penyaring `Milik saya` yang tetap dapat dibuka) dan `FR-TASK-06` (penyaring tri-state kini duduk sebaris dengan penyaring lain). Pengukurannya di peramban, bukan klaim: `70-TESTING.md` §3.14e.
>
> Sesi **P-046** juga tidak menambah requirement baru: halaman **Tasks** (daftar ber-penyaring termasuk **tri-state overdue**, dialog buat, detail dengan ketiga transisi status) adalah **lapisan UI** dari `FR-TASK-01`..`FR-TASK-07` yang sudah `DONE` di backend sejak P-025, sehingga status baris-baris itu **tidak** berubah; bukti UI tambahan dicatat di `FR-TASK-03` (transisi di klien) dan `FR-TASK-06` (tri-state di klien). Yang **belum** tertutup di UI dan sengaja tidak dikarang: form ubah task (PATCH lengkap), komentar, dan activity log di halaman detail — semuanya menyebut dirinya belum dibangun beserta alasannya. Seluruh klaim halaman dibuktikan `scripts/probe-task-module.py` **41/41 PASS** di server nyata (`70-TESTING.md` §3.14d).

> Sesi **P-044** dan **P-045** juga tidak menambah requirement baru: halaman **Documents** (daftar, unggah dua langkah, detail, versi + unduh, arsip) adalah **lapisan UI** dari `FR-DOC-01`..`FR-DOC-07` dan `FR-VER-01`..`FR-VER-06` yang sudah `DONE` di backend, sehingga status baris-baris itu **tidak** berubah (bukan requirement baru) dan hanya mendapat bukti tambahan: `FR-DOC-01` (alur dua langkah di klien) dan `FR-VER-04` (timeline versi + unduh byte-identik). Yang **belum** tertutup di UI dan sengaja tidak dikarang: sub-item "Milik saya" pada `51-UX.md` §2.1 menunggu parameter pemilik di `GET /documents` (**Q-016**), dan bagian Workflow/Comments/Activity/Related Tasks di halaman detail menyebut dirinya belum dibangun beserta alasannya. Bagian yang **bukan** lapisan UI dan karena itu dicatat sebagai temuan, bukan requirement: **C-070** (kontrak arsip tanpa bentuk amplop — kini tertulis di `42-API.md` §4), **C-071** (pesan penjaga append-only menyebut tabel yang salah — migrasi `011`), dan **C-072** (MIME unggahan dibandingkan utuh sehingga `.txt`/`.csv` selalu `422`).

> Sesi **P-041** juga tidak menambah requirement baru: halaman **Projects** adalah **lapisan UI** dari `FR-PROJ-01`..`FR-PROJ-07` yang sudah `DONE` di backend, sehingga baris-baris itu **tidak** naik status (bukan requirement baru, dan statusnya sudah `DONE`) melainkan mendapat kolom bukti tambahan yang menyebut halaman dan test kliennya. Yang **belum** tertutup di UI dan sengaja tidak dikarang: pemilihan `Owner` (temuan **C-063**, Q-024) dan penambahan/pencabutan anggota — keduanya menuntut endpoint daftar pengguna yang belum ada.
>
> Sesi **P-037** tidak menambah requirement baru: pekerjaan itu (`DESIGN.md` + kerangka frontend) adalah **lapisan UI dari requirement yang sudah dilacak**, terutama `NFR-USABLE-03` (WCAG AA, kini 2.2 dan naik dari `TODO` ke `PARTIAL` dengan bukti test) dan aturan arah desain antislop R-37 yang tidak punya ID `FR-*`/`NFR-*` di `20-SRS.md`. Satu koreksi yang penting untuk pembaca berikutnya: ambang aksesibilitas proyek ini adalah **WCAG 2.2 AA**, bukan 2.1 — `51-UX.md` §1 diperbaiki pada sesi yang sama (temuan **C-060**), dan test kontrasnya mengukur 2.2.

> Sesi **P-026** menjawab sembilan temuan audit yang tersisa dengan dasar best practice (`OPEN-QUESTIONS.md` §3), dan tabel ini ikut diselaraskan karena empat ADR menyentuh requirement: **FR-AUTH-06** turun ke `PARTIAL` (requirement kini juga menuntut auto-lock `423`, ADR-0022 — `T-041`), **FR-DOC-03** dan **FR-VER-03** turun ke `PARTIAL` (nilai `archived` dan trigger append-only `document_versions`, ADR-0019 — `T-039`), **FR-AUTH-04/08/09** mendapat batasan yang lebih tegas (mekanisme pencabutan sesi **diputuskan** ADR-0021, kodenya `T-040`), **FR-AUDIT-01** menyatakan eksplisit bahwa percobaan login gagal berada di `login_attempts`, bukan `audit_logs`, dan **FR-WF-03** masuk tabel sebagai `TODO` dengan catatan C-010 (`responsible_user_id` ditunda, MVP role-based). **Tiga baris yang turun ke `PARTIAL` bukan kemunduran implementasi** — requirement-nya yang menjadi lebih tepat, dan statusnya mencerminkan selisih itu secara jujur. Aturan yang dipakai: satu baris traceability hanya boleh `DONE` bila seluruh isi requirement (termasuk yang baru ditambahkan ADR) sudah berjalan. Sesi **P-030** memulihkan status baris-baris itu ketika implementasinya selesai: **FR-AUTH-04** dan **FR-AUTH-06** kembali `DONE` (`T-040`/`T-041`), sementara **FR-AUTH-08** dan **FR-AUTH-09** naik dari `TODO` ke `PARTIAL` karena mekanisme pencabutan sesi kini berjalan dan hanya endpoint-nya yang tersisa.

## 2. Non-Functional Requirements

| Requirement | Target | Cara verifikasi | Status | Bukti |
|---|---|---|---|---|
| NFR-PERF-02 | API p95 < 500ms | `70-TESTING.md` §6 (k6) | TODO | — |
| NFR-SEC-01 | bcrypt cost 12 | Unit test + code review | TODO | — |
| NFR-SEC-03 | Prepared statements | Code review + grep query concat | TODO | — |
| NFR-MAIN-01 | Modular packages, clear separation | Struktur = `40-TSD.md` §2.0 (satu folder = satu package, arah `handler` → `service` → `repository` → `model`); `go build ./...` + `go vet ./...` bersih | PARTIAL | P-018: kerangka paket berdiri (`config`, `pkg/filestorage`, `handler`, `cmd/server`) dan terverifikasi; lapisan `service`/`repository`/`model`/`dto` masih kosong (mulai `T-004`) |
| NFR-MAIN-02 | Code comments, README, API docs | Komentar paket + penunjuk dokumen desain pada setiap berkas baru; `42-API.md` tetap sumber tunggal kontrak HTTP | PARTIAL | P-018: berkas skeleton memuat komentar paket dan rujukan §dokumen; README root & API docs menyusul |
| NFR-MAIN-03 | Structured JSON logs | Cek output runtime | PARTIAL | P-018: `slog` JSON di `cmd/server/main.go` (level dari `LOG_LEVEL`); terlihat pada log startup `{"msg":"konfigurasi dimuat",...}` saat `/health` diuji |
| NFR-USABLE-03 | WCAG **2.2** AA | `51-UX.md` §1/§9 + `DESIGN.md` §2/§4 + antislop Delivery Gate Block 1; **token diukur test**, bukan diklaim: `frontend/src/styles/tokens.contrast.test.ts` menghitung rasio tiap pasangan (`--text`/`--text-soft`/`--text-muted` ≥ 4,5:1 di setiap permukaan; `--line-strong` & `--accent-strong` ≥ 3:1 — SC 1.4.11; badge status ≥ 4,5:1) di **kedua tema**; `frontend/src/test/a11y.test.tsx` menjalankan `axe-core` atas shell + Login + Dashboard | PARTIAL | **P-037:** lapisan token dan halaman yang sudah ada memenuhi ambangnya — 31 test kontras + 4 test `axe-core` hijau, dan test kontrasnya **punya gigi** (mengubah `--color-ink-500` → 3 test gagal). Warna tidak pernah menjadi satu-satunya pembawa makna (badge selalu berlabel teks, ADR-0012). **Belum `DONE`** karena halaman bisnis (Projects, Documents, Tasks, Approvals, Reports, Administration) belum ada, sehingga cakupan WCAG-nya masih sebagian |
| NFR-PORT-02 | Linux amd64/arm64 | Cross-build di CI/lokal | TODO | — |
| NFR-PORT-01 | Binary Go + aset statis + PostgreSQL; Docker Compose sebagai referensi (ADR-0004) | `.env.example` + `docker-compose.yml` ada; `docker compose --env-file .env.example -f docker-compose.yml config -q` -> exit 0; jalur **tanpa Docker** terbukti: binary `backend/bin/bwdcs` jalan di host, PostgreSQL 16.10 + role/database `bwdcs` di `localhost:5432`, `GET /health` → 200. `Dockerfile` **belum ada** (target `docker-build` di Makefile menunggu task kontainerisasi) | PARTIAL | P-006/P-010/P-018: konfigurasi tervalidasi dan runtime host terbukti; runtime kontainer belum |

## 3. Contoh Baris yang Benar (format saja, bukan data nyata)

| Requirement | Desain | Implementasi | Test | Status | Bukti |
|---|---|---|---|---|---|
| FR-AUTH-02 | `40-TSD.md` §5 | `backend/internal/pkg/jwt/jwt.go` (`Service.Generate`), `backend/internal/service/auth_service.go` (`Login` menerbitkan token sesudah kredensial sah) | `internal/pkg/jwt/jwt_test.go::TestGenerateAndValidate` + `internal/service/auth_service_test.go::TestLoginSuccess` (keduanya memeriksa `jti` terisi dan masa berlaku) | **DONE** | P-034: `jwt` 17 test hijau; login menyerahkan `token` **dan** `refresh_token` (ADR-0023) |