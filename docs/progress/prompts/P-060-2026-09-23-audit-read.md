# P-060 — 2026-09-23 — Audit read: GET /audit

| Field | Isi |
|---|---|
| ID | P-060 |
| Waktu mulai | 2026-09-23 23:30 (Asia/Jakarta) |
| Aktor | agen |
| Model / agen | muse-spark-1.2-contributor-free |
| Fase roadmap | 2 — Audit |
| Task terkait | `T-077` — `GET /audit` (`42-API.md` §9) |
| Status akhir | DONE |

---

## 1. Prompt User

> "Lanjutkan sesuai CONTINUE.md" — setelah `T-076` (notifications) selesai, next adalah Audit read (`42-API.md` §9, `audit:read`).

## 2. Interpretasi & Scope

- Yang diminta: `GET /audit?actor_id=&action=&entity=&entity_id=&date_from=&date_to=&page=&limit=` — terbaru dulu, `audit:read` (Administrator saja, `44-SECURITY.md` §3.1.2), tanpa migrasi baru (`audit_logs` sudah ada `41-DATABASE.md` §2.5, append-only `44-SECURITY.md` §6).
- Yang TIDAK termasuk: `PATCH`/`DELETE` audit (append-only), frontend `Audit` page (`51-UX.md` §2.1 `Reports > Audit` masih `pending`).

## 3. Rencana

| # | Langkah | Hasil yang diharapkan |
|---|---|---|
| 1 | `model/audit.go` + `repository/audit_repository.go` (`List` `COUNT(*) OVER()` + filter, `LEFT JOIN users` untuk `actor_name`) | Scoped tidak perlu (Admin melihat semua) |
| 2 | `service/audit_read_service.go` + `handler/audit_handler.go` (`page`/`limit` `422`, `actor_id` UUID `422`, `date_from`/`date_to` `422`) | Validasi `422` |
| 3 | Wiring `router.go` (`/audit` `audit:read`) + `main.go` + `main_test.go` | Route 46 (1 audit) |
| 4 | `handler/audit_handler_test.go` (6 subtest) | `make test` 277 |

## 4. Aksi yang Dilakukan

| # | Aksi | Alasan | Hasil |
|---|---|---|---|
| 1 | `model/audit.go` — `AuditLog` (`id`, `actor_id`, `actor_name`, `action`, `entity`, `entity_id`, `metadata`, `created_at`) | `41-DATABASE.md` §2.5 | Struct |
| 2 | `repository/audit_repository.go` — `AuditListFilter` + `List` (`WHERE actor_id=$1 AND action=$2 AND entity=$3 AND entity_id=$4 AND date_from/to`, `ORDER BY created_at DESC`, `COUNT(*) OVER()` + `count` untuk offset>0) | `42-API.md` §9 | — |
| 3 | `service/audit_read_service.go` — `List` via `AuditRepository` | Service layer | — |
| 4 | `handler/audit_handler.go` — `List` + `parseAuditListQuery` (`page`/`limit` `422`, `actor_id` UUID `422`, `date_from`/`date_to` `YYYY-MM-DD` atau RFC3339 `422`, `date_to < date_from` `422`) | Validasi | `401`/`403`/`422` |
| 5 | `handler/router.go` — `Audit` deps + group `/audit` `audit:read`; `cmd/server/main.go` + `handler/main_test.go` `engineParts.audit` | Route 46 | `check-readme-facts` 46 |
| 6 | `handler/audit_handler_test.go` — `TestAuditListValidation` (5 subtest) + `TestAuditListSuccess` | Kunci `page`/`actor_id`/`401`/`403`/`200` | 2 `func Test` (6 subtest) |

## 5. File yang Berubah

| File | Jenis | Ringkasan perubahan | Requirement terkait |
|---|---|---|---|
| `backend/internal/model/audit.go` | Added | Struct `AuditLog` | `41-DATABASE.md` §2.5 |
| `backend/internal/repository/audit_repository.go` | Added | `AuditListFilter` + `List`/`count` (`LEFT JOIN users`) | `42-API.md` §9 |
| `backend/internal/service/audit_read_service.go` | Added | `List` via `AuditRepository` | `42-API.md` §9 |
| `backend/internal/handler/audit_handler.go` | Added | `List` + `parseAuditListQuery` | `42-API.md` §9 |
| `backend/internal/handler/router.go` | Changed | `Audit` deps + 1 route `audit:read` | `42-API.md` §9 |
| `backend/cmd/server/main.go` | Changed | Wiring `AuditReadService`/`Handler` | — |
| `backend/internal/handler/main_test.go` | Changed | `engineParts.audit` | — |
| `backend/internal/handler/audit_handler_test.go` | Added | 2 `func Test` (6 subtest) | `42-API.md` §9 |
| `scripts/check-readme-facts.sh` | Changed | `n_audit`, `known_receivers` + audit, `actual_parts` 11 | — |
| `README.md` | Changed | `45 route` → `46 route` (1 audit) | — |
| `docs/progress/TASKS.md` | Changed | `T-077` TODO → DONE | FR-AUDIT-04 |
| `docs/progress/prompts/P-060-...md` | Added | Log prompt ini | — |

> Daftar ini disalin ke `CHANGELOG.md` §2026-09-23 P-060.

## 6. Verifikasi (WAJIB)

| # | Command | Output ringkas | Kesimpulan |
|---|---|---|---|
| 1 | `go vet ./...` | OK | PASS |
| 2 | `go build ./...` | OK | PASS |
| 3 | `cd backend && make test` | 9 paket OK, **277 test** (naik 2) | PASS |
| 4 | `bash scripts/check-ledger.sh` | `ledger OK — 275 test` → `277 test` setelah `273→275→277` | PASS |
| 5 | `bash scripts/check-doc-links.sh` | `BROKEN 0` | PASS |
| 6 | `bash scripts/check-api-contract.sh` | `120 pemeriksaan, 56 endpoint` | PASS |
| 7 | `bash scripts/check-readme-facts.sh` | `46 route` `46 fakta` | PASS |
| 8 | `bash scripts/check-navigation.sh` | `navigation OK` | PASS |
| 9 | `bash scripts/check-antislop-refs.sh` | `antislop-refs OK` | PASS |

- [x] Typecheck / build dijalankan
- [x] Test relevan dijalankan
- [x] Perubahan dokumen dicek konsisten
- [x] Jika UI: tidak ada (backend)

## 7. Hasil & Dampak

- Selesai: **GET /audit** hidup — `?actor_id=` UUID `422`, `?page`/`?limit` `422`, `?date_from`/`date_to` `YYYY-MM-DD`/RFC3339 `422`, `date_to < date_from` `422`, `audit:read` (Admin saja) `403` untuk Viewer, `401` tanpa token, `200` dengan `data` array + `meta`. `audit_logs` terbaru dulu (`ORDER BY created_at DESC`). Route total `46` (1 audit).
- Belum selesai / sisa: `T-074` backlog penuh (department/SLA/expiry — Phase 5, Q-DASH), frontend `Audit` page (`51-UX.md` §2.1 `Reports > Audit` masih `pending`), `T-050` lisensi.

## 8. Update Ledger (Checklist Wajib)

- [x] `STATE.md` diperbarui — `277 test`
- [x] `SESSION-LOG.md` ditambah entri P-060
- [x] `CHANGELOG.md` ditambah entri P-060
- [x] `TASKS.md` diperbarui — `T-077` DONE
- [x] `TRACEABILITY.md` diperbarui — FR-AUDIT-04
- [x] `OPEN-QUESTIONS.md` tidak berubah
- [x] ADR tidak ada

## 9. Next Action

| Prioritas | Aksi | Pemilik |
|---|---|---|
| 1 | Frontend `Audit` page (`51-UX.md` §2.1 `Reports > Audit`) — tabel `audit_logs` dengan filter | agen |
| 2 | Reports `GET /reports/export` CSV (`42-API.md` §10, `report:export`) | agen |
| 3 | `T-074` Phase 5 menunggu Q-DASH | agen Phase 5 |
