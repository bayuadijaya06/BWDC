# AUDIT-001 — Kontradiksi Dokumen Desain (2026-09-17)

**Sifat:** laporan temuan. **Tidak ada dokumen desain yang diubah** dalam audit ini.
**Cakupan:** seluruh `docs/design/*.md` (17 file), `AGENTS.md`, `DESIGN.md`, `IDEA.md`, `antislop.md`, `docker-compose.yml`, `.env.example`.
**Metode:** pencarian terstruktur (enum, daftar, endpoint, matriks, struktur folder, env, dials) + pembacaan bagian yang relevan. Setiap temuan menyertakan bukti `file:line`.
**Diminta oleh:** user, prompt P-007 ("laporkan temuannya sebelum memperbaiki apa pun").

---

## 1. Ringkasan

<!-- audit-summary total=82 fixed=80 approved=0 open=2 rejected=0 rinci=37 -->

Baris di atas adalah **marker mesin**: `scripts/check-ledger.sh` membandingkan ketujuh
angkanya dengan isi tabel tindak lanjut di berkas ini, lalu memastikan salinan yang
sama ada di `docs/progress/audits/README.md`, `AGENTS.md`, `docs/progress/STATE.md`,
dan `CONTINUE.md`. Perbarui marker **hanya** dengan menghitung ulang dari tabelnya,
bukan dari angka sesi sebelumnya — kelas cacat C-044/C-055/C-057.

| Severitas | Arti | Jumlah |
|---|---|---|
| **S1** | Menghalangi atau menyesatkan implementasi modul yang bersangkutan | 18 |
| **S2** | Ambigu, harus diputuskan sebelum modul itu dikerjakan | 13 |
| **S3** | Inkonsistensi ringan / kosmetik | 6 |

Angka di atas adalah **jumlah bagian rinci** per severitas: 18 + 13 + 6 = **37** temuan yang punya bagiannya sendiri di §2–§4. Sebelumnya tabel ini menulis 17/11/4 (32) — angka yang tidak pernah diperbarui ketika temuan baru (C-021..C-037) disisipkan ke bagian yang sesuai; dikoreksi pada P-030 dengan menghitung judul `### C-` di setiap bagian, dan dicatat sebagai temuan **C-055**.

**82 temuan** secara keseluruhan: 37 di atas, ditambah **45 temuan berikutnya (C-038..C-082) yang hanya punya baris di tabel tindak lanjut** — semuanya ditemukan dari **menjalankan** kode atau test, atau dari memeriksa ulang berkasnya sendiri, dan masing-masing sudah menyertakan bukti, dampak, serta perbaikannya di barisnya sendiri. (Angka 54/17 yang tertulis sebelumnya dikoreksi pada P-030 dengan menghitung baris `| C-` di tabel tindak lanjut — seluruh id punya baris di sana, jadi yang "hanya baris" adalah jumlah baris dikurangi 37. Setelah **C-056**, **C-057**, **C-058**, dan **C-059** bertambah, jumlahnya **59**; **C-060** bertambah pada P-037 sehingga menjadi **60**, lalu **C-061** pada P-038 menjadi **61**, **C-062** pada P-039 menjadi **62**, dan **C-063** pada P-041 menjadi **63**, **C-064** + **C-065** + **C-066** pada P-042 menjadi **66**, **C-067** + **C-068** + **C-069** pada P-043 menjadi **69**, **C-070** + **C-071** pada P-044 menjadi **71**, **C-072** pada P-045 menjadi **72**, **C-073** + **C-074** + **C-075** pada P-048 menjadi **75**, **C-076** + **C-077** + **C-078** pada P-050 menjadi **78**, dan **C-079** + **C-080** + **C-081** + **C-082** pada P-051 menjadi **82**. Periksa ulang dengan `grep -cE '^\| C-'` atas tabel tindak lanjut, atau jalankan `bash scripts/check-ledger.sh` — sejak P-036 angka-angka di paragraf ini juga dikunci marker mesin di bawah.)

Temuan yang paling berbahaya: **C-001** (lapisan audit log) dan **C-002** (struktur folder) karena keduanya memengaruhi setiap modul, bukan satu modul saja.

> **Temuan C-021, C-022, dan C-023 ditambahkan pada 2026-09-18 (sesi P-013)** saat mengerjakan C-005: ketiganya berada di berkas dan transisi yang sama, sehingga meninggalkannya berarti memperbaiki separuh cacat. C-021 dan C-023 `FIXED` di sesi yang sama; C-022 `FIXED` pada sesi P-014 lewat ADR-0016 (arah rollback mengikuti FR-WF-09: step sebelumnya, batas bawah step 1).
>
> **Temuan C-024 ditambahkan pada 2026-09-18 (sesi P-016)** saat mengerjakan C-016: satu requirement ID hantu (`FR-DOC-08`) di contoh commit, ditemukan di modul yang sama. `FIXED` di sesi yang sama.
>
> **Temuan C-025 ditambahkan pada 2026-09-18 (sesi P-017)** saat menetapkan kontrak endpoint re-submit (`T-028`): aturan "satu aksi per step" di `43-WORKFLOW.md` §4.2 membuat alur revisi ADR-0016 mustahil dijalankan. `FIXED` di sesi yang sama.
>
> **Temuan C-026 dan C-027 ditambahkan pada 2026-09-18 (sesi P-018)** saat menulis backend skeleton (`T-003`): dua kontrak infrastruktur yang dipakai langsung oleh kode tidak dapat dijalankan apa adanya (signature storage dan cuplikan health check). Keduanya `FIXED` di sesi yang sama.
>
> **Temuan C-028 ditambahkan pada 2026-09-18 (sesi P-019)** saat memperbaiki C-020: begitu audit log benar-benar dijaga trigger, setting "Audit log retention" di `50-FSD.md` §10.5 berubah dari butir kosong menjadi tabrakan nyata dengan FR-AUDIT-03. Masih `OPEN` — butuh keputusan user.
>
> **Temuan C-029 sampai C-032 ditambahkan pada 2026-09-18 (sesi P-020)** saat mengerjakan `T-004` (migrasi `001`-`009` + bootstrap): seluruhnya baru terlihat ketika migrasi **dijalankan**, bukan dibaca. Tiga di antaranya (C-029, C-030, C-031) membuat `goose up` gagal atau skema tidak lengkap, dan C-032 membuat dua kontrak yang sudah terkunci tidak dapat dipenuhi bersamaan. Keempatnya `FIXED` di sesi yang sama.

---

## 2. S1 — Menghalangi Implementasi

### C-001 — Audit log: di handler atau di service?

- **Bukti:** `40-TSD.md:341-347` contoh handler memanggil `audit.Log` (`// 3. Call audit.Log`). `90-AGENT-GUIDE.md:210-215` contoh service yang memanggil audit. `44-SECURITY.md:243` menyebut audit trail security. `IDEA.md` bagian 13 menempatkan audit di business layer. Definition of Done di `12-DEVELOPMENT-WORKFLOW.md:104` menuntut audit log "di dalam transaksi yang sama".
- **Kontradiksi:** dua dokumen menunjukkan lapisan berbeda; yang paling kritis, `40-TSD.md` menaruh di handler sementara transaksi dimiliki service.
- **Dampak:** setiap modul (document, workflow, task, admin) bisa menulis audit dengan pola berbeda, dan audit di handler tidak ikut rollback saat transaksi gagal.
- **Usul resolusi:** audit hanya di **service**, di dalam transaksi yang sama; handler tidak pernah memanggil audit. Perbaiki contoh di `40-TSD.md` §2.6.

### C-002 — Struktur folder backend punya tiga versi

- **Bukti:** `30-ARCHITECTURE.md:130-140` (`internal/` dengan `handler/auth`, `handler/project`, ... subfolder per modul), `01-AGENT-WORKFRAME.md:325-333` (`config, middleware, handler, service, repository, model, dto, migration` — tanpa `pkg`), `90-AGENT-GUIDE.md:53-62` (sama dengan 01 tetapi **dengan** `pkg`).
- **Tambahan:** tidak satu pun memuat `internal/bootstrap` yang baru ditetapkan ADR-0010 dan `40-TSD.md:352`.
- **Dampak:** agen berikutnya akan membuat struktur yang berbeda dari agen lain; import path tidak konsisten.
- **Usul resolusi:** tetapkan `40-TSD.md` §2 sebagai satu-satunya definisi struktur (karena sudah memuat `bootstrap`), lalu dua dokumen lain menaut ke sana.

### C-003 — Status kanonik vs label, dan status "Overdue"

- **Bukti:** nilai kanonik di `41-DATABASE.md:164-165` (`draft, in_review, revision_required, approved, rejected`) dan `:263-264` (`open, in_progress, completed`). Label tampilan "In Review", "Revision Required", "Overdue" di `50-FSD.md` §4.3, `51-UX.md:52`, `43-WORKFLOW.md` §7. `IDEA.md` bagian 2.G menulis "Task = Overdue" seolah status. `20-SRS.md:162` FR-TASK-06 meminta penandaan otomatis.
- **Kontradiksi:** tidak ada tabel pemetaan kanonik ↔ label di mana pun, dan "Overdue" bisa disalahartikan sebagai nilai status (padahal `CHECK` di DB tidak mengizinkannya).
- **Dampak:** risiko menambah `'overdue'` ke constraint, atau badge status menampilkan nilai mentah.
- **Usul resolusi:** tambahkan satu tabel pemetaan di `50-FSD.md` (atau `51-UX.md`) + satu kalimat tegas: overdue = turunan (`due_date < NOW() AND status <> 'completed'`), bukan nilai kolom.

### C-004 — Dokumen: hapus atau arsip?

- **Bukti:** `40-TSD.md:540` `documents.DELETE("/:id", ...)`, `42-API.md` `DELETE /documents/:id`, `50-FSD.md:168` "Delete (Admin/Manager, no workflow running)". Sementara itu `80-ROADMAP.md` Phase 1 (#17 archive) dan `00-README.md` menyebut archive, dan untuk project polanya arsip: `POST /projects/:id/archive` + kolom status `active/archived` (`41-DATABASE.md:122`). Tabel `documents` **tidak** punya kolom `archived_at`/`deleted_at` (`41-DATABASE.md:155-172`). Requirement `FR-DOC-*` tidak memuat delete maupun archive sama sekali.
- **Dampak:** keputusan salah akan menghapus riwayat dokumen yang justru menjadi inti sistem audit, dan pelanggaran FR-VER-03 (versi immutable) karena cascade delete menghapus versi.
- **Usul resolusi:** dokumen memakai **archive** (samakan dengan project), `DELETE /documents/:id` dihapus dari API/route, dan requirement archive ditambahkan ke SRS.

### C-005 — Kolom `version` untuk optimistic locking tidak ada di skema

- **Bukti:** `43-WORKFLOW.md` §6 memakai `UPDATE workflow_instances SET current_step = 2 ... WHERE id = $1 AND version = $2`, tetapi DDL `41-DATABASE.md:225-234` tidak punya kolom `version`.
- **Dampak:** approve ganda (dua reviewer bersamaan) tidak tercegah saat implementasi; solusi di dokumen tidak dapat dijalankan apa adanya.
- **Usul resolusi:** tambahkan `version INTEGER NOT NULL DEFAULT 0` ke `workflow_instances` (dan tentukan pola update-nya).

### C-006 — Role "Reviewer" muncul di dokumen tetapi tidak ada di model role

- **Bukti:** `10-BRD.md:55` ("Reviewer / Approver"), `20-SRS.md:53` (baris "Reviewer" di tabel pengguna), `51-UX.md:52` (akses menu Approvals = "Reviewer+"). Sedangkan `20-SRS.md:83` FR-ROLE-01 menetapkan **hanya 4 role**: Administrator, Manager, Contributor, Viewer.
- **Dampak:** implementasi RBAC dan visibilitas menu tidak dapat ditentukan; agen bisa membuat role kelima atau mengabaikan menu Approvals.
- **Usul resolusi:** nyatakan Reviewer sebagai **peran fungsional** (user yang menjadi responsible step), bukan role sistem; perbaiki tabel stakeholder BRD/SRS dan akses nav di 51-UX.

### C-007 — Hierarki role mencampur role sistem dan role project, "Owner" hilang

- **Bukti:** `44-SECURITY.md:130-134` menulis `Administrator > Manager > Contributor > Viewer`. `20-SRS.md` FR-PROJ-05 menetapkan role **dalam project**: Owner, Manager, Contributor, Viewer.
- **Dampak:** pengecekan `CanAccessProject` tidak jelas dasar hierarkinya; "Owner" tidak punya peringkat.
- **Usul resolusi:** pisahkan dua hierarki (sistem vs project) dan tambahkan Owner di hierarki project.

### C-008 — Akses audit log: admin saja atau admin+manager?

- **Bukti:** `44-SECURITY.md:259` "Akses ke audit log dibatasi hanya untuk Administrator role". `40-TSD.md` §5.3 matriks: "View Audit Log: ✅ ❌ ❌ ❌" (admin saja). Sedangkan `51-UX.md:53` menu Reports > Audit diberi akses "Admin/Manager".
- **Dampak:** menu bisa tampil untuk Manager lalu API menolak (400/403) atau sebaliknya, membocorkan audit ke role yang tidak berhak.
- **Usul resolusi:** ikuti dua sumber yang sepakat (Administrator saja) dan perbaiki `51-UX.md`.

### C-009 — Auto-lock akun dijanjikan tanpa kolom pendukung

- **Bukti:** `44-SECURITY.md:73` "Account Lockout: Auto-lock after threshold, unlocked by admin or after timeout". Tabel `users` (`41-DATABASE.md:52-66`) hanya punya `is_active`; `system_settings` (`41-DATABASE.md:330-334`) menyimpan `auth.max_login_attempts` dan `auth.lockout_duration_minutes` tetapi tidak ada kolom `locked_until`/`failed_login_count`.
- **Dampak:** rate limit jalan, auto-lock tidak; admin tidak punya cara "unlock" karena status lock berada di luar skema.
- **Usul resolusi:** putuskan: (a) tambahkan kolom lock, atau (b) turunkan janji di 44 menjadi rate-limit saja.

### C-010 — Penugasan step ke user tertentu: requirement vs "Future"

- **Bukti:** `20-SRS.md` FR-WF-03 "responsible role/user". `43-WORKFLOW.md` §5 menyatakan specific user sebagai "(Future: field `responsible_user_id`)". DDL `workflow_steps` hanya punya `responsible_role`.
- **Dampak:** requirement High tidak dapat dipenuhi penuh; agen bisa menambah kolom yang tidak diinginkan atau mengabaikan requirement.
- **Usul resolusi:** tandai FR-WF-03 sebagai role-based untuk MVP, atau tambahkan `responsible_user_id` sekarang. Harus dipilih, tidak boleh dibiarkan.

### C-021 — Kolom `current_step_deadline` dipakai dokumen, tidak ada di skema

- **Bukti:** `43-WORKFLOW.md` §7 (`WHERE wi.status = 'running' AND wi.current_step_deadline < NOW()`), `50-FSD.md` §11.4 (baris "Step workflow terlambat"), dan ADR-0012 §Keputusan butir 6 memakai `workflow_instances.status = 'running' AND current_step_deadline < NOW()`. DDL `workflow_instances` di `41-DATABASE.md` §2.4 tidak memuat kolom itu.
- **Kelas yang sama dengan C-005:** kolom dirujuk dokumen tetapi tidak ada di skema. Beda nama, sama akibatnya.
- **Dampak:** deteksi overdue step (turunan resmi ADR-0012) tidak dapat dijalankan pada skema saat ini; agen bisa menghitung deadline ulang dari `workflow_steps.deadline_days` (berbeda untuk step yang sudah berganti) atau menambah kolom dengan nama lain.
- **Usul resolusi:** tambahkan `current_step_deadline TIMESTAMP WITH TIME ZONE` (nullable) ke `workflow_instances`, isi saat submit dan saat instance maju; `NULL` bila step tidak punya `deadline_days`.
- **Status:** FIXED pada P-013 lewat ADR-0015.

### C-025 — "Satu aksi per step" memblokir reviewer pada step yang di-rollback

- **Bukti:** `43-WORKFLOW.md` §4.2 langkah 4 menetapkan "Actor must not have already acted on this step". ADR-0016 menetapkan `request_revision` mengembalikan instance ke **step sebelumnya**, lalu review dilanjutkan pada instance yang sama — sehingga reviewer step tujuan **sudah pernah** memutuskan di step itu (dialah yang meng-approve-nya sebelum rollback).
- **Kontradiksi:** aturan "sekali bertindak" bersifat seumur instance; rollback mensyaratkan orang yang sama bertindak lagi di step yang sama. Keduanya tidak dapat berlaku bersamaan.
- **Dampak:** alur revisi (FR-WF-09 + ADR-0016) tidak dapat dijalankan — pada step hasil rollback tidak ada aktor yang sah, sehingga dokumen yang direvisi tidak pernah dapat dilanjutkan. Agen yang menyadari hal ini akan menambal aturan sesuka hati (mis. mengizinkan aksi berulang **selamanya**, yang membuka approve-ubah-pikiran).
- **Usul resolusi:** nyatakan aturan itu berlaku **per siklus** — aksi setelah `request_revision` terakhir — bukan seumur instance, dan tutup jendela jeda dengan menolak semua aksi selama `documents.status = 'revision_required'`.
- **Status:** FIXED (P-017) — `43-WORKFLOW.md` §4.2 langkah 4 diberi batas siklus, §4.6 baru menetapkan re-submit (`42-API.md` §5 `POST /workflows/instances/:id/resubmit`), jeda revisi ditegakkan di `/actions` (`409 CONFLICT`), dan §3.6 `70-TESTING.md` menutupnya dengan dua test (aksi ditolak saat jeda; reviewer dapat memutuskan lagi setelah re-submit). Ditemukan saat menetapkan kontrak `T-028`, bukan temuan lama.

### C-026 — `FileStorage.Save` tidak dapat menghasilkan `file_key` yang didokumentasikan

- **Bukti:** `40-TSD.md` §2.4 mendefinisikan `Save(orgID, projectID, docID, version string, data io.Reader) (string, error)`. Contoh nilai `file_key` di `42-API.md` §4 adalah `orgs/abc/projects/123/docs/456/v1/file.pdf`, dan kolom `document_versions.file_key VARCHAR(500)` menyimpan key itu bersama `original_name` dari berkas yang diunggah klien. Contoh penyimpanan di `90-AGENT-GUIDE.md` mengikuti signature yang sama.
- **Kontradiksi:** signature tidak memuat nama berkas, sehingga implementasi harus **mengarang** bagian terakhir key (nama berbeda dari contoh di API) atau menebaknya dari ID — padahal `original_name` justru berasal dari klien, dan nama dari klien tidak boleh menentukan path.
- **Dampak:** dua agen menghasilkan dua bentuk key berbeda untuk tabel yang sama (`file.pdf` vs `v1` vs `doc-1-v1.pdf`), dan agen yang menambal dengan `filepath.Join(dir, originalName)` membuka **path traversal** dari nama berkas unggahan — kelas cacat yang sama dengan C-016 (aturan format yang tidak dapat ditegakkan).
- **Usul resolusi:** tambahkan parameter `originalName` pada `Save`, nyatakan key bersifat **opaque** bagi service (service tidak menyusun/menafsirkan key), wajibkan sanitasi nama di implementasi, dan tegakkan larangan menimpa berkas versi lama (ADR-0005, FR-VER-03).
- **Status:** FIXED (P-018) — `40-TSD.md` §2.4 diperbarui (signature + skema key + aturan imutabilitas), implementasi `internal/pkg/filestorage/local.go` (sanitasi nama, penolakan segmen `..`, `O_EXCL`), test di `local_test.go`.

### C-027 — Cuplikan health check tidak dapat dikompilasi dan salah membaca kesehatan storage

- **Bukti:** `60-DEPLOYMENT.md` §5 memetakan `map[string]func() error` tetapi mengisi `"storage": func() error { return storage.Exists("healthcheck") }` — `Exists` mengembalikan `bool`, bukan `error`. Key `healthcheck` juga tidak pernah dibuat oleh siapa pun.
- **Kontradiksi:** tipe tidak cocok (kode contoh tidak dapat dibangun), dan semantiknya terbalik: `Exists` bernilai `false` untuk key yang tidak ada, sehingga **storage yang sehat akan dilaporkan `unhealthy`**.
- **Dampak:** agen yang menyalin cuplikan ini akan gagal `go build`, lalu menambal sendiri — atau membuat berkas probe `healthcheck` permanen yang tidak pernah dibersihkan, sehingga akhirnya ada berkas sampah di direktori storage setiap deployment.
- **Usul resolusi:** periksa kesehatan storage lewat operasi yang benar-benar membuktikan direktori dapat ditulisi (probe tulis-hapus), bukan lewat keberadaan key tertentu; perbaiki cuplikan agar dapat dikompilasi.
- **Status:** FIXED (P-018) — `60-DEPLOYMENT.md` §5 memakai `storage.Ping`, ditambah `filestorage.Prober` + `LocalStorage.Ping` (berkas probe sementara yang langsung dihapus), handler `internal/handler/health_handler.go`, dan test `TestPingMenandaiStorageYangTidakDapatDipakai`.

---

### C-029 — FK `documents.workflow_instance_id` menunjuk tabel yang belum ada saat migrasi `004`

- **Bukti:** `41-DATABASE.md` §2.3 mendefinisikan `workflow_instance_id UUID REFERENCES workflow_instances(id)`, sedangkan `workflow_instances` baru dibuat di §2.4 — yaitu migrasi `005` menurut daftar berkas di §4. §4 hanya menyebut urutan berkas, tanpa mencatat ketergantungan terbalik ini.
- **Dampak:** `goose up` **gagal** pada migrasi `004` (`relation "workflow_instances" does not exist`). Dibiarkan, setiap agen akan menambal dengan cara berbeda: menghapus kolomnya (integritas referensial hilang), memindahkan tabel antar berkas (daftar migrasi berubah), atau membiarkan kolom yatim. Terbukti saat menjalankan `T-004`.
- **Usul:** kolom tetap di `004` **tanpa** FK; constraint dipasang di `005` lewat `ALTER TABLE documents ADD CONSTRAINT fk_documents_workflow_instance ...` (kolom nullable, sehingga `ALTER` tidak pernah gagal karena baris lama).
- **Status:** FIXED (P-020) — usul diambil apa adanya; `41-DATABASE.md` §2.3/§4 diperbarui, migrasi `004`/`005` diimplementasikan, dan test `TestDocumentsWorkflowInstanceForeignKey` menjaga constraint-nya tetap ada.

### C-030 — `system_settings` tidak punya rumah di daftar migrasi

- **Bukti:** `41-DATABASE.md` §2.6 mendefinisikan tabel `system_settings` + lima baris default, tetapi daftar sembilan berkas di §4 tidak menyebutnya; §4 hanya menetapkan isi `004`, `005`, `007`, dan `008`.
- **Dampak:** skema hasil `001`-`009` **tidak sama** dengan §2 — `system_settings` tidak pernah terpasang, padahal `42-API.md` §11 (`GET`/`PATCH /admin/settings`) dan default `file.max_upload_mb` yang dipakai validasi upload `44-SECURITY.md` §4.2 bergantung padanya.
- **Usul:** tetapkan rumahnya di `002` (tabel tanpa dependensi ke tabel lain, dan setting dasar dibutuhkan lebih awal daripada `009`) lalu tulis pemetaan isi tiap berkas di §4 supaya tidak perlu diputuskan ulang.
- **Status:** FIXED (P-020) — migrasi `002` memuatnya, `41-DATABASE.md` §2.6/§4 memuat pemetaan seluruh sembilan berkas, dan `TestSystemSettingsDefaults` menjaga nilai awalnya.

### C-031 — Badan fungsi PL/pgSQL tidak dapat dijalankan goose tanpa anotasi

- **Bukti:** menjalankan `goose up` pada migrasi `007` yang memuat `CREATE FUNCTION ... AS $$ ... $$;` gagal: `failed to execute SQL query "CREATE FUNCTION prevent_audit_modification() ...": ERROR: unterminated dollar-quoted string at or near "$$" (SQLSTATE 42601)`. Penyebabnya pengurai goose memecah berkas per titik-koma, dan badan fungsi memuat titik-koma.
- **Dampak:** SQL yang sudah diverifikasi benar di `psql` (perbaikan C-020) tetap **tidak jalan lewat migrasi** — kelas cacat yang sama: janji yang tidak dapat dijalankan alatnya. Agen yang menyalin `44-SECURITY.md` §6 apa adanya akan menemui kegagalan identik. Terbukti saat menjalankan `T-004`.
- **Usul:** bungkus badan fungsi dengan sepasang anotasi `StatementBegin` dan `StatementEnd`, dan catat kewajiban itu di `44-SECURITY.md` §6 serta `41-DATABASE.md` §4.
- **Status:** FIXED (P-020). Sekaligus ditemukan jebakan kedua di kelas yang sama: pengurai goose mencari penanda anotasi di **mana pun** dalam baris, sehingga kalimat komentar yang menyebutkan penandanya membuat migrasi gagal dengan `invalid annotation`. Larangan itu ikut dicatat di §4 dan §6.

---

### C-033 — `token_revocations` tidak dapat mencabut "seluruh token aktif user"

- **Bukti:** ADR-0009 butir 3 mewajibkan logout mencabut "seluruh token aktif milik sesi tersebut (dan seluruh token user bila `logout_all` diminta)", dan `42-API.md` §2 menuliskan `logout_all: true`, sedangkan tabel `token_revocations` hanya menyimpan `jti` yang **sudah** dicabut (`41-DATABASE.md` §2.1, migrasi `009`). Sistem tidak menyimpan daftar sesi/token aktif, dan `AuthService.RevokeAllForUser` (`40-TSD.md` §2.4) juga didefinisikan tanpa sumber data untuk melakukannya.
- **Dampak:** bagian kontrak itu tidak dapat dijalankan apa adanya. Implementasi dapat memilih jalan pintas yang salah dengan dua akibat berbeda: membalas 200 sambil hanya mencabut satu token (klien mengira perangkat lain sudah keluar), atau memakai `created_at` sebagai "epoch" per user sehingga logout di satu perangkat mematikan perangkat lain — padahal justru itu yang membedakan `logout_all`.
- **Usul resolusi (butuh ADR):** pilih satu mekanisme dan tulis akibatnya. (a) simpan `session_id` di dalam token dan di `token_revocations`, sehingga satu login = satu sesi yang dapat dicabut utuh; (b) tambahkan kolom ``users.tokens_valid_after`` dan tolak token dengan `iat` lebih lama; (c) turunkan janji ADR-0009/`42-API.md` §2 menjadi "logout mencabut token yang dipakai" dan buang `logout_all` dari MVP. Ditemukan saat mengerjakan `T-005` (P-021); perilaku sementara yang dipilih: `logout_all: true` dibalas `501 NOT_IMPLEMENTED`, bukan sukses palsu.

---

## 3. S2 — Ambigu Sebelum Modul Dikerjakan

### C-011 — Dua endpoint untuk membuat workflow definition

- **Bukti:** `42-API.md` memuat `POST /workflows/definitions` **dan** `POST /admin/workflow-definitions`.
- **Dampak:** dua jalur otorisasi berbeda untuk operasi yang sama.
- **Usul:** pertahankan `/workflows/definitions` (dipakai TSD §6) dan hapus yang di `/admin`.

### C-012 — Requirement tanpa endpoint, dan endpoint tanpa requirement

- **Bukti (requirement tanpa endpoint):** FR-AUTH-08 reset password, FR-AUTH-09 ubah password sendiri, FR-ROLE-04 assign/unassign role, FR-ORG-03 buat & kelola organization, FR-REP-01 export CSV, FR-AUDIT-04 filter audit log — tidak ada endpoint eksplisit di `42-API.md`.
- **Bukti (endpoint tanpa requirement):** `DELETE /documents/:id` (lihat C-004).
- **Dampak:** traceability bocor; agen tidak tahu kontrak yang harus dibuat.
- **Usul:** tambahkan endpoint untuk kelima requirement itu di `42-API.md`, lalu catat pemetaannya di `TRACEABILITY.md`.

### C-013 — API tidak memuat endpoint yang sudah diregistrasi

- **Bukti:** `40-TSD.md:536-538` meregistrasi `GET /workflows/definitions/:id` dan `POST /workflows/definitions/:id/steps`, keduanya tidak ada di `42-API.md` §5.
- **Usul:** lengkapi `42-API.md` atau hapus dari registrasi route.

### C-014 — Ringkasan environment di `90-AGENT-GUIDE.md` usang dan melanggar guard ADR-0010

- **Bukti:** `90-AGENT-GUIDE.md:350-356` hanya memuat `DB_PASSWORD, JWT_SECRET, ADMIN_USERNAME, ADMIN_PASSWORD, APP_ENV, LOG_LEVEL` — tanpa `DB_HOST/PORT/NAME/USER`, `ADMIN_ORG_*`, `STORAGE_*`, `APP_PORT`. Contoh nilainya masih `ADMIN_PASSWORD=admin123`, padahal ADR-0010 melarang nilai contoh itu dan `.env.example` sudah memakai placeholder yang benar.
- **Dampak:** agen yang menyalin blok ringkasan akan menghasilkan aplikasi yang gagal start.
- **Usul:** ganti blok itu dengan tautan ke `60-DEPLOYMENT.md` §2.1 + `.env.example` tanpa contoh nilai.

### C-015 — Dials desain bertentangan

- **Bukti:** `01-AGENT-WORKFRAME.md:123-129` menetapkan **ENERGY 1 / RHYTHM 2 / MOTION 1** sebagai dial proyek. `DESIGN.md:6,51,55`, `11-DESIGN-DIRECTION.md:48`, dan ADR-0007 menetapkan **1/1/1** selama arah desain belum ada.
- **Dampak:** RHYTHM 2 menjanjikan variasi komposisi antar halaman, sementara status resmi proyek adalah "tanpa arah" yang wajib 1/1/1.
- **Usul:** selama `DESIGN.md` kosong, pakai 1/1/1; tandai 1/2/1 di `01` sebagai usulan yang menunggu arah desain.
- **Status:** FIXED (P-037). Usulnya **tidak** dipakai apa adanya karena keputusannya berpindah: user memilih **jalur 2 ADR-0007** (agen menyusun arah desain atas instruksi eksplisit), sehingga tidak ada lagi status "tanpa arah" yang menuntut 1/1/1. `DESIGN.md` terisi (ADR-0006/0007 `ACCEPTED`), dial resminya **ENERGY 1 / RHYTHM 2 / MOTION 1** beserta alasan per dial di `DESIGN.md` §5, dan `01-AGENT-WORKFRAME.md` §3.3 kini menyatakan angka itu sebagai **resmi**, bukan usulan. Ditemukan sekaligus ditutup pada sesi yang sama: **C-060** (palet & tipografi `51-UX.md` §3/§4 yang bertentangan dengan arah desain yang baru ditetapkan). Lihat tabel Status Tindak Lanjut di §6.

### C-016 — Format nomor dokumen tidak ditetapkan

- **Bukti:** `50-FSD.md:136` "Document Number (required, auto-increment suggestion)"; DDL `41-DATABASE.md:158` `document_number VARCHAR(100)` dengan unique `(project_id, document_number)`; contoh di `12-DEVELOPMENT-WORKFLOW.md:85`, `02-AGENT-PROGRESS-PROTOCOL.md:142`, `IDEA.md` memakai `DOC-001`.
- **Dampak:** agen bisa membuat generator berbeda (per project, per organisasi, atau manual), dan data lama tidak konsisten.
- **Usul:** tetapkan format (mis. `{PROJECT_CODE}-{URUT}` per project), siapa yang menomori, dan apakah boleh diisi manual.
- **Status:** FIXED (P-016) — usul diambil pada bentuk formatnya; bagian "boleh diisi manual" **ditolak** dengan alasan tertulis. ADR-0017 `ACCEPTED`: format `{PROJECT_CODE}-{NNN}`, dibangkitkan **server** secara atomik di tabel `document_sequences` (di dalam transaksi yang sama), immutable, tanpa penomoran manual (`document_number` di request → `422`). Cacat keluarga yang ikut ketahuan: tag validator `alphanum` di `44-SECURITY.md` §4.1 **menolak tanda hubung** sehingga tidak satu pun contoh nomor yang ada lolos validasi, dan `projects.code` harus permanen karena menjadi prefiks nomor. Lihat tabel Status Tindak Lanjut di §6.

### C-017 — Isi migrasi `008_seed_default_roles.sql` tidak ada di dokumen

- **Bukti:** `41-DATABASE.md:374` hanya mencantumkan nama berkas. Matriks RBAC di `40-TSD.md` §5.3 hanya 15 baris aksi dan tidak selaras dengan pasangan `resource`/`action` di tabel `role_permissions` (`41-DATABASE.md:75-84`).
- **Dampak:** RBAC tidak dapat diimplementasikan konsisten; setiap agen akan mengarang daftar permission.
- **Usul:** tulis matriks permission lengkap (resource × action × role) di satu tempat dan jadikan sumber migrasi `008`.

### C-022 — Request revision: kembali ke step sebelumnya atau reset ke step 1?

- **Bukti:** `20-SRS.md` FR-WF-09 "Action: Request Revision → dokumen Revision Required, workflow kembali ke **step sebelumnya**". `43-WORKFLOW.md` §4.5 menulis "Optionally roll back to previous step or reset to step 1 … **Default: reset to step 1**".
- **Dampak:** dua perilaku berbeda untuk aksi yang sama. Perbedaan ini terlihat oleh user (dokumen yang diminta revisi bisa kembali ke step 1 dan harus melewati semua approval lagi, atau hanya mundur satu langkah) dan mengubah beban approval. Karena FR-WF-09 adalah requirement High, agen berikutnya harus memilih satu — dan pilihannya tidak tercatat di mana pun.
- **Catatan:** pada DDL MVP `workflow_steps` **tidak punya `order` negatif atau riwayat step per instance**, jadi keduanya dapat diimplementasikan (`current_step = current_step - 1` vs `= 1`); yang belum ada hanyalah keputusannya.
- **Usul resolusi:** pilih **kembali ke step sebelumnya** agar sesuai FR-WF-09 apa adanya, dan jadikan "reset ke step 1" sebagai opsi definisi workflow bila kelak dibutuhkan (butuh kolom, bukan perilaku bawaan). Alternatif lain: ubah FR-WF-09 menjadi "kembali ke step yang ditentukan definisi".
- **Status:** FIXED (P-014) — usul resolusi diambil apa adanya; lihat tabel Status Tindak Lanjut di §6.

### C-023 — Route aksi workflow memakai izin `approve` untuk semua aksi

- **Bukti:** `40-TSD.md` §5.3 dan §6 memasang `RequirePermission("workflow_instance", "approve")` pada `POST /workflows/instances/:id/actions`, sementara `44-SECURITY.md` §3.1.2 memisahkan `workflow_instance:approve`, `:reject`, dan `:request_revision` (dan satu-satunya contoh di `43-WORKFLOW.md` §4.2 memilih cabang dari isi body).
- **Dampak:** `RequirePermission` hanya menerima resource/action statis; route ini akan menolak `reject` untuk role yang hanya punya `:reject`, atau meloloskan `reject` bagi pemegang `:approve` saja. Izin yang ditulis di middleware tidak akan pernah cocok dengan matriks — kelas kesalahan yang sama dengan C-017.
- **Usul resolusi:** route hanya memasang izin baca (`workflow_instance:read`), lalu service memilih izin dari `input.Action` setelah body divalidasi; catat bahwa ini pengecualian yang disengaja.
- **Status:** FIXED pada P-013.

### C-028 — Setting "Audit log retention" bertabrakan dengan audit log append-only

- **Bukti:** `50-FSD.md:414` mencantumkan "Audit log retention" sebagai setting di `/admin/settings`, sementara `20-SRS.md` §3.11 tidak memuat requirement retensi apa pun, `41-DATABASE.md` §2.6 tidak memuat kunci retensi pada `system_settings`, dan `42-API.md` §11 tidak punya endpoint/field untuknya. Di sisi lain FR-AUDIT-03 (High) menyatakan audit log **tidak dapat diedit/dihapus**, dan sejak C-020 hal itu ditegakkan trigger di database (`44-SECURITY.md` §6, migrasi `007`).
- **Dampak:** implementasi halaman Settings akan membuat kontrol mati (tidak ada perilakunya), atau — lebih buruk — agen menulis purge `DELETE` yang langsung ditolak SQLSTATE `23001` oleh trigger dan menghabiskan waktu mencari penyebabnya. Tafsir alternatif ("retensi = berapa lama ditampilkan") juga tidak tertulis di mana pun.
- **Usul:** putuskan satu, lalu tulis di sumbernya: (a) **hapus** butir itu dari MVP — paling sesuai FR-AUDIT-03 dan paling murah; atau (b) tetapkan retensi sebagai **operasi pemeliharaan terjadwal** yang memakai jalur `bwdcs.audit_maintenance` dengan ADR baru (menentukan masa simpan, izin, dan jejak audit operasi itu sendiri).
- **Status:** OPEN — menunggu keputusan user. Ditemukan saat mengerjakan C-020: trigger append-only-lah yang membuat tabrakan ini nyata, bukan hipotetis.

### C-032 — `internal/migration/` dijanjikan "bukan paket Go", padahal migrasi startup menuntut embed

- **Bukti:** `docs/adr/0013` butir 1 menyatakan `internal/` memuat `migration/` **(berkas `.sql`, bukan paket Go)**, dan `40-TSD.md` §2.0 menulis hal yang sama di pohon strukturnya — sejalan dengan aturan "satu folder = satu package" (ADR-0013). Di sisi lain `41-DATABASE.md` §4 menyatakan migrasi **dijalankan saat startup**, dan ADR-0010 butir 1 menetapkan urutan `koneksi → migrasi → bootstrap → HTTP`. `//go:embed` hanya dapat menjangkau berkas **di dalam direktori package**, jadi berkas `.sql` di `internal/migration/` tidak dapat di-embed tanpa direktori itu menjadi package.
- **Dampak:** dua kontrak yang sudah terkunci tidak dapat dipenuhi bersamaan. Tanpa keputusan, agen berikutnya akan memilih sendiri: memindahkan berkas migrasi ke direktori lain (tiga dokumen memanggil `goose -dir internal/migration`), menjalankan `goose` CLI dari Go (bergantung tool di `PATH`), atau melepas migrasi dari startup (melanggar ADR-0010 butir 1).
- **Usul:** ADR baru yang memperbarui butir 1 ADR-0013 — jangan mengedit ADR yang sudah `ACCEPTED` (aturan `docs/adr/README.md` §2.4).
- **Status:** FIXED (P-020) lewat **ADR-0018**: `internal/migration/` menjadi package `migration` (berkas `.sql` + satu berkas `migration.go` yang hanya meng-embed & menjalankannya), goose dipin v3.24.1 (library **dan** CLI) karena toolchain Go 1.22.5, dan berkas `.sql` tetap sumber kebenaran skema. `docs/adr/README.md` menandai butir 1 ADR-0013 diperbarui ADR-0018; `40-TSD.md` §2.0 diperbarui.

---

### C-034 — Package `auth` hantu dan signature `AuditService.Log` tanpa transaksi

- **Bukti:** `40-TSD.md` §2.2 dan §6 memakai `*auth.PermissionChecker` (`func Setup(r *gin.Engine, pc *auth.PermissionChecker)`), padahal pohon struktur di §2.0 — sumber tunggal struktur backend (ADR-0013) — tidak pernah memuat package `auth`. Terpisah: `AuditService.Log(actorID, action, entity, entityID, description string, metadata)` di §2.4 tidak memuat parameter `pgx.Tx`, sementara ADR-0011 butir 2 mewajibkan entri audit ditulis di transaksi pemanggil supaya ikut rollback.
- **Dampak:** agen yang mengikuti §2.2/§6 akan membuat package di luar struktur yang disepakati, atau menulis audit di luar transaksi — pelanggaran langsung ADR-0011.
- **Usul resolusi:** ganti rujukannya ke interface yang benar. **Selesai pada P-021**: §2.2 mendefinisikan `TokenValidator`/`RevocationChecker`/`PermissionChecker` sebagai interface di package `middleware` (tanpa package `auth`), §2.4 memakai `Log(ctx, tx pgx.Tx, ...)`, dan lokasi `Setup` disebut eksplisit (`internal/handler/router.go`).

### C-035 — "Login" wajib diaudit, tetapi login gagal tidak dapat ditulis ke `audit_logs`

- **Bukti:** FR-AUDIT-01 (`20-SRS.md`) mewajibkan setiap aksi kritis — termasuk **login** — dicatat di `audit_logs`, sedangkan `audit_logs.actor_id` bersifat `NOT NULL REFERENCES users(id)` (`41-DATABASE.md` §2.5). Percobaan login dengan username yang tidak ada tidak punya baris user untuk dirujuk, sehingga kegagalannya tidak dapat direpresentasikan.
- **Dampak:** percobaan brute force tidak meninggalkan jejak di audit log; satu-satunya jejak ada di log aplikasi, yang tidak punya jaminan append-only. Bila seorang agen "menambal" ini dengan user palsu atau `actor_id` karangan, audit trail berisi baris yang menyesatkan.
- **Usul resolusi:** putuskan. (a) `actor_id` menjadi nullable untuk entri sistem dan login gagal dicatat dengan `actor_id = NULL` + username di `metadata`; (b) tabel/domain audit tersendiri untuk peristiwa autentikasi; (c) turunkan FR-AUDIT-01 sehingga "login" berarti login berhasil saja, dan kegagalan cukup di log aplikasi. Ditemukan saat mengerjakan `T-005` (P-021).

---

## 4. S3 — Inkonsistensi Ringan

### C-018 — Modul "Approvals" ada di navigasi tetapi tidak ada di FSD/routing agen

- **Bukti:** `51-UX.md:52` (menu Approvals), `30-ARCHITECTURE.md:84` (folder handler `Approvals/`), `70-TESTING.md:253` (`/approvals`). Tidak ada bagian modul Approvals di `50-FSD.md` (hanya §5.3 Approval Panel) dan tidak ada baris routing Approvals di `AGENTS.md`.
- **Usul:** nyatakan Approvals sebagai **view** dari workflow instance, bukan modul baru; tambahkan baris routing di `AGENTS.md`.
- **Status:** FIXED (P-015) — usul diambil: `50-FSD.md` §5.4 menetapkan Approvals sebagai view workflow instance (bukan modul/baru package backend), plus dua halaman sekelas dari perluasan cakupan P-012: Reports → §10.6, Administration > Workflows → §10.7. Endpoint list instance `GET /workflows/instances` (dengan `scope=assigned_to_me` untuk antrean) ditambahkan ke `42-API.md` §5 karena halaman antrean tidak mungkin memakai endpoint detail saja. Baris routing Approvals di `AGENTS.md` ditambahkan. Lihat tabel Status Tindak Lanjut di §6.

### C-019 — Label navigasi berbeda dari nama status

- **Bukti:** `51-UX.md:50` "Documents | All, My, Pending, Revision, Approved" vs `IDEA.md` "Pending Review, Revision Required, Approved".
- **Usul:** pakai istilah IDEA agar konsisten dengan label status.

### C-020 — Cuplikan SQL trigger immutable yang tidak valid

- **Bukti:** `44-SECURITY.md:248-252` memakai `EXECUTE FUNCTION raise_exception('...')`; PostgreSQL tidak punya fungsi `raise_exception`.
- **Usul:** ganti dengan fungsi PL/pgSQL yang memanggil `RAISE EXCEPTION`, atau gunakan pola revoke `UPDATE/DELETE` pada tabel.
- **Status:** FIXED (P-019) — diganti fungsi PL/pgSQL `prevent_audit_modification()` + **dua** trigger: `trg_audit_logs_append_only` (`BEFORE UPDATE OR DELETE`, row-level) dan `trg_audit_logs_no_truncate` (`BEFORE TRUNCATE`, statement-level — row-level trigger tidak menyala untuk `TRUNCATE`), keduanya menolak dengan SQLSTATE `23001` (`restrict_violation`). Trigger diikat ke migrasi `007_create_comments_notifications_audit.sql` (`41-DATABASE.md` §4, sebelumnya tidak ada yang memasangnya), test di `70-TESTING.md` §4.3, checklist `44-SECURITY.md` §8. Pola `REVOKE` **ditolak** sebagai pengaman utama karena migrasi dijalankan aplikasi sendiri sehingga aplikasi adalah owner tabel. Lihat tabel Status Tindak Lanjut di §6.

### C-024 — Requirement ID hantu `FR-DOC-08` di contoh commit

- **Bukti:** `12-DEVELOPMENT-WORKFLOW.md:118` contoh commit `feat(document): upload versi baru dengan revision note [FR-DOC-01, FR-DOC-08]`, sedangkan `20-SRS.md` §3.5/§3.6 hanya memuat `FR-DOC-01`..`FR-DOC-07` dan `FR-VER-01`..`FR-VER-06`.
- **Dampak:** commit berikutnya akan mengutip requirement yang tidak ada; `TRACEABILITY.md` tidak dapat memetakannya, dan agen bisa merasa perlu "melengkapi" SRS dengan requirement karangan.
- **Usul:** ganti dengan ID yang nyata (`FR-VER-01` untuk upload versi baru, `FR-VER-04` untuk revision note).
- **Status:** FIXED (P-016) — rujukan diganti menjadi `[FR-VER-01, FR-VER-04]`. Ditemukan saat mengerjakan C-016 (modul yang sama), bukan temuan lama.

---

### C-036 — Test integrasi tidak terisolasi dari data bersama (dua sebab)

- **Bukti (sebab 1 — paralel):** `70-TESTING.md` §3/§4/§8 menetapkan satu `TEST_DATABASE_URL` bersama untuk semua test integrasi, tetapi dokumentasi menjalankan `go test ./... -cover` (mis. `12-DEVELOPMENT-WORKFLOW.md` §4) — perintah itu menjalankan paket **paralel**. Dua test `internal/bootstrap` menghitung `users` secara global dan menghapus tabel yang sama di dalam transaksinya, sehingga paket yang menulis user ter-commit (auth) membuatnya gagal.
- **Bukti (sebab 2 — data nyata):** `newCleanTx` di `internal/bootstrap/bootstrap_test.go` menjalankan `DELETE FROM users` untuk memenuhi prasyarat bootstrap ("tabel `users` kosong"). Setelah aplikasi benar-benar dipakai — entri audit `LOGIN`/`LOGOUT` admin pertama dari pembuktian `T-005` — perintah itu gagal dengan `audit_logs_actor_id_fkey` (SQLSTATE 23503), karena `audit_logs.actor_id` merujuk `users(id)` tanpa `ON DELETE`. Akibatnya **seluruh paket bootstrap gagal** (cakupan turun ke 8%) hanya karena database dev berisi jejak audit asli.
- **Dampak:** test yang bergantung pada "database kosong" akan gagal justru ketika sistem dipakai — kebalikan dari gunanya. Kegagalannya juga mudah disalahartikan sebagai bug modul lain, dan dapat mendorong "perbaikan" yang merusak (mis. men-truncate `audit_logs` sungguhan).
- **Usul resolusi:** (a) jalankan paket test serial, (b) izinkan test membersihkan dirinya lewat jalur pemeliharaan yang sudah ada **di dalam transaksi yang digulung balik**. **Selesai pada P-021**: `backend/Makefile` memakai `-p 1`; `newCleanTx` menyetel `SET LOCAL bwdcs.audit_maintenance = 'on'` lalu menghapus `audit_logs`/`token_revocations`/`user_roles`/`users`/`organizations` di dalam transaksi test (entri audit asli tetap utuh karena rollback — dibuktikan dengan `psql` setelah suite berjalan); `70-TESTING.md` §8 memuat peringatan eksplisit beserta alasannya.

### C-037 — "Admin role has all permissions (special case)" bertentangan dengan matriks dan aturan test

- **Bukti:** `44-SECURITY.md` §3.2 butir 4 menyebut cabang khusus "Admin role has all permissions (special case)", padahal §3.1.2 sudah memberi Administrator seluruh 44 izin dan `70-TESTING.md` §4.1 melarang bypass di kode dipakai sebagai bukti pemenuhan FR-ROLE-03.
- **Dampak:** dua jalur kebijakan izin (tabel dan kode). Bila keduanya berbeda — mis. matriks dikurangi tetapi kode tetap membolehkan — perbedaan itu tidak akan terlihat sampai terjadi kebocoran akses.
- **Usul resolusi:** periksa hanya tabel `role_permissions`. **Selesai pada P-021**: butir itu diganti catatan eksplisit di `44-SECURITY.md` §3.2, dan `service.PermissionChecker` tidak memiliki cabang Administrator.

---

## 5. Temuan yang Sudah Terbukti Bukan Kontradiksi (jangan diutak-atik tanpa alasan)

Diperiksa dan **konsisten**, agar tidak diubah tanpa dasar:

| Hal | Hasil |
|---|---|
| Whitelist upload | `44-SECURITY.md` §4.2 dan `50-FSD.md` §4.2 sama-sama pdf/txt/csv/xls/xlsx/jpg/png (kekurangan `.doc/.docx/.pptx` = keputusan produk Q-008, bukan kontradiksi) |
| Batas ukuran file | 100 MB di SRS, FSD, `system_settings`, dan `70-TESTING` |
| Masa berlaku token | 24 jam di SRS FR-AUTH-03, `44-SECURITY.md` §2.2, `60-DEPLOYMENT.md` (`JWT_EXPIRY=24h`) |
| Status project | `active/archived` sama di DDL dan FR-PROJ-03 |
| Status instance workflow | `running/completed/rejected` sama di DDL dan `43-WORKFLOW.md` §2.2 |
| Daftar migrasi | Kini hanya satu sumber (`41-DATABASE.md` §4) setelah perbaikan P-006 |
| Role dasar | 4 role (Administrator, Manager, Contributor, Viewer) konsisten di SRS, DB, dan matriks RBAC |

---

## 6. Cara Menindaklanjuti

1. Pilih temuan yang akan diperbaiki (boleh sebagian).
2. Untuk temuan yang mengubah keputusan arsitektur (C-004, C-007, C-009, C-010), buat ADR baru alih-alih mengedit diam-diam. C-005, C-016, dan C-017 sudah diselesaikan lewat ADR-0015, ADR-0017, dan ADR-0014.
3. Setiap perbaikan wajib: ubah dokumen sumber, catat di `docs/progress/CHANGELOG.md`, tulis log prompt baru, dan perbarui `TRACEABILITY.md` bila menyentuh requirement.
4. Temuan yang sudah diperbaiki diberi nomor tetap dan **tidak dihapus** dari file ini; cukup ditambahi status di tabel di bawah.

### Status Tindak Lanjut

| ID | Status | Diputuskan pada | Catatan |
|---|---|---|---|
| C-001 | **FIXED** | 2026-09-18 (P-008) | ADR-0011 `ACCEPTED`: audit log hanya ditulis di **service**, di dalam transaksi yang sama; handler tidak menyimpan/memanggil `AuditService`. Diperbaiki di `40-TSD.md` §2.4/§2.6, `90-AGENT-GUIDE.md` §3.1, `12-DEVELOPMENT-WORKFLOW.md` §5, `30-ARCHITECTURE.md` §3.3 |
| C-002 | **FIXED** | 2026-09-18 (P-008) | ADR-0013 `ACCEPTED`: `40-TSD.md` §2.0 menjadi **satu-satunya** definisi struktur folder backend (flat, satu folder = satu package, tanpa subfolder per modul, memuat `internal/bootstrap`, `pkg` hanya infrastruktur). Pohon duplikat di `30-ARCHITECTURE.md` §3.2, `01-AGENT-WORKFRAME.md` §6, dan `90-AGENT-GUIDE.md` §2.1 diganti tautan |
| C-003 | **FIXED** | 2026-09-18 (P-008) | ADR-0012 `ACCEPTED`: tabel pemetaan status kanonik ↔ label ↔ warna di `50-FSD.md` §11 (baru), dan "overdue" dinyatakan **turunan** (`due_date` lewat + status bukan `completed`), bukan nilai kolom. Diselaraskan di `51-UX.md`, `43-WORKFLOW.md` §7, `20-SRS.md` FR-TASK-06, `41-DATABASE.md` §2.3/§2.5, `IDEA.md` 2.G, `80-ROADMAP.md` Phase 3, `70-TESTING.md` |
| C-004 | **FIXED** | 2026-09-19 (P-029) | **Arsip sebagai default, hapus berkaskade dibatalkan** — **ADR-0019** `ACCEPTED`: `DELETE /documents/:id` diganti `POST /documents/:id/archive`, `documents.archived_at` + nilai kanonik `archived`, penghapusan permanen tidak ada di MVP, dan izin arsip memakai `document:update` (baris `document:delete` disediakan untuk penghapusan permanen yang belum ada). Karena kaskade hilang, `44-SECURITY.md` §6 dapat memasang trigger append-only pada `document_versions` (menutup pengecualian yang selama ini tertulis di sana). Kontrak `42-API.md` §4, `20-SRS.md` FR-DOC-03/FR-VER-03, `50-FSD.md` §4.3/§11.1/§11, `41-DATABASE.md` §2.3/§4, dan `44-SECURITY.md` §3.1/§6.1 sudah diselaraskan. **Kode menyusul di `T-039`** — dan itulah yang terjadi: `P-029` mengerjakan `T-039`, sehingga temuan ini berubah menjadi `FIXED`. **Bukti (`70-TESTING.md` §3.12a):** migrasi `010` terpasang (`versi_skema 10`) dengan `documents.archived_at` + `CHECK` enam nilai + dua trigger `document_versions` (`TestDocumentVersions_*`, juga membuktikan kaskade `DELETE FROM documents` ikut tertahan); `POST /documents/:id/archive` → `200` (`status=archived`, `archived_at` terisi) sementara baris, versi, berkas, dan unduhan tetap ada; daftar default `total 0` sedangkan `?status=archived` `total 1`; unggahan versi baru dan arsip ulang `409`; `DELETE /documents/:id` kini `404` (tidak ada alias); Viewer `403` (izinnya `document:update`, bukan `document:delete`). Kosakata kode = `CHECK` database diuji `TestDocumentStatusVocabularyIncludesArchived` |
| C-006 | **FIXED** | 2026-09-19 (P-026) | **Label "Reviewer" adalah peran fungsional, bukan role sistem kelima.** Diselaraskan di empat dokumen: `10-BRD.md` §4/BR-07 (peran fungsional = penanggung jawab step berjalan), `20-SRS.md` §2.3 (tidak ada role `reviewer` di tabel `roles`; izinnya dari role sistem user itu), `51-UX.md` §2.1 ("Reviewer+" = role berwenang bertindak pada step itu; *melihat* antrean pakai `workflow_instance:read` yang dimiliki semua role, *bertindak* butuh approve/reject/request_revision yang hanya Admin/Manager), dan `44-SECURITY.md` §3.1 (catatan penutup sudah ada sejak P-009). Tidak ada ADR karena hanya menyelaraskan dokumen — rekomendasi `OPEN-QUESTIONS.md` §3 |
| C-007 | **FIXED** | 2026-09-19 (P-026) | **Dua hierarki role dipisahkan dan tidak pernah digabung.** `44-SECURITY.md` §3.3 kini menyatakan urutan role **sistem** (`administrator` > `manager` > `contributor` > `viewer`) dan role **project** (`owner` > `manager` > `contributor` > `viewer` pada `project_members`) hidup di dua ruang berbeda, dan rantai gabungan ("owner project lebih tinggi daripada manager sistem") **bukan** pernyataan yang sah karena tidak ada keputusan izin yang membutuhkannya. `Owner` hanya ada di tingkat project — karena itu tidak ada di tabel `roles` (seed `008` tetap empat nama, ADR-0014). Tanpa ADR: hanya menyelaraskan dokumen |
| C-005 | **FIXED** | 2026-09-18 (P-013) | ADR-0015 `ACCEPTED`: `workflow_instances.version INTEGER NOT NULL DEFAULT 0` + pola guard conditional `UPDATE ... WHERE id AND version AND status = 'running' AND current_step`, `rowsAffected = 0` → rollback transaksi dan `409 WORKFLOW_CONFLICT` (`42-API.md` §5/§12), tanpa retry otomatis. Diperbaiki di `41-DATABASE.md` §2.4/§4, `43-WORKFLOW.md` §4.1/§4.2/§6, `40-TSD.md` §2.3/§2.4, `70-TESTING.md` §3.3 |
| C-008 | **FIXED** | 2026-09-18 (P-009) | Tertutup bersama C-017: matriks permission menetapkan `audit:read` hanya untuk Administrator (ADR-0014), dan `51-UX.md` §2.1 diselaraskan (baris Reports dipisah, Audit = Admin) |
| C-009 | **FIXED** | 2026-09-19 (P-030) | **Auto-lock dijalankan lewat tabel `login_attempts` + kolom `users.locked_until`** — **ADR-0022** `ACCEPTED`: ambang & durasi dari `system_settings` yang **sudah ada** (`auth.max_login_attempts`, `auth.lockout_duration_minutes`), lock **sementara** (terbuka sendiri, dan dapat dibuka lebih awal Administrator lewat `POST /admin/users/:id/unlock`), alasan memilih sementara bukan permanen adalah agar penyerang tidak dapat mengunci akun orang lain. Kontrak `44-SECURITY.md` §2.3/§8, `20-SRS.md` FR-AUTH-06, `42-API.md` §2/§11/§12 (`423 LOCKED`), `40-TSD.md` §5.2.2, `41-DATABASE.md` §2.1/§4, `70-TESTING.md` §3.12 sudah diselaraskan. **Dijalankan pada P-030 (`T-041`)**: `internal/repository/login_attempt_repository.go` (tulis percobaan + hitung kegagalan menurut jam database), `AuthService.Login`/`applyLockout` (ambang & durasi dari kebijakan `system_settings` yang dibaca `cmd/server/main.go`), `UserRepository.LockUntil`/`ClearLock`, `POST /admin/users/:id/unlock` (`service.UserService` + `handler.UserHandler`, izin `user:update`), dan **`internal/service/login_guard.go` dihapus** — penghitung di memori tidak lagi ada, sehingga tidak ada dua pembatas yang berbeda pendapat (ADR-0022 butir 6). Bukti pada server nyata: lima percobaan gagal → `401`×4 lalu **`423`** + `details.retry_after_seconds`, password benar saat terkunci → `423`, `unlock` → `200` (panggilan kedua `200` tanpa audit kedua), `audit USER_UNLOCKED=1`, dan `login_attempts` memuat `succeeded=true`=4 / `false`=6. Ringkasan: `70-TESTING.md` §3.12b |
| C-010 | **FIXED** | 2026-09-19 (P-026) | **Otorisasi step bersifat role-based untuk MVP.** `20-SRS.md` FR-WF-03 kini menyatakan bagian **role** yang diberlakukan dan `43-WORKFLOW.md` §5 menandai `responsible_user_id` sebagai **ditunda** beserta alasannya (dua sumber kebenaran untuk pertanyaan "siapa yang boleh bertindak di step ini?" tidak punya jawaban yang dapat diuji; kebutuhan delegasi belum nyata). Tidak ada perubahan skema — dan tidak ada janji yang tertinggal, karena dokumennya kini menyebut penundaan itu secara eksplisit. Tanpa ADR: hanya menyelaraskan requirement dengan dokumen workflow |
| C-011 | **FIXED** | 2026-09-18 (P-011) | Endpoint kembar `POST /admin/workflow-definitions` **dihapus**; satu jalur dipertahankan (`POST /workflows/definitions`) sesuai usul resolusi audit. Pembatasan hanya-Administrator dipindahkan ke izin `workflow_definition:manage` (ADR-0014), bukan prefiks path. `40-TSD.md` §6 juga memuat catatan agar tidak kembali ke bentuk lama |
| C-012 | **FIXED** | 2026-09-18 (P-012) | Enam requirement dilengkapi kontrak endpointnya di `42-API.md`: `POST /auth/change-password` (FR-AUTH-09, §2), `POST /admin/users/:id/reset-password` (FR-AUTH-08, §11), `PUT /admin/users/:id/roles` (FR-ROLE-04, §11), `POST` + `PATCH /admin/organizations` (FR-ORG-03, §11), `GET /reports/export` (FR-REP-01, §10 baru), dan filter eksplisit pada `GET /audit` (FR-AUDIT-04, §9). Setiap endpoint diberi izin dari matriks `44-SECURITY.md` §3.1. `PATCH /admin/users/:id` dipersempit ke status/profil agar perubahan role punya izin dan jejak audit tersendiri |
| C-013 | **FIXED** | 2026-09-18 (P-011) | `GET /workflows/definitions/:id` dan `POST /workflows/definitions/:id/steps` ditambahkan ke `42-API.md` §5 lengkap dengan contoh request dan izinnya. Akar masalahnya juga dibereskan: **`42-API.md` ditetapkan sebagai sumber tunggal daftar endpoint**, dan `40-TSD.md` §6 dipersempit menjadi contoh pemasangan route (dua daftar hampir lengkap sebelumnya terus berbeda) |
| C-014 | **FIXED** | 2026-09-18 (P-010) | Ringkasan env usang di `90-AGENT-GUIDE.md` §7 **dihapus** dan diganti penunjuk ke `60-DEPLOYMENT.md` §2.1 + `.env.example`, dengan larangan eksplisit menyalin daftar/nilai contoh ke dokumen lain. Dua duplikasi sekelas ikut dibersihkan: blok YAML compose kedua di `30-ARCHITECTURE.md` §5.1 (memuat `ports: ["8080:8080"]` yang bertentangan dengan port host `8081`) dan kredensial `admin123` pada test E2E `70-TESTING.md` §5.1 |
| C-015 | **FIXED** | 2026-09-21 (P-037) | **Dijawab user, lalu diisi agen — dan itu memang jalur 2 ADR-0007.** User memilih **mode antislop `during`** untuk **Q-001** dan **menyuruh agen menyusun arah desain** untuk **Q-002**; keduanya dicatat sebagai **ADR-0006 `ACCEPTED`** dan **ADR-0007 `ACCEPTED` (jalur 2)** di sesi yang sama. `DESIGN.md` kini `TERISI`: identitas, palet + alasan per token, tipografi, dials, motif, tema, dan Design Read, dengan **peringatan eksplisit** bahwa butir *rasa* adalah draf agen sedangkan butir *struktural* (dials, aksesibilitas, semantik status, aturan mono) yang dipertanggungjawabkan. Dial yang bertentangan di `01-AGENT-WORKFRAME.md` §3.3 (`1/2/1`) kini **resmi** dan tidak lagi ditandai usulan; nilai 1/1/1 sementara gugur. Mekanismenya tidak lagi prosa: palet dan skala dikunci di `frontend/src/styles/tokens.css` dan **dihitung ulang test** (`src/styles/tokens.contrast.test.ts`, 31 test, WCAG 2.2 AA) |
| C-016 | **FIXED** | 2026-09-18 (P-016) | ADR-0017 `ACCEPTED`: nomor dokumen selalu dibangkitkan server dengan format `{PROJECT_CODE}-{NNN}` (penghitung per project di `document_sequences`, atomik lewat `INSERT ... ON CONFLICT ... RETURNING` di transaksi yang sama), immutable, tanpa penomoran manual (`document_number` di request → `422`). `projects.code` menjadi permanen karena jadi prefiks. `41-DATABASE.md` §2.2/§2.3/§3/§4, `42-API.md` §3/§4/§12, `50-FSD.md` §3.2/§4.2, `20-SRS.md` FR-DOC-02, `40-TSD.md` §2.1/§5.4, `44-SECURITY.md` §4.1, `90-AGENT-GUIDE.md` §3.1, `70-TESTING.md` §3.1/§3.5/§5.1 |
| C-017 | **FIXED** | 2026-09-18 (P-009) | ADR-0014 `ACCEPTED`: matriks permission lengkap (17 resource × 15 action, 44 baris × 4 role) menjadi **sumber tunggal** migrasi `008_seed_default_roles.sql` di `44-SECURITY.md` §3.1, dengan kosakata tertutup, aturan "satu sel Y = satu baris", jumlah baris yang diharapkan (44/30/18/12 = 104), dan pemisahan izin dari cakupan data. `40-TSD.md` §5.3 menjadi penunjuk; `41-DATABASE.md` §4 memuat prosedur migrasinya |
| C-018 | **FIXED** | 2026-09-18 (P-015) | Tiga halaman nav tanpa spec masing-masing punya bagian FSD: Approvals → `50-FSD.md` §5.4 (**view** workflow instance, bukan modul baru — sesuai usul resolusi), Reports → §10.6 (daftar + export, tiga sub-menu memakai spec halaman daftar yang sudah ada), Administration > Workflows → §10.7 (terikat ke endpoint §5 `42-API.md`; tanpa edit/delete definisi di MVP). Endpoint daftar instance `GET /workflows/instances` (`?status=&scope=assigned_to_me`) ditambahkan ke `42-API.md` §5 sebagai prasyarat halaman antrean; routing `AGENTS.md` dilengkapi |
| C-019 | **FIXED** | 2026-09-18 (P-008) | Tertutup sebagai efek samping C-003: sub-menu di `51-UX.md` §2.1 memakai label dari tabel pemetaan ("Pending Review", "Revision Required") |
| C-020 | **FIXED** | 2026-09-18 (P-019) | `EXECUTE FUNCTION raise_exception(...)` — fungsi yang tidak ada di PostgreSQL — diganti fungsi PL/pgSQL `prevent_audit_modification()` + dua trigger: `trg_audit_logs_append_only` (`BEFORE UPDATE OR DELETE`, row-level) dan `trg_audit_logs_no_truncate` (`BEFORE TRUNCATE`, statement-level), keduanya menolak `23001`. Satu jalur pemeliharaan eksplisit (`SET LOCAL bwdcs.audit_maintenance = 'on'`), tanpa itu semua perubahan ditolak — dipakai teardown test dan operasi terjadwal. Trigger diikat ke migrasi `007` (`41-DATABASE.md` §4; sebelumnya tidak ada migrasi yang memasangnya), test `70-TESTING.md` §4.3 + catatan teardown §8, checklist `44-SECURITY.md` §8. Pola dijalankan & diuji pada PostgreSQL 16.10 sebelum ditulis ke dokumen. Memunculkan temuan baru **C-028** |
| C-021 | **FIXED** | 2026-09-18 (P-013) | Tertutup bersama C-005 lewat ADR-0015: `workflow_instances.current_step_deadline TIMESTAMP WITH TIME ZONE` ditambahkan ke `41-DATABASE.md` §2.4, diisi saat submit dan saat instance maju (`43-WORKFLOW.md` §4.1/§7), dan diberi indeks parsial `WHERE status = 'running'`. Ditemukan saat mengerjakan C-005, bukan temuan lama |
| C-022 | **FIXED** | 2026-09-18 (P-014) | ADR-0016 `ACCEPTED`: `request_revision` mengembalikan instance ke **step sebelumnya** (`current_step = current_step - 1`, batas bawah step 1), sesuai FR-WF-09 apa adanya; instance tetap `running`, re-submit melanjutkan instance yang sama, dan "reset ke step 1" ditolak sebagai default maupun opsi konfigurasi (butuh ADR baru bila kelak dibutuhkan). `43-WORKFLOW.md` §4.5 ditulis ulang, `20-SRS.md` FR-WF-09 diberi penunjuk ADR, `42-API.md` §5 (`/workflows/submit` + perilaku aksi), `41-DATABASE.md` §2.4, `50-FSD.md` §8.1, `70-TESTING.md` §3.4; kontrak endpoint re-submit versi baru dicatat sebagai tugas `T-027` (belum ada di `42-API.md`) |
| C-023 | **FIXED** | 2026-09-18 (P-013) | Route `POST /workflows/instances/:id/actions` tidak lagi memasang izin aksi tunggal `workflow_instance:approve`; middleware memakai `workflow_instance:read` dan service memilih izin dari `input.Action`. Dicatat sebagai pengecualian yang disengaja di `40-TSD.md` §6 (aturan 3) dan `42-API.md` §5 |
| C-024 | **FIXED** | 2026-09-18 (P-016) | Requirement ID hantu `FR-DOC-08` pada contoh commit di `12-DEVELOPMENT-WORKFLOW.md` §5 diganti `[FR-VER-01, FR-VER-04]` (ID yang benar-benar ada di `20-SRS.md`). Ditemukan saat mengerjakan C-016 |
| C-026 | **FIXED** | 2026-09-18 (P-018) | Signature `FileStorage.Save` tidak memuat nama berkas sehingga tidak dapat menghasilkan `file_key` yang didokumentasikan (`42-API.md` §4), sementara memakai nama dari klien langsung berarti membuka path traversal. `40-TSD.md` §2.4 diperbarui (`Save(..., originalName string, ...)`, key opaque, wajib sanitasi, larangan menimpa), implementasi `internal/pkg/filestorage/local.go`, test `local_test.go`. Ditemukan saat menulis skeleton backend (T-003) |
| C-027 | **FIXED** | 2026-09-18 (P-018) | Cuplikan health check `60-DEPLOYMENT.md` §5 tidak dapat dikompilasi (`func() error` diisi `storage.Exists(...)` yang mengembalikan `bool`) dan semantiknya terbalik (key `healthcheck` tidak ada → storage sehat dilaporkan rusak). Diganti `storage.Ping` (`filestorage.Prober`, probe tulis-hapus), handler `internal/handler/health_handler.go`, test `local_test.go`. Ditemukan saat menulis skeleton backend (T-003) |
| C-025 | **FIXED** | 2026-09-18 (P-017) | "Satu aksi per step" (`43-WORKFLOW.md` §4.2) kini berlaku **per siklus** (aksi setelah `request_revision` terakhir), sehingga reviewer pada step hasil rollback dapat memutuskan lagi; jeda revisi ditegakkan dengan menolak semua aksi selama dokumen `revision_required` (`409 CONFLICT`). Kontrak re-submit `POST /workflows/instances/:id/resubmit` ditetapkan di `42-API.md` §5, perilaku engine di `43-WORKFLOW.md` §4.6, antarmuka di `40-TSD.md` §2.4/§6, test di `70-TESTING.md` §3.6. Tanpa perubahan skema |
| C-028 | **FIXED** | 2026-09-19 (P-026) | **Retensi audit ditetapkan sebagai operasi pemeliharaan, bukan kontrol UI** — **ADR-0020** `ACCEPTED`: lantai **12 bulan** sebagai konstanta kebijakan (bukan kunci `system_settings`, agar tidak ada kunci yatim), pemangkasan lewat jalur `bwdcs.audit_maintenance` (`SET LOCAL`, per transaksi) dengan prosedur bernomor di `60-DEPLOYMENT.md` **§6.4**, trigger tidak pernah dilepas, dan jejak pemangkasan ditulis ke log aplikasi (baris yang mencatatnya ikut terpangkas — itu sebabnya). Butir "Audit log retention" **dihapus** dari `50-FSD.md` §10.5, yang kini memuat tabel kunci yang benar-benar ada + penjelasan mengapa masa berlaku token dan password policy sengaja tidak ada di sana. Tidak ada kode yang tertunda: yang dijanjikan adalah prosedur operator, bukan fitur. Ditemukan saat mengerjakan C-020 (P-019) |
| C-029 | **FIXED** | 2026-09-18 (P-020) | FK `documents.workflow_instance_id` tidak dapat ditulis di migrasi `004` karena `workflow_instances` baru ada di `005`; kolomnya dibuat tanpa FK di `004` dan constraint `fk_documents_workflow_instance` dipasang di `005` lewat `ALTER TABLE`. `41-DATABASE.md` §2.3/§4 diperbarui; test `TestDocumentsWorkflowInstanceForeignKey`. Ditemukan saat menjalankan `goose up` (T-004) |
| C-030 | **FIXED** | 2026-09-18 (P-020) | `system_settings` (§2.6) tidak punya rumah di daftar sembilan berkas migrasi; ditetapkan masuk `002` dan seluruh pemetaan isi berkas ditulis di `41-DATABASE.md` §4 (tabel + alasan), supaya tidak ada tabel §2 yang tertinggal. Test `TestSystemSettingsDefaults`. Ditemukan saat mengerjakan T-004 |
| C-031 | **FIXED** | 2026-09-18 (P-020) | Badan fungsi PL/pgSQL terpotong pengurai goose (`unterminated dollar-quoted string`, SQLSTATE 42601) karena berkas dipecah per titik-koma; badan fungsi kini dibungkus `StatementBegin`/`StatementEnd` di migrasi `007`, dan kewajiban itu dicatat di `44-SECURITY.md` §6 + `41-DATABASE.md` §4 — termasuk larangan menulis penanda anotasi goose di komentar biasa (`invalid annotation`). Ditemukan saat menjalankan `goose up` (T-004) |
| C-032 | **FIXED** | 2026-09-18 (P-020) | ADR-0013 butir 1 ("`migration/` … bukan paket Go") tidak dapat dipenuhi bersamaan dengan migrasi-saat-startup (ADR-0010 butir 1), karena `go:embed` hanya menjangkau direktori package. Ditutup lewat **ADR-0018**: `internal/migration/` menjadi package, goose dipin v3.24.1 (library + CLI), `.sql` tetap sumber skema. `docs/adr/README.md` + `40-TSD.md` §2.0 diperbarui |
| C-033 | **FIXED** | 2026-09-19 (P-030) | **Pencabutan seluruh sesi lewat `users.tokens_invalid_before`** — **ADR-0021** `ACCEPTED` (melengkapi ADR-0009): satu kolom per user, middleware menolak token dengan `iat < tokens_invalid_before` (`401 TOKEN_REVOKED`, kode yang sama dengan `jti` tercabut), dan tiga peristiwa menulisnya (`logout_all`, `change-password` + terbitkan token baru untuk sesi yang dipakai, reset password Administrator). `token_revocations` tetap untuk pencabutan satu token. Kontrak `42-API.md` §2/§11/§12, `44-SECURITY.md` §2.2/§2.3, `20-SRS.md` FR-AUTH-08/FR-AUTH-09, `40-TSD.md` §5.2.1, `41-DATABASE.md` §2.1/§4, `70-TESTING.md` §3.12 sudah diselaraskan. **Dijalankan pada P-030 (`T-040`)**: `RevocationRepository.SessionRevoked` (satu kueri untuk dua sebab, cache per `jti` dengan invalidasi per user), `RevokeAllForUser` (menulis `users.tokens_invalid_before`), `middleware.AuthConfig.Sessions`, `AuthService.Logout` (mencabut `jti` request **dan** seluruh token lama + audit `LOGOUT_ALL`), dan `jwt.Validate` menolak token tanpa `iat`. `501 NOT_IMPLEMENTED` **dihapus** dari `42-API.md` §12 dan dari `internal/pkg/response`. Bukti pada server nyata: `logout_all` → `200`, tiga token (termasuk dua perangkat lain) → `401 TOKEN_REVOKED`, dan token hasil login ulang tepat sesudahnya → `200`. Dua temuan yang lahir dari menjalankannya (**C-052**, **C-053**) dicatat terpisah. Ringkasan: `70-TESTING.md` §3.12b. Menyertai `T-034` yang tetap terbuka (endpoint `change-password` belum terdaftar) |
| C-034 | **FIXED** | 2026-09-18 (P-021) | Package `auth` hantu di `40-TSD.md` §2.2/§6 diganti interface nyata di package `middleware` (`TokenValidator`, `RevocationChecker`, `PermissionChecker`) dan lokasi `Setup` disebutkan (`internal/handler/router.go`); `AuditService.Log` di §2.4 kini memuat `ctx` + `pgx.Tx` sesuai ADR-0011 butir 2. Ditemukan saat mengerjakan `T-005` |
| C-035 | **FIXED** | 2026-09-19 (P-030) | **Percobaan login gagal dicatat di tabel `login_attempts`, bukan di `audit_logs`** — **ADR-0022** `ACCEPTED` butir 2: `actor_id` tetap `NOT NULL` karena `audit_logs` bermakna "tindakan aktor yang terautentikasi", dan `44-SECURITY.md` §2.3 kini menyatakan FR-AUDIT-01 "login" = login **berhasil** (keputusan, bukan kekurangan). `login_attempts.username_attempted` tanpa FK justru supaya percobaan atas username yang tidak ada tetap tercatat (`user_id` boleh `NULL`), dengan retensi 90 hari. Kontrak `44-SECURITY.md` §2.3/§8, `20-SRS.md` FR-AUDIT-01, `41-DATABASE.md` §2.1/§4, `40-TSD.md` §5.2.2, `70-TESTING.md` §3.12 sudah diselaraskan. **Dijalankan pada P-030 (`T-041`)**: `AuthService.recordAttempt` menulis satu baris `login_attempts` untuk **setiap** percobaan (berhasil dan gagal), termasuk username yang tidak ada dengan `user_id NULL` — dibuktikan pada server nyata (dua percobaan atas username hantu → dua baris `user_id IS NULL`, `succeeded=false`, bertahan setelah server berhenti) dan di test `TestFailedLoginRecordedForUnknownUsername`. `audit_logs` **tetap** tidak memuat percobaan gagal (dihitung di test yang sama) |
| C-036 | **FIXED** | 2026-09-18 (P-021) | Test integrasi berbagi satu database tetapi tidak terisolasi, dengan dua sebab: paket dijalankan **paralel** (dua test `internal/bootstrap` gagal saat paket auth berjalan bersamaan) dan `DELETE FROM users` di `newCleanTx` menabrak FK `audit_logs_actor_id_fkey` begitu ada entri audit **nyata** (login admin `T-005`). Perbaikan: `backend/Makefile` memakai `-p 1`; `newCleanTx` membersihkan lewat jalur pemeliharaan `SET LOCAL bwdcs.audit_maintenance = 'on'` di dalam transaksi yang digulung balik (audit asli tetap utuh — diperiksa dengan `psql` sesudah suite); `70-TESTING.md` §8 diperjelas. Ditemukan saat menjalankan `go test ./...` (P-021) |
| C-037 | **FIXED** | 2026-09-18 (P-021) | Butir "Admin role has all permissions (special case)" di `44-SECURITY.md` §3.2 diganti catatan eksplisit tanpa bypass; pemeriksa izin hanya membaca `role_permissions` (ADR-0014) — sejalan dengan `70-TESTING.md` §4.1. Ditemukan saat mengimplementasikan RBAC (P-021) |

| C-038 | **FIXED** | 2026-09-19 (P-024) | **`TEST_DATABASE_URL` menunjuk database dev yang sama** dengan server berjalan, sehingga data nyata membuat suite gagal. Bukti nyata (P-022): setelah `POST /projects` dijalankan pada server dev (dua baris `projects` milik `admin`), `go test ./... -p 1` gagal di lima test `internal/bootstrap` dengan `update or delete on table "users" violates foreign key constraint "projects_owner_id_fkey"` — `newCleanTx` menghapus **seluruh** `users`/`organizations` (karena memang menguji "tabel `users` kosong"), dan `projects.owner_id REFERENCES users(id)` bersifat `ON DELETE RESTRICT`. Ini lanjutan kelas masalah **C-036** (test berbagi database). **Perbaikan (P-024, `T-036`, dengan izin user):** database test terpisah `bwdcs_test` dibuat (owner role `bwdcs`; role aplikasi tidak diberi `CREATEDB`), dan `backend/Makefile` kini **menurunkan** `TEST_DATABASE_URL` dari kredensial `.env` ke database `bwdcs_test` (`TEST_DB_NAME` dapat mengganti namanya), memakai `-count=1`, serta **berhenti dengan pesan** bila DSN yang diberikan menunjuk database dev — jadi varian kesalahan ini tertutup oleh konstruksi, bukan oleh ingatan. Bukti: dengan project nyata `LIVE-DEV` hidup di database dev (`POST /projects` → 201) dan server berjalan, `make test` hijau untuk delapan paket, dua kali berturut-turut, sementara perintah lama yang menunjuk database dev gagal `SQLSTATE 23503` tepat seperti didokumentasikan. Prosedurnya dicatat di `70-TESTING.md` §8.1 |

| C-043 | **FIXED** | 2026-09-19 (P-024) | **`60-DEPLOYMENT.md` §3.1 menyalin isi `Makefile` yang sudah menyimpang.** Cuplikan di dokumen menampilkan `test: go test ./... -cover` (tanpa `-p 1`), tanpa pemuatan `.env`, tanpa `vet`/`fmt`/`migrate-status`, dan dengan DSN migrasi selesai-sandi alih-alih variabel — padahal `backend/Makefile` sudah lama berbeda, dan `70-TESTING.md` §8 secara eksplisit melarang `go test` paralel untuk database bersama. Kelas yang sama dengan **C-014** (salinan yang hidup sendiri, lalu menyesatkan yang membacanya). **Perbaikan:** cuplikan dihapus, diganti tabel target + penunjuk ke `backend/Makefile` sebagai sumber tunggal, termasuk aturan `make test` yang baru. Ditemukan saat menyiapkan database test P-024 |
| C-044 | **FIXED** | 2026-09-19 (P-024) | **Hitungan audit salah satu angka.** Footer `AUDIT-001`, `audits/README.md`, `STATE.md`, `CONTINUE.md`, dan `AGENTS.md` menulis **31 FIXED**, sedangkan tabel tindak lanjut sendiri memuat **32** baris `FIXED` — daftar temuan di footer memang 32 butir. Kesalahan ini bertahan beberapa sesi karena setiap sesi menaikkan angka dari angka sebelumnya, bukan dari tabel. **Perbaikan:** angka diambil ulang dari tabel (dan dari baris `FIXED`/`OPEN` per temuan), lalu dinaikkan sesuai temuan sesi ini; total 44 temuan / **35 FIXED** / 9 OPEN — 35 + 9 = 44, jadi hitungannya kini dapat diperiksa silang. Ditemukan saat menghitung ulang untuk menutup C-038 |
| C-039 | **FIXED** | 2026-09-19 (P-023) | Test keamanan JWT **gagal secara acak**: `TestValidateRejectsTampered` (`internal/pkg/jwt/jwt_test.go`) "mengubah token" dengan mengganti **karakter terakhir** string base64url. Segmen tanda tangan HMAC-SHA256 berakhir dengan karakter yang sebagian bitnya tidak terpakai, sehingga penggantian itu kadang menghasilkan byte tanda tangan yang sama persis → token tetap sah dan test gagal tanpa perubahan kode apa pun. Terlihat nyata saat `go test ./... -p 1` pada P-023 (gagal sekali, lalu `ok` delapan kali berturut-turut saat diulang). Perbaikan: helper `tamperSignature` mendekode segmen tanda tangan, membalik satu **byte**, lalu menyusun ulang token — hasilnya deterministik. Aturan umumnya dicatat di `70-TESTING.md` §3.9. Ditemukan saat menjalankan suite modul dokumen |
| C-040 | **FIXED** | 2026-09-19 (P-023) | Contoh response `POST /documents/:id/upload` di `42-API.md` §4 mengirim `"checksum": "sha256:..."`, padahal kolom penyimpannya `document_versions.checksum VARCHAR(64)` (`41-DATABASE.md` §2.3) — prefiks `sha256:` (7 karakter) plus digest 64 karakter menjadi 71 karakter dan **tidak muat**. Skema dianggap sumber kebenaran (mengubah kolom hanya demi kosmetik menuntut migrasi baru), sehingga contoh diselaraskan menjadi heksadesimal 64 karakter dan komentar kolom di `41-DATABASE.md` §2.3 diperjelas ("TANPA prefiks"). Ditemukan saat menulis kontrak unggahan |
| C-041 | **FIXED** | 2026-09-19 (P-023) | Normalisasi `projects.code` menjadi huruf besar hanya dilakukan **handler** (`dto.NormalizeProjectCode`), sehingga pemanggil `ProjectService` yang lain dapat menyimpan kode huruf kecil — dan karena ADR-0017 memakai `projects.code` sebagai prefiks `document_number`, nomor dokumen bisa lahir sebagai `webdocs-001` alih-alih `WEBDOCS-001`. Terlihat nyata pada test handler dokumen (dua kegagalan: nomor dan `project_code`), bukan hasil pembacaan dokumen. Perbaikan: `normalizeProjectCode` dijalankan juga di `ProjectService.Create` (idempoten, jalur HTTP tidak berubah). Pola "akhirnya diterapkan di service, bukan hanya di handler" ini yang diharapkan untuk invariant berikutnya. Ditemukan saat menjalankan test modul dokumen |
| C-045 | **FIXED** | 2026-09-19 (P-026) | **`422` untuk UUID yang tidak sah di body menyebut alasan yang salah dan tidak menamai field-nya.** Uji nyata pada server (P-025): `PATCH /api/v1/tasks/:id` dengan body `{"document_id":"bukan-uuid"}` — JSON-nya **sah** — dijawab `422 VALIDATION_ERROR` dengan `details` = `[{"field":"body","error":"harus JSON objek yang sah"}]`. Penyebabnya `bindJSON` (`internal/handler/project_handler.go`) hanya mengenali `*json.UnmarshalTypeError`; kegagalan penguraian `uuid.UUID` bukan tipe itu, sehingga jatuh ke cabang umum yang mengklaim JSON-nya rusak. Akibatnya klien diarahkan memperbaiki hal yang tidak salah, dan `details.field` kehilangan atribusi padahal kontrak §12 memakai daftar `{field, error}` justru untuk itu. **Sifatnya lintas modul** (project, document, task memakai `bindJSON` yang sama), jadi perbaikannya bukan tambalan pada satu handler. Pilihan ada di `OPEN-QUESTIONS.md` **Q-017**. **Perbaikan (P-026, opsi (a) diperluas):** `bindJSON` (`internal/handler/project_handler.go`) kini membedakan tiga sebab — tipe salah (nama field dari `json.UnmarshalTypeError`), JSON rusak (`json.SyntaxError`/`json.Valid` → `body`), dan JSON sah dengan nilai yang tidak dapat diurai, yang field-nya dicari lewat `undecodableField` (reflection atas field yang ada di body) dengan pesan dari bentuk tipe (`uuid.UUID` → "harus UUID yang sah", waktu → "harus waktu RFC 3339 yang sah", pembungkus tanggal → "harus tanggal yang sah"). Dasar best practice: `uuid.UUID`/`time.Time` adalah `json.Unmarshaler` kustom yang errornya tidak membawa nama field (diperiksa langsung di Go 1.22: `uuid.invalidLengthError`), dan panduan umum adalah memvalidasi field secara eksplisit alih-alih menyerahkan pelaporan ke `encoding/json`. Kontrak `42-API.md` §12 kini memuat tabel pemetaannya; test `TestBindJSONErrorsNameFieldAndReason` (4 sebab) + `TestPatchTaskNamesUndecodableField`; dua test lama yang menuntut `field: "body"` diperbarui ke field yang benar (`start_date`, `project_id`). Ditemukan saat menjalankan bukti HTTP modul task, bukan dari membaca kode |
| C-046 | **FIXED** | 2026-09-19 (P-026) | **`50-FSD.md` §6.1 memuat penyaring yang tidak punya kontrak endpoint.** Daftar penyaring halaman Task berbunyi "Status, Priority, Project, Assignee, Due date range", sedangkan `42-API.md` §6 hanya memuat tiga penyaring (`project_id`, `status`, `assignee_id`) tanpa catatan apa pun soal yang lain — sehingga pembaca FSD dapat menyimpulkan kemampuan yang tidak ada. Sejak P-025 empat dari lima sudah nyata (`priority` dan `overdue` ditambahkan; sub-halaman Overdue hanya benar bila disaring sebelum paginasi), dan `42-API.md` §6 kini menyebut sisa yang **belum** ada secara eksplisit. Yang masih terbuka adalah **rentang tanggal**: semantiknya belum ditetapkan di dokumen mana pun (tanggal vs waktu, batas inklusif, zona waktu, dan apakah memakai `due_date` atau `created_at`). **Perbaikan (P-026):** `?due_from=`/`?due_to=` ditambahkan sebagai interval **setengah terbuka** `[due_from, due_to)` dengan batas RFC 3339 ber-offset eksplisit — konvensi yang dipakai API publik besar (Stripe `created[gte]`/`created[lt]`) dan mencegah rentang bersebelahan tumpang tindih/melewatkan baris; pilihan itu dicatat di `OPEN-QUESTIONS.md` **Q-017** butir (10) beserta alternatif dan dasar risetnya. `parseRFC3339Query` menerima bentuk `+07:00` (yang sampai ke handler sebagai spasi karena pengurai query) dan `%2B07:00`; rentang terbalik/berdiri di satu titik ditolak `422`. Ditemukan saat mencocokkan FSD §6.1 dengan kontrak API sebelum menulis dokumen |
| C-047 | **FIXED** | 2026-09-19 (P-026) | **Tabel status fase di `80-ROADMAP.md` tertinggal, dan label fase modul Task tidak sesuai tabelnya sendiri.** §3 masih menulis Phase 0/Phase 1 "Belum dimulai" (dengan alasan "tooling belum diverifikasi, git belum diinisialisasi") padahal keduanya sudah berjalan, sedangkan sesi-sesi sebelumnya (termasuk `STATE.md`, `CONTINUE.md`, dan log `P-025`) melabeli modul Task sebagai "Phase 1" sementara `80-ROADMAP.md` menempatkan Task & Comment di **Phase 3** dan `TASKS.md` menaruh "Task, overdue detection, comment" di backlog **fase 3**. Dampaknya nyata: agen berikutnya dapat menyimpulkan urutan fase yang salah atau menganggap pekerjaan sudah selesai pada fase yang belum. **Perbaikan:** tabel §3 diperbarui dengan bukti per fase + catatan koreksi eksplisit bahwa sumber urutan fase adalah dokumen itu, dan baris `T-038` di `TASKS.md` kini menyebut "Phase 3, dikerjakan lebih awal"; urutan fase berikutnya (Comment lalu Workflow, atau kembali ke Phase 2) dicatat sebagai **Q-018**. Ditemukan saat memeriksa konsistensi ledger terhadap dokumen desain |
| C-048 | **FIXED** | 2026-09-20 (P-032) | **`meta.total` berbohong pada halaman di luar rentang.** Keempat endpoint daftar (`GET /projects`, `/documents`, `/tasks`, `/comments`) menghitung total dengan `COUNT(*) OVER()`, dan jendela itu dievaluasi **per baris hasil** — jadi permintaan halaman yang tidak memuat baris apa pun (offset melewati akhir data) menjawab `total: 0`, bukan jumlah sebenarnya. Klien yang memakai `meta.total`/`total_page` untuk paginator akan menampilkan "0 halaman" padahal datanya ada. Ketahuan dari test paginasi komentar sendiri (3 baris diminta pada halaman 3 dengan `limit=2`), bukan dari membaca kode; modul task tidak terdeteksi karena seluruh kasusnya memakai halaman yang berisi baris. **Perbaikan sebagian (P-027):** `CommentRepository.List` menjalankan kueri hitung terpisah **hanya** ketika halaman kosong dan bukan halaman pertama (`CommentRepository.count`, memakai `commentFrom` + `commentReadPredicate` yang sama), sehingga jalur cepat `COUNT(*) OVER()` tetap utuh pada halaman normal — dibuktikan pada server nyata: `?page=5&limit=2` → `data: []` dengan `total: 2`. **Perbaikan lengkap (P-032, `T-043`):** tambalan yang sama disalin ke **tiga** repository — `ProjectRepository`, `DocumentRepository`, `TaskRepository` masing-masing punya `count` yang memakai `FROM` + `WHERE` yang sama dengan `List` (predikat diangkat jadi `projectListWhere`/`documentListWhere`/`taskListWhere`, sehingga jalur cepat dan jalur hitung tidak dapat menyimpang). Kueri hitung hanya dijalankan saat `len(rows) == 0 && offset > 0`, jadi halaman normal tetap satu perjalanan ke database. Test `TestProjectListOutOfRangePageKeepsTotal`, `TestDocumentListOutOfRangePageKeepsTotal`, `TestTaskListOutOfRangePageKeepsTotal` membuktikan halaman di luar rentang melaporkan total sebenarnya, penyaring ikut dihormati (termasuk aturan arsip dokumen dan cakupan baca task), dan test project **gagal dengan `total 0`** saat tambalannya dinonaktifkan sementara. Ringkasan: `70-TESTING.md` §3.13a |
| C-049 | **FIXED** | 2026-09-19 (P-027) | **Bentuk rute daftar komentar di draf §7 tidak dapat dipasang.** `42-API.md` §7 menulis `GET /comments/:entityType/:entityId`, sedangkan `GET /comments/:id` ada di daftar endpoint yang sama; Gin menolak dua nama wildcard berbeda pada posisi path yang sama dan **panik saat registrasi rute** (`':entityType' in new path ... conflicts with existing wildcard ':id'`), sehingga server tidak dapat dijalankan sama sekali — bukan sekadar konflik dokumentasi. **Perbaikan (P-027):** kontrak §7 memakai bentuk kueri `GET /comments?entity_type=&entity_id=&page=&limit=`, sejalan dengan `GET /tasks` §6 dan `GET /documents` §4 (keduanya sudah memakai bentuk itu untuk penyaring), dengan `entity_type`/`entity_id` **wajib** karena daftar komentar selalu daftar komentar satu entitas. Alternatif yang ditolak: `GET /comments/entity/:entityType/:entityId` (prefix statis memang menghilangkan konflik) — ditolak karena menambah satu variasi bentuk rute hanya untuk satu resource; dan menjadikan `?entity_type=` opsional dengan satu rute `/comments` untuk semua komentar, yang berarti satu endpoint lintas-project tanpa layar di `51-UX.md`. Ditemukan **sebelum** menulis handler, dengan menjalankan probe registrasi rute, bukan sesudah |
| C-055 | **FIXED** | 2026-09-19 (P-030) | **Hitungan audit di berkasnya sendiri tidak dapat diperiksa silang.** Tabel ringkasan severitas §1 menulis S1 17 / S2 11 / S3 4 (= 32), sementara berkasnya memuat **37** bagian temuan dengan pembagian yang berbeda (18/13/6) — angka tabelnya tidak pernah diperbarui ketika temuan C-021..C-037 disisipkan ke bagian yang sesuai pada sesi-sesi berikutnya. `audits/README.md` menambah lapisan kedua: ia menulis 51 temuan dengan rincian `18 S1, 15 S2, 7 S3, 5 S1-hasil-jalan, 3 S2-hasil-jalan, 1 S3-hasil-jalan` yang bila dijumlahkan hanya **49**, dan memakai taksonomi severitas yang tidak ada di berkas auditnya. Ini kelas yang sama dengan **C-044** (angka dinaikkan dari angka sesi sebelumnya, bukan dari tabel) dan bertahan karena setiap sesi hanya mengubah angka totalnya. Ketahuan saat verifikasi silang penutupan P-030: total temuan, jumlah per status, dan jumlah per severitas tidak bertemu di satu angka. Petunjuk ketiga yang sejenis muncul di ledger task: `T-024` menulis "45/55 endpoint beranotasi" sementara `STATE.md` menulis "40/51", dan tidak satu pun disertai cara menghitungnya — hasil `awk` atas `42-API.md` pada P-030: **55** judul endpoint, **38** punya baris `Izin:` langsung, **7** endpoint dokumen izinnya ada di satu tabel bab §4, dan **10** belum punya keterangan (-2+3+5). **Perbaikan:** §1 dihitung ulang dari judul `### C-` (37 = 18 + 13 + 6) dengan pernyataan metode + catatan bahwa 17 temuan (C-038..C-054) hanya punya baris tabel tindak lanjut; baris `audits/README.md` menggantikan taksonomi lama dengan dua angka yang dapat diperiksa (rinci vs baris-tabel); dan `T-024`/`STATE.md` memakai **satu** angka (45/55 = 38 + 7) beserta perintah `awk`-nya, dengan 10 endpoint yang belum disebut namanya. Alasan pemisahan itu tetap dipertahankan: temuan dari menjalankan kode memang tidak selalu butuh bagian naratif baru, yang wajib adalah **satu** tempat yang menjumlahkannya |
| C-052 | **FIXED** | 2026-09-19 (P-030) | **Contoh `423` di `42-API.md` §12 bertentangan dengan prosanya sendiri.** Contoh JSON menampilkan `details` sebagai **daftar** `[{"field":"locked_until","error":"..."}]` — bentuk `422` — sedangkan kalimat di bawahnya menjanjikan `details.retry_after_seconds` sebagai objek. Dua bentuk untuk satu kode status berarti implementasi pertama yang menulisnya akan menetapkan bentuk secara diam-diam, dan klien membangun parser untuk bentuk yang salah. Ketahuan saat menulis kontrak `423` untuk implementasi `T-041` (P-030). **Perbaikan:** bentuknya ditetapkan **objek** `{"retry_after_seconds": 900, "locked_until": "..."}` — tidak ada field yang salah pada permintaan yang ditolak karena akunnya terkunci, dan yang dibutuhkan klien adalah lama tunggu — diwakili `dto.LockedDetails`, ditambah header `Retry-After` (nilai sama) supaya klien generik pun benar. Alasan bentuk daftar ditolak dicatat di baris §12 itu |
| C-053 | **FIXED** | 2026-09-19 (P-030) | **`tokens_invalid_before = NOW()` menolak token yang lahir pada detik yang sama — termasuk token hasil login ulang sesudah `logout_all`.** `iat` JWT berpresisi detik (`jwt.NewNumericDate`), sedangkan `NOW()` menyimpan mikrodetik, sehingga `iat < NOW()` benar untuk token yang diterbitkan pada detik yang sama. Akibatnya nyata dan bukan teoretis: pengguna yang menekan "logout semua perangkat" lalu **login lagi** dalam detik yang sama menerima token yang langsung ditolak `401 TOKEN_REVOKED` — ia ter-logout sendiri, dan tidak ada pesan yang menjelaskannya. ADR-0021 menyebut risiko "presisi satu detik" untuk perangkat **lain**, tidak untuk perangkat yang **sedang** login. Ketahuan dari test `TestLoginRightAfterLogoutAllStillWorks` yang gagal pada implementasi pertama, bukan dari membaca kode. **Perbaikan:** nilai kolomnya ditulis `date_trunc('second', NOW())`, sehingga token yang terbit pada detik yang sama **tidak** ikut mati — jendela maksimum satu detik untuk token lain, dan login ulang selalu menghasilkan token yang sah; `logout_all` tetap mencabut `jti` request itu eksplisit sebagai jaring. Konsekuensinya dinyatakan di `42-API.md` §2 dan `41-DATABASE.md` §2.1, dengan test `TestLoginRightAfterLogoutAllStillWorks` (service) dan `TestLogoutAllEndToEnd` (HTTP, sengaja menyeberangi batas detik) |
| C-056 | **FIXED** | 2026-09-19 (P-030) | **Database test tidak kembali kosong di antara sesi begitu `login_attempts` ada.** `projectHTTPFixture` (`internal/handler/project_handler_test.go`) adalah fixture yang dipakai **semua** test handler modul (project, document, task, comment — yang terakhir menumpang struct itu) dan ia masuk lewat `POST /auth/login` **yang nyata** (`loginToken`), sehingga setiap aktor uji meninggalkan satu baris di `login_attempts`; daftar pembersihannya tidak memuat tabel itu. Barisnya **selamat** dari `DELETE FROM users` karena `login_attempts.user_id` bersifat `ON DELETE SET NULL`, jadi ia hanya kehilangan id-nya. Ketahuan saat memeriksa database test setelah `make test` pada P-030: **220 baris** `succeeded = true` dengan `user_id IS NULL` dan username `uji-projh-*`, sementara **seluruh tabel lain kosong** — akumulasi satu baris per aktor per test sejak `T-041` dikerjakan. Ini kelas yang sama dengan **C-038/C-044**: ledger mengklaim "kosong dari data uji di antara sesi" (`70-TESTING.md` §8.1, `STATE.md`) dan tidak ada test yang memeriksa klaim itu. **Perbaikan:** langkah `DELETE FROM login_attempts WHERE user_id = ANY($1::uuid[])` ditambahkan **sebelum** `DELETE FROM users` di `projectHTTPFixture.clean()` — satu tempat karena fixture itu dipakai seluruh test handler modul, dan itu satu-satunya kesempatan: sesudah user-nya hilang, barisnya hanya dapat ditemukan lewat `username_attempted`. 220 baris sisa dibersihkan, lalu `make test` **dua kali berturut-turut** meninggalkan `login_attempts = 0` (beserta `users`, `organizations`, `projects`, dan `audit_logs` = 0). Aturan yang mengikat sekarang: fixture yang login lewat HTTP wajib membersihkan telemetri login **sebelum** menghapus user-nya |
| C-057 | **FIXED** | 2026-09-20 (P-034) | **Angka test per berkas di `STATE.md` §3 tidak sesuai isi repo, dan tidak satu pun disertai cara menghitungnya.** Baris auth menulis "service auth **14** + user 4 · handler auth **9** + user 3 · **total suite 213 test**", baris Project menulis `project_service_test.go` (14) dan `project_handler_test.go` (10), baris Task (12)/(10), dan baris Comment (11) — hasil hitung ulang pada P-034: **17 / 4 / 11 / 3**, Project **17 / 13**, Task **14 / 12**, Comment **9**, dan total suite **222**. Penyimpangannya tidak seragam (ada yang kurang, ada yang lebih) karena tiap sesi menaikkan angkanya dari angka sesi sebelumnya alih-alih menghitung ulang berkasnya — kelas yang sama dengan **C-044** dan **C-055**, dan begitulah ia bertahan: angkanya masuk akal sehingga tidak ada yang memeriksanya. Ketahuan saat menutup `T-034`: test auth yang baru ditambahkan menuntut angka di baris itu diperbarui, dan justru angka lamanya yang tidak cocok. **Perbaikan:** seluruh angka per berkas di `STATE.md` §3 dihitung ulang dengan satu perintah yang dicatat (`grep -cE '^func Test' <berkas>` untuk per berkas; `grep -rhoE '^func Test[A-Za-z0-9_]+' --include=*_test.go . | wc -l` untuk total), dan angka yang salah dikoreksi. Aturan yang dipakai sekarang: **angka yang tidak dapat diperiksa ulang dengan satu perintah tidak ditulis** |
| C-058 | **FIXED** | 2026-09-20 (P-036) | **Rujukan test yang sudah tidak ada lagi tersimpan di dokumen status dan ADR — termasuk berkas test yang sudah berganti nama.** Empat tempat: `TRACEABILITY.md` FR-AUTH-02 menunjuk `internal/service/auth_test.go::TestIssueToken` (berkasnya bernama `auth_service_test.go` sejak `T-005`, dan testnya kini `TestGenerateAndValidate` di `internal/pkg/jwt/jwt_test.go` + `TestLoginSuccess`); FR-AUTH-06 masih menyebut `TestLoginRateLimited` (dihapus bersama penghitung di memori pada ADR-0022 — batas laju kini `internal/middleware/auth_test.go::TestRateLimitMiddleware`, C-054); FR-VER-06 menyebut `TestDocumentDeleteCascadesVersionsAndFiles` padahal jalur hapus dokumen **sudah tidak ada** sejak ADR-0019 (`T-039`) — penggantinya `TestArchiveDocumentKeepsVersionsAndFiles`, dan kolom buktinya masih menulis "sesudah hapus, keduanya hilang"; dan ADR-0022 §Konsekuensi memakai `TestFailedLoginRecorded` + `TestSuccessfulLoginNotLocked` yang tidak pernah ada dengan nama itu (`TestFailedLoginRecordedForUnknownUsername` + `TestSuccessfulLoginWritesAuditAndAttempt`). Bahayanya bukan salah ketik: pembaca berikutnya mencari test yang tidak ada, lalu menyimpulkan cakupan lebih sempit daripada kenyataan, sementara baris `DONE` membuatnya tampak terverifikasi. **Perbaikan:** keempat baris dikoreksi ke nama yang benar-benar ada; satu kutipan nama lama yang memang berfungsi sebagai riwayat (`TASKS.md` baris `T-038`, menjelaskan nama pra-P-028) dipertahankan dengan penanda eksplisit `<!-- ledger-check: skip: nama test lama, bukan rujukan hidup -->`. **Pencegahan:** `scripts/check-ledger.sh` memeriksa setiap sitasi `path::TestXxx` pada dokumen live (berkasnya dan testnya harus ada) serta setiap nama `TestXxx` pada `STATE.md`, `TASKS.md`, `TRACEABILITY.md`, `OPEN-QUESTIONS.md`, `CONTINUE.md`, `AGENTS.md`, `README.md`, dan `docs/adr/**`; log historis (`prompts/**`, `SESSION-LOG.md`, `CHANGELOG.md`) sengaja tidak diperiksa karena mengutip keadaan masa lalu |
| C-059 | **FIXED** | 2026-09-20 (P-036) | **Papan `TASKS.md` bertentangan dengan dirinya sendiri, sehingga \"apa yang masih terbuka\" tidak dapat dibaca dari papan.** Tiga wujudnya: (1) `T-044` duduk di kolom `TODO` sementara teksnya sendiri berbunyi `**DONE 2026-09-19 (P-028)**`; (2) `T-006` muncul di `TODO` **dan** `BLOCKED` sekaligus; (3) ID `T-008` dipakai dua kali — sekali di `DONE` (penyerahan `CONTINUE.md`, 2026-09-17) dan sekali di `TODO` untuk kewajiban berulang menjaga berkas itu, yaitu dua pekerjaan berbeda di bawah satu ID. Akibatnya satu task selesai terlihat seperti pekerjaan yang menunggu, dan riwayat kepemilikan ID menjadi ambigu — kelas yang sama dengan C-044/C-055/C-057: keadaan yang tidak dapat diperiksa silang. **Perbaikan:** baris `T-044` dipindah ke kolom `DONE` lengkap dengan tanggal dan buktinya; `T-006` hanya tinggal di `BLOCKED` (statusnya yang benar) dengan fase, dokumen, requirement, dan kriteria bukti dari baris `TODO`-nya dipindah ke teks baris itu sehingga tidak ada keterangan yang hilang; kewajiban berulang diberi ID sendiri **`T-046`** dengan rujukan balik ke `T-008`. **Pencegahan:** `scripts/check-ledger.sh` menolak satu ID di dua kolom, baris `DONE` tanpa tanggal, dan baris non-`DONE` yang sudah mengklaim selesai |
| C-060 | **FIXED** | 2026-09-21 (P-037) | **`51-UX.md` §3/§4 menetapkan palet dan tipografi yang bertentangan dengan sumber arah desain.** §3 menuliskan `Primary #1D4ED8` (biru), `Secondary #475569` (slate), sukses/warning/danger hijau-amber-merah, latar `#F8FAFC`; §4 menulis `H1 28px` dan font stack campuran `Inter, -apple-system, BlinkMacSystemFont, sans-serif`. Ketiganya bertabrakan dengan `DESIGN.md` §2 (Paper/Ink/**satu** accent Signal teal, status semantik ADR-0012), §3 (font **system stack**, tanpa berkas font, **plafon 26px**), dan ADR-0002 (tanpa aset CDN). Dokumen yang sama juga menyebut "WCAG 2.1 AA" padahal yang diperiksa adalah 2.2 AA. Bahayanya persis kelas C-015: agen yang membaca `51-UX.md` sebagai spec halaman akan membangun UI biru-slate ber-H1 28px yang **lolos review dokumen** tetapi melanggar arah desain yang sudah diputuskan — dan `51-UX.md` justru dokumen yang paling sering dibuka untuk kerja halaman. **Perbaikan:** §3 dan §4 `51-UX.md` diganti **penunjuk** ke `DESIGN.md` §2/§3 + `frontend/src/styles/tokens.css`, dengan tabel token semantik (bukan hex) dan empat aturan yang tidak boleh dilanggar (komponen tidak menulis warna langsung; accent maksimum satu kali per layar; warna tidak pernah satu-satunya pembawa makna; warna status bukan warna brand); §1 dinaikkan ke **WCAG 2.2 AA**. Cerminnya di `30-ARCHITECTURE.md` §2.1/§2.2 juga diselaraskan (React 19, Router 7, Tailwind v4 CSS-first tanpa `tailwind.config.js`) lewat **ADR-0024**, dan teks CJK yang salah tulis (`Sesuai antislop 原则`) dibersihkan |
| C-062 | **FIXED** | 2026-09-21 (P-039) | **Baris "Teknologi" di `README.md` §2 menyebut versi dan status yang keduanya sudah basi.** Isinya `| Frontend (rencana) | React 18 + TypeScript + Vite + TailwindCSS | Belum diinisialisasi |`, padahal `frontend/package.json` memuat **React 19** dan `frontend/` sudah berisi 51 berkas sejak P-037. Dua cacat dalam satu baris: **angka** (React 18 vs 19) dan **klaim ketiadaan** ("belum diinisialisasi") — dan keduanya lolos dari dua sesi pembersihan sebelumnya, termasuk P-038 yang menyapu klaim basi README dengan `grep`. Sebabnya: yang disapu P-038 adalah klaim tentang `frontend/` sebagai folder dan jumlah route; baris tabel versi ini tidak memuat kata kunci yang dicari, dan pada berkas yang sama ada enam tempat lain yang memuat versi benar sehingga pembacaan sekilas terasa konsisten. Ini temuan yang **tidak dapat diandalkan pada mata manusia**. **Perbaikan:** barisnya diganti keadaan sebenarnya (`React 19 + TypeScript 5.9 + Vite 8 + Tailwind v4`, tanpa label "rencana"), dan seluruh klaim versi/angka di README kini diperiksa `scripts/check-readme-facts.sh` — ia membandingkan setiap penyebutan versi dengan `go.mod`/`package.json`, jumlah route dan rincian per modul dengan `router.go`, rentang migrasi dengan berkas di `internal/migration/`, serta klaim "belum ada" dengan keberadaan path. Pemeriksa itu **gagal pada percobaan pertama**, dan itu buktinya bekerja. **Aturan pencegahan:** klaim yang dapat dihitung dari kode tidak boleh dijaga oleh proses manual (ingatan, `grep` sesekali, daftar berkas per sesi) — ia harus punya pemeriksa yang berjalan di CI |
| C-061 | **FIXED** | 2026-09-21 (P-038) | **`README.md` tertinggal enam klaim setelah P-037, padahal itu berkas yang pertama dibaca orang luar.** Empat di antaranya salah tentang keadaan repo: baris status Frontend masih "Belum ada — direktori `frontend/` masih kosong; UI menunggu arah desain (`DESIGN.md`) dan mode antislop dipilih", diagram §3 masih menulis "Frontend SPA (belum ada)", dan pohon §4 masih memuat `DESIGN.md  # Arah desain; masih placeholder, belum diisi` serta `frontend/  # Belum diisi` — sementara P-037 sudah mengisi `DESIGN.md` dan membangun 51 berkas di `frontend/`. Dua klaim lagi bukan soal frontend melainkan **angka**: baris "`POST /auth/refresh` dan `POST /auth/change-password` Belum ada" bertahan sejak P-031 padahal keduanya hidup sejak **P-034**, dan jumlah route ditulis **30** (1 health, 3 auth, 1 admin, 8 project, 7 document, 5 task, 5 comment) padahal `internal/handler/router.go` memuat **32** (1 health, **5** auth, sisanya sama). Akarnya bukan kelalaian acak: README tidak masuk daftar dokumen desain yang diselaraskan P-037 (ia lahir P-031 dan tidak terdaftar di `00-README.md` sebagai dokumen desain), sehingga seluruh pembersihan dokumen sesi itu melewatkannya — kelas yang sama dengan **C-060**. Ketahuan saat sesi P-038 membuka README untuk menambah bagian kontribusi, lalu menyapu klaim basi dengan `grep` alih-alih mengandalkan ingatan. **Perbaikan:** keempat klaim keadaan diganti keadaan sebenarnya (kerangka frontend berdiri, halaman bisnis belum; pohon folder diperluas termasuk `.github/workflows/` dan `scripts/check-ledger.sh`), dua klaim angka diperbaiki dengan angka yang **dihitung dari `router.go`** dan disebutkan sumbernya di teks, dan baris `LICENSE` ditambahkan ke pohon dengan penanda "belum ada — lihat §13" supaya ketiadaannya terlihat, bukan hilang diam-diam. **Aturan pencegahan:** bila sebuah klaim muncul di beberapa dokumen, perbaikan harus menyapu **semua** pemuatnya — `grep` klaim basi dijalankan, bukan mengandalkan daftar berkas dari sesi sebelumnya |
| C-054 | **FIXED** | 2026-09-19 (P-030) | **Pemicu `429` di dokumen tidak lagi sesuai mekanismenya.** `42-API.md` §12 dan `44-SECURITY.md` §2.3 menyebut pemicu `429` sebagai "percobaan gagal per 15 menit **per username** (ambang `auth.max_login_attempts`)" plus batas per alamat klien — kalimat itu benar sebelum ADR-0022, ketika penghitung di memori membalas `429`. Setelah ambang itu berujung pada lock akun `423`, kedua kalimat menjadi salah: dokumen mengajarkan dua kode status untuk satu sebab, dan pembaca berikutnya dapat menambahkan kembali pembatas per username di memori. **Perbaikan:** `429` dinyatakan sebagai batas **per alamat klien** (20/menit, `internal/handler/router.go`) di `42-API.md` §2/§12, `44-SECURITY.md` §2.3, dan `40-TSD.md` §5.2.2 — ketiganya kini juga memuat perbedaan `429` (laju permintaan) vs `423` (keadaan akun) |
| C-051 | **FIXED** | 2026-09-19 (P-029) | **FK kaskade `document_versions` masih menggantung, padahal kaskadenya kini tertahan trigger.** `41-DATABASE.md` §2.3 menulis `document_id UUID NOT NULL REFERENCES documents(id) ON DELETE CASCADE`, dan sampai P-029 itu memang perilaku nyata (`DELETE /documents/:id` menghapus baris beserta versinya). Sejak ADR-0019 + migrasi `010`, `document_versions` append-only: kaskade dari `documents` melewati trigger `BEFORE DELETE` dan **gagal `23001`** — jadi pembersihan project/dokumen yang punya versi **wajib** lewat `SET LOCAL bwdcs.audit_maintenance = 'on'`, sama seperti `audit_logs`. Ini bukan kecelakaan: ADR-0019 butir 2 menyatakan "tidak ada lagi jalur sah yang menghapus baris versi", dan endpoint arsip menggantikan hapus. **Perbaikan (P-029):** catatan eksplisit di `41-DATABASE.md` §2.3 (di sebelah kolomnya) tentang perbedaan "FK tetap ada" vs "kaskade ditahan", plus `STATE.md`/`AGENTS.md` menyebut jalur pemeliharaan sebagai satu-satunya cara membersihkan dokumen berversi; dikunci test `internal/migration/audit_append_only_test.go::TestDocumentVersions_DeleteFromDocumentsIsRejected` (memeriksa SQLSTATE `23001` **dan** barisnya tetap ada). Alternatif yang ditolak: mengubah FK menjadi `ON DELETE RESTRICT` (tidak menambah jaminan apa pun — trigger sudah menahan — tetapi memaksa migrasi baru yang menyentuh constraint hanya untuk kosmetik) dan membuang FK-nya (kehilangan jaminan integritas untuk baris yang sah) |
| C-050 | **OPEN** | 2026-09-19 (P-027) | **`50-FSD.md` §7 menjanjikan balasan ber-thread, sedangkan skemanya tidak punya tempatnya.** FSD §7 mencantumkan field "Reply (optional, threaded)", tetapi tabel `comments` (`41-DATABASE.md` §2.5, migrasi `007`) hanya punya `id`, `entity_id`, `entity_type`, `content`, `created_by_id`, `created_at` — tidak ada `parent_id`/`reply_to_id`, dan tidak ada `FR-CMT-*` yang menuntut threading (`FR-CMT-03` hanya meminta timeline). Karena skema tidak dapat diubah tanpa migrasi + ADR, modul komentar **tidak** mengarang relasi: balasan ditulis sebagai komentar biasa pada entitas yang sama, ditampilkan dalam satu timeline datar, dan perilaku itu dikunci test (`TestCommentThreadingIsNotSupported` memeriksa `information_schema` dan gagal bila kelak kolom induk muncul). FSD §7 kini menyatakan janji itu secara eksplisit sebagai **belum didukung**, dengan pertanyaan keputusan di `OPEN-QUESTIONS.md` **Q-019**. Rekomendasi agen: **tunda** — mengurutkan balasan bersarang menuntut perubahan bentuk respons (rekursif vs datar + urutan per cabang), penyaring, notifikasi per balasan, dan halaman `51-UX.md`; tidak ada requirement yang menuntutnya, dan menambahkannya tanpa pengguna nyata berisiko membekukan bentuk API yang salah. Catatan terkait yang **bukan** temuan: `@mention`/`COMMENT_MENTION` (`50-FSD.md` §8.1) memang milik modul Notification yang belum dibangun, dan modul komentar hanya menyimpan isinya — sudah dinyatakan di FSD §7 |

| C-042 | **FIXED** | 2026-09-19 (P-023) | Komentar `IsArchived()` di `internal/model/project.go` menjanjikan aturan **"project arsip tidak menerima dokumen baru"**, padahal tidak ada dokumen desain yang menetapkannya: `42-API.md`, `50-FSD.md`, `41-DATABASE.md`, dan ADR-0017 semuanya diam soal itu, dan `FR-DOC-*` tidak memuatnya. Komentar (janji yang tidak dapat dijalankan) diganti catatan bahwa kebijakan itu **belum ada** dan pertanyaannya dicatat sebagai Q-016; endpoint dokumen karena itu tidak menegakkannya. Ditemukan saat memeriksa apakah aturan itu punya sumber sebelum diimplementasikan |
| C-063 | **OPEN** | 2026-09-21 (P-041) | **`50-FSD.md` §3.2 menuntut field `Owner` yang wajib dan dapat dipilih pengguna, sedangkan tidak ada satu pun endpoint yang dapat menyebutkan daftar pengguna.** Barisnya berbunyi `| Owner | dropdown | Yes | User select |`, dan butir di bawahnya menambahkan aturan server — "`Owner` yang dipilih langsung menjadi anggota project dengan role `Owner`" — yang mengandaikan klien dapat mengirim `owner_id` orang lain. Kenyataannya: `42-API.md` §3 tidak memuat endpoint pengguna sama sekali, `GET /admin/users` (satu-satunya kandidat, §11) **belum diimplementasikan** dan izinnya `user:read` yang menurut matriks `44-SECURITY.md` §3.1.2 hanya dimiliki **Administrator**, sementara halaman yang menciptakannya (§10 Administration) juga belum ada. Akibatnya halaman Projects yang dibangun pada P-041 **tidak dapat** memenuhi field itu dan tidak berpura-pura memenuhinya: dialog membuat project menampilkan pemilik sebagai teks tetap **(Anda)** dan menyebutkan alasannya di layar (`CreateProjectDialog.tsx`, `ProjectDetail.tsx`), dan itu dikunci test supaya tidak berubah menjadi dropdown kosong atau daftar pengguna karangan. Yang **tidak** dapat dikerjakan tanpa keputusan: apakah API perlu endpoint pencarian pengguna (dan bila ya, dengan izin apa), atau FSD §3.2 diturunkan untuk MVP menjadi "pemilik selalu pembuat project" — pilihan itu mengubah kontrak `42-API.md` §3 dan matriks izin, jadi ia butuh ADR, bukan tebakan agen. Temuan ini juga menahan pekerjaan anggota project: menambah/mencabut anggota (`42-API.md` §3) menuntut pemilihan pengguna yang sama. **Rekomendasi agen:** tambahkan satu endpoint baca `GET /users?q=&limit=` (izin baru `user:list` atau pemakaian ulang `user:read` yang dilonggarkan **hanya** untuk daftar terpaginasi tanpa detail sensitif — diputuskan lewat ADR), karena `Owner` pada FSD bukan hiasan: tanpanya project hanya dapat dibuat oleh dan untuk pembuatnya, dan Administrator tidak dapat menyerahkan kepemilikan. Pertanyaannya dicatat di `OPEN-QUESTIONS.md` **Q-024**. Ditemukan saat **membangun** halamannya, bukan dari membaca kontrak: ketiadaannya hanya terasa ketika form harus diisi |
| C-064 | **FIXED** | 2026-09-22 (P-042) | **`AGENTS.md` mendaftarkan lima skill antislop yang tidak ada di disk.** Blok penunjuk antislop di entry file menyebut `skills/antislop-ui/SKILL.md`, `-copywriting`, `-human`, `-layoutmobile`, dan `-code` — lengkap dengan keterangan bidang kerjanya masing-masing — padahal direktori `skills/` **tidak pernah dibuat**. Entry file karena itu menyatakan sesuatu yang tidak benar tentang repo, dan agen berikutnya yang mengikuti petunjuknya akan gagal membuka berkas (atau, lebih buruk, menyimpulkan isinya). Cacat ini tetap tidak terlihat selama berbulan-bulan sesi karena **tidak ada pemeriksa yang membaca entry file**: `check-doc-links.sh` memeriksa rujukan di dalam backtick pada semua `*.md` dan justru **tidak** menandainya karena pola `skills/...` dicatat sebagai `PLANNED` — jalur kode yang memang belum dibuat. **Perbaikan:** skill benar-benar dipasang, isinya byte-identik dari tag rilis upstream, dengan tabel provenans `sha256` di `skills/README.md` (ADR-0025); dan `scripts/check-antislop-refs.sh` memeriksa dua arah — path yang disebut `AGENTS.md` wajib ada di disk, dan setiap berkas instruksi `skills/<nama>/SKILL.md` di disk wajib terdaftar. **Aturan pencegahan:** daftar berkas di dokumen bukan dokumentasi, ia **klaim**; klaim yang dapat diperiksa mesin harus punya pemeriksanya, dan klasifikasi `PLANNED` tidak boleh diberikan pada berkas yang benar-benar dijanjikan akan ada |
| C-065 | **FIXED** | 2026-09-22 (P-042) | **Ada dua versi core antislop di satu repo, dan daftar aturannya sudah disalin ke dokumen desain.** `antislop.md` di root adalah **varian lama** (686 baris: Delivery Gate tanpa penanda `[ ]`, tanpa butir *scope* pada R-02), sementara `01-AGENT-WORKFRAME.md` §5.3 memuat salinan Gate yang **lebih baru** — sehingga dokumen yang dibaca agen untuk bekerja tidak sama dengan berkas yang disebut sebagai sumbernya. Ditambah dua salinan isi aturan di dokumen desain (daftar 23 aturan di §3.2 dan blok Gate di §5.3), yang berarti setiap perubahan upstream dapat membuat dua salinan menyimpang tanpa ada yang tahu — persis pola **C-014** yang sudah pernah dijadikan aturan (jangan menyalin, tunjuk) tetapi belum diterapkan pada berkas antislop. **Perbaikan:** core di root diganti berkas pristine dari tag `v3.2.12`, salinan daftar aturan dan blok Gate di `01-AGENT-WORKFRAME.md` **dihapus** dan diganti penunjuk + keputusan proyek (angka aturan tetap diperiksa mesin), dan `scripts/check-antislop-refs.sh` menahan empat kelas berikutnya: `sha256` yang menyimpang dari tabel provenans, salinan core di `skills/antislop/SKILL.md` yang berbeda dari `antislop.md`, nomor `R-XX` yang tidak ada, dan kalimat khas aturan/Gate upstream yang tersalin ke dokumen proyek. **Aturan pencegahan:** berkas pihak ketiga yang disalin ke repo adalah salinan yang dapat membusuk seperti salinan aturan lain — ia butuh `sha256` tercatat **dan** pemeriksa, bukan sekadar niat memperbaruinya |
| C-066 | **FIXED** | 2026-09-22 (P-042) | **Pohon folder `README.md` §4 menyebut dua dari lima skrip yang sudah berdiri.** Saat pohon itu diperbarui untuk memuat `skills/`, terlihat bahwa blok `scripts/` masih berbunyi `check-doc-links.sh` dan `check-ledger.sh` saja, padahal `check-readme-facts.sh` (P-039), `check-api-contract.sh` (P-040), dan `check-antislop-refs.sh` (P-042) sudah dijalankan CI di setiap push. Jadi README menyembunyikan tiga perintah verifikasi yang justru wajib dijalankan kontributor — dan ia tetap menyembunyikannya **sesudah** `check-readme-facts.sh` ada, karena pemeriksa itu memeriksa **angka dan versi**, bukan **daftar berkas**: dua sesi menambah skrip tanpa ada yang menyentuh pohonnya, dan tidak ada pemeriksa yang membandingkan daftar di README dengan isi direktori. Kelasnya sama dengan C-061/C-062 (klaim tentang keadaan repo), tetapi jenis klaimnya berbeda: *himpunan*, bukan *angka*. **Perbaikan:** pohon §4 memuat kelima skrip beserta fungsinya dan direktori `skills/`, dan `scripts/check-readme-facts.sh` mendapat **§6** yang membandingkan dua arah — setiap `scripts/*.sh` wajib disebut di README, dan setiap path `scripts/...`/`skills/...` yang ditulis README wajib ada di disk (37 fakta diperiksa, naik dari 25). Gigi dibuktikan dengan dua cacat disuntikkan sementara: satu baris skrip dihapus dari pohon → `FAIL … skrip … tidak pernah disebut`; satu path `skills/...` hantu ditambahkan → `FAIL … menunjuk path yang tidak ada`. **Aturan pencegahan:** klaim berbentuk daftar/himpunan tidak tertangkap pemeriksa yang hanya membandingkan angka; setiap daftar di dokumen yang bisa dibandingkan dengan isi direktori harus punya pembandingnya, dua arah |
| C-067 | **FIXED** | 2026-09-22 (P-043) | **`51-UX.md` §8 menetapkan state tengah sebagai "collapsed sidebar (icon-only)", padahal yang diimplementasikan adalah sidebar kompak berlabel.** Tabel Responsive Behavior menulis `768-1023px | Collapsed sidebar (icon-only)` sementara `AppShell.tsx` memberi kolom 12rem dengan **labelnya tetap** dan komentarnya sendiri menyebut bahwa penyimpangan itu "dicatat sebagai temuan C-067" — padahal temuan dengan nomor itu **belum pernah dibuat**: kode menunjuk ke catatan yang tidak ada, kelas yang sama dengan rujukan hampa. Penyebabnya bukan kelalaian menulis, melainkan keputusan yang diambil saat implementasi (rail ikon menuntut bahasa ikon yang belum ada dan dilarang `DESIGN.md` §1/R-04) yang **tidak pernah dikembalikan ke dokumennya**. Akibatnya `51-UX.md`, dokumen yang paling sering dibuka untuk kerja halaman, menjanjikan bentuk yang tidak akan ditemukan siapa pun di layar. **Perbaikan:** tabel §8 diganti tiga state yang benar-benar berlaku (laci < 768px, kolom kompak berlabel 768-1023px, kolom penuh ≥ 1024px) beserta **alasan** penyimpangannya, dan perilaku laci (latar penutup, kunci gulir, `inert`, fokus masuk lalu kembali, dilupakan saat jendela dilebarkan) ditulis di sana karena janji itu kini diukur mesin. **Aturan pencegahan:** keputusan yang diambil saat implementasi wajib kembali ke dokumen di sesi yang sama; komentar kode yang menyebut nomor temuan **wajib** disertai barisnya di tabel ini — kalau tidak, ia rujukan hampa |
| C-068 | **FIXED** | 2026-09-22 (P-043) | **Dua test baru mengklaim menguji perilaku yang tidak mereka kunci: keduanya tetap lulus sesudah perilakunya dihapus.** Ditemukan justru karena gigi-nya diuji dengan menyuntikkan cacat, bukan karena testnya merah: (a) test "menutup laci sendiri saat jendela dilebarkan" lulus **tanpa** `useMediaQueryEnter`, karena laci memang sudah tersembunyi oleh turunan `drawerOpen = sidebarOpen && !isTablet` — yang diuji hanya penyembunyian, bukan **melupakan permintaan**; (b) test latar penutup mencari elemen lewat `panel.nextElementSibling`, sehingga mengubah `aria-hidden` pada latar tetap lulus. Dua cacat serupa muncul di pemeriksa tata letak barunya: ia mengukur **tetangga panel** (latar penutup, lebar satu viewport) sebagai "konten" sehingga laci yang mendorong halaman terlihat baik-baik saja, dan ia menutup laci dengan Escape **sebelum** jendela dilebarkan sehingga urutan yang seharusnya diuji tidak pernah terjadi. **Perbaikan:** test (a) diperluas (jendela dipersempit lagi → menu tidak boleh muncul sendiri, gulir tidak boleh terkunci lagi), test (b) memakai penanda eksplisit `[data-drawer-backdrop]` dan memeriksa `aria-hidden`, sedangkan pemeriksa tata letak mengukur elemen `main` dan melebarkan jendela saat laci **masih terbuka** — keenam cacat yang disuntikkan kini tertangkap. **Aturan pencegahan:** test yang baru ditulis belum terbukti apa pun sampai ada cacat yang disuntikkan dan ia menangkapnya; "semua test lulus" tidak membedakan test yang mengunci perilaku dari test yang kebetulan hijau |
| C-069 | **FIXED** | 2026-09-22 (P-043) | **Keputusan R-02 di dokumen aturan lebih luas daripada yang diperiksa mesin, dan teks pengguna masih memuat em dash.** `01-AGENT-WORKFRAME.md` §3.2 menulis "semua teks UI dan **dokumentasi baru** bebas em dash", tetapi tidak ada pemeriksa yang membaca keputusan itu — dan tiga tempat di layar memang masih memakai em dash sebagai pengganti nilai kosong atau pemisah: `?? "—"` pada kolom pemilik/tanggal di daftar dan detail project, satu kalimat penjelas di dialog buat project, dan satu pemisah role anggota. Bentuknya khas: aturan yang ditulis sebagai **keputusan proyek** tidak dijaga apa pun, sementara aturan yang sama pada berkas upstream dijaga pemeriksa — persis pola C-014/C-065 pada tingkat yang lebih kecil. **Perbaikan:** dua arah sekaligus — `scripts/check-antislop-refs.sh` mendapat **butir 8** yang memindai `frontend/src` tanpa komentar dan menolak em dash (giginya dibuktikan tiga kali: string UI gagal, komentar lolos, dan em dash sesudah `https://` tetap tertangkap), dan teksnya diganti kalimat yang menerangkan keadaan (`EMPTY_VALUE`/`EMPTY_DATE` di `src/utils/format.ts`) alih-alih tanda pisah yang tidak menjelaskan apa pun (R-27). Baris §3.2 **dipersempit** menjadi apa yang benar-benar diperiksa mesin, dan bagian yang tidak disapu (prosa dokumen serta komentar kode lama) dinyatakan sebagai batas beserta pertanyaannya: **Q-025**. **Aturan pencegahan:** keputusan proyek tentang sebuah aturan harus menyebut **apa yang memeriksanya**; keputusan yang lebih luas dari pemeriksanya adalah klaim, bukan kepatuhan |
| C-070 | **FIXED** | 2026-09-22 (P-044) | **Kontrak respons arsip dokumen tidak menyebut bentuk amplopnya, dan klien mengisi kekosongan itu dengan tebakan yang salah.** `42-API.md` §4 menutup bagian `POST /documents/:id/archive` dengan satu baris: "Response 200: dokumen terarsip (baris, versi, berkas, dan jejak auditnya **tetap ada**)". Baris itu menyebut **isi**, bukan **bentuk**: pembaca tidak dapat mengetahui apakah dokumennya dikirim telanjang atau dibungkus. Server mengirim `{"success":true,"data":{"document":{…}}}`, dan klien yang dibangun pada sesi ini mengetik `ApiSuccess<DocumentRecord>` (bentuk datar) — sehingga `archiveDocument()` mengembalikan `undefined` saat dijalankan, dan hook React Query menyimpan `{…previous, document: undefined}` ke cache: halaman detail dokumen akan meledak pada render berikutnya (`documentStatus(undefined)`), bukan menampilkan kesalahan. Yang membuat ini khas: **seluruh test hijau** karena mock-nya menebak bentuk yang sama dengan kodenya, dan kontraknya tidak dapat membantah. Ketahuan hanya dari satu panggilan HTTP nyata ke server yang sedang berjalan (sesi probe UI P-044), bukan dari membaca atau dari `make test`. **Perbaikan:** `42-API.md` §4 menyebut amplop arsip secara eksplisit beserta contoh JSON-nya (dan membandingkannya dengan `POST /documents` serta `POST /documents/:id/upload` yang bentuknya berbeda), `services/documents.ts` membuka `data.document`, dan test service memakai bentuk respons yang **disalin dari server**, bukan dikarang. **Aturan pencegahan:** kalimat "Response 200: <entitas>" tanpa bentuk adalah janji yang tidak dapat diperiksa — setiap endpoint yang diimplementasikan wajib menuliskan amplopnya, karena mock test selalu setuju dengan kode yang menebaknya |
| C-071 | **FIXED** | 2026-09-22 (P-044) | **Penjaga append-only yang dipakai bersama tiga tabel selalu memperkenalkan diri sebagai `audit_logs`.** Fungsi `prevent_audit_modification()` dipasang pada `audit_logs` (migrasi `007`) **dan** pada `document_versions` serta `documents` (migrasi `010`, ADR-0019), dan pesannya ditulis tetap: `RAISE EXCEPTION 'audit_logs bersifat append-only …'`. Akibatnya `DELETE FROM document_versions` dijawab "audit_logs bersifat append-only", padahal `audit_logs` tidak tersentuh sama sekali — pesan yang menunjuk tabel yang salah, dan itu terbaca di log aplikasi maupun di `psql` ketika operator membersihkan data (sesi ini sendiri menabraknya dua kali sebelum menyadari bahwa yang menolak adalah penjaga versi). Cacat ini **tidak akan pernah tertangkap** test yang ada: semuanya memeriksa SQLSTATE `23001`, yang memang benar dan tidak berubah. **Perbaikan:** migrasi **`011`** menulis ulang fungsi memakai `TG_TABLE_NAME` (nama tabel sasaran sudah tersedia di trigger, tanpa argumen tambahan) sebagai `CREATE OR REPLACE` supaya ketiga tabel tidak pernah kehilangan penjaganya, dan `TestAppendOnlyMessageNamesTheOffendingTable` mengunci pesannya — giginya dibuktikan dengan memasang versi lama langsung di `bwdcs_test`: test **gagal** dengan pesan yang salah, lalu hijau sesudah fungsi diperbaiki. **Aturan pencegahan:** objek bersama tidak boleh memuat konteks pemanggilnya secara tetap; pesan runtime adalah bagian dari perilaku, dan yang selama ini diperiksa hanya kode galatnya |
| C-072 | **FIXED** | 2026-09-22 (P-045) | **Dua dari delapan jenis berkas yang dijanjikan tidak pernah dapat diunggah, dan test-nya hijau karena menulis MIME dengan tangan.** `44-SECURITY.md` §4.2 membandingkan hasil `http.DetectContentType` dengan daftar tertutup yang memuat `text/plain`, sedangkan Go mengembalikan **`text/plain; charset=utf-8`** untuk berkas teks — parameter `charset` adalah bagian dari header media type, bukan bagian dari tipe medianya. Akibatnya setiap unggahan `.txt` **dan** `.csv` dibalas `422 VALIDATION_ERROR`, padahal `50-FSD.md` §4.2 dan pesan `422`-nya sendiri menyebut kedua ekstensi itu didukung; `.pdf`, `.xls`, `.xlsx`, `.jpg`, `.png` tidak terpengaruh karena detektornya mengembalikan tipe tanpa parameter. Cacat ini tidak pernah tampak karena dua hal sekaligus: (a) **seluruh** test service menulis `MimeType: "text/plain"` dengan tangan, bukan memakai nilai yang benar-benar dikirim handler, sehingga nilai yang diuji tidak pernah sama dengan nilai produksi; dan (b) satu kasus test HTTP justru menuntut `422` untuk `palsu.pdf` yang isinya teks — kasus itu lulus **hanya** karena parameter `charset` ikut dibandingkan, jadi ia mengunci perilaku yang salah alih-alih menangkapnya. Ketahuan dari satu unggahan `.txt` sungguhan ke server yang sedang berjalan (sesi P-045), bukan dari `make test`. **Perbaikan:** `normalizeMimeType` membuang parameter sebelum pencocokan (hanya tipe media, sesuai RFC 7231), test service baru memakai MIME **hasil deteksi byte** (`http.DetectContentType`) untuk lima golongan berkas, dan kasus HTTP pengganti memakai isi yang memang di luar daftar (`.pdf` berisi ZIP, `.sh`); satu test HTTP baru mengunggah `.txt`/`.csv` multipart sungguhan lalu memeriksa `mime_type` tersimpan serta `Content-Type` dan isi unduhannya. Gigi dibuktikan: dengan `normalizeMimeType` dikembalikan ke bentuk lama, **enam subtest gagal** tepat pada nilai `text/plain; charset=utf-8`. **Aturan pencegahan:** test berkas wajib memakai nilai yang **dihasilkan** sistem yang diuji, bukan nilai yang ditulis test; dan setiap daftar tertutup harus menyatakan apakah parameter header ikut dibandingkan, karena runtime yang memutuskan bentuk nilainya |
| C-075 | **FIXED** | 2026-09-23 (P-048) | **Pemeriksa ledger melewatkan dua klaim test karena polanya tidak mengenali cara penulisannya, dan satu klaim versi skema di baris yang sama sudah basi.** `scripts/check-ledger.sh` memverifikasi hitungan test per berkas di `STATE.md` §3 lewat pola yang hanya menerima dua bentuk: `` `file_test.go` (N) `` dan `` `file_test.go` (**N** …) ``. Dua klaim di §3 ditulis dengan bentuk **ketiga** — `` `bootstrap_test.go` (10 test: …) `` dan `` `audit_append_only_test.go` (14 test: 7 `audit_logs` + 7 `document_versions`) `` — sehingga **tidak cocok pola mana pun dan tidak pernah diperiksa**; nilainya kini 8 dan 15. Angka total suite dan hitungan per direktori tetap dijaga, jadi cacatnya tidak terlihat dari keluaran skrip: pemeriksa itu **melaporkan OK sambil tidak memeriksa** dua klaim yang persis ditulis dalam gaya berbeda. Satu klaim sejenis juga tertinggal di `AGENTS.md` — "anotasi `Izin:` per endpoint, kini **40/51**" padahal sudah **48/55** sejak P-040 — dan itu jenis angka yang **memang** dihitung `check-api-contract.sh` di setiap jalannya, tetapi tidak pernah dibandingkan dengan kalimat siapa pun. Klaim versi skema yang menunjuk **keadaan sekarang** juga tertinggal di dua dokumen — `STATE.md` §3 ("Terpasang di `bwdcs` **versi 10**") dan `CONTINUE.md` §2 ("versi goose **10**, 22 tabel") — padahal migrasi `011` sudah diterapkan sejak P-045 (versi **11**, 23 tabel); keduanya jenis klaim yang **tidak** diperiksa skrip mana pun. Bagian bukti yang **sengaja** merekam versi saat dijalankan (`70-TESTING.md` §3.12a/§3.12b menulis `versi_skema 10` untuk eksekusi P-029/P-035) **tidak** diubah: itu snapshot eksekusi, bukan klaim keadaan. Keduanya ketahuan bukan dari menjalankan pemeriksa (yang justru hijau), tetapi dari **menghitung ulang angka-angka §3 satu per satu** saat sesi ini menyentuh baris itu untuk modul Workflow. **Perbaikan:** pola skrip meluas ke bentuk ketiga (tiga alternatif regex + tiga aturan `sed`), versi skema dikoreksi ke **11** di kedua dokumen, dan kedua hitungan diperbaiki. **Gigi dibuktikan:** nilai `bootstrap_test.go` dikembalikan sementara ke `10` → `ledger GAGAL — STATE.md §3: bootstrap_test.go ditulis 10 test, sebenarnya 8`; dipulihkan lalu hijau. Butir baru di `check-api-contract.sh` melakukan hal yang lebih baik daripada memperbaiki satu angka: ia **membandingkan** klaim itu dengan hasil hitungannya sendiri, sehingga ketiga angka yang sama tidak dapat basi lagi. **Aturan pencegahan:** pola yang tidak mengenali sebuah klaim adalah cara paling sunyi mematikan pemeriksaan — sama seperti klasifikasi `PLANNED` pada C-064 — jadi setiap pola pencocokan di skrip pemeriksa harus menolak bentuk yang tidak dikenalnya, bukan mengabaikannya |
| C-074 | **FIXED** | 2026-09-23 (P-048) | **Dua tempat dalam satu berkas menyebut himpunan yang sama dengan jumlah berbeda.** `42-API.md` §5 menutup blok `POST /workflows/instances/:id/actions` dengan "ini **satu-satunya** route di sistem yang izinnya bergantung pada isi body", sedangkan `42-API.md` §6 (`PATCH /tasks/:id`) menulis "ini route **kedua** yang izinnya bergantung pada isi body" dan `40-TSD.md` §6 aturan 3 menulis "ada **dua** route seperti itu" lengkap dengan keduanya. Kalimat "satu-satunya" benar saat ditulis (P-013: pola itu lahir di modul Workflow) dan menjadi salah pada P-026 ketika `task:assign` ditambahkan — dua tempat diperbarui, yang ketiga tidak. Akibatnya bukan kosmetik: pembaca yang mengerjakan modul Task dari §5 akan menyimpulkan `PATCH /tasks/:id` tidak punya pemeriksaan izin tambahan, dan pembaca modul Workflow akan menganggap hanya satu tempat di sistem yang menanggung pola izin-dari-body — padahal `40-TSD.md` §6 aturan 3 justru mewajibkan setiap route baru dengan pola itu menuliskan alasannya. Cacat ini **tidak dapat ditangkap** pemeriksa yang ada: `check-api-contract.sh` membandingkan pasangan izin, bukan kardinalitas himpunan dalam prosa; `check-doc-links.sh` memeriksa rujukan, dan rujukannya memang benar. Ketahuan dari membaca ulang kontrak §5 saat memverifikasi siapa yang memeriksa izin di modul yang baru selesai. **Perbaikan:** §5 ditulis ulang menjadi "route **pertama** dari **dua**" beserta rujukan silang ke §6 dan `40-TSD.md` §6 aturan 3, sehingga ketiga tempat menyebut himpunan yang sama. **Aturan pencegahan:** setiap kata "satu-satunya"/"pertama"/"kedua" yang menunjuk sebuah himpunan adalah klaim yang dapat basi seperti angka — sebut **nama** himpunannya, bukan hanya jumlahnya, supaya tempat yang tertinggal dapat ditemukan dengan mencari namanya |
| C-073 | **FIXED** | 2026-09-23 (P-048) | **Pseudokode `ExecuteAction` memberi Administrator jalan pintas yang tidak ada di dokumen mana pun yang mengikat.** `43-WORKFLOW.md` §4.1 memuat baris komentar `Actor must have the responsible_role OR be admin`, sehingga penunjukan sebagai penanggung jawab step dan kepemilikan izin menjadi **saling menggantikan**. Tiga dokumen lain menetapkan keduanya sebagai **syarat bersamaan**: `44-SECURITY.md` §3.1/§3.3 (`workflow_instance:<action>` adalah izinnya, penunjukan step adalah **tambahan**), `50-FSD.md` §5.4 ("aksi terbatas oleh izin aksi **dan** penunjukan sebagai penanggung jawab step aktif"), dan `51-UX.md` §2.1. Akibat aturan `OR` bukan kosmetik: Administrator dapat memutuskan step yang ditugaskan ke Manager — approval atas nama orang lain — dan `403` untuk "bukan penanggung jawab step" tidak akan pernah terjadi bagi role tertinggi, padahal justru itu yang membedakan approval workflow dari penyuntingan biasa. Cacat ini tidak dapat tertangkap dari membandingkan daftar: `42-API.md` §5 memang **tidak** menyebut penunjukan step di blok `Izin:` (dan itu benar — penunjukan bukan izin, sehingga tidak ada pasangan di matriks `44-SECURITY.md` §3.1.2 yang dapat dibandingkan), jadi satu-satunya tempat aturan gabungannya hidup adalah pseudokode ini. Ketahuan saat menulis guard `canActOnStep` dan mencocokkannya dengan §3.3 **sebelum** test ditulis, bukan dari kegagalan test. **Perbaikan:** pseudokode ditulis ulang (izin DAN penunjukan, dengan alasan dan rujukannya), implementasi memeriksanya berurutan (izin diperiksa sesudah body divalidasi, penunjukan step sesudahnya), dan `scripts/probe-workflow-module.py` menambah asersi hidup `administrator memegang workflow_instance:approve tetapi bukan penanggung jawab step → 403` — dijalankan pada server nyata dan **46/46 PASS**. **Aturan pencegahan:** pseudokode di dokumen desain adalah **kontrak** bagi implementasi; bila ia memuat jalan pintas yang tidak dinyatakan dokumen pengikat, jalan pintas itu wajib dihapus atau dinaikkan ke ADR — bukan diselaraskan diam-diam oleh kode |
| C-076 | **FIXED** | 2026-09-23 (P-050) | **Kolom penyaring tidak dapat menyusut, sehingga halaman melebar 44px pada 375px — dan cacatnya bergantung pada data yang kebetulan ada.** Sapuan tata letak menyapu 375/640/768/1024/1440px dan menemukan `halaman documents @ 375px: gulir mendatar 44px`; elemen penyebabnya ikut terukur: `#penyaring-project-dokumen` selebar **403px** beserta kolomnya (`div.flex flex-col gap-1.5`) yang selebar itu juga, sehingga dokumen melebar dari 375px menjadi 419px. Sebabnya bukan penataan yang salah, melainkan **ukuran minimum otomatis item flex**: kolom penyaring adalah item flex, dan `min-width: auto`-nya adalah **min-content** anaknya — untuk `<select>`, min-content ditentukan **teks pilihan terpanjang**, yaitu **data pengguna** (nama project `BWDCS · Business Development …`). Jadi cacatnya tidak selalu ada: pada database yang lebih sepi ia hilang sendiri, dan itulah yang membuat pemeriksa yang mengandalkan data yang kebetulan ada akan melaporkan OK. Halaman **Projects** membawa cacat yang sama meski pilihannya pendek dan tetap — ditemukan hanya sesudah sapuan menyisipkan **pilihan sengaja panjang** ke setiap `select` penyaring (`#penyaring-status` melebarkan halaman **65px** pada 375px). Kelasnya sama dengan C-061/C-062 (klaim tentang keadaan repo) tetapi arahnya terbalik: yang menentukan perilaku adalah **bentuk data pengguna**, bukan bentuk kode. **Perbaikan:** `min-w-0` pada **setiap** kolom ber-`select` di ketiga halaman (pilihan panjang dipotong browser di dalam kontrolnya, bukan melebarkan halaman), sedangkan kolom rentang tenggat justru tidak diberi `min-w-0` karena isinya dua `datetime-local` yang memang tidak dapat menyusut; sapuan tata letak menyisipkan pilihan panjang ke setiap select penyaring dan menuntut **pertambahan lebar 0**, dan test klien mengunci `min-w-0` pada setiap kolom ber-select dengan penjaga hampa. **Aturan pencegahan:** ukuran minimum yang datang dari **data** tidak terlihat dari membaca kelas CSS; kontrol yang lebarnya mengikuti isi harus diuji dengan isi yang sengaja ekstrem, bukan dengan isi yang kebetulan ada di database pengembang |
| C-077 | **FIXED** | 2026-09-23 (P-050) | **Utility ambang sentuh hanya menetapkan tinggi, sehingga satu kontrol terukur 43x44px — satu piksel di bawah ambangnya sendiri.** `51-UX.md` §9 dan R-03 menuntut target **44x44px** di layar sentuh, dan `tokens.css` memenuhi nilai itu lewat `@utility tap-target` — tetapi hanya pada `min-height`. Tab sub-halaman **"Tim"** pada halaman Tasks terukur **43x44px** pada 375px dan 768px: tingginya memenuhi ambang, lebarnya tidak. Ia satu-satunya kontrol setipis itu karena labelnya yang terpendek, sehingga cacatnya menunggu sampai ada label yang cukup pendek — persis bentuk janji yang berlaku atau tidak tergantung panjang kata. Ambang yang hanya berlaku pada satu sumbu juga membuat aturannya tidak dapat dipindahkan: `51-UX.md` menulis kotak, kelasnya memenuhi garis. **Perbaikan:** utility menetapkan **kedua sisi** (44px di layar sentuh, 36px di desktop), dan `src/styles/tap-target.test.ts` membaca `tokens.css` lalu menuntut keduanya hadir pada kedua rentang lebar — sekaligus melarang `width`/`height` tetap yang akan membuat isian panjang terpotong. **Aturan pencegahan:** ambang yang ditulis sebagai ukuran dua sumbu harus dinyatakan pada kedua sumbu; selama hanya satu yang dinyatakan, yang menguji membaca nilai yang benar dan yang diuji memakai aturan yang lain |
| C-078 | **FIXED** | 2026-09-23 (P-050) | **Pemeriksa tata letak itu sendiri membawa lima cacat, dan tiga di antaranya membuatnya memeriksa lebih sedikit daripada yang tampak.** Semuanya ketahuan dari satu pekerjaan: memperluas sapuan per lebar agar mencakup **setiap** halaman, bukan hanya `/projects`. **(1) Cakupan:** sapuan per lebar sejak P-043 hanya mengunjungi `/projects`, sehingga dua halaman lain hanya diukur di dua ujung lebar — dan justru di situlah C-076 bersembunyi selama dua sesi. **(2) Argumen yang diam-diam diabaikan:** `argOf` hanya mengenali `--widths=375,768`, sedangkan komentar pemakaian di berkasnya sendiri menulis bentuk berspasi `--widths 375,768`; bentuk itu diabaikan **tanpa pesan**, sehingga pengukuran berjalan pada lebar bawaan sementara pemanggilnya yakin ia mengukur lebar lain — dan keluarannya terlihat sah. **(3) Balapan jeda tetap:** `resize()` menunggu 1500ms tetap sementara font, potongan route yang dimuat malas, dan kueri data mengubah tata letak sesudahnya; satu laporan "gulir mendatar 44px" muncul sekali lalu tidak muncul lagi pada urutan yang sama. Positif palsu pada pemeriksa tata letak lebih buruk daripada tidak memeriksa, karena ia melatih pembacanya mengabaikan kegagalan. Ukuran kini menunggu tata letak **berhenti berubah** (dua sampel identik berturut-turut atas sidik jari kotak elemen). **(4) Cacat pada kendali majemuk:** pemeriksaan "kolom melanjutkan di bawah kontrolnya" menandai `Tenggat dari` pada 375px — padahal isian pertama dari dua isian yang menumpuk memang punya ekor di bawahnya (itu isian kedua, bukan kolom yang melanjutkan tanpa isi); pengecualiannya kini **dilaporkan** (`liftedComposite`), bukan disembunyikan. **(5) Tuduhan yang salah alamat:** probe stres melaporkan **luas halaman sesudah** probe, bukan **pertambahannya**, sehingga pada halaman yang sudah melebar setiap `select` tampak bersalah — `#penyaring-status-dokumen` ikut dituduh 44px padahal halaman sudah selebar itu sebelum probe. Satu fase yang ternyata **hampa pada beberapa masukan** juga tertangkap: dengan hanya satu lebar, fase "lebarkan lalu persempit saat laci terbuka" tidak pernah melebarkan jendela sehingga tuntutannya tidak bermakna, dan menjalankannya apa adanya melaporkan tiga kegagalan yang tidak pernah terjadi; fase itu kini dilewati **dengan alasan tertulis** di laporan. **Perbaikan:** kelimanya ditutup pada sesi yang sama, dan masing-masing disertai pemisahan yang mengembalikan maknanya (pertambahan vs luas, pengecualian yang dilaporkan, lewatan yang dicatat). **Aturan pencegahan:** pemeriksa adalah kode yang dapat salah seperti kode lain, dan yang paling berbahaya adalah bentuk yang **mengurangi pemeriksaan tanpa suara** (pola yang tidak cocok, argumen yang tidak dikenali, jeda tetap yang kalah balapan) — kelas yang sama dengan C-055 dan C-064/C-075, kini ketiga kalinya muncul di alat pemeriksa sendiri |
| C-079 | **FIXED** | 2026-09-23 (P-051) | **Aturan "sidebar memuat modul saja" tidak dijaga pemeriksa mana pun, dan tidak ada yang akan menangkapnya karena dua alat yang tampak menjaganya memang tidak melihat kasus ini.** `51-UX.md` §2.1 menetapkan sidebar muat **modul saja** — satu entri per modul, tanpa sub-item, penyaring hidup di halamannya sendiri — dan aturan itu ditegakkan di klien oleh `frontend/src/config/navigation.ts` (sejak P-047 membuang `subItems`). Yang tidak ada adalah **penegaknya di mesin**: `.github/workflows/ci.yml` tidak punya job frontend sama sekali, sehingga test klien (`navigation.test.ts`, `AppShell.test.tsx`) **tidak pernah** berjalan di CI, dan satu-satunya pemeriksa yang menyentuh bentuk menu — bagian `navigation` pada `scripts/responsive-evidence.mjs` — hanya mencari item menu yang membawa **kueri penyaring**, sehingga path bersarang tidak masuk cakupannya. Akibatnya halaman **Audit** yang berada di bawah **Reports** dapat berdiri sebagai entri sidebar (`/reports/audit`, satu segmen tambahan) tanpa ada yang gagal, dan diagram delapan butir di `51-UX.md` §2 beserta tabel §2.1 kembali menyimpang dari kode — persis pola yang P-047 baru saja perbaiki. Kelasnya sama dengan C-067/C-069 (aturan desain yang lebih luas daripada yang diperiksa mesin) tetapi lebih sunyi: yang tidak ada bukan pemeriksaan yang lemah, melainkan **pemeriksaan yang tidak pernah diadakan**. Ketahuan justru saat mengerjakan halaman anak: pertanyaannya bukan "bagaimana menampilkannya", melainkan "apa yang mencegah entri ini dipasang di sidebar" — dan jawabannya tidak ada. **Perbaikan:** `scripts/check-navigation.sh` (baru, `T-068`) membaca **teks** model navigasi dan tabel §2.1, lalu menolak entri sidebar berkueri/berfragmen, entri sidebar lebih dari satu segmen, label menu bergaya remah, halaman anak tanpa induk atau di luar induknya, path/label ganda, dan daftar menu yang menyimpang dari tabel §2.1 — **dua arah** — serta menuntut §2.1 masih menyatakan "modul saja", supaya melonggarkan aturannya juga menuntut perubahan skrip. **Gigi dibuktikan dengan tujuh cacat disuntikkan sementara:** entri sidebar menunjuk sub-halaman, entri sidebar membawa `?view=mine`, halaman anak berinduk hantu, menu baru di kode tanpa baris dokumen, halaman anak dipindah modulnya di dokumen, teks §2.1 dilunakkan, dan kunci `label:` diganti nama — ketujuhnya tertangkap, dan berkasnya dipulihkan byte-identik (`diff -q`) sebelum sesi ditutup. **Aturan pencegahan:** setiap aturan bentuk di dokumen desain harus punya penegaknya di mesin; kalau penegaknya hanya ada di test klien sedangkan CI tidak menjalankannya, aturannya **tidak** ditegakkan — dan penegak yang hanya mencari satu gejala (kueri) tidak menutup gejala lain (path bersarang) |
| C-080 | **FIXED** | 2026-09-23 (P-051) | **Versi pertama pemeriksa baru itu sendiri melaporkan kegagalan untuk sebab yang salah, dan penjaga hampa yang dipasang untuk mencegahnya tidak menyala.** Karena `scripts/check-navigation.sh` membaca **teks** berkas TypeScript (repo ini tidak punya runner TypeScript untuk skrip pemeriksa), bentuk berkas yang tidak dikenali wajib **gagal**. Versi pertama hanya menghitung jumlah menu (`< 5` → gagal), dan angka itu **tidak** menangkap berkas yang kuncinya berganti nama: parser tetap memancarkan tujuh objek, hanya `label`/`path`-nya kosong, sehingga penjaga hampa lolos dan pemeriksa melanjutkan ke perbandingan himpunan dengan nilai kosong — keluarannya **25 kegagalan**, semuanya menyesatkan (`path "" tidak diawali "/"`, `menu "/" tidak punya baris di §2.1`), dan sebab sebenarnya tidak disebut sama sekali. Itu bentuk yang sama dengan C-078 butir (2) dan C-075: pemeriksa yang tetap **berisik** tetapi berhenti **menjelaskan**, dan pembacanya dilatih mencari cacat yang tidak ada di tempat yang ditunjuknya. Ketahuan bukan dari membaca skripnya, melainkan dari menguji giginya: menyuntikkan `label:` → `title:` sebagai cacat ketujuh. **Perbaikan:** penjaga bentuk menggantikan penjaga hitungan — skrip menghitung objek yang **tidak lengkap** (tanpa label, tanpa path, atau tanpa induk) dan **berhenti di situ** dengan satu pesan yang menyebut sebabnya (`N objek tanpa label/path/induk yang terbaca — bentuk berkasnya berubah dan parser ini tidak mengenalinya; perbarui parsernya, jangan nilai hasil di bawahnya`), diuji ulang untuk berkas yang seluruhnya rusak maupun satu objek yang rusak. **Aturan pencegahan:** pemeriksa yang membaca teks sumber harus menolak **bentuk** yang tidak dikenalnya sebelum menilai **isi**-nya; menghitung berapa banyak yang terbaca tidak sama dengan memeriksa apakah yang terbaca itu masuk akal |
| C-081 | **FIXED** | 2026-09-23 (P-051) | **Satu-satunya tempat yang menyatakan jumlah temuan "secara keseluruhan" adalah satu-satunya tempat yang tidak diperiksa mesin.** Paragraf §1 laporan ini menutup riwayatnya dengan "**75 temuan** secara keseluruhan: 37 di atas, ditambah **38 temuan berikutnya (C-038..C-075)**" — angka yang benar saat P-048, lalu **tidak** ikut bergerak ketika P-050 menambahkan C-076/C-077/C-078 dan marker `audit-summary` naik ke 78. Akibatnya laporan audit memuat **dua** total yang berbeda: marker (78, yang mengikat) dan paragraf yang dibaca manusia (75), sehingga pembaca yang mencari "berapa temuan yang ada" dari prosa mendapat angka yang salah sementara `check-ledger.sh` melaporkan **OK**. Sebabnya struktural, bukan salah tulis: aturan §2a skrip itu hanya membaca baris prosa yang memuat **`AUDIT-001`** *dan* `FIXED` (yg membedakan klaim keadaan dari catatan peristiwa), sedangkan paragraf §1 berada di dalam berkas audit itu sendiri dan **tidak menyebut namanya sendiri** — jadi ia tidak pernah masuk cakupan, bersama seluruh prosa lain di berkas itu. Pola yang sama dengan C-075 (klaim yang tidak dikenali polanya) dan C-060/C-061 (klaim tentang keadaan yang menua tanpa pembanding), dan ia jenis yang paling tidak terlihat: yang menjaga angka ini selama ini adalah **kemampuan manusia mengingat untuk menyentuhnya**, dan itu gagal tiga sesi. Ketahuan saat sesi ini harus menyambung rantai "... menjadi **N**" yang sama untuk C-079/C-080, dan angka 75 di ujungnya tidak cocok dengan 78 di marker. **Perbaikan:** paragraf dikoreksi, dan `check-ledger.sh` mendapat aturan yang mengikatnya — ringkasan §1 laporan audit wajib sama dengan `total=` pada markernya, sehingga angka itu tidak dapat tertinggal lagi. **Versi pertama aturannya ternyata mengulang cacat C-080 di sesi yang sama:** regexnya dikirim ke `awk` lewat `-v`, dan `-v` memproses escape sehingga `\*` menjadi `*` — hasilnya pola yang **tidak pernah cocok**, perbandingannya tidak berjalan sekali pun, dan `check-ledger.sh` melaporkan **OK**. Ketahuan bukan dari membaca skripnya melainkan dari menguji giginya: mengembalikan `75` ke paragraf §1 tidak memunculkan kegagalan apa pun. Polanya kini ditulis di dalam program `awk`, dan kedua tempat ringkasan diperiksa **terpisah** (pagar yang hanya menuntut "salah satu ada" akan melewatkan tempat yang berubah bentuk, lalu tempat itu berhenti diperiksa tanpa suara). **Gigi dibuktikan dengan empat cacat disuntikkan sementara:** (1) paragraf §1 dikembalikan ke `75` → `FAIL … :28: ringkasan temuan di dalam laporan audit menulis 75, marker audit-summary bilang 82`; (2) baris `> Ringkasan:` dikembalikan ke `78` → `FAIL … :470: … menulis 78, marker audit-summary bilang 82`; (3) paragraf §1 kehilangan penanda tebalnya → `FAIL … paragraf §1 tidak lagi cocok pola yang diperiksa`; (4) baris `> Ringkasan:` berubah bentuk → `FAIL … baris "> Ringkasan: **N temuan**" tidak lagi cocok pola yang diperiksa`. Keempatnya tertangkap, lalu berkasnya dipulihkan byte-identik (`diff -q`). **Aturan pencegahan:** setiap angka yang dinyatakan di dalam berkas sumbernya sendiri (di sini: total di dalam laporan audit) harus diperiksa terhadap sumber angkanya; pemeriksa yang membaca "lima dokumen lain" akan selalu melewatkan dokumen tempat angka itu lahir |
| C-082 | **FIXED** | 2026-09-23 (P-051) | **Suite frontend gagal berpindah-pindah antar-berkas pada kode yang sama, dan pesan kegagalannya menuduh aplikasi alih-alih mesin.** Terukur pada sesi ini: lima kali `npm run test:run` berturut-turut menghasilkan **tiga** kegagalan yang berbeda-beda — `Documents.test.tsx` (`Unable to find role="link" and name "BRD"`), `Tasks.test.tsx` (`Unable to find role="link" and name "Tinjau BRD"`), `TaskDetail.test.tsx` (`Unable to find role="heading"...`), dan satu pemeriksaan `axe` pada `DocumentDetail.test.tsx` — sementara dom yang dicetak kegagalan itu menunjukkan halaman berhenti di **kerangka pemuatan** (`aria-hidden="true"`, tiga baris tulang) padahal service-nya di-mock `mockResolvedValue` sehingga promise-nya selesai di mikrotask. Sebabnya bukan halaman, juga bukan mock: jendela tunggu bawaan `findBy*`/`waitFor` adalah **1000ms**, dan angka itu tidak menyesuaikan beban mesin — dengan 25 berkas test berjalan paralel, prosesor yang berebut dapat menunda penyajian hasil itu melewati satu detik. Diperiksa dengan mengubah beban alih-alih menebak: `npx vitest run --maxWorkers=2` dan `--maxWorkers=1` **hijau penuh 25/25** pada kode yang sama, jadi yang berubah bukan aplikasinya melainkan jadwal CPU-nya. Cacat ini kelas yang sama dengan C-068/C-078 (test/pemeriksa yang tidak menguji apa yang diklaimnya) tetapi bentuknya paling menyesatkan: kegagalannya **bukan** lulus-hampa, melainkan **gagal-hampa**, dan satu-satunya penyembuhnya yang tersedia bagi pembacanya adalah **mengulang** — kebiasaan yang membuat kegagalan sungguhan kelak ikut diabaikan. Bahwa ia muncul di sesi yang sama dengan kerja navigasi bukan kebetulan: yang berubah adalah **banyaknya** test (241 → 254 sejak P-049), dan itulah satu-satunya sebabnya — tidak satu pun berkas yang gagal menyentuh navigasi. **Perbaikan:** `configure({ asyncUtilTimeout: 5000 })` di `src/test/setup.ts` dan `testTimeout: 20000` di `frontend/vite.config.ts`, keduanya dengan alasan tertulis di tempatnya. **Asersi tidak dilonggarkan sama sekali** — yang naik hanya lamanya sebuah test boleh menunggu. **Gigi dibuktikan dari arah sebaliknya:** tiga kali `npm run test:run` berturut-turut pada worker bawaan kini **25/25 hijau**, sedangkan sebelum perubahan tiga dari lima kali gagal; dan `--maxWorkers=1`/`=2` tetap hijau, jadi yang diperbaiki memang anggaran waktunya, bukan dengan menutupi testnya. **Aturan pencegahan:** batas waktu bawaan sebuah alat adalah klaim tentang kecepatan mesin, bukan tentang kode; test yang gagal hanya saat mesin sibuk tidak menguji kode, dan pesannya harus membuat sebab itu dapat dibedakan (`kerangka pemuatan` pada dom vs `unsupported`) — kalau tidak, "jalankan ulang" menjadi ritual, dan ritual itu menghapus kemampuan reponya mendeteksi regresi |

> **Temuan C-079, C-080, C-081, dan C-082 ditambahkan pada 2026-09-23 (sesi P-051)**, keduanya dari satu pekerjaan yang sama: membuat penegak mesin untuk aturan `51-UX.md` §2.1. **C-079** adalah aturannya sendiri (sidebar memuat modul saja) yang selama ini hanya dijaga test klien, sementara CI tidak menjalankan test klien dan pemeriksa di peramban hanya mencari menu berkueri — sehingga halaman anak di bawah Reports dapat masuk sebagai entri sidebar tanpa ada yang gagal. **C-080** adalah cacat pada penegak barunya, ditemukan justru karena giginya diuji dengan cacat yang **bukan** pelanggaran aturan melainkan kerusakan bentuk berkas: penjaga hampa yang hanya menghitung jumlah menu tidak menyala, dan 25 kegagalan menyesatkan menggantikan satu sebab yang benar. **C-081** menyusul pada sesi yang sama, dari menyunting paragraf §1 laporan ini: total "secara keseluruhan" di dalam berkas audit **tidak** diperiksa pemeriksa mana pun — aturan prosa `check-ledger.sh` hanya membaca baris yang menyebut `AUDIT-001`, sedangkan paragraf itu ada di berkas audit itu sendiri sehingga tidak pernah masuk cakupannya (75, padahal marker sudah 78 sejak P-050). Ketiganya menunjuk pelajaran yang sama dengan C-067/C-069 dan C-078: aturan yang dipegang manusia akan menyimpang tanpa suara; penegaknya sendiri harus diuji dengan bentuk yang tidak ia kenal, bukan hanya dengan bentuk yang ia tolak; dan angka yang lahir di dalam sebuah berkas wajib dibandingkan dengan sumbernya di **berkas itu juga**. **C-082** menyusul dari verifikasi rutin di sesi yang sama: suite frontend gagal berpindah-pindah antar-berkas pada kode yang sama, karena jendela tunggu bawaan `findBy*` (1000ms) adalah klaim tentang **kecepatan mesin**, bukan tentang aplikasi — `--maxWorkers=1`/`=2` hijau penuh pada kode yang sama menjadi buktinya. Anggarannya dinaikkan tanpa melonggarkan satu asersi pun, dan tiga kali jalannya hijau berturut-turut. Pelajarannya melengkapi tiga sebelumnya: kegagalan yang bergantung beban tidak sama dengan kegagalan yang bergantung kode, dan test yang hanya lulus pada mesin yang tidak sibuk akan mengajari pembacanya untuk mengulang — kebiasaan yang menghapus kemampuan repo mendeteksi regresi.

> **Temuan C-070 dan C-071 ditambahkan pada 2026-09-22 (sesi P-044)** saat membangun halaman **Documents**, dan keduanya lolos dari semua pemeriksa yang ada karena jenisnya berbeda dari temuan sebelumnya: keduanya **bukan** kontradiksi antar dokumen dan **bukan** angka yang basi, melainkan **kalimat kontrak yang terlalu pendek untuk diperiksa**. **C-070**: `42-API.md` §4 menulis arsip sebagai "Response 200: dokumen terarsip", tanpa menyebut bentuk amplopnya, sedangkan server mengirim `{"data": {"document": {…}}}` — klien yang membaca kalimat itu menebak bentuk datar, dan tebakan itu **lolos seluruh test** karena test-nya memakai tiruan yang menebak hal yang sama; yang menangkapnya hanya panggilan HTTP nyata ke server yang sedang berjalan. **C-071**: satu fungsi penjaga append-only dipakai bersama `audit_logs`, `document_versions`, dan `documents`, tetapi pesannya menyebut `audit_logs` secara tetap, sehingga menolak `DELETE FROM document_versions` menjawab "audit_logs bersifat append-only" — pesan yang salah tabel dan menyesatkan operator. Keduanya menunjuk kelemahan yang sama dengan C-068 (test yang mengklaim lebih dari yang dikuncinya): **klaim di dokumen dan pesan di runtime sama-sama harus menyebut hal yang benar, dan hanya jalur nyata yang membuktikannya.** Keduanya `FIXED` di sesi yang sama — C-070 dengan menuliskan bentuk amplop arsip di kontrak **dan** mengubah mock test menjadi bentuk yang disalin dari respons server, C-071 dengan migrasi `011` yang memakai `TG_TABLE_NAME` beserta test yang gagal bila pesannya kembali menyebut tabel lain.

> **C-076, C-077, dan C-078 lahir dari satu perubahan pada alat ukurnya (P-050), dan ketiganya kelas yang tidak dapat ditangkap pemeriksa berbasis dokumen.** Yang diperluas: sapuan tata letak `scripts/responsive-evidence.mjs` yang sejak P-043 mengunjungi `/projects` saja, kini **setiap halaman × setiap lebar** (15 pengukuran tata letak, 18 pengukuran tema), ditambah penyisipan **pilihan sengaja panjang** ke setiap `select` penyaring. **C-076** (halaman melebar 44px karena kolom flex tidak dapat menyusut di bawah min-content sebuah `select`, yang ditentukan teks pilihan — yaitu data pengguna) dan **C-077** (ambang sentuh hanya menetapkan tinggi, sehingga tab "Tim" terukur 43x44px) adalah dua cacat nyata di antarmuka. **C-078** adalah lima cacat pada pemeriksanya sendiri — termasuk cakupan yang tidak pernah meluas, argumen CLI yang diam-diam diabaikan, dan jeda tetap yang kalah balapan sehingga menghasilkan positif palsu. Ketiganya menunjuk pelajaran yang sama: **pemeriksa yang mengukur halaman pertama lalu menyimpulkan halaman lain, dan yang mengandalkan data yang kebetulan ada, akan melaporkan OK tepat pada cacat yang paling perlu ditemukan** — dan pemeriksa itu sendiri harus diuji seperti kode lain.
>
> **C-072 menyusul di sesi yang sama (P-045), dan ia satu kelas dengan keduanya.** Yang salah bukan dokumennya dan bukan pesannya, melainkan **nilai yang dibandingkan**: runtime memproduksi `text/plain; charset=utf-8`, sedangkan daftar tertutup di dokumen menulis `text/plain`. Dua akibatnya terasa: fitur yang dijanjikan (`.txt`, `.csv`) tidak pernah dapat dipakai, dan test yang menulis MIME dengan tangan tidak pernah bertemu nilai produksi itu — sama seperti mock C-070 yang menebak bentuk respons dan test C-068 yang tidak mengunci perilaku yang diklaimnya. Pola yang berulang pada P-044/P-045 karena itu bukan "lupa menulis", tetapi **klaim yang tidak diuji terhadap kenyataan**: kontrak tanpa bentuk, pesan tanpa nama tabel, dan test tanpa nilai produksi.
>
> **Temuan C-075 ditambahkan pada 2026-09-23 (sesi P-048)** sesudah sesi yang sama menghitung ulang angka-angka `STATE.md` §3 untuk baris modul Workflow, dan menemukan bahwa **pemeriksa yang dirancang untuk menahan kelas ini justru melewatkan dua klaimnya**. Ia kelas yang sama dengan C-057 (angka test yang menyimpang) dan C-064 ("klasifikasi yang keliru mematikan pemeriksaan"), tetapi bentuknya lebih halus: bukan skrip yang berhenti memeriksa, melainkan **klaim yang tidak dikenali polanya** sehingga diperlakukan seolah tidak ada. Pelajarannya sama dengan C-074 — kalimat dan angka baru bernilai setelah ada yang membandingkannya, dan sebuah pemeriksa hanya sekuat pola pencocokannya.
>
> **Temuan C-074 ditambahkan pada 2026-09-23 (sesi P-048)** saat sesi yang sama menyelaraskan `42-API.md` §5 dengan implementasi: dua tempat di **satu berkas** menyebut himpunan yang sama dengan jumlah berbeda. `42-API.md` §5 menutup bagian `POST /workflows/instances/:id/actions` dengan "**satu-satunya** route di sistem yang izinnya bergantung pada isi body", sedangkan `42-API.md` §6 pada `PATCH /tasks/:id` menulis "ini route **kedua** yang izinnya bergantung pada isi body", dan `40-TSD.md` §6 aturan 3 menulis "ada **dua** route seperti itu" beserta keduanya. Angka "satu" itu benar ketika kalimatnya ditulis (P-013, pola itu lahir di modul workflow) dan menjadi salah pada P-026 ketika `task:assign` menyusul; dua tempat lain diperbarui, kalimat di §5 tidak — persis kelas C-060/C-061/C-062 (klaim yang tinggal di berkas yang tidak ikut disapu), tetapi pada **satu himpunan yang sama di dalam satu berkas**, sehingga pembaca yang hanya membuka §5 akan menyimpulkan `PATCH /tasks/:id` tidak ada. Ketahuan dari membaca ulang kontrak §5 saat memverifikasi siapa yang memeriksa izin di modul yang baru selesai, bukan dari pemeriksa: `check-api-contract.sh` memeriksa **pasangan izin**, bukan pernyataan kardinalitas dalam prosa. **Perbaikan:** kalimat §5 ditulis ulang menjadi "route **pertama** dari **dua**" dengan rujukan silang ke §6 dan `40-TSD.md` §6 aturan 3, jadi ketiga tempat kini menyebut himpunan yang sama. **Aturan pencegahan:** setiap kalimat "satu-satunya", "pertama", atau "kedua" yang menunjuk sebuah himpunan adalah klaim yang dapat basi seperti angka — himpunannya harus disebut namanya (di sini: "route yang izinnya bergantung pada isi body"), bukan hanya jumlahnya.
>
> **Temuan C-073 ditambahkan pada 2026-09-23 (sesi P-048)** saat mengimplementasikan modul **Workflow** — modul terakhir yang tersisa di backend, dan yang paling lama tinggal sebagai kontrak (`43-WORKFLOW.md` sudah ada sejak P-007, `42-API.md` §5 sejak P-013/P-017). Jenisnya berbeda dari seluruh temuan P-044/P-045: bukan angka yang basi, bukan bentuk respons yang tidak ditulis, dan bukan nilai runtime yang tidak cocok, melainkan **aturan yang lebih lemah di satu tempat daripada di tiga tempat lain**. Yang membuatnya bertahan lama adalah bentuknya yang tidak dapat diperiksa mesin: penunjukan penanggung jawab step **bukan** pasangan resource/action, jadi ia tidak punya baris di matriks `44-SECURITY.md` §3.1.2 dan tidak muncul di blok `Izin:` kontrak mana pun — `check-api-contract.sh` memeriksa 114 hal tanpa pernah menyentuhnya, dan itu memang benar, karena yang salah bukan izinnya melainkan **syarat tambahan** yang ditulis sebagai pilihan. Temuan ini juga menutup pertanyaan yang sebelumnya menggantung pada C-006 ("Reviewer = peran fungsional"): begitu penunjukan step menjadi syarat yang berdiri sendiri, jawabannya terlihat dari perilaku — role sistem tetap empat, dan penugasan tetap fungsional, seperti yang diputuskan pada P-026. Aturan turunannya dicatat di `AGENTS.md` bersama aturan modul lain, dan pengukurannya di `scripts/probe-workflow-module.py`.
> **Koreksi semantik pada C-046, 2026-09-19 (sesi P-028).** Baris C-046 di atas mencatat bahwa penyaring `due_from`/`due_to` ditambahkan sebagai interval **setengah terbuka** — itu benar untuk P-026, tetapi **bukan perilaku yang berlaku sekarang**: user meminta batas inklusif, sehingga `GET /tasks?due_from=&due_to=` memakai interval **tertutup** `[due_from, due_to]` (`due_to == due_from` sah; hanya rentang terbalik yang `422`), dengan konsekuensi tumpang tindih rentang bersebelahan yang dinyatakan di `42-API.md` §6. Ini **koreksi keputusan**, bukan temuan baru: baris C-046 tetap `FIXED`, statusnya tidak berubah, dan catatan ini ditulis sebagai entri baru karena berkas audit bersifat append-only. Pekerjaannya dicatat sebagai `T-044` (DONE) dan log `prompts/P-028-2026-09-19-rentang-tanggal-inklusif-q017.md`; butir (11) pada baris C-045 diverifikasi ulang pada sesi yang sama tanpa perubahan kode.
>
> **Temuan C-051 ditambahkan pada 2026-09-19 (sesi P-029)** saat mengerjakan `T-039`: FK `document_versions.document_id` masih `ON DELETE CASCADE` di skema, tetapi sejak migrasi `010` kaskadenya **ditahan** trigger append-only — `DELETE FROM documents` gagal `23001` bila dokumennya punya versi. Bukan kontradiksi dokumen (perilaku itu konsekuensi yang diterima ADR-0019), tetapi **mudah menyesatkan** siapa pun yang membaca skema dan menyimpulkan pembersihan bebas hambatan; karena itu dicatat sebagai temuan ber-status `FIXED` dengan catatan di `41-DATABASE.md` §2.3 + test `TestDocumentVersions_DeleteFromDocumentsIsRejected`.
>
> **Temuan C-048, C-049, dan C-050 ditambahkan pada 2026-09-19 (sesi P-027)** saat mengerjakan modul Comment, dan ketiganya kembali menunjukkan pola yang sama: yang menemukannya adalah **menjalankan** sesuatu, bukan membaca kode. C-048 dari test paginasi sendiri (halaman di luar rentang), C-049 dari probe registrasi rute sebelum handler ditulis (Gin panik), C-050 dari mencocokkan daftar field `50-FSD.md` §7 dengan kolom tabel `comments` saat menulis test threading. Bandingkan C-045/C-046 (P-025) dan C-040/C-041 (P-023) — pola yang sama sudah tiga sesi berturut-turut.
>
> **Temuan C-047 ditambahkan pada 2026-09-19 (sesi P-026)** saat memeriksa ulang ledger terhadap dokumen desain setelah diminta menutup gap: tabel status fase `80-ROADMAP.md` tertinggal jauh dan label fase Task berbeda antar dokumen. `FIXED` di sesi yang sama.
>
> **Temuan C-045 dan C-046 ditambahkan pada 2026-09-19 (sesi P-025)** saat mengerjakan modul Task: keduanya hanya terlihat ketika implementasi **dijalankan** dan dicocokkan dengan dokumen desain, bukan dari membaca kode. C-045 dari uji HTTP nyata (pesan `422` yang menyesatkan), C-046 dari pencocokan daftar penyaring `50-FSD.md` §6.1 dengan `42-API.md` §6. Keduanya **`FIXED` pada P-026** (lihat barisnya di tabel di atas): atribusi error body memakai tabel pemetaan `42-API.md` §12, dan penyaring rentang ditetapkan setengah terbuka. Statusnya sempat tertulis `OPEN` di catatan ini sesudah diperbaiki — dikoreksi pada 2026-09-19; tabel di atas yang berlaku.
>
> **Sembilan temuan yang tersisa diputuskan pada 2026-09-19 (P-026, lanjutan sesi yang sama)** dengan dasar riset best practice di `OPEN-QUESTIONS.md` §3, setelah user meminta agar pertanyaan terbuka dijawab — bukan lagi diserahkan sebagai pilihan tanpa data:
>
> - **`FIXED` tanpa perubahan kode:** C-006 (Reviewer = peran fungsional), C-007 (dua hierarki role tidak digabung), C-010 (`responsible_user_id` ditunda; MVP role-based), C-028 (retensi audit = operasi pemeliharaan, ADR-0020).
> - **`APPROVED`, menunggu implementasi:** C-009 & C-035 (ADR-0022 → `T-041`), C-033 (ADR-0021 → `T-040`). `APPROVED` berarti **keputusan sudah ada dan dokumen sudah selaras; kode belum** — temuan ini tidak boleh dibaca sebagai selesai. **C-004 sudah meninggalkan kelompok ini pada P-029** (`T-039` selesai → `FIXED`), dan **ketiga sisanya pada P-030** (`T-040`/`T-041` selesai → `FIXED`). Sejak itu **tidak ada lagi temuan berstatus `APPROVED`**: setiap temuan yang punya keputusan juga sudah punya kode dan test.
> - **`OPEN`:** C-015 — keputusan rasa/identitas pemilik produk (Q-002), tidak dapat dijawab agen maupun riset.
>
> **Temuan C-052, C-053, dan C-054 ditambahkan pada 2026-09-19 (sesi P-030)** saat mengimplementasikan ADR-0021/0022 (`T-040`/`T-041`). Ketiganya hanya terlihat ketika kode **dijalankan**, bukan dari membaca dokumen: **C-052** dari menulis kontrak `423` (contoh `details` berbentuk daftar vs prosa yang menjanjikan objek), **C-053** dari test `TestLoginRightAfterLogoutAllStillWorks` yang **gagal** pada implementasi pertama (`NOW()` mentah menolak token hasil login ulang pada detik yang sama), dan **C-054** dari mencocokkan pemicu `429` di dokumen dengan mekanisme yang baru. Ketiganya `FIXED` di sesi yang sama. **C-055** ditambahkan di sesi yang sama sesudah memeriksa silang angka temuan: tabel severitas §1 dan baris `audits/README.md` tidak dapat dijumlahkan dengan isi berkasnya (kelas C-044), dan kini keduanya dihitung dari bagian temuan.
>
> **Temuan C-063 ditambahkan pada 2026-09-21 (sesi P-041)** saat membangun halaman **Projects** — halaman bisnis pertama yang benar-benar menyentuh kontrak FSD §3.2. `50-FSD.md` §3.2 menuliskan field `Owner` sebagai dropdown wajib "User select", tetapi tidak ada endpoint yang dapat menyebutkan daftar pengguna: `GET /admin/users` belum ada dan `user:read` hanya milik Administrator. Bedanya dengan temuan-temuan sebelumnya: cacat ini **tidak terlihat** dari membaca kontrak, karena `42-API.md` §3 memang tidak pernah menjanjikan endpoint pengguna — yang menjanjikannya justru FSD, dan hanya terasa ketika formnya harus diisi. Yang dikerjakan sesi itu bukan mengarang pemilih pengguna, melainkan menampilkan batasnya di layar dan mengunci test. Statusnya **`OPEN`** karena pilihannya (endpoint pencarian pengguna dengan izin baru, atau menurunkan FSD §3.2 untuk MVP) menyentuh kontrak API dan matriks izin — keputusan yang butuh ADR; pertanyaannya di `OPEN-QUESTIONS.md` **Q-024**. Temuan ini juga menampakkan kelas baru yang patut diwaspadai agen berikutnya: **lapisan UI menggandakan janji dokumen**, sehingga setiap halaman baru dapat memunculkan temuan yang selama ini tidak terlihat karena tidak ada yang mencoba memenuhinya.
>
> **Temuan C-066 ditambahkan pada 2026-09-22 (sesi P-042)**, dan ia jenis baru di tabel ini: bukan angka yang basi, melainkan **himpunan** yang basi. Pohon folder `README.md` §4 menyebut dua dari lima skrip yang sudah berjalan di CI — dua sesi menambah pemeriksa tanpa menyentuh pohonnya, dan `check-readme-facts.sh` tidak menangkapnya karena ia membandingkan klaim **angka & versi**, bukan daftar berkas. Ditemukan justru ketika pohon itu dibuka untuk keperluan lain (menambahkan `skills/`), bukan oleh pemeriksa: persis pola C-060/C-061, tetapi pada klaim yang bentuknya himpunan. Pelajarannya: setiap daftar di dokumen yang dapat dibandingkan dengan isi direktori butuh pembanding **dua arah** — skrip yang ada harus disebut, dan yang disebut harus ada; kini `check-readme-facts.sh` §6 memeriksa keduanya (37 fakta), dan gigi-nya dibuktikan dengan dua cacat sementara.

> **Temuan C-067, C-068, dan C-069 ditambahkan pada 2026-09-22 (sesi P-043)**, dan ketiganya lahir dari satu hal yang sebelumnya tidak pernah dilakukan: **mengukur klaim tata letak dan membuktikan gigi testnya**. C-067 klasnya paling mencolok — kode menunjuk nomor temuan yang tidak pernah dibuat, jadi keputusan desain yang diambil saat implementasi hanya hidup di komentar. C-068 lebih halus dan tidak dapat ditemukan dengan membaca test: dua test baru tetap hijau sesudah perilaku yang mereka klaim **dihapus**, dan dua pemeriksaan di skrip bukti barunya juga lulus hampa karena mengukur elemen yang salah dan menjalankan urutan yang salah. C-069 menunjuk pola yang sama pada tingkat keputusan: `01-AGENT-WORKFRAME.md` §3.2 menjanjikan "semua teks UI bebas em dash" tanpa satu pun pemeriksa yang membacanya, sehingga tiga teks di layar memang masih memuatnya. Pelajarannya seragam dan pantas diingat agen berikutnya: **klaim (tentang tata letak, tentang test, tentang kepatuhan aturan) baru bernilai setelah ada yang mengukurnya atau ada cacat yang disuntikkan untuk membuktikan pengukurnya bekerja**; selebihnya ia hanya kalimat di dokumen yang terasa benar.

> **Temuan C-064 dan C-065 ditambahkan pada 2026-09-22 (sesi P-042)** sesudah user meminta sistem antislop "diterapkan dengan proper": ketidaksinkronan yang selama ini tidak terlihat menjadi terbaca begitu yang dipersoalkan adalah **berkasnya sendiri**. Keduanya menunjuk kelemahan yang sama pada pemeriksa yang sudah ada: `check-doc-links.sh` mengklasifikasikan rujukan `skills/...` sebagai `PLANNED` (jalur kode yang belum dibuat), sehingga daftar skill yang **dijanjikan** di `AGENTS.md` justru aman dari pemeriksaan selama `skills/` tidak ada (C-064), sementara salinan aturan di dokumen desain tidak pernah dibandingkan dengan sumbernya karena tidak ada yang menghubungkannya ke sana (C-065). Pelajarannya bukan "tambahkan pemeriksa", melainkan **klasifikasi yang keliru mematikan pemeriksaan**: `PLANNED` adalah izin untuk tidak ada, dan izin itu tidak boleh diberikan pada sesuatu yang sudah diklaim ada kepada pembaca. Keduanya `FIXED` di sesi yang sama, dan `scripts/check-antislop-refs.sh` menutup kelasnya — termasuk `sha256` atas berkas pihak ketiga, supaya salinan yang membusuk tertangkap seperti angka yang basi.

> **Temuan C-062 ditambahkan pada 2026-09-21 (sesi P-039)** sesudah memeriksa `README.md` dengan alat yang baru ditulis di sesi itu sendiri (`scripts/check-readme-facts.sh`): baris `| Frontend (rencana) | React 18 + TypeScript + Vite + TailwindCSS | Belum diinisialisasi |` di §2 masih menyebut versi **salah** dan status **salah** — React 18 padahal `frontend/package.json` memuat React 19, dan "Belum diinisialisasi" padahal `frontend/` sudah berisi 51 berkas sejak P-037 (§1 di berkas yang sama bahkan sudah menyebutnya "Kerangka selesai"). Ini temuan yang **tidak dapat ditangkap pembaca manusia dengan cepat** karena README memuat versi yang benar di enam tempat lain: yang basi hanya satu baris tabel, dan baris itu ada di bab "Teknologi" yang justru dibaca orang yang menilai proyek. Ketahuan bukan dengan membaca ulang, melainkan karena pemeriksa barunya membandingkan setiap penyebutan versi di README dengan `package.json` dan **gagal pada percobaan pertama** — bukti bahwa aturan lama ("sapu semua pemuat klaim setelah dokumen berubah", C-061) tidak cukup: yang dibutuhkan adalah pemeriksa yang berjalan tanpa diingat. `FIXED` di sesi yang sama, dan barisnya kini di bawah pemeriksaan mesin.
>
> **Temuan C-061 ditambahkan pada 2026-09-21 (sesi P-038)** saat menambahkan bagian kontribusi ke `README.md`: empat tempat di berkas itu masih menyebut `frontend/` kosong dan `DESIGN.md` placeholder, plus dua klaim angka yang juga tertinggal — baris status `POST /auth/refresh`/`change-password` masih "Belum ada" padahal keduanya hidup sejak P-034, dan jumlah route ditulis **30** (1 health, 3 auth) padahal router memuat **32** (1 health, 5 auth). Akarnya sama seperti C-060 — P-037 menyelaraskan sembilan dokumen desain tetapi **tidak menyentuh README**, karena README lahir pada P-031 dan tidak terdaftar di daftar dokumen yang diperiksa saat itu. Ditemukan oleh `grep` atas klaim basi, bukan oleh ingatan; `FIXED` di sesi yang sama. Pelajaran yang dicatat sebagai aturan: **pembersihan dokumen harus menyapu seluruh berkas yang memuat klaim yang sama**, bukan berkas yang paling sering dibuka.
>
> **Temuan C-060 ditambahkan pada 2026-09-21 (sesi P-037)** saat menutup **C-015**: begitu arah desain benar-benar ada, `51-UX.md` §3/§4 ternyata masih memuat palet biru-slate dan `H1 28px` dari sebelum keputusan itu. Ditemukan bukan dengan membaca dokumen baru, melainkan dengan **membandingkan** dokumen lama terhadap `DESIGN.md` yang baru terisi — dan hanya ketahuan karena sesi itu memang menyentuh dokumen yang bersangkutan. `FIXED` di sesi yang sama, bersama penunjuk ke `frontend/src/styles/tokens.css` dan test kontrasnya yang membuat penyimpangan berikutnya **gagal di mesin**, bukan di mata reviewer.
>
> **Temuan C-056 ditambahkan pada 2026-09-19 (sesi P-030)** dari memeriksa **database test** sesudah `make test`, bukan dari membaca dokumen: `projectHTTPFixture` login lewat HTTP dan tidak membersihkan `login_attempts`, sehingga 220 baris telemetri menumpuk dengan `user_id` yang sudah di-NULL-kan `ON DELETE SET NULL`. Ia tanda bahwa klaim "database test kosong di antara sesi" (`70-TESTING.md` §8.1) belum pernah diperiksa test mana pun — dan itulah alasan temuan ini dicatat meski satu barisnya sudah ditambal: klaim tanpa pemeriksaan akan terulang pada tabel berikutnya.

> Ringkasan: **82 temuan** (C-001 .. C-082; nomor disisipkan sesuai urutan, tidak pernah dipakai ulang). **80 FIXED** (C-001, C-002, C-003, C-004, C-005, C-006, C-007, C-008, **C-009**, C-010, C-011, C-012, C-013, C-014, **C-015**, C-016, C-017, C-018, C-019, C-020, C-021, C-022, C-023, C-024, C-025, C-026, C-027, C-028, C-030, C-031, C-032, **C-033**, C-034, **C-035**, C-036, C-037, C-038, C-039, C-040, C-041, C-042, C-043, C-044, C-045, C-046, C-047, **C-048**, C-049, C-051, **C-052**, **C-053**, **C-054**, **C-055**, **C-056**, **C-057**, C-058, C-059, **C-060**, **C-061**, **C-062**, **C-064**, **C-065**, **C-066**, **C-067**, **C-068**, **C-069**, **C-070**, **C-071**, **C-072**, **C-073**, **C-074**, **C-075**, **C-076**, **C-077**, **C-078**, **C-079**, **C-080**, **C-081**, **C-082**), **0 APPROVED**, **2 OPEN** (C-050 — threading tidak didukung skema, Q-019; **C-063** — `50-FSD.md` §3.2 menuntut pemilih `Owner` tanpa endpoint daftar pengguna, Q-024). 80 + 0 + 2 = 82, jadi hitungannya dapat diperiksa silang (`grep -cE '^\| C-'` → 82 baris; `grep -cE '^\| C-[0-9]+ \| \*\*FIXED\*\*'` → 80; `^\| C-[0-9]+ \| \*\*OPEN\*\*'` → 2). Ini adalah **hitungan per-paragraf**; angka yang mengikat tetap marker `audit-summary` di §1, karena paragraf ini sendiri pernah salah (**C-044**, **C-055**). `C-015` keluar dari kolom `OPEN` pada P-037 setelah user menjawab Q-001/Q-002; yang tersisa hanyalah `C-050` (keputusan produk: threading), sehingga **setiap temuan bersifat teknis kini sudah tertutup** — tidak ada lagi temuan yang menunggu keputusan agen maupun riset. Angka ini diambil dari tabel di atas, bukan diturunkan dari angka sesi sebelumnya — kesalahan jenis itu pernah terjadi (**C-044** dan **C-055**), dan P-030 memeriksanya silang dengan `grep -cE "^\| C-"` atas tabel ini. Riwayat perbaikan: `docs/progress/prompts/P-008-2026-09-18-perbaikan-audit-c001-c003.md`, `P-009-2026-09-18-matriks-permission-rbac.md`, `P-010-2026-09-18-sumber-tunggal-konfigurasi-runtime.md`, `P-011-2026-09-18-rekonsiliasi-daftar-endpoint.md`, `P-012-2026-09-18-kontrak-endpoint-requirement-tanpa-endpoint.md`, `P-013-2026-09-18-optimistic-locking-workflow-instance.md`, `P-014-2026-09-18-request-revision-rollback-step-sebelumnya.md`, `P-015-2026-09-18-spec-fsd-halaman-tanpa-spec.md`, `P-016-2026-09-18-format-nomor-dokumen.md`, `P-017-2026-09-18-kontrak-endpoint-resubmit.md`, `P-018-2026-09-18-phase-0-toolchain-dan-skeleton-backend.md`, `P-019-2026-09-18-trigger-append-only-audit-log.md`, `P-020-2026-09-18-migrasi-001-009-dan-bootstrap-admin.md`, `P-021-2026-09-18-auth-login-jwt-rbac-dan-revokasi-token.md`, `P-022-2026-09-19-modul-project-dan-cakupan-data.md` (sesi itu menambahkan C-038), `P-023-2026-09-19-modul-document-unggah-versi-dan-penomoran.md`, `P-024-2026-09-19-database-test-terpisah-bwdcs-test.md` (sesi itu menutup C-038, C-043, dan C-044), `P-025-2026-09-19-modul-task-dan-cakupan-baris-kedua.md` (sesi itu menambahkan C-045 dan C-046), `P-026-2026-09-19-perbaikan-c045-c046-dan-riset-best-practice.md` (sesi itu menutup C-045, C-046, dan C-047; pada lanjutan sesi yang sama, sesudah user meminta pertanyaan terbuka **dijawab**, sembilan temuan yang tersisa diputuskan: C-006/C-007/C-010/C-028 `FIXED`, dan C-004/C-009/C-033/C-035 `APPROVED` lewat **ADR-0019/0020/0021/0022** dengan tugas implementasi `T-039`/`T-040`/`T-041`), dan `P-027-2026-09-19-modul-comment-dan-kepemilikan.md` (sesi itu menutup C-049 dan menambahkan C-048 serta C-050), `P-028-2026-09-19-rentang-tanggal-inklusif-q017.md`, `P-029-2026-09-19-arsip-dokumen-dan-trigger-versi.md` (sesi itu menutup **C-004** lewat `T-039`), `P-030-2026-09-19-pencabutan-sesi-dan-auto-lock.md` (sesi itu menutup **C-009**, **C-033**, dan **C-035** lewat `T-040`/`T-041`, serta menambahkan **C-052**, **C-053**, **C-054**, **C-055**, dan **C-056**), dan `P-034-2026-09-20-change-password-dan-status-refresh.md` (sesi itu menutup **C-057** lewat hitung ulang angka test, dan memisahkan `POST /auth/refresh` ke `T-045` karena bentuk tokennya belum diputuskan), `P-043-2026-09-22-antislop-frontend-dan-laci-menu.md` (sesi itu menutup **C-067**, **C-068**, dan **C-069**), `P-044-2026-09-22-halaman-documents-dan-lapisan-data.md` (sesi itu membangun halaman **Documents** dan menutup **C-070** serta **C-071**), `P-045-2026-09-22-mime-berkas-dan-ledger-p044.md` (sesi itu menutup **C-072**), dan `P-048-2026-09-23-modul-workflow-dan-probe-46-pass.md` (sesi itu membangun **modul Workflow** — sembilan endpoint `42-API.md` §5 — dan menambahkan **C-073**, **C-074**, serta **C-075**), dan `P-050-2026-09-23-sapuan-per-lebar-setiap-halaman.md` (sesi itu memperluas sapuan tata letak ke **setiap halaman × setiap lebar** dan menambahkan **C-076**, **C-077**, serta **C-078**), dan `P-051-2026-09-23-pemeriksa-batas-sidebar.md` (sesi itu membuat penegak mesin untuk aturan sidebar `51-UX.md` §2.1 dan menambahkan **C-079**, **C-080**, **C-081**, serta **C-082**) — semua di `docs/progress/prompts/`.
