# STATE — Kondisi Proyek Saat Ini

**Terakhir diperbarui:** 2026-09-19 (P-026)
**Diperbarui oleh:** agen perbaikan gap + riset best practice (P-026): **C-045** ditutup (`bindJSON` kini menamai field yang bermasalah untuk UUID/waktu/tipe/JSON rusak di body — berlaku lintas modul, terbukti pada `PATCH /tasks/:id` dan `POST /projects`), **C-046** ditutup (penyaring rentang `?due_from=`/`?due_to=` sebagai interval **setengah terbuka** ber-batas RFC 3339, melengkapi kelima penyaring `50-FSD.md` §6.1), dan **C-047** ditemukan sekaligus ditutup (tabel status fase `80-ROADMAP.md` tertinggal jauh; label fase modul Task diselaraskan ke Phase 3). Riset best practice untuk sembilan temuan yang masih OPEN dicatat di `OPEN-QUESTIONS.md` §3 beserta rekomendasi per temuan, dan urutan fase berikutnya menjadi **Q-018**. **Log sesi sebelumnya** (P-025): agen `T-038` — modul **Task** (5 endpoint `42-API.md` §6) dengan **cakupan baris kedua** `44-SECURITY.md` §3.1.3: baca mengikuti keanggotaan project (Contributor/Viewer) atau seluruh organisasi (Manager/Administrator), tulis hanya task milik Contributor; penanda overdue tetap turunan ADR-0012 dan kini juga penyaring `?overdue=` di database. Dua temuan baru dicatat (**C-045** pesan `422` yang menyesatkan untuk UUID tidak sah di body, **C-046** penyaring rentang tanggal di FSD tanpa kontrak) dan sebelas kontrak modul task diputuskan di **Q-017**. **Log sesi sebelumnya** (P-024): agen `T-036` — **database test terpisah `bwdcs_test`** (menutup C-038): `make test` menurunkan `TEST_DATABASE_URL` dari `.env`, menolak DSN yang menunjuk database dev, dan suite hijau sementara database dev berisi project nyata; dua temuan baru (C-043 salinan Makefile di `60-DEPLOYMENT.md` §3.1, C-044 hitungan audit 31 vs 32) ditutup di sesi yang sama. **Log sesi sebelumnya** (P-023): agen `T-037` — modul **Document** (7 endpoint `42-API.md` §4: metadata, unggah versi, daftar versi, unduh ber-audit, hapus berkaskade) dengan generator `document_number` `{PROJECT_CODE}-{NNN}` **di dalam transaksi** (ADR-0017) dan cakupan data yang sama dengan project (`44-SECURITY.md` §3.1.3), dibuktikan pada server nyata; delapan kontrak yang diputuskan agen dicatat sebagai Q-016, dan empat temuan baru (C-039..C-042) ditemukan serta ditutup di sesi yang sama; **P-024** menutup C-038 (database test terpisah) beserta C-043/C-044
**Titik masuk resume:** `CONTINUE.md` (root repo). Snapshot §0 di file itu harus selaras dengan dokumen ini; bila berbeda, dokumen ini yang berlaku.

> Agen WAJIB memperbarui file ini di akhir setiap sesi. File ini menggambarkan **kondisi sekarang**, bukan riwayat. Riwayat ada di `SESSION-LOG.md`.

---

## 1. Ringkasan Posisi

| Aspek | Nilai |
|---|---|
| Fase roadmap | **Phase 0 selesai; Phase 1 berjalan; Phase 3 sebagian (Task) dikerjakan lebih awal; Phase 2 belum.** Sumber urutan fase adalah `80-ROADMAP.md` **§1**, dan §3 dokumen itu yang melaporkan status per fase — tabelnya diperbaiki pada P-026 lewat temuan **C-047** (sebelumnya masih menulis Phase 0/1 "Belum dimulai" dan melabeli Task sebagai Phase 1). Sudah hidup: `git init` + struktur repo + skeleton backend, migrasi `001`-`009` + bootstrap admin (P-020), auth + RBAC (`T-005`, P-021), **modul Project** (`T-035`, P-022), **modul Document** (`T-037`, P-023), dan **modul Task** (`T-038`, P-025 — Phase 3, dikerjakan lebih awal): **20 endpoint** dengan cakupan data di kueri (task memakai **dua baris** cakupan §3.1.3 sekaligus). Urutan berikutnya dicatat di `OPEN-QUESTIONS.md` **Q-018** (asumsi: **Comment** lebih dulu untuk menutup Phase 3, lalu **Workflow** Phase 2). Koreksi rujukan lama: `T-006`/`T-007` adalah task UI/`DESIGN.md`, **bukan** modul backend — nomor task modul backend adalah `T-035`/`T-037`/`T-038` |
| Kode aplikasi | **Ada dan berjalan, auth lengkap.** `backend/` (module Go `bwdcs/backend`): `internal/config`, `internal/pkg/{filestorage,response,jwt}`, `internal/model`, `internal/dto`, `internal/repository` (user, `token_revocations`, `system_settings`), `internal/service` (auth, `PermissionChecker`, `AuditService`, `LoginGuard`), `internal/middleware` (Auth, RequirePermission, RateLimit, CorrelationID, Logger, CORS), `internal/handler` (health, auth, `router.go`), `internal/migration` (ADR-0018), `internal/bootstrap` (ADR-0010), `cmd/server/main.go`, `Makefile`. Endpoint hidup: `GET /health`, `POST /api/v1/auth/login`, `POST /api/v1/auth/logout`, `GET /api/v1/auth/me`, dan **delapan endpoint modul project** (`GET|POST /api/v1/projects`, `GET|PATCH /api/v1/projects/:id`, `POST /api/v1/projects/:id/archive`, `GET|POST /api/v1/projects/:id/members`, `DELETE /api/v1/projects/:id/members/:userId`) — P-022. Tambahan paket: `internal/service/project_service.go`, `internal/repository/project_repository.go`, `internal/model/project.go`, `internal/dto/project_dto.go`, `internal/handler/project_handler.go`. **Modul document hidup sejak P-023** (`T-037`): tujuh endpoint `42-API.md` §4 (`GET|POST /api/v1/documents`, `GET|DELETE /api/v1/documents/:id`, `GET /:id/versions`, `POST /:id/upload`, `GET /:id/download/:versionId`) dengan paket `internal/service/{document_service,document_service_upload,scope}.go`, `internal/repository/document_repository.go`, `internal/model/document.go`, `internal/dto/document_dto.go`, `internal/handler/document_handler.go`; berkas disimpan lewat `internal/pkg/filestorage` (ADR-0005). **Modul task hidup sejak P-025** (`T-038`): lima endpoint `42-API.md` §6 (`GET|POST /api/v1/tasks`, `GET|PATCH /api/v1/tasks/:id`, `POST /api/v1/tasks/:id/complete`) dengan `internal/service/task_service.go`, `internal/repository/task_repository.go`, `internal/model/task.go`, `internal/dto/task_dto.go`, `internal/handler/task_handler.go`. Belum ada: modul comment, workflow, notification, audit, report, admin, `POST /auth/refresh` + `change-password` (menunggu Q-013), frontend |
| Skema database | **Terpasang pada `bwdcs` (PostgreSQL 16.10, port 5432).** Versi skema goose **9**; **22 tabel** (21 tabel `41-DATABASE.md` §2 + `goose_db_version`), 104 baris `role_permissions` (44/30/18/12), 2 trigger append-only `audit_logs`, FK `fk_documents_workflow_instance`, 5 baris `system_settings`. Dijalankan otomatis saat startup dari berkas yang di-embed (ADR-0018) |
| Blocker utama | **UI**: arah desain (Q-002) dan mode antislop (Q-001). Backend tidak terblokir: modul Phase 1 dapat langsung dikerjakan. Dua keputusan backend bersifat non-blocking: Q-013 (mekanisme pencabutan sesi → `logout_all`/`change-password`/`refresh`) dan Q-014 (audit login gagal) |
| Audit terbuka | `docs/progress/audits/AUDIT-001-2026-09-17-kontradiksi-dokumen.md` — **47 temuan: 42 FIXED, 4 APPROVED, 1 OPEN** (42 + 4 + 1 = 47, angka diambil dari tabel tindak lanjut, bukan dari angka sesi sebelumnya). **`APPROVED` berarti keputusan sudah ada dan dokumen sudah selaras, kode belum** — jangan dibaca sebagai selesai. Yang `APPROVED`: **C-004** (ADR-0019 → `T-039`), **C-033** (ADR-0021 → `T-040`), **C-009** & **C-035** (ADR-0022 → `T-041`). Yang masih `OPEN`: **C-015** saja — arah desain `DESIGN.md`, milik user (Q-002). Diputuskan pada P-026 dengan dasar riset best practice di `OPEN-QUESTIONS.md` §3: C-006 (Reviewer = peran fungsional), C-007 (dua hierarki role, tidak digabung), C-010 (`responsible_user_id` ditunda; MVP role-based), C-028 (retensi audit = operasi pemeliharaan, ADR-0020) — keempatnya `FIXED`. FIXED lainnya: C-001..C-003, C-019 (P-008), C-017, C-008 (P-009) lewat ADR-0011..0014, C-014 (P-010), C-011, C-013 (P-011), C-012 (P-012), C-005, C-021, C-023 (P-013) lewat ADR-0015, C-022 (P-014) lewat ADR-0016, C-018 (P-015), C-016 lewat ADR-0017 dan C-024 (P-016), C-025 (P-017), C-026 + C-027 (P-018), C-020 (P-019), C-029 + C-030 + C-031 dan C-032 lewat ADR-0018 (P-020), C-034 + C-036 + C-037 (P-021), C-039 + C-040 + C-041 + C-042 (P-023), C-038 + C-043 + C-044 (P-024), C-045 + C-046 + C-047 (P-026). Ditemukan saat mengerjakan: C-021..C-023 (C-005), C-024 (C-016), C-025 (`T-028`), C-026/C-027 (`T-003`), C-028 (C-020), C-029..C-032 (`T-004`), C-033..C-037 (`T-005`), **C-038** (`T-035` — test memakai database dev yang sama, gagal begitu ada project nyata), **C-039..C-042** (`T-037` — muncul dari **menjalankan** test dan dari memeriksa sumber sebelum menerapkan aturan), **C-045**/**C-046** (`T-038` — dari uji HTTP nyata dan dari mencocokkan FSD dengan kontrak), **C-047** (P-026 — dari memeriksa ulang ledger terhadap dokumen desain) |
| Konvensi yang sudah terkunci sebelum kode pertama | ADR-0011 (audit ditulis di service, dalam satu transaksi) · ADR-0012 (kolom `status` hanya nilai kanonik; "overdue" turunan) · ADR-0013 (`40-TSD.md` §2.0 = sumber tunggal struktur backend, flat, satu folder = satu package) · ADR-0014 (`44-SECURITY.md` §3.1 = sumber tunggal matriks permission, seed `008` = 104 baris) · ADR-0015 (transisi `workflow_instances` hanya lewat conditional `UPDATE` dengan `version` + `status = 'running'` + `current_step`; `rowsAffected = 0` = rollback + `409 WORKFLOW_CONFLICT`, tanpa retry) · ADR-0016 (`request_revision` kembali ke step sebelumnya, batas bawah step 1; instance tetap `running`; re-submit melanjutkan instance yang sama) · ADR-0017 (`document_number` selalu dibangkitkan server `{PROJECT_CODE}-{NNN}`, atomik di `document_sequences`, immutable, tanpa penomoran manual; `projects.code` permanen) · append-only `audit_logs` (`44-SECURITY.md` §6, dipasang migrasi `007`): trigger menolak `UPDATE`/`DELETE`/`TRUNCATE` dengan SQLSTATE `23001`, hanya `INSERT` yang lolos, dengan satu jalur pemeliharaan eksplisit `bwdcs.audit_maintenance` (`SET LOCAL`, per transaksi). Trigger itu **hanya** untuk `audit_logs` — `document_versions` sengaja tidak diberi trigger serupa karena `DELETE /documents/:id` cascade to versions (temuan C-004 masih OPEN) · ADR-0018 (migrasi di-embed dan dijalankan aplikasi saat startup oleh package kecil di `internal/migration`; goose library **dan** CLI dipin v3.24.1; berkas `.sql` tetap sumber kebenaran skema) |
| Skema workflow | `workflow_instances` memuat `version INTEGER NOT NULL DEFAULT 0` dan `current_step_deadline TIMESTAMP WITH TIME ZONE` (keduanya masuk migrasi `005_create_workflows.sql`, ADR-0015). Overdue step tetap turunan dari `current_step_deadline`, tidak pernah menjadi nilai `status` |
| Skema dokumen | `documents.document_number` dibangkitkan server `{PROJECT_CODE}-{NNN}` lewat tabel `document_sequences` (keduanya masuk migrasi `004_create_documents.sql`, ADR-0017); nomor immutable, tidak pernah dipakai ulang, dan `projects.code` tidak dapat diubah setelah dibuat. **Skema ini kini dipakai penuh sejak P-023**: `documents` (status `draft` → lima nilai kanonik), `document_versions` (versi `major.minor`, checksum SHA-256 64 heksadesimal, `file_key` ADR-0005), dan `document_categories` (hanya dirujuk; belum ada endpoint kategori) |
| Atribusi error body | `bindJSON` (`internal/handler/project_handler.go`, dipakai **semua** modul) membedakan tiga sebab dan selalu menamai field: tipe salah (`json.UnmarshalTypeError`), JSON rusak (`json.SyntaxError`/`json.Valid` → `body`), JSON sah dengan nilai yang tidak dapat diurai (field dicari lewat `undecodableField`/reflection, pesan dari bentuk tipe: "harus UUID yang sah", "harus waktu RFC 3339 yang sah", "harus tanggal yang sah"). Kontrak pemetaannya di `42-API.md` §12; sebelumnya ketiganya dijawab "`body` / harus JSON objek yang sah" (temuan **C-045**, FIXED P-026) |
| Penyaring rentang tanggal | `?due_from=`/`?due_to=` pada `GET /tasks`: interval **setengah terbuka** `[due_from, due_to)` ber-batas RFC 3339 (offset eksplisit), karena `+` sampai ke handler sebagai spasi maka `parseRFC3339Query` menerima `+07:00` maupun `%2B07:00`; rentang terbalik/berdiri di satu titik → `422` pada `due_to`; task tanpa `due_date` tidak masuk rentang (C-046, FIXED P-026) |
| Cakupan data | Dua penyusun cakupan di satu tempat, `internal/service/scope.go`. **(a)** `systemScope` → `repository.ProjectScope`, dipakai project **dan** document lewat `projectScopePredicate` di `WHERE`. **(b)** `taskScope` → `repository.TaskScope`, dipakai `taskReadPredicate`/`taskWritePredicate` — modul pertama yang memakai **dua baris** §3.1.3 sekaligus (baca ≠ tulis): Contributor/Viewer membaca task miliknya dan task pada project yang diikutinya sementara Manager/Administrator seluruh organisasi, sedangkan penulisan lebih sempit (Contributor hanya task yang ditugaskan kepadanya atau dibuatnya). Seluruh pembeda role dikirim sebagai **parameter** ke kueri, bukan dirangkai ke SQL. Pelanggaran cakupan → `404`, bukan `403` (`44-SECURITY.md` §3.1.3) — termasuk kasus "boleh dibaca karena anggota project, tetapi tidak boleh diubah". Pola inilah yang wajib dipakai modul berikutnya (comment/notification) |
| Aturan versi dokumen | Versi berikutnya ditentukan server (`model.NextVersion`): `1.0` pada unggahan pertama, minor naik pada unggahan biasa, dan **major naik (`2.0`) bila `documents.status = revision_required`** — itulah wujud "major based on revision" `50-FSD.md` §4.2 tanpa input jenis versi dari klien (Q-016 butir 1). Versi lama tidak pernah ditimpa (FR-VER-03) |
| Aturan modul task | Status kanonik `open`/`in_progress`/`completed` dan prioritas `low`/`medium`/`high`/`urgent` (`model.TaskStatuses`/`TaskPriorities`; nilai dinormalkan sebelum diperiksa). Task **selalu lahir `open`** (`status` dari klien → `422`); `assignee_id` dan `due_date` wajib; `priority` kosong → default kolom `medium`. Transisi hanya tiga aksi `50-FSD.md` §6.3: Start (`open` → `in_progress`) dan Reopen (`completed` → `open`) lewat `PATCH` (`task:update`), Complete lewat **`POST /tasks/:id/complete`** (`task:complete`) dan hanya dari `in_progress` — `in_progress` → `completed` lewat `PATCH` sengaja `409`. `project_id` tidak dapat diubah (`409`). `?overdue=` tri-state (`true`/`false`/tidak dikirim) disaring di `WHERE` sebelum paginasi. Audit: `TASK_CREATED` (assignee awal di metadata), `TASK_UPDATED`, `TASK_ASSIGNED` (hanya bila assignee berubah), `TASK_COMPLETED` — semuanya di transaksi yang sama (ADR-0011). Rincian: `42-API.md` §6 |
| Task aktif | Sesi P-026 selesai: **perbaikan gap** (C-045 + C-046 + C-047 yang ditemukan saat memeriksa ulang) **dan sembilan temuan lama diputuskan** — C-006/C-007/C-010/C-028 `FIXED`, C-004/C-009/C-033/C-035 `APPROVED` lewat **ADR-0019/0020/0021/0022** dengan tugas `T-039`/`T-040`/`T-041`. Sebelumnya P-025: **`T-038` DONE** (modul task; C-045/C-046 dicatat, Q-017). Sebelumnya P-024: **`T-036` DONE** (database test `bwdcs_test`; C-038 + C-043 + C-044). Sebelumnya P-023: `T-037` (document) + C-039..C-042; P-022: `T-035` (project); P-021: `T-005` + `T-033`; P-020: `T-004` + ADR-0018. Berikutnya: **`T-039`/`T-040`/`T-041`** (implementasi tiga ADR — inilah satu-satunya pekerjaan yang tertunda karena kode, bukan karena keputusan), lalu **Comment** (Q-018), dengan selipan **`T-024`** (anotasi izin endpoint, kini **40/51**). |
| Kontrak API | **`42-API.md` adalah sumber tunggal daftar endpoint** (51 endpoint, 13 bab — termasuk **§6 Tasks yang ditulis penuh pada P-025**: izin per endpoint, tabel transisi status, penyaring, dan kode error; termasuk daftar `GET /workflows/instances` untuk halaman Approvals dan `POST /workflows/instances/:id/resubmit` untuk melanjutkan review setelah revisi). `40-TSD.md` §6 hanya contoh pemasangan route. Definisi workflow hanya di `/workflows/definitions` (izin `workflow_definition:manage`), bukan prefiks `/admin` (temuan C-011/C-013). Bab: §9 Audit, **§10 Reports**, §11 Administration, §12 Error Responses, §13 Swagger |
| Konfigurasi runtime | **Satu sumber:** kontrak di `60-DEPLOYMENT.md` §2/§2.1, cermin yang dapat dieksekusi `.env.example`, compose di `docker-compose.yml`. Tiga dokumen (`90-AGENT-GUIDE` §7, `30-ARCHITECTURE` §5.1, `12-DEVELOPMENT-WORKFLOW` §7) hanya menunjuk — tidak ada daftar variabel atau YAML compose yang disalin (temuan C-014). Sejak P-024 **tidak ada lagi salinan `Makefile` di dokumen**: `60-DEPLOYMENT.md` §3.1 menunjuk `backend/Makefile` sebagai sumber tunggal (temuan C-043 / kelas C-014) |
| Konfigurasi test | **Database test terpisah `bwdcs_test`** (owner role `bwdcs`, dibuat P-024 dengan izin user; role aplikasi tidak diberi `CREATEDB`). `TEST_DATABASE_URL` **diturunkan** `backend/Makefile` dari kredensial `.env` (`TEST_DB_NAME` mengubah nama), dijalankan `-count=1`, dan `make test` **menolak** DSN yang menunjuk database dev. Skema database test dimigrasikan otomatis oleh `TestMain` (ADR-0018). Kontrak: `70-TESTING.md` §8/§8.1 |
| Next action | **`T-039`** (arsip dokumen, ADR-0019) → **`T-040`** (pencabutan sesi, ADR-0021) → **`T-041`** (`login_attempts` + auto-lock, ADR-0022). Ketiganya **sudah diputuskan**; yang tersisa hanya kode + test, dan test-nya sudah ditulis lebih dulu di `70-TESTING.md` **§3.12** supaya tidak dikarang mengikuti implementasi. Ketiganya menyentuh migrasi `010` yang sama (`41-DATABASE.md` §4), jadi urutannya boleh digabung dalam satu sesi. Setelah itu **Comment** (`50-FSD.md` §7, `42-API.md` §7) untuk menutup Phase 3 (asumsi Q-018), lalu **Workflow** (`43-WORKFLOW.md`). Selipan murah: `T-024` (anotasi izin, **40/51** endpoint). Test: cukup `cd backend && make test` (database test terpisah `bwdcs_test`, `-p 1`, `-count=1`) — tidak perlu membersihkan database dev. Keputusan yang menunggu Anda tinggal **satu**: **C-015/Q-002** (arah desain `DESIGN.md`). Q-015/Q-016/Q-017 hanya menunggu konfirmasi atas keputusan agen yang sudah berjalan |

## 2. Environment & Tooling (hasil T-001, 2026-09-17)

| Item | Persyaratan | Hasil | Status |
|---|---|---|---|
| OS / arsitektur | — | Darwin x86_64 (Intel Mac), disk 107 GiB bebas | Cukup |
| Go | 1.22+ | 1.22.5 di `/usr/local/go/bin/go`; sejak P-018 masuk `PATH` dari `~/.zshrc` (blok `BWDCS toolchain`) | **OK** (`T-011` DONE) |
| Node.js | 20+ | v26.7.0 | OK |
| npm | — | 11.19.0 | OK |
| PostgreSQL server | 16+ | **16.10 (Postgres.app) berjalan di `localhost:5432`**, data dir `~/Library/Application Support/Postgres/var-16`. Dipakai bersama proyek lain (`finmo`, `glid_gateway`, `restaurant`, `wms`); sejak P-018 ada **role dan database `bwdcs`** di instance yang sama (0 tabel publik — menunggu migrasi `T-004`) | **OK** (`T-013` DONE). Instalasi terpisah di `5433` tidak dipakai |
| PostgreSQL client | 16+ | Client 16 dari Postgres.app masuk `PATH` lewat `~/.zshrc`; instalasi Homebrew 14.6 tidak diubah dan tidak dihapus | **OK** (`T-011` DONE) |
| Database test | terpisah dari database aplikasi | **`bwdcs_test`** di instance yang sama (owner role `bwdcs`; dibuat P-024 lewat peran superuser lokal karena role aplikasi **tidak** diberi `CREATEDB`). 22 tabel, `role_permissions` 104, versi skema goose **9**, dan kosong dari data uji di antara sesi | **OK** (`T-036` DONE, C-038 tertutup) |
| Perintah test kanonik | — | `cd backend && make test` (menurunkan `TEST_DATABASE_URL` → `bwdcs_test`, `-p 1`, `-count=1`, menolak DSN database dev). `make test-dsn` mencetak target database dengan sandi disamarkan. `go test` telanjang **bukan** bukti: tanpa `TEST_DATABASE_URL` test integrasi memanggil `t.Skip` | **OK** (`70-TESTING.md` §8/§8.1) |
| Bukti runtime modul (server dev) | — | Jalankan server **di dalam command yang sama** dengan probe HTTP-nya: runner perintah dapat mengambil alih grup proses sehingga server `nohup` mati begitu command selesai (terjadi pada P-025, `health` 200 lalu SIGTERM). Job `launchctl submit` juga tidak dapat membaca berkas di `~/Documents` pada mesin ini (proses berhenti di `dyld: open` dan tidak pernah mendengarkan port). **Bangun ulang binari sebelum membuktikan perubahan kode** — P-025 sempat membaca "penyaring tidak bekerja" dari binari lama; bila hasil tampak mustahil, curigai binari basi lebih dulu |
| Formulasi Homebrew PostgreSQL (tidak dipakai) | — | `postgresql` dan `postgresql@14` 14.6 terpasang di `/usr/local/Cellar`, binary server-nya rusak (`libicu4c 71` hilang, yang ada `icu4c@78`) | Tidak dipakai; **jangan diandalkan** |
| goose (migrasi) | terbaru | **v3.24.1** di `~/go/bin/goose` — **dipin** ke versi yang sama dengan library yang dipakai aplikasi saat startup (ADR-0018 butir 3), karena v3.24.2+ menuntut Go ≥ 1.23 dan v3.28.0 menuntut 1.26. Pemasangan awal (P-018) memakai v3.28.0 lalu diturunkan pada P-020. `~/go/bin` juga berisi `dlv`, `gopls`, `migrate`, `staticcheck` | **OK** (`T-012` DONE, pin P-020) |
| Docker CLI + Compose | opsional | 24.0.2 / v2.19.1, daemon tidak berjalan | **Tidak diperlukan** karena PostgreSQL 16 sudah ada (`T-014` jadi opsional) |
| git | terbaru | 2.39.5; repo **sudah diinisialisasi** di branch `main`, `.gitignore` + `.editorconfig` aktif, `.env` terabaikan | **OK** (`T-002`/`T-002a` DONE) |
| Go module & ketergantungan | sesuai ADR-0008 | Module `bwdcs/backend`, `go 1.22.5`; gin v1.10.0, `jackc/pgx/v5` **v5.7.4**, `pressly/goose/v3` **v3.24.1**, viper v1.19.0, `golang.org/x/crypto` v0.31.0 (bcrypt), **`golang-jwt/jwt/v5` v5.3.1** dan **`google/uuid` v1.6.0** (ditambahkan P-021; keduanya tidak menaikkan direktif `go`). Ketiga pin terakhir menjaga toolchain 1.22.5: pgx v5.7.5, goose v3.24.2+, dan seterusnya menuntut Go ≥ 1.23. Perintah penyiapan memakai `GOTOOLCHAIN=local` agar tidak menarik toolchain lain | **OK**. Kenaikan toolchain harus dicatat di tabel ini sebelum pin dilepas — naikkan library **dan** CLI goose bersamaan (ADR-0018) |
| make | — | GNU Make 3.81 | OK |
| bash + `scripts/check-doc-links.sh` | — | Jalan; kriteria lulus hanya jumlah `BROKEN` = 0 (jumlah `PLANNED` berubah seiring dokumen bertambah, jangan dicek manual) | OK |
| docker compose (validasi konfigurasi) | — | `docker compose config -q` jalan tanpa daemon; `docker compose up` **belum pernah** dijalankan | Sebagian |

### Porta yang sudah terpakai di mesin ini

| Port | Pemakai | Konsekuensi |
|---|---|---|
| `8080` | `wms-backend` (proyek lain) | Backend BWDCS dev memakai **8081** (dicatat di `12-DEVELOPMENT-WORKFLOW.md` §7.1) |
| `5432` | PostgreSQL 16.10 (Postgres.app) | BWDCS memakai port ini dengan **database sendiri (`bwdcs`)**, bukan server baru |
| `5433` | bebas | Cadangan bila kelak butuh cluster terpisah |
| `5173` | bebas | Aman untuk Vite |

## 3. Modul (Status Implementasi)

| Modul | Dokumen Desain | Implementasi | Test | Catatan |
|---|---|---|---|---|
| Migrasi & skema | `41-DATABASE.md` §2/§4 — ADR-0003/ADR-0018 | **Selesai untuk `001`-`009`**: 9 berkas `.sql` + runner embed (`internal/migration/migration.go`) | `migration_test.go` (13 test: kelengkapan 21 tabel, FK tertunda C-029, seed 44/30/18/12, kosakata tertutup, spot check, tanpa duplikat, `system_settings`) | Terpasang di `bwdcs` versi 9; `down-to 0` + `up` terbukti bersih |
| **Project** | `50-FSD.md` §3, `42-API.md` §3, `44-SECURITY.md` §3.1.3 — ADR-0014/ADR-0017 | **Selesai** (`T-035`, P-022): `internal/model/project.go`, `internal/repository/project_repository.go` (cakupan di `WHERE`), `internal/service/project_service.go` (transaksi + audit), `internal/dto/project_dto.go`, `internal/handler/project_handler.go`, 8 route di `router.go` | `internal/service/project_service_test.go` (14), `internal/handler/project_handler_test.go` (10, termasuk 401 kedelapan endpoint) | Cakupan: anggota project (non-admin) / seluruh organisasi (admin) / tetap satu tenant. Invariant: owner selalu anggota & tidak dapat dihapus, `code` permanen, arsip = status. Audit 5 aksi. Tiga kontrak diputuskan agen → Q-015 |
| **Document & Version** | `50-FSD.md` §4, `42-API.md` §4, `44-SECURITY.md` §3.1.3/§4.2 — ADR-0017/ADR-0005 | **Selesai** (`T-037`, P-023): `internal/model/document.go` (status kanonik + `NextVersion`), `internal/repository/document_repository.go` (cakupan di `WHERE`, `NextNumber` atomik, versi, `HasRunningWorkflow`), `internal/service/document_service.go` + `document_service_upload.go` (transaksi + audit, checksum, `limitedReader`), `internal/dto/document_dto.go`, `internal/handler/document_handler.go` (multipart + streaming unduhan), 7 route di `router.go` | `document_number_test.go` (5 test penomoran), `document_service_test.go` (9), `document_upload_limit_internal_test.go` (unit batas), `document_handler_test.go` (11, termasuk 401 ketujuh endpoint) | Versi: `1.0` → minor pada unggahan biasa → **major** sesudah `revision_required`. Unduhan diaudit di transaksi singkat tersendiri. Hapus = kaskade baris + berkas, ditolak `409` bila workflow `running`. Delapan kontrak agen → Q-016 |
| Bootstrap data awal | `docs/adr/0010-first-run-bootstrap.md`, `41-DATABASE.md` §4.1 | **Ada** (`internal/bootstrap/bootstrap.go`) | `bootstrap_test.go` (10 test: validasi password lemah/contoh, org+admin+role, cost 12, idempotent, pesan bila seed 008 hilang, dilewati saat sudah ada user) | Membuat organisasi + admin pertama hanya bila `users` kosong; password tidak pernah masuk log |
| Config & logging | `40-TSD.md` §2.1 | **Skeleton** (`internal/config`, slog JSON, `.env` opsional, pesan gagal jelas) | `config_test.go`, `envfile_test.go` (84.1%, hermetis sejak `T-033`) | `GET /health` memeriksa database + storage (`T-003`) |
| Auth & RBAC | `40-TSD.md` §5.2/§5.2.1/§5.2.2, `44-SECURITY.md` §2/§3.1/§3.2, ADR-0009/0014 | **Selesai untuk lingkup `T-005`**: `internal/pkg/jwt` (HS256 + `jti` wajib), `internal/service/auth_service.go` (login, logout, profil), `login_guard.go` (FR-AUTH-06), `permission_checker.go` (matriks tanpa bypass), `internal/repository/{user,token_revocation,setting}_repository.go`, `internal/middleware/*` (Auth, RequirePermission, RateLimit, CorrelationID, Logger, CORS), `internal/handler/auth_handler.go` + `router.go`. **Belum:** `POST /auth/refresh`, `change-password`, `logout_all` (Q-013) | jwt 9 test · middleware 10 · service 19 (termasuk RBAC `70-TESTING.md` §4.1) · handler 9 (end-to-end HTTP) | Login admin pertama `admin`/`ADMIN_PASSWORD` → 200 + 44 izin; logout → token ditolak `401 TOKEN_REVOKED` |
| Organization & User | `41-DATABASE.md` | Belum | Belum | |
| Workflow engine | `43-WORKFLOW.md` | Belum | Belum | |
| **Task** | `50-FSD.md` §6, `42-API.md` §6, `44-SECURITY.md` §3.1.3 — ADR-0012/ADR-0014 | **Selesai** (`T-038`, P-025; **Phase 3 dikerjakan lebih awal**): `internal/model/task.go`, `internal/repository/task_repository.go` (dua predikat cakupan di `WHERE`), `internal/service/task_service.go` + `taskScope` di `scope.go`, `internal/dto/task_dto.go`, `internal/handler/task_handler.go` (penjaga `task:assign` bergantung isi body), 5 route di `router.go` | `internal/model/task_test.go` (3), `internal/service/task_service_test.go` (12), `internal/handler/task_handler_test.go` (10 lewat HTTP) | Modul pertama dengan **baca ≠ tulis** pada `44-SECURITY.md` §3.1.3. Pindah project → `409`; `in_progress` → `completed` lewat `PATCH` → `409` (jalurnya `POST /complete`, izin terpisah). Dua temuan (C-045/C-046) ditutup P-026; sebelas kontrak → Q-017 |
| Comment | `50-FSD.md` §7 | Belum | Belum | |
| Notification | `50-FSD.md` §8 | Belum | Belum | |
| Audit trail | `44-SECURITY.md` §6, ADR-0011 | **Mulai nyata**: `internal/service/audit_service.go` menulis entri di transaksi pemanggil; dipakai login (`LOGIN`) dan logout (`LOGOUT`) | Trigger append-only (6 test) + audit terverifikasi lewat `psql` pada login/logout admin | `audit:read` hanya Administrator. Percobaan login **gagal** tidak masuk `audit_logs` — keputusan ADR-0022 (temuan C-035, `APPROVED`): jejaknya di tabel `login_attempts` yang belum ada sampai `T-041` |
| Dashboard & Report | `50-FSD.md` §9, §10 | Belum | Belum | |
| Frontend (semua halaman) | `50-FSD.md`, `51-UX.md` | Belum | Belum | Diblokir `DESIGN.md` |

## 4. Keputusan Pending (Ringkas)

Lihat detail dan opsi di `OPEN-QUESTIONS.md`.

| Kode | Pertanyaan | Dampak jika tidak dijawab |
|---|---|---|
| Q-001 | Mode antislop: during atau after? | UI work tidak boleh dimulai (antislop core) |
| Q-002 | Siapa yang mengisi arah desain di `DESIGN.md`? | Semua UI hanya boleh berstatus "draft without direction" |
| Q-003 | Skill antislop (`skills/*/SKILL.md`) belum ada di repo; user yang menyediakan | Agen tidak boleh mengunduh dari jaringan; filter hanya memakai `antislop.md` |
| ~~Q-004~~ | ~~Izin `git init` + `.gitignore` untuk T-002~~ | **RESOLVED 2026-09-18 (P-018): user memberi izin; `git init -b main` + `.gitignore` + `.editorconfig` selesai** |
| ~~Q-006~~ | ~~Strategi invalidasi token saat logout (ADR-0009)~~ | **RESOLVED 2026-09-17 (P-004): ADR-0009 `ACCEPTED` — daftar revokasi `jti` di `token_revocations`; dijalankan pada `T-005`** |
| ~~Q-007~~ | ~~Bootstrap organisasi & admin pertama~~ | **RESOLVED 2026-09-17 (P-004): ADR-0010 `ACCEPTED` — `EnsureAdminFirstRun`; dijalankan pada `T-004`** |
| Q-008 | Whitelist ekstensi `.doc/.docx/.pptx` (non-blocking, Phase 1) | Document module terbatas ke pdf/xls/gambar |
| ~~Q-009~~ | ~~Izin menyiapkan toolchain (`PATH` Go, `goose`, role+database `bwdcs`)~~ | **RESOLVED 2026-09-18 (P-018): user memberi izin; `T-011`/`T-012`/`T-013` DONE tanpa menyentuh PostgreSQL 14.6 Homebrew maupun database proyek lain** |
| ~~Q-010~~ | ~~Temuan audit mana yang diperbaiki berikutnya~~ | **RESOLVED 2026-09-19 (P-026): user meminta seluruh pertanyaan terbuka dijawab memakai best practice.** Hasil: 42 FIXED, 4 APPROVED (`T-039`/`T-040`/`T-041`), 1 OPEN (C-015 — arah desain, milik user). Tidak ada lagi temuan yang menunggu keputusan |
| **Q-017** | Kontrak modul task (11 butir): sembilan diputuskan agen saat implementasi, dua di antaranya (**penyaring rentang tanggal**, **atribusi error body**) diminta keputusan Anda | **Keduanya diputuskan & dikerjakan P-026** memakai rekomendasi (a) dengan dasar riset (§3): rentang **setengah terbuka** RFC 3339, dan `bindJSON` yang menamai field. Reversibel tanpa menyentuh skema; bila Anda ingin arah lain, sebutkan dan dikembalikan |
| **Q-018** | Urutan fase berikutnya: **Comment** (menutup Phase 3) atau kembali ke **Workflow** (Phase 2) sesuai `80-ROADMAP.md`? | Tidak memblokir — keduanya dapat dikerjakan; asumsi sementara: **Comment** lebih dulu karena murah dan menutup satu fase, sedangkan Workflow menuntut engine + notifikasi |
| ~~Q-013~~ | ~~Mekanisme pencabutan "seluruh token aktif user"~~ | **RESOLVED 2026-09-19 (P-026): `users.tokens_invalid_before` — ADR-0021 `ACCEPTED`.** Opsi `session_id` (daftar sesi) ditolak **untuk MVP** tetapi tidak dimatikan sebagai kemungkinan. Kodenya menyusul di `T-040` (menyertai `T-034`); sampai itu `logout_all` tetap `501` |
| ~~Q-014~~ | ~~Apakah login **gagal** harus masuk `audit_logs`~~ | **RESOLVED 2026-09-19 (P-026): tidak — tabel `login_attempts` — ADR-0022 `ACCEPTED`.** `actor_id` tetap `NOT NULL`; FR-AUDIT-01 "login" = login **berhasil**. Tabelnya sekaligus penghitung auto-lock C-009. Kode di `T-041` |
| **T-039/T-040/T-041** | Implementasi tiga ADR yang sudah `ACCEPTED` (arsip dokumen, pencabutan sesi, auto-lock + `login_attempts`) | Bukan pertanyaan — **pekerjaan**. Sampai ketiganya selesai, kode **sengaja** berbeda dari dokumen di tiga titik (karena itu C-004/C-033/C-009 berstatus `APPROVED`, bukan `FIXED`): `DELETE /documents/:id` masih hidup, `logout_all` masih `501`, dan auto-lock masih penghitung di memori |
| **Q-015** | Tiga kontrak modul project yang diputuskan agen (NON-BLOCKING): owner selalu anggota; pelanggaran cakupan → **404** bukan 403; batas `limit` 1–100 → 422 | Tidak ada yang terblokir — implementasi sudah berjalan dan terdokumentasi (`42-API.md` §3). Bila Anda memilih sebaliknya, koreksinya satu perubahan kecil per butir. Ditemukan saat `T-035` |
| **Q-016** | Delapan kontrak modul document yang diputuskan agen (NON-BLOCKING): aturan versi (major bila `revision_required`); daftar versi memakai `document:read`; berkas tidak sah → `422` bukan `413`; owner = pembuat; hapus = kaskade + ditolak saat workflow berjalan; `entity_id` audit = nomor dokumen; project arsip **belum** ditolak; filter kategori/owner/tanggal belum ada | Tidak ada yang terblokir. Satu butir (**project arsip menerima dokumen baru?**) benar-benar keputusan Anda; sisanya konfirmasi. Detail + alternatif yang ditolak di `OPEN-QUESTIONS.md`. Ditemukan saat `T-037` |

## 5. File Penting dari Sesi Terakhir

| File | Perubahan |
|---|---|
| `backend/Makefile` | Changed (P-024): `test` memuat `.env`, **menurunkan `TEST_DATABASE_URL`** ke `bwdcs_test` (`TEST_DB_NAME`), `-count=1`, menolak DSN database dev, mencetak database yang dipakai dengan sandi disamarkan; target baru `test-dsn` |
| `backend/internal/bootstrap/bootstrap_test.go` | Changed (P-024): pesan kegagalan menyebut penyebabnya (`TEST_DATABASE_URL` harus menunjuk database test terpisah, `70-TESTING.md` §8.1) |
| `docs/design/70-TESTING.md` §8/§8.1 | Changed (P-024): database test terpisah sebagai praktik wajib + prosedur pembuatan `bwdcs_test`; peringatan bahwa `go test` tanpa `TEST_DATABASE_URL` **bukan bukti** (test integrasi `t.Skip`) |
| `docs/design/60-DEPLOYMENT.md` §3.1 | Changed (P-024): cuplikan Makefile yang menyimpang **dihapus**, diganti tabel target + penunjuk ke `backend/Makefile` (temuan C-043) |
| `docs/design/12-DEVELOPMENT-WORKFLOW.md` §8, `90-AGENT-GUIDE.md` §7 | Changed (P-024): perintah verifikasi memakai `make test`, bukan `go test` telanjang |
| `docs/progress/audits/AUDIT-001-...md` + `audits/README.md` | Changed (P-024): C-038 FIXED, C-043 & C-044 baru (FIXED); hitungan 44 temuan / **35 FIXED** / 9 OPEN (diambil ulang dari tabel — C-044) |
| `docs/adr/0019-arsip-sebagai-default-penghapusan-dokumen.md`, `0020-retensi-audit-log-sebagai-operasi-pemeliharaan.md`, `0021-pencabutan-seluruh-sesi-tokens-invalid-before.md`, `0022-login-attempts-dan-auto-lock.md` + `docs/adr/README.md` | Added (P-026): empat keputusan `ACCEPTED` yang menjawab sembilan temuan tersisa (C-004, C-009, C-028, C-033, C-035 dan pendampingnya), masing-masing dengan konteks, alternatif yang ditolak, konsekuensi, dan dasar best practice |
| `docs/design/41-DATABASE.md` §2.1/§2.3/§3/§4 | Changed (P-026): `users.tokens_invalid_before` + `users.locked_until`, tabel `login_attempts` + indeks, `documents.archived_at` dan nilai kanonik `archived`, serta migrasi **`010`** beserta alasan mengapa perubahan ini tidak disunting ke berkas `001`-`009` yang sudah terpasang |
| `docs/design/42-API.md` §2/§4/§11/§12 | Changed (P-026): `logout_all` & pencabutan sesi (ADR-0021), lockout `423 LOCKED` + `POST /admin/users/:id/unlock` (ADR-0022), arsip dokumen menggantikan `DELETE` (ADR-0019), dan tabel `TOKEN_REVOKED` yang menyebut kedua sebab |
| `docs/design/44-SECURITY.md` §2.3/§3.1/§3.3/§6/§8 | Changed (P-026): auto-lock + `login_attempts` (C-009/C-035), dua hierarki role (C-007), izin arsip vs `document:delete` (C-004), §6.1 trigger `document_versions`, §6.2 retensi (C-028), dan checklist keamanan bertambah empat butir |
| `docs/design/50-FSD.md` §4.3/§10.5/§11.1, `20-SRS.md` (FR-AUTH-06/08/09, FR-DOC-03, FR-VER-03, FR-WF-03, FR-AUDIT-01), `10-BRD.md` §4/BR-07, `51-UX.md` §2.1, `43-WORKFLOW.md` §5, `40-TSD.md` §5.2.1/§5.2.2, `70-TESTING.md` §3.12, `60-DEPLOYMENT.md` §6.4 | Changed (P-026): kontrak & requirement diselaraskan dengan empat ADR baru; halaman Settings hanya memuat kunci yang benar-benar ada; §3.12 menulis **test yang wajib ada** sebelum implementasinya; §6.4 memuat prosedur retensi bernomor |
| `backend/internal/handler/project_handler.go` | Changed (P-026): `bindJSON` membedakan tiga sebab kegagalan decode dan menamai field; helper `undecodableField` + `messageForUndecodable` (temuan C-045) |
| `backend/internal/repository/task_repository.go`, `internal/service/task_service.go`, `internal/handler/task_handler.go` | Changed (P-026): penyaring `due_from`/`due_to` sebagai interval setengah terbuka + `parseRFC3339Query` yang menerima `+07:00` maupun `%2B07:00` (temuan C-046) |
| `backend/internal/handler/project_handler_test.go`, `task_handler_test.go`, `internal/service/task_service_test.go` | Changed (P-026): test C-045 (4 sebab decode, lintas modul) dan C-046 (6 kasus service + 7 kasus HTTP) |
| `docs/design/42-API.md` §6/§12, `50-FSD.md` §6.1, `70-TESTING.md` §3.11, `80-ROADMAP.md` §3 | Changed (P-026): kontrak penyaring rentang + tabel atribusi error; inventaris test; tabel status fase diperbaiki (temuan C-047) |
| Database `bwdcs_test` (bukan berkas) | Added (P-024): database test terpisah, owner role `bwdcs`, termigrasi otomatis saat suite berjalan |
| `AGENTS.md` | Ditambah protokol progress wajib & routing dokumen baru |
| `DESIGN.md` | Baru, placeholder jujur (belum diisi) |
| `docs/design/02-AGENT-PROGRESS-PROTOCOL.md` | Baru |
| `docs/design/12-DEVELOPMENT-WORKFLOW.md` | Baru |
| `docs/progress/**` | Baru (ledger progress, 9 file) |
| `docs/adr/**` | Baru (index + 7 ADR) |
| `scripts/check-doc-links.sh` | Baru; jalankan sebelum menutup sesi dokumentasi (BROKEN=0) |
| `CONTINUE.md` | Baru (P-002); titik masuk resume untuk agen/model apa pun |
| `.env.example` | Baru (P-006); 17 variabel, cermin `60-DEPLOYMENT.md` §2.1 |
| `docker-compose.yml` | Baru (P-006); tervalidasi `docker compose config -q`, app 8081->8080, postgres 5433->5432 |
| `docs/design/60-DEPLOYMENT.md` | §2/§2.1 menjadi tabel kontrak (duplikasi YAML dihapus) |
| `docs/adr/0008-backend-library-lockin.md` | Baru (P-003); mengunci stack backend, menghapus "Chi/Gin" dan "SQLX/pgx" |
| `docs/adr/0009-logout-token-invalidation.md` | Baru (P-003); PROPOSED, menunggu Q-006 |
| `docs/adr/0011-audit-log-layer.md` | Baru (P-008); ACCEPTED — audit hanya di service, satu transaksi |
| `docs/adr/0012-status-kanonik-dan-overdue-turunan.md` | Baru (P-008); ACCEPTED — kanonik vs label, overdue turunan |
| `docs/adr/0013-struktur-paket-backend.md` | Baru (P-008); ACCEPTED — struktur backend flat, satu sumber |
| `docs/design/40-TSD.md` | §2.0 pohon struktur kanonik (baru); §2.4 aturan transaksi & audit; §2.6 handler tanpa audit (P-008) |
| `docs/design/50-FSD.md` | §11 tabel status kanonik vs label + tabel nilai turunan (baru) (P-008) |
| `docs/design/30-ARCHITECTURE.md`, `01-AGENT-WORKFRAME.md`, `90-AGENT-GUIDE.md` | Pohon struktur duplikat diganti tautan ke `40-TSD.md` §2.0; contoh audit di service (P-008) |
| `docs/design/51-UX.md`, `43-WORKFLOW.md`, `20-SRS.md`, `41-DATABASE.md`, `80-ROADMAP.md`, `70-TESTING.md`, `12-DEVELOPMENT-WORKFLOW.md`, `02-AGENT-PROGRESS-PROTOCOL.md`, `IDEA.md` | Diselaraskan dengan ADR-0011/0012/0013 (P-008) |
| `docs/adr/0014-matriks-permission-rbac.md` | Baru (P-009); ACCEPTED — matriks permission = sumber migrasi `008`, 104 baris |
| `docs/design/44-SECURITY.md` | §3.1 menjadi sumber tunggal matriks permission (17 resource × 15 action, scoping §3.1.3); §3.3 hierarki role dipisah (P-009) |
| `docs/design/40-TSD.md`, `41-DATABASE.md`, `70-TESTING.md`, `51-UX.md`, `50-FSD.md`, `20-SRS.md`, `12-DEVELOPMENT-WORKFLOW.md`, `AGENTS.md` | Diselaraskan dengan ADR-0014: `RequirePermission`, prosedur migrasi `008`, test matriks, rute routing (P-009) |
| `docs/design/90-AGENT-GUIDE.md` | §7 env: salinan usang dihapus, diganti penunjuk (P-010, C-014) |
| `docs/design/30-ARCHITECTURE.md` | §5.1: blok YAML compose kedua dihapus, diganti penunjuk (P-010) |
| `docs/design/70-TESTING.md` | §5.1: kredensial E2E dari env, bukan `admin123` (P-010) |
| `docs/design/42-API.md` | Header sumber tunggal endpoint; §5 +2 endpoint definisi workflow; §10 endpoint kembar dihapus; 9 fence markdown liar dibersihkan (P-011) |
| `docs/design/40-TSD.md` | §6 daftar route → contoh wiring + aturan pemetaan izin (P-011) |
| `docs/design/43-WORKFLOW.md`, `60-DEPLOYMENT.md`, `70-TESTING.md` | Satu fence markdown liar di akhir berkas dihapus (P-011) |
| `docs/design/42-API.md` | §2 `change-password`, §9 filter audit, **§10 Reports baru** (export), §11 `reset-password`/`roles`/`organizations`, `PATCH /admin/users/:id` dipersempit; bab Error→§12, Swagger→§13 (P-012) |
| `docs/design/50-FSD.md` | §2.2, §10.1, §10.3 diberi penunjuk endpoint (P-012) |
| `docs/adr/0015-optimistic-locking-workflow-instance.md` | Baru (P-013); ACCEPTED — guard `version` di database, tanpa retry, `409 WORKFLOW_CONFLICT` |
| `docs/design/41-DATABASE.md` | §2.4 `workflow_instances.version` + `current_step_deadline` + indeks parsial + blok aturan transisi; §4 isi migrasi `005` (P-013) |
| `docs/design/43-WORKFLOW.md` | §4.1/§4.2/§4.5/§6/§7: satu pola guard, deadline dari kolom (P-013) |
| `docs/design/42-API.md` | §5 `version`/`current_step_deadline`/`409 WORKFLOW_CONFLICT`; §12 beda `CONFLICT` vs `WORKFLOW_CONFLICT` (P-013) |
| `docs/design/40-TSD.md` | §2.3 model `Version`/`CurrentStepDeadline`; §2.4 kontrak `ExecuteAction`; §6 route aksi memakai izin baca (P-013) |
| `docs/adr/0016-request-revision-rollback-step-sebelumnya.md` | Baru (P-014); ACCEPTED — `request_revision` kembali ke step sebelumnya sesuai FR-WF-09; "reset ke step 1" ditolak |
| `docs/design/43-WORKFLOW.md` | §4.5 ditulis ulang (satu perilaku rollback + kasus tepi step 1); §7 catatan salah rujuk C-021 diperbaiki (P-014) |
| `docs/design/42-API.md` | §5: `/workflows/submit` hanya untuk draft, aksi `request_revision` rollback satu step, re-submit lanjut instance yang sama (P-014) |
| `docs/design/20-SRS.md`, `41-DATABASE.md`, `50-FSD.md`, `70-TESTING.md` | Diselaraskan dengan ADR-0016: FR-WF-09 + penunjuk ADR, §2.4, §8.1, test §3.4 (P-014) |
| `docs/design/70-TESTING.md` | §3.3 test konkurensi; §5.1 test E2E konflik; §8.1 env test (P-013) |
| `docs/design/50-FSD.md` | §5.4 Approvals, §10.6 Reports, §10.7 Workflow Definition Management (baru); §5.1 diberi penunjuk ke §10.7 (P-015, C-018) |
| `docs/design/42-API.md` | §5 endpoint daftar `GET /workflows/instances` (`?status=&scope=assigned_to_me`); rujukan T-027 → T-028; §11 catatan → §10.7 (P-015) |
| `docs/design/51-UX.md` | §2.1 catatan penunjuk ke tiga spec FSD baru (P-015) |
| `AGENTS.md` | Baris routing "Halaman Approvals"; status audit 15 FIXED / 8 OPEN (P-015) |
| `docs/adr/0017-format-nomor-dokumen.md` | Baru (P-016); ACCEPTED — `{PROJECT_CODE}-{NNN}` dibangkitkan server, atomik, immutable, tanpa penomoran manual |
| `docs/design/41-DATABASE.md` | §2.2 komentar `projects.code` (UPPERCASE, pola, permanen); §2.3 `document_sequences` + komentar nomor; §3 index; §4 isi migrasi `004` berikut kueri pembangkit (P-016) |
| `docs/design/42-API.md` | §3 `code` project (POST + PATCH menolak perubahan); §4 `POST /documents` tanpa `document_number` dari klien; §9 contoh; §12 `409` dirapikan (P-016) |
| `docs/design/50-FSD.md`, `20-SRS.md`, `51-UX.md`, `IDEA.md` | §3.2/§4.2 (nomor read-only), FR-DOC-02 menunjuk ADR-0017, contoh nomor seragam `WEB-001`/`API-001` (P-016) |
| `docs/design/40-TSD.md`, `44-SECURITY.md`, `90-AGENT-GUIDE.md` | `document_number` dihapus dari input DTO; tag `alphanum` yang salah diganti catatan regex; contoh service membangkitkan nomor di dalam transaksi (P-016) |
| `docs/design/70-TESTING.md` | §3.1 assert `TEST-001`; §3.5 test penomoran (berurutan, rollback, konkurensi, tolak nomor dari klien); §5.1 E2E tidak mengisi nomor (P-016) |
| `docs/design/12-DEVELOPMENT-WORKFLOW.md` | Contoh commit: ID hantu `FR-DOC-08` → `[FR-VER-01, FR-VER-04]` (C-024) (P-016) |
| `docs/design/42-API.md` | §5 endpoint baru `POST /workflows/instances/:id/resubmit` (prasyarat, dua guard, efek, izin `workflow_instance:submit`); jeda revisi pada `/actions`; `document_status` pada endpoint daftar (P-017) |
| `docs/design/43-WORKFLOW.md` | §4.6 baru (re-submit: instance sama, deadline dihitung ulang, tanpa baris `workflow_actions`, jeda, siklus); §4.2 langkah 4 jadi per siklus; §4.5 butir 3 diperbaiki (P-017) |
| `docs/design/40-TSD.md` | §2.4 `WorkflowService.Resubmit`; §6 route resubmit dengan `RequirePermission("workflow_instance", "submit")` (P-017) |
| `docs/design/50-FSD.md` | §4.3 aksi "Resubmit for Review"; §5.2 tombol berubah setelah revisi; §5.4 tab Pending menyaring jeda revisi (P-017) |
| `docs/design/70-TESTING.md` | §3.6 tujuh test re-submit (instance sama, deadline, wajib versi baru, status salah, stale version, jeda, siklus) (P-017) |
| `~/.zshrc` | Blok `BWDCS toolchain` (idempoten): `/usr/local/go/bin`, `$HOME/go/bin`, client psql 16 Postgres.app (T-011) |
| `.env` | Berkas lokal **tidak di-commit** (mode 600): kredensial DB `bwdcs` di `localhost:5432`, `JWT_SECRET` acak, `APP_PORT=8081`, nilai `ADMIN_*` untuk bootstrap; nilai berspasi dikutip agar aman di-source shell |
| `.gitignore`, `.editorconfig` | Baru (T-002/T-002a): `.env`/`storage/`/`bin/`/`node_modules/` terabaikan; tab untuk Go, 2 spasi untuk sisanya |
| `backend/go.mod`, `go.sum` | Module `bwdcs/backend`, `go 1.22.5`; gin v1.10.0, pgx v5.7.4, viper v1.19.0 (T-003) |
| `backend/internal/config/config.go` | Loader viper: `.env` opsional, environment menang, validasi wajib (DB_PASSWORD, JWT_SECRET ≥ 32) yang melaporkan semua masalah sekaligus (T-003) |
| `backend/internal/pkg/filestorage/` | `FileStorage` + `Prober` + `LocalStorage` (skema key ADR-0005, sanitasi nama, tolak path traversal, tolak penimpaan versi) + 9 test (T-003/T-031) |
| `backend/internal/handler/health_handler.go` | `GET /health`: 200 `healthy` / 503 `unhealthy` dengan nama pemeriksaan yang gagal (T-003/C-027) |
| `backend/cmd/server/main.go` | Urutan startup config → storage → pool pgx (lazy) → HTTP; `gin.SetMode(release)` di produksi; shutdown rapi; TODO `T-004`/`T-005` (T-003) |
| `backend/Makefile` | Target `60-DEPLOYMENT.md` §3.1 + `vet`/`fmt`/`migrate-status`; memuat `../.env` bila ada tanpa menimpa environment (T-003) |
| `frontend/README.md` | Placeholder blokir `DESIGN.md` (Phase 4), bukan skeleton aplikasi (T-002) |
| `docs/design/40-TSD.md` | §2.0 nama module + catatan toolchain (pgx v5.7.4); §2.1 field `Bootstrap` + perilaku loader; §2.4 `FileStorage.Save` dengan `originalName` (C-026) |
| `docs/design/60-DEPLOYMENT.md` | §5: cuplikan health check memakai `storage.Ping` dan dapat dikompilasi (C-027) |
| `docs/design/44-SECURITY.md` | §6 ditulis ulang: fungsi PL/pgSQL `prevent_audit_modification()` + `trg_audit_logs_append_only`/`trg_audit_logs_no_truncate` (tolak `23001`), jalur pemeliharaan `bwdcs.audit_maintenance`, down migration, dan alasan `REVOKE` tidak dipakai; §8 checklist ditambah test append-only (P-019, C-020) |
| `docs/design/41-DATABASE.md` | §2.5 penunjuk penegakan append-only ke `44-SECURITY.md` §6; §4 isi migrasi `007` kini memuat kedua trigger (sebelumnya tidak ada migrasi yang memasangnya) (P-019) |
| `docs/design/70-TESTING.md` | §4.3 baru: enam test append-only audit log (UPDATE/DELETE/TRUNCATE ditolak, INSERT lolos, dua trigger terpasang, GUC harus opt-in); §8 catatan `teardownTestDB` memakai GUC pemeliharaan (P-019) |
| `docs/adr/0018-migrasi-di-embed-dan-dijalankan-saat-startup.md` | Baru (P-020); ACCEPTED — migrasi di-embed & dijalankan saat startup, `internal/migration` menjadi package (memperbarui ADR-0013 butir 1), goose dipin v3.24.1 library+CLI |
| `backend/internal/migration/**` | Baru (T-004): 9 berkas migrasi `001`-`009` + `migration.go` (embed + goose) + `main_test.go`, `migration_test.go`, `audit_append_only_test.go` (13 test) |
| `backend/internal/bootstrap/**` | Baru (T-004): `bootstrap.go` (`EnsureAdminFirstRun`, `ValidatePassword`, `Querier`) + `bootstrap_test.go` (10 test) |
| `backend/cmd/server/main.go` | Urutan startup diisi: migrasi (embed) → bootstrap (ADR-0010) → HTTP; TODO `T-004` dihapus (P-020) |
| `backend/go.mod`, `backend/go.sum` | Tambah `pressly/goose/v3` v3.24.1 dan `golang.org/x/crypto` v0.31.0 sebagai dependensi langsung (bcrypt) (P-020) |
| `docs/design/41-DATABASE.md` | §2.3 komentar FK tertunda; §2.6 penunjuk rumah migrasi; §4 pemetaan isi sembilan berkas + catatan C-029/C-030/C-031 (P-020) |
| `docs/design/44-SECURITY.md` | §6 butir baru: badan fungsi wajib dibungkus `StatementBegin`/`StatementEnd` (C-031) (P-020) |
| `docs/design/40-TSD.md` | §2.0: `migration/` kini memuat package kecil (ADR-0018); catatan pin pgx **dan** goose v3.24.1 (P-020) |
| `docs/design/60-DEPLOYMENT.md` | §4.2: berkas migrasi tidak perlu ikut di-deploy karena di-embed (ADR-0018) (P-020) |
| `backend/internal/pkg/jwt/jwt.go` | Baru (T-005): penerbit/validator token HS256, `jti` wajib, `iss=bwdcs`, `exp` wajib, tolak alg none/issuer lain (ADR-0009) |
| `backend/internal/pkg/response/response.go` | Baru (T-005): amplop response + kode error kanonik `42-API.md` §12 (termasuk `TOKEN_REVOKED`, `INVALID_CREDENTIALS`, `ACCOUNT_INACTIVE`, `TOO_MANY_REQUESTS`, `NOT_IMPLEMENTED`) |
| `backend/internal/repository/{user,token_revocation,setting}_repository.go` | Baru (T-005): akses `users`/`role_permissions` (termasuk `HasPermission`), `token_revocations` dengan cache TTL 30 detik + `CleanupExpired`, dan kebijakan login dari `system_settings` |
| `backend/internal/service/{auth_service,login_guard,permission_checker,audit_service}.go` | Baru (T-005): login/logout/profil, batas percobaan gagal FR-AUTH-06, pemeriksa izin tanpa bypass Administrator (C-037), penulisan audit di transaksi pemanggil (ADR-0011) |
| `backend/internal/middleware/*` | Baru (T-005): `AuthMiddleware` (+ cek revokasi → `401 TOKEN_REVOKED`), `RequirePermission`, `RateLimitMiddleware`, `CorrelationID`, `Logger`, `CORS` (hanya origin loopback di dev) |
| `backend/internal/handler/{auth_handler,router}.go`, `internal/dto/auth_dto.go` | Baru (T-005): endpoint `42-API.md` §2 dan pemasangan route (`handler.Setup`) |
| `backend/cmd/server/main.go` | Changed (T-005): rakitan modul auth + pembersihan `token_revocations` saat startup dan setiap 1 jam |
| `backend/Makefile` | Changed (T-005): `test` memakai `-p 1` — paket test integrasi berbagi satu database (C-036) |
| `docs/design/42-API.md` | §2: anotasi `Izin:` kelima endpoint auth + catatan `logout_all` (501), `refresh`/`change-password` belum dijalankan; §12: kode `401`/`403`/`429`/`501` diperjelas (P-021) |
| `docs/design/40-TSD.md` | §2.2 interface middleware nyata (package `auth` hantu dihapus — C-034) + pembagian rate limit; §2.4 `AuditService.Log(ctx, tx, …)`; §5.2.2 batas percobaan login & batas auto-lock (C-009); §6 lokasi `Setup` (P-021) |
| `docs/design/44-SECURITY.md` | §2.3 batas auto-lock + audit login gagal (C-035); §3.2 tanpa bypass Administrator (C-037) (P-021) |
| `docs/design/70-TESTING.md` | §8: test integrasi berbagi satu database → jalankan serial (`-p 1`) + catatan pembersihan data yang sudah commit (C-036) (P-021) |
| `backend/internal/model/document.go` | Baru (T-037): status kanonik dokumen + `DocumentStatuses`/`IsDocumentStatus`; aturan versi `ParseVersion`/`FormatVersion`/`NextVersion` (major bila `revision_required`) |
| `backend/internal/repository/document_repository.go` | Baru (T-037): cakupan di `WHERE` (JOIN `projects p`, memakai `projectScopePredicate` yang sama), `NextNumber` atomik, `CategoryExists`, versi (`AddVersion`/`Versions`/`LatestVersion`/`FindVersion`), `VersionKeys`, `Delete` berkaskade, `HasRunningWorkflow` |
| `backend/internal/service/document_service.go`, `document_service_upload.go`, `scope.go` | Baru (T-037): `DocumentService` (create dengan nomor di dalam transaksi, list/get/delete, upload versi + checksum + `limitedReader`, versi, unduh ber-audit). `scope.go` mengekstrak `Actor` + `systemScope` supaya project dan document memakai satu aturan cakupan |
| `backend/internal/service/document_service_test.go`, `document_number_test.go`, `document_upload_limit_internal_test.go` | Baru (T-037): 9 test integrasi service, 5 test penomoran ADR-0017 (`70-TESTING.md` §3.5), dan unit `limitedReader` |
| `backend/internal/handler/document_handler.go`, `document_handler_test.go`, `internal/dto/document_dto.go` | Baru (T-037): tujuh handler (multipart unggah dengan magic bytes, streaming unduh ber-`Content-Disposition`, pemetaan error terpusat) + 11 test HTTP + bentuk request/response `42-API.md` §4 |
| `backend/internal/handler/router.go`, `main.go`, `internal/handler/main_test.go` | Changed (T-037): tujuh route `/documents` dengan `RequirePermission` matriks ADR-0014; wiring `DocumentService` + `FileStorage`; harness test dipecah (`newEngineParts`) supaya test dokumen memakai storage nyata |
| `backend/internal/service/project_service.go`, `internal/model/project.go` | Changed (T-037): normalisasi `projects.code` juga di service (C-041); komentar `IsArchived()` dikoreksi — aturan "project arsip tidak menerima dokumen baru" tidak punya sumber desain (C-042) |
| `backend/internal/pkg/jwt/jwt_test.go` | Changed (T-037): `tamperSignature` mengubah **byte** tanda tangan, bukan karakter terakhir base64url — test tidak lagi gagal acak (C-039) |
| `docs/design/42-API.md` §4 | Ditulis ulang (T-037): tabel izin 7 endpoint + aturan penomoran/versi/berkas/unduhan/hapus, contoh `current_version` dan `checksum` 64 heksadesimal (C-040) |
| `docs/design/40-TSD.md` §2.3-§2.6/§6 | Diselaraskan (T-037): model document/version nyata, `DocumentService`/`DocumentRepository` sesuai implementasi, pola handler multipart + streaming, tujuh route dokumen |
| `docs/design/44-SECURITY.md` §3.1.3/§4.2 | Diselaraskan (T-037): cakupan satu fungsi (`systemScope`) untuk project+document; daftar kebijakan unggahan menjadi bentuk yang benar-benar berjalan (termasuk `limitedReader` dan alasan `422` bukan `413`) |
| `docs/design/50-FSD.md` §4.2/§4.3, `41-DATABASE.md` §2.3, `70-TESTING.md` §3.5/§3.8/§3.9 | Diselaraskan (T-037): tabel versi minor/major + owner = pembuat + syarat hapus; komentar kolom `checksum`; status §3.5 dijalankan, daftar test modul dokumen §3.8, aturan test deterministik §3.9 (C-039) |
| `docs/progress/TRACEABILITY.md` | FR-DOC-01..07 → DONE (FR-DOC-07 PARTIAL); **enam baris FR-VER-01..06 ditambahkan**; FR-AUDIT-01 diperluas (T-037) |
| `docs/progress/OPEN-QUESTIONS.md` | **Q-016** baru: delapan kontrak modul document (T-037) |
| `docs/progress/audits/AUDIT-001-...md` + `audits/README.md` | C-039..C-042 ditambahkan (FIXED di sesi yang sama); hitungan waktu itu ditulis 42 temuan / 31 FIXED / 10 OPEN — angka FIXED itu **salah** dan dikoreksi di P-024 (C-044) |
| `docs/progress/TASKS.md`, `STATE.md`, `SESSION-LOG.md`, `CHANGELOG.md`, `CONTINUE.md`, `AGENTS.md` | Ledger diselaraskan dengan modul document (T-037) |
