# STATE — Kondisi Proyek Saat Ini

**Terakhir diperbarui:** 2026-09-24 (P-067)
**Diperbarui oleh:** agen (P-067) — **perbaikan create button dark contrast + dashboard tabs + chart dark contrast + audit antislop (T-090).** Create button `primary` semula `bg-accent text-paper-000` (dark `1.74:1` FAIL) → `bg-text text-surface-raised border-text hover:opacity-90` (light `17.8:1` dark `14.1:1` setara Daftar Project). Dashboard 8 chart grid penuh → 3 tab `Dokumen`/`Workflow`/`Antrian` (`role=tablist aria-selected`) — Dokumen: Sebaran Status Donut + Funnel + Kategori; Workflow: Volume Trend + Approval Trend + Activity Trend; Antrian: Aging + Avg Time — per chart `CartesianGrid var(--line)`, `tick var(--text-muted)` `stroke var(--line-strong)`, `Tooltip bg var(--surface-raised) border var(--line)`, `Legend var(--text-muted)`. Audit 38 aturan R-01..R-38: 0 pelanggaran baru; Delivery Gate 4 blok PASS. Verifikasi: frontend **294 test / 30 berkas** hijau, backend **279 test**, enam pemeriksa hijau (`ledger OK 279`, `BROKEN 0`, `readme-facts 46`, `api-contract 115/55`, `antislop-refs OK 212`, `navigation OK`).

**Diperbarui sebelumnya:** agen (P-066) — Polish filter, tabel Tasks, dan tombol Create (T-089). Filter `bg-surface-raised border p-3 shadow-sm` + kolom `flex-1 min-w-[140px] max-w-[200px] min-w-0`, tabel Tasks `density comfortable` + kolom sempit, tombol Create `primary` `bg-accent text-paper-000 border-accent-strong shadow-sm`; verifikasi: frontend **294/30 PASS**.

**Diperbarui sebelumnya:** agen (P-053) — **halaman Approvals berdiri (`T-070`), antrean terlihat sebagai tab, bukan menu terpisah.** Daftar memakai `GET /workflows/instances` (`42-API.md` §5): `Pending` = `status=running&scope=assigned_to_me` dengan `revision_required` dikecualikan **di klien** (jeda revisi tidak dapat ditindak sampai re-submit), `Approved` = `completed`, `Rejected` = `rejected`; kolom `document_number/title/project/step/deadline/status` dari `WorkflowInstanceListItemResponse`, pagination dari `meta`. Detail `/approvals/:id` menampilkan metadata instance, penanganan jeda revisi, dan aksi `approve`/`reject`/`request_revision` dengan `version`+`comment` serta `409 WORKFLOW_CONFLICT`→alert+refetch. Lapisan data `services/workflows.ts` + `queries/workflows.ts` mengikuti kontrak apa adanya; `responsive-evidence` mencakup `/approvals` (4 halaman × widths, expectedRanges 0 untuk Approvals). Verifikasi: frontend **282 test / 28 berkas** hijau, backend **279 test**, enam pemeriksa hijau.

**Diperbarui sebelumnya:** agen (P-052) — **penyaring tanggal Documents berhenti menjadi dua isian yang tidak berhubungan, dan pemeriksa baris penyaring berhenti milik satu halaman (`T-069`).** Kontraknya duluan: `50-FSD.md` §4.1 menyebut "Date range" sejak lama tetapi `GET /documents` tidak punya parameternya (sisi tanggal Q-016) — kini `updated_from`/`updated_to` hidup dengan semantik yang **sama persis** dengan `due_from`/`due_to` tasks: interval **tertutup**, instan RFC 3339 ber-offset eksplisit (tanggal tanpa offset `422`), rentang terbalik `422 field=updated_to`, dan parameter yang sama mengalir ke tambalan `COUNT(*) OVER()` milik C-048 sehingga `meta.total` tidak dapat menyimpang dari daftarnya. Halamannya memakai pola P-049: kelompok `role="group"` berlabel "Rentang pembaruan", berdampingan dari `sm:` dan menumpuk **di dalam kelompok yang sama** di bawahnya — dengan satu keputusan bentuk baru: **satu aksi terapkan** untuk pencarian dan rentang, karena Enter di dalam form memicu submit tombol pertama sehingga dua aksi terpisah membuat Enter di isian tanggal diam-diam membuang draft rentangnya. Pemeriksa baris `measureTaskFilters` digeneralisasi menjadi `measureFilterRow(formLabel)` untuk **ketiga** halaman pada **setiap** lebar; kelompok dicari lewat **struktur** (semua `[role="group"]` berisi tepat dua `datetime-local`), bukan id tetap, dan setiap halaman menyatakan `expectedRanges` (Projects 0, Tasks 1, Documents 1) — tanpa itu, menghapus `role="group"` sama sekali tetap hijau. **Dua cacat lahir dan ditutup di sesi yang sama:** tombol bersebelahan kelompok ber-label dituduh melanjutkan kolomnya (ekornya bahasa tata letak `items-end`, kini dibedakan dengan alasan tertulis), dan test batas yang pernah dibenahi **gagal lagi pada sapuan penuh** karena fixture memotong `created_at` ke detik sehingga tiga dokumen lahir dalam detik yang sama punya tepi identik dan lebih awal dari `updated_at` ber-mikrodetik (`RFC3339Nano` yang memperbaikinya).
**Diperbarui sebelumnya:** agen (P-051) — **aturan sidebar `51-UX.md` §2.1 akhirnya punya penegak mesin, dan membuat penegaknya menemukan tiga cacat lain (`T-067`/`T-068`).** Permintaannya satu: pemeriksa yang menolak item sidebar membawa kueri penyaring atau menunjuk sub-halaman. Yang dikerjakan bukan "satu test lagi": CI repo ini **tidak punya job frontend**, sehingga test klien tidak pernah berjalan di sana, dan satu-satunya pemeriksa yang menyentuh bentuk menu (`scripts/responsive-evidence.mjs`) hanya mencari item **berkueri** — path bersarang luput. Jadi halaman **Audit** (di bawah Reports) dapat berdiri sebagai entri sidebar tanpa ada yang gagal (**C-079**). Dua hal dibangun: halaman anak itu sendiri mendapat jalan masuk lewat daftar baru **`subPages`** (`parent`; barisnya dirender `ModulePending.tsx`, rute pending dibangun dari kedua daftar, `T-067`), dan penegaknya dibuat sebagai skrip shell `scripts/check-navigation.sh` yang membaca teks model navigasi **dan** tabel §2.1 lalu menolak entri berkueri, entri lebih dari satu segmen, label bergaya remah, halaman anak tanpa induk/berinduk hantu/di luar induknya, path/label ganda, penyimpangan himpunan menu **dua arah**, dan teks aturan yang dilunakkan (`T-068`). Gigi dibuktikan dengan **tujuh** cacat disuntikkan sementara, dan ketujuhnya tertangkap; berkasnya dipulihkan byte-identik. Cacat ketujuh **bukan** pelanggaran aturan melainkan kerusakan bentuk berkas, dan dari situ lahir **C-080**: penjaga hampa yang hanya menghitung jumlah menu tidak menyala saat kunci `label:` diganti nama, sehingga skrip melaporkan 25 kegagalan menyesatkan — kini penjaga **bentuk** berhenti dengan satu sebab yang benar. Menyunting paragraf §1 laporan audit untuk mencatatnya menemukan **C-081**: total "secara keseluruhan" di dalam laporan audit **sendiri** tertinggal tiga sesi (75 padahal marker 78) karena aturan prosa `check-ledger.sh` hanya membaca baris yang menyebut `AUDIT-001` — kini ringkasan §1 wajib sama dengan markernya, dan giginya dibuktikan. Verifikasi rutin menutup dengan **C-082**: suite frontend gagal **berpindah-pindah antar-berkas pada kode yang sama** (tiga dari lima kali `npm run test:run`), sementara `--maxWorkers=1`/`=2` hijau penuh pada kode yang sama — jendela tunggu bawaan `findBy*` (1000ms) adalah klaim tentang **kecepatan mesin**. Anggarannya dinaikkan (`asyncUtilTimeout: 5000`, `testTimeout: 20000`) **tanpa melonggarkan satu asersi pun**, dan tiga kali jalannya hijau berturut-turut. **Backend tidak disentuh**: tanpa perubahan kode, kontrak, izin, atau skema.
**Diperbarui sebelumnya:** agen (P-050) — **pengukuran tata letak per lebar kini berjalan untuk setiap halaman, dan perluasan cakupan itu menemukan tiga kelas cacat (`T-066`).** Sapuan per lebar sejak P-043 hanya mengunjungi `/projects`; halaman lain diukur hanya di dua ujung, dan justru di situlah cacatnya bersembunyi. Kini `projects`/`tasks`/`documents` diukur pada `375/640/768/1024/1440` — **15 pengukuran tata letak** dan **18 pengukuran tema** — dan pada setiap lebar ikut diperiksa sidebar, bilah tab Documents, serta baris penyaring Task. **C-076:** kolom penyaring tidak dapat menyusut, sehingga `#penyaring-project-dokumen` selebar **403px** pada viewport 375px membuat halaman menggulir mendatar **44px**; sebabnya min-width otomatis item flex = **min-content** anaknya, dan min-content sebuah `<select>` ditentukan **teks pilihan terpanjang** — yaitu **data pengguna**, sehingga cacatnya muncul dan hilang tergantung isi database. Halaman **Projects** menyimpan cacat yang sama meski pilihannya pendek, dan itu baru terlihat sesudah sapuan menyisipkan **pilihan sengaja panjang** ke setiap `select` penyaring (**65px**). **C-077:** `@utility tap-target` hanya menetapkan `min-height`, sehingga tab sub-halaman **"Tim"** terukur **43x44px** — satu piksel di bawah ambangnya sendiri, dan ia satu-satunya yang setipis itu karena labelnya terpendek. **C-078:** lima cacat pada alat ukurnya sendiri — cakupan yang tidak pernah meluas; argumen berspasi `--widths 375,768` yang **diam-diam diabaikan** parser padahal bentuk itulah yang tertulis di komentar pemakaiannya; jeda tetap yang kalah balapan sehingga pernah menghasilkan **positif palsu**; pemeriksaan "kolom melanjutkan di bawah kontrolnya" yang menandai kendali majemuk yang sah (dua isian dalam satu kelompok); dan probe stres yang menuduh kontrol yang salah karena melaporkan **luas** halaman alih-alih **pertambahannya**. Perbaikannya: `min-w-0` pada setiap kolom ber-`select` di ketiga halaman (kolom rentang tenggat sengaja **tidak**, karena dua `datetime-local` memang tidak dapat menyusut), `tap-target` menetapkan **kedua sisi**, penantian "tata letak berhenti berubah" menggantikan jeda tetap, satu fase yang hampa pada masukan tertentu dilewati **dengan alasan tertulis**, dan probe pilihan panjang membuat aturannya tidak lagi bergantung pada data yang kebetulan ada. **Gigi dibuktikan** dengan memasang kembali dua cacat sekaligus → `responsive-evidence` **FAIL enam butir** (`Tim=43x44`, `gulir mendatar 44px (#penyaring-project-dokumen)`, `pilihan yang panjang melebarkan halaman 21px`, dan tiga pengukuran tema). Verifikasi: `tsc --noEmit` + `eslint .` bersih, frontend **247 test / 25 berkas** hijau (naik dari 241/24), `vite build` 455,5 kB js / 23,4 kB css, `responsive-evidence OK` (0px gulir mendatar di kelimabelas pengukuran, 0 kontrol di bawah ambang, 0 pertambahan dari stres), lima pemeriksa hijau, **audit 78/76/0/2**. **Tanpa perubahan backend, kontrak API, izin, atau skema.** Log: `prompts/P-050-2026-09-23-sapuan-per-lebar-setiap-halaman.md`.
**Diperbarui sebelumnya:** agen (P-049) — **penyaring rentang tenggat di halaman Tasks berhenti menjadi dua penyaring yang tidak berhubungan, dan ukurannya menemukan cacat yang baru saja saya buat (`T-065`).** Kekurangannya sudah dinyatakan sendiri sesi P-047 (`70-TESTING.md` §3.14e) dan sebabnya kini **terukur**: baris penyaring melipat, sehingga dua kolom terpisah jatuh ke garis berbeda — bentuk lama mencatat batas awal di `top 220px` dan batas akhir di `289px` pada 1440px. Kedua isian berpindah ke **satu kelompok ber-peran `group` berlabel "Rentang tenggat"**: berdampingan pada lebar lebar, dan **menumpuk di dalam kelompok yang sama** pada lebar sempit. Pilihan terakhir itu bukan tafsiran, melainkan hasil ukuran: percobaan pertama menempatkan keduanya berdampingan di semua lebar dan pada 375px halaman menggulir mendatar **71px** (dua `datetime-local` berdampingan menuntut ~400px) — memaksakan "sebaris" akan menuntut memotong nilai isian, jadi yang dipertahankan **hubungannya** (satu kelompok), bukan posisinya. Hasil terukur: 1440px `fromTop == toTop == 289` + `sameLine` + `sameGroup`; 375px `sameGroup` + `overflowX = 0` + tinggi kontrol 44px seragam. **Gigi dibuktikan** dengan memasang kembali bentuk lamanya → `responsive-evidence` **FAIL tiga butir sekaligus**. **Satu cacat alat ukur ikut ketahuan dan ditutup:** butir itu semula mencari isian lewat `aria-label`, sehingga pada bentuk lama ia berbunyi "tidak ditemukan" — berhenti **mengukur** tepat pada bentuk yang harus ditangkapnya (kelas **C-075**); pencariannya dipindah ke **`id`** yang stabil dan pesan kegagalannya dikoreksi. Verifikasi: `tsc --noEmit` + `eslint .` bersih, frontend **241 test / 24 berkas** hijau (naik dari 240/24), `responsive-evidence OK` (375/768/1024/1440px, gulir mendatar 0 di keempat lebar). **Tanpa perubahan backend, kontrak API, izin, atau skema** — sesi ini tidak menyentuh satu berkas pun di `backend/`; tidak ada temuan audit baru. Log: `prompts/P-049-2026-09-23-rentang-tenggat-satu-kelompok.md`.
**Diperbarui sebelumnya:** agen (P-048) — **modul terakhir yang menggantung di Phase 2 akhirnya berjalan: Workflow, sembilan endpoint `42-API.md` §5, 29 test, dan 46 asersi pada server nyata (`T-064`).** Modul ini adalah yang **paling lama tinggal sebagai kontrak** — `43-WORKFLOW.md` ada sejak P-007, kontraknya di `42-API.md` §5 sejak P-013/P-017, dan tiga ADR pendukungnya (`0014` matriks izin, `0015` optimistic locking, `0016` arah rollback + re-submit) sudah `ACCEPTED` bertahun-sesi sebelum satu baris kode pun ditulis. Tiga aturan yang paling mudah dikarang justru yang ditegakkan paling ketat. **(1) Guard optimistic ada di kueri, bukan di memori:** `ApplyTransition` menuntut **empat** kondisi `WHERE` (id, `version`, `status = running`, `current_step` yang sudah divalidasi) dan `rowsAffected = 0` berarti rollback + `409 WORKFLOW_CONFLICT` ber-`details` **objek** keadaan terkini (ADR-0015) — bukan `500`, bukan pesan sukses, bukan retry otomatis. **(2) Jeda revisi dibaca dari status dokumen, bukan status instance:** selama `revision_required` instance **tetap `running`** (ADR-0016), jadi bila yang diperiksa hanya `status` instance maka **seluruh aksi tetap akan diterima** selama pemilik dokumen menyiapkan versi baru — probe membuktikannya (`409` walau `running`). **(3) Urutan pemeriksaan `ExecuteAction`** (izin dari isi body → cakupan → baca instance → tolak dini `version` → status instance → status dokumen → role penanggung jawab → belum-bertindak-dalam-siklus → baru `ApplyTransition`) membuat setiap penolakan **tidak meninggalkan jejak**, seperti dituntut `70-TESTING.md` §3.4. **Tiga temuan audit ditutup dari modul ini, dan ketiganya kelas yang tidak dapat ditangkap pemeriksa yang ada.** **C-073** (`FIXED`): pseudokode `43-WORKFLOW.md` §4.1 memuat `Actor must have the responsible_role OR be admin` — jalan pintas yang tidak ada di `44-SECURITY.md` §3.1/§3.3, `50-FSD.md` §5.4, maupun `51-UX.md` §2.1, yang semuanya menetapkan izin **dan** penunjukan step sebagai syarat **bersamaan**; akibatnya Administrator dapat menyetujui step milik Manager, dan `403` "bukan penanggung jawab step" tidak akan pernah terjadi bagi role tertinggi. Ia lolos dari `check-api-contract.sh` (114 pemeriksaan) karena penunjukan step **bukan** pasangan resource/action sehingga tidak punya baris di matriks dan tidak muncul di blok `Izin:` kontrak mana pun. **C-074** (`FIXED`): `42-API.md` §5 menyebut "**satu-satunya** route di sistem yang izinnya bergantung pada isi body", sedangkan `42-API.md` §6 dan `40-TSD.md` §6 aturan 3 menyebut himpunan yang sama berisi **dua** — kalimat yang benar saat ditulis (P-013) dan menjadi salah pada P-026, dengan dua tempat diperbarui dan yang ketiga tertinggal. **C-075** (`FIXED`) justru tentang alat pemeriksanya sendiri: `scripts/check-ledger.sh` hanya mengenali dua bentuk penulisan klaim hitungan test per berkas, sehingga dua klaim di `STATE.md` §3 yang ditulis `(N test: …)` **tidak pernah diperiksa** (`bootstrap_test.go` 10 padahal 8, `audit_append_only_test.go` 14 padahal 15) sementara skripnya melaporkan OK — dan klaim versi skema yang menunjuk keadaan sekarang masih menulis **10** padahal **11** di `STATE.md` §3 **dan** `CONTINUE.md` §2; polanya diperluas, ketiga angka dikoreksi, dan gigi dibuktikan dengan memasang kembali nilai lama (`ledger GAGAL — bootstrap_test.go ditulis 10 test, sebenarnya 8`). Bagian bukti yang sengaja merekam versi saat dijalankan (`70-TESTING.md` §3.12a/§3.12b) **tidak** diubah: itu snapshot eksekusi, bukan klaim keadaan. Satu test juga **diperbaiki karena lulus hampa**: `TestWorkflowConcurrentApprovalAcceptsExactlyOne` memakai dua step ber-penanggung jawab berbeda sehingga tepat satu approval diterima apa pun urutan eksekusinya — ia kini menyatakan bahwa yang dibuktikannya adalah **guard secara keseluruhan**, sedangkan kondisi `version` dikunci `TestWorkflowTransitionGuardIsOptimistic`; pembagian peran itu dibuktikan **mutasi** (guard `version` diganti sementara menjadi selalu-benar → test itu **gagal** tepat pada `menyentuh 1 baris, diharapkan 0 (ADR-0015 §6)`, sedangkan test konkurensi tetap hijau — dan klaim awal yang salah atribusi diperbaiki sebelum sesi ditutup). **Bukti server nyata:** `scripts/probe-workflow-module.py` **46/46 asersi PASS** dengan lima aktor login sungguhan (definisi & `order` duplikat, submit + notifikasi + audit, izin dari isi body, administrator `403` saat bukan penanggung jawab step, `version` basi `409`, approve tengah/akhir, rollback satu step, jeda revisi, re-submit tanpa baris `workflow_actions` baru, `is_overdue` turunan, cakupan non-anggota `total 0`/`404`, baseline pulih — audit 49/49). Verifikasi: `make test` sembilan paket / **270 test** (naik dari 239), `gofmt`/`vet` bersih, `ledger OK`, `api-contract OK — 115 pemeriksaan`, `readme-facts OK — 44 fakta` (ember route `workflows` ditambahkan setelah pemeriksa itu **gagal pada percobaan pertama**), `BROKEN: 0`, `antislop-refs OK`. **Tanpa perubahan skema, kontrak endpoint di luar §5, izin baru, atau migrasi** — versi goose tetap **11**. Log: `prompts/P-048-2026-09-23-modul-workflow-dan-probe-46-pass.md`.
**Diperbarui sebelumnya:** agen (P-047) — **dua keluhan nyata pada halaman yang sudah berdiri ditutup beserta sebabnya, dan keduanya berhenti menjadi klaim lisan.** **(1) Penyaring `Penanggung jawab` di Tasks** tingginya berbeda dari penyaring lain **bukan** karena warna atau ukuran, melainkan karena **kolomnya lebih tinggi**: kolom itu memuat `<p>` catatan di bawah `<select>`-nya, sehingga kontrolnya terangkat sendiri dari baris ber-`items-end` (terukur `top 197` melawan `top 220` milik tujuh kontrol lain). Catatannya dipindah ke blok catatan di bawah baris penyaring — bukan dihapus, karena sumber pilihan penanggung jawab dan batas **C-063** tetap harus dinyatakan — dan ketujuh kontrol pada garis pertama kembali berbagi `bottom 256`. **(2) Sidebar memuat sub-item yang isinya hanya tab dan penyaring halaman** — `config/navigation.ts` mendaftarkan `subItems` untuk hampir setiap modul, sehingga delapan entri menu menghasilkan **lima belas** tautan yang semuanya dapat dicapai dari halamannya sendiri. `subItems` dibuang dari model, `Sidebar.tsx` kembali merender satu tautan per modul, dan sub-navigasi dipindah ke halaman yang memiliki daftarnya: **Documents** mendapat baris tab `Semua / Milik saya / Pending Review / Revision Required / Approved` (sehingga penyaring `?view=mine` tetap dapat dibuka dan alasan **Q-016** tetap terbaca di layar, bukan hanya lewat URL yang harus dihafal), **Tasks** mempertahankan baris tabnya, dan **Projects** tidak butuh tab karena "List" adalah halaman itu sendiri sedangkan "Create" adalah tombol aksi di header. `51-UX.md` §2.1 ditulis ulang mengikuti keputusan itu. Penanda menu aktif kini **diturunkan dari daftar path** (`requiresExactMatch`), bukan ditulis tangan: sebelumnya `/reports` ikut menyala saat yang dibuka `/reports/audit` karena `NavLink` mencocokkan awalan. Yang membuat sesi ini bukan sekadar penataan ulang: `scripts/responsive-evidence.mjs` diperluas dengan bagian `navigation`, sehingga sidebar (8 modul, **0** tautan berkueri, **0** tautan sub-halaman, tepat **1** menu bertanda aktif), kesebarisan baris penyaring Task (2 garis, **0** kontrol terangkat, tinggi seragam 36px), dan tab Documents (5 tautan, satu penanda; klik `Milik saya` memindahkan penanda sekaligus memunculkan alasan Q-016) **diukur di peramban sungguhan** dan **dapat menggagalkan sesi**. **Gigi dibuktikan dua kali:** cacat (1) dipasang kembali → skrip bukti **FAIL** tepat pada `kolom "Penanggung jawab" melanjutkan 23px di bawah kontrolnya`; aturan awalan lama dipasang kembali → test AppShell **gagal** pada `Reports` yang ikut `aria-current="page"`. Bukti: `tsc --noEmit` + `eslint .` bersih, **240 test / 24 berkas** hijau (naik dari 234/24), `vite build` 455,3 kB js / 23,3 kB css, dan `node scripts/responsive-evidence.mjs` → `OK` (375/768/1024/1440px, gulir mendatar 0 di keempat lebar, 6 pengukuran tema, laci 375px membereskan dirinya). **Tanpa perubahan backend, kontrak API, izin, atau skema** — sesi ini tidak menyentuh satu berkas pun di `backend/`. Tidak ada temuan audit baru. Log: `prompts/P-047-2026-09-23-sidebar-modul-dan-penyaring-sebaris.md`.
**Diperbarui sebelumnya:** agen (P-046) — **halaman bisnis ketiga berdiri: Tasks (`T-062`), dan dua perilakunya yang paling mudah dikarang justru yang dibuktikan paling keras.** Lapisan data mengikuti `42-API.md` §6 apa adanya: penyaring **tri-state overdue** dikirim tiga keadaannya (`""`/`"true"`/`"false"` — "Hanya yang belum overdue" memuat task tanpa tenggat, yang tidak pernah overdue), rentang tenggat RFC 3339 `[due_from, due_to]` inklusif, dan paginasi dari `meta`; dialog buat mengambil penanggung jawab dari anggota project (batas C-063 dinyatakan di layar, bukan ditutupi). Halaman detail merender tabel transisi §6.3 (aksi → transisi → endpoint → izin) dan menawarkan tombol menurut status berjalan — `Complete` tidak pernah muncul pada task `open` karena server membalas `409`. Bukti frontend: `tsc --noEmit` + `eslint .` bersih, **234 test / 24 berkas** (naik dari 188/21), `vite build` 455,8 kB js / 23,5 kB css; test mengunci perilakunya (`Tasks.test.tsx` untuk tiga keadaan penyaring, `TaskDetail.test.tsx` untuk ketiga transisi + `409` + pesan izin). Bukti server nyata: `scripts/probe-task-module.py` — **41/41 PASS** pada binari dari kode sesi ini (`versi_skema 11`, aktor kedua viewer): tri-state (`total=4` → `true total=2` → `false total=2`), rentang inklusif (`due_to==due_from` sah, terbalik `422 field=due_to`), halaman di luar rentang `total` tetap 4 (tambalan C-048 terbukti), ketiga transisi + `409` Complete-dari-Open + `409` PATCH status + `409` pindah project, penanda overdue hilang saat selesai, cakupan viewer non-anggota (`total 0`, `404`, `403` → `200` sesudah jadi anggota), jejak audit task, dan baseline pulih otomatis oleh probe. **Tidak ada temuan baru** — tidak ada satu pun langkah probe yang gagal dan tidak ada klaim dokumen yang perlu ditepati. Log: `prompts/P-046-2026-09-22-halaman-tasks-dan-probe-41-pass.md`.
**Diperbarui sebelumnya:** agen (P-045) — **dua hal ditutup: satu cacat unggahan berkas dan ledger sesi sebelumnya.** **C-072** (`FIXED`): `validateUpload` membandingkan hasil `http.DetectContentType` (`text/plain; charset=utf-8`) dengan daftar tertutup yang menulis `text/plain`, sehingga `.txt` dan `.csv` — dua dari delapan jenis berkas yang dijanjikan `50-FSD.md` §4.2 — **selalu** dibalas `422` oleh server nyata; ketahuan dari satu unggahan `.txt` sungguhan, bukan dari `make test`, karena seluruh test menulis MIME dengan tangan dan satu kasus HTTP justru menuntut `422` untuk `.pdf` berisi teks sehingga mengunci perilaku yang salah. Kini parameter dibuang sebelum pencocokan (`normalizeMimeType`, tipe media saja), lima golongan berkas diuji memakai MIME **hasil deteksi byte**, dan kasus HTTP palsu diganti isi yang memang di luar daftar; gigi dibuktikan dengan mengembalikan cacatnya — **enam subtest gagal** tepat pada `text/plain; charset=utf-8`. Sesi ini juga menyelesaikan **ledger P-044** yang tertinggal: `T-059` (halaman **Documents**: `services/documents.ts` + `queries/documents.ts`, daftar ber-penyaring hidup di URL, dialog unggah **dua langkah** dengan `document_number` read-only dari server, detail berisi metadata + versi + unduh + arsip, dan bagian "belum dibangun" beserta alasannya) dan `T-060` (**C-070** bentuk amplop `POST /documents/:id/archive` kini tertulis di `42-API.md` §4 — dokumennya di `data.document`, bukan telanjang — dan **C-071** pesan penjaga append-only lewat migrasi **`011`** memakai `TG_TABLE_NAME`). Bukti segar di server nyata: unggah `.txt` 39 byte → `201` (`mime_type` `text/plain; charset=utf-8`), unduh **byte-identik** (`sha256` `c25e7b6e…`, `cmp IDENTIK`), arsip `200` (`data.document.status=archived`), daftar default `total 0` / `?status=archived` `total 1`, unggahan sesudah arsip `409`, viewer non-anggota `404` + `total 0` dan `200` sesudah ditambahkan sebagai anggota. Versi skema **11** di `bwdcs` maupun `bwdcs_test`, lalu database dev dikembalikan ke baseline (`users 1`, `projects 0`, `documents 0`, `audit_logs 43`, `login_attempts 0`, tanpa berkas yatim di `storage/`). Frontend `tsc --noEmit` + `eslint .` bersih, **188 test / 21 berkas** hijau, `vite build` 426,03 kB js / 22,79 kB css; backend `make test` hijau (**239 test**). Log: `prompts/P-045-2026-09-22-mime-berkas-dan-ledger-p044.md`.
**Diperbarui sebelumnya:** agen (P-044) — **halaman bisnis kedua berdiri: `Documents` (`T-059`), dan dua cacat ketahuan justru dari menjalankannya (`C-070`/`C-071`, `T-060`).** Lapisan data mengikuti `42-API.md` §4 apa adanya, halamannya memakai primitives yang sudah ada (`DataTable`, `Dialog`, `States`, `StatusBadge`) dengan penyaring status/project/search hidup di URL, dialog unggah **dua langkah** (metadata lebih dulu, nomor dokumen read-only dari server, lalu berkas), dan halaman detail berisi metadata + daftar versi + unduh + unggah versi baru + arsip. Dua celah yang seluruh test hijau tidak melihat: **C-070** — kontrak §4 menulis arsip sebagai "Response 200: dokumen terarsip" **tanpa bentuk amplopnya**, sehingga klien mengetik `ApiSuccess<DocumentRecord>` dan `archiveDocument()` mengembalikan `undefined` pada panggilan nyata; **C-071** — satu fungsi penjaga append-only dipakai `audit_logs`, `document_versions`, dan `documents`, tetapi pesannya menyebut `audit_logs` secara tetap, sehingga `DELETE FROM document_versions` menjawab "audit_logs bersifat append-only". Log: `prompts/P-044-2026-09-22-halaman-documents-dan-lapisan-data.md`.
**Diperbarui sebelumnya:** agen (P-043) — **aturan antislop diterapkan pada frontend, laci menu di layar sempit diperbaiki, dan klaim tata letak berhenti diklaim (`T-056`/`T-057`/`T-058`).** Gap diperiksa lebih dulu, dan yang merah memang ada di fondasi pekerjaan yang belum selesai: satu error `tsc` dan dua error `eslint` (state disalin di dalam effect) diperbaiki dengan `useSyncExternalStore` + callback langganan, bukan diabaikan. Menu pada resolusi kecil sebelumnya hidup di dalam baris flex sehingga membukanya **menyempitkan** halaman; kini ia lapisan modal yang sungguhan (fixed 288px, latar penutup ber-penanda, `inert` di konten, gulir terkunci, fokus masuk lalu **kembali**, permintaan membukanya dilupakan saat jendela melewati titik henti) dan perilaku modal itu ditulis **sekali** di `hooks/useModalLayer.ts` untuk laci **dan** `Dialog`. Tiga state lebar kini nyata (laci < 768px, kolom kompak **berlabel** 768-1023px, kolom penuh ≥ 1024px). Yang paling penting dari sesi ini: **klaim tata letak diukur mesin di peramban sungguhan** — `scripts/responsive-evidence.mjs` (Chrome lewat protokol DevTools, tanpa dependensi, tidak di CI) mengukur gulir mendatar, ukuran target sentuh per ambang lebar, state sidebar, perilaku laci, dan kedua tema pada 375/768/1024/1440px **dengan satu project nyata** sehingga baris tabel ikut terukur: 0px gulir mendatar di keempat lebar, 0 kontrol di bawah ambang, dan enam pengukuran tema tanpa gulir. Gigi-nya dibuktikan dengan **enam cacat disuntikkan sementara** (keenamnya tertangkap) — dan justru dari situ lahir **C-068**: dua test baru tetap hijau sesudah perilaku yang diklaimnya dihapus, dan dua pemeriksaan di skrip baru itu sendiri lulus hampa (mengukur tetangga panel sebagai "konten", menutup laci sebelum melebarkan jendela). **C-067** (`51-UX.md` §8 menjanjikan sidebar "icon-only" yang tidak ada di layar) dan **C-069** (keputusan R-02 lebih luas daripada pemeriksaannya, sehingga tiga teks pengguna memang masih memuat em dash) juga ditutup: `51-UX.md` §8/§9 diselaraskan beserta alasannya, `check-antislop-refs.sh` mendapat **butir 8** yang menolak em dash pada teks yang dibaca pengguna, dan nilai kosong kini dijawab kalimat (`EMPTY_VALUE`/`EMPTY_DATE`). Audit menjadi **69 temuan / 67 FIXED / 0 APPROVED / 2 OPEN**, ditambah dua pertanyaan pemilik baru: **Q-025** (sejauh mana em dash disapu) dan **Q-026** (36px desktop vs 44px). Bukti: `make test` sembilan paket (236 test), frontend `typecheck`/`lint` bersih + **137 test / 16 berkas** + `vite build` 397 kB js, `ledger OK`, `BROKEN: 0`, `readme-facts OK — 39 fakta`, `api-contract OK`, `antislop-refs OK — 8 pemeriksaan`, `responsive-evidence OK`; database dev dikembalikan persis ke baseline (`audit_logs` 43, `login_attempts` 0, `projects` 0, `users` 1, versi skema 10). Log: `prompts/P-043-2026-09-22-antislop-frontend-dan-laci-menu.md`.
**Diperbarui sebelumnya:** agen (P-042) — **sistem aturan antislop diselaraskan dengan rilis resminya dan berhenti disalin (`T-054`).** Lima skill yang sejak P-037 didaftarkan `AGENTS.md` tetapi **tidak ada di disk** (**C-064**) kini terpasang byte-identik dari **tag rilis `v3.2.12`**, core di root diganti berkas pristine (sebelumnya **varian lama** yang berbeda dari salinan Gate di dokumen desain — **C-065**, dua versi core di satu repo), dan dua salinan isi aturan di `01-AGENT-WORKFRAME.md` §3.2/§5.3 **dihapus** lalu diganti penunjuk + keputusan proyek per nomor aturan (pola C-014: salinan yang tidak diperiksa mesin selalu kalah dari sumbernya). Pemeriksa baru `scripts/check-antislop-refs.sh` menahan kelasnya di CI — 7 pemeriksaan, termasuk `sha256` berkas pihak ketiga terhadap tabel provenans `skills/README.md` dan salinan core di `skills/antislop/SKILL.md` yang **wajib identik** dengan core di root; gigi dibuktikan dengan enam cacat disuntikkan sementara (keenamnya tertangkap). Keputusan dicatat **ADR-0025**: pin ke tag rilis, lisensi MIT, sumber aturan tunggal `antislop.md`, dan **amandemen butir 2 ADR-0006** — izin unduh agen itu **satu kali untuk sesi itu**, bukan aturan tetap. **Q-003 `RESOLVED`.** Tidak ada kode backend/frontend, migrasi, atau dependency yang disentuh. Sesi itu juga menemukan **C-066** (`FIXED`): pohon folder `README.md` §4 menyebut dua dari lima skrip yang sudah jalan di CI — kelas klaim **himpunan**, bukan angka, sehingga pemeriksa angka tidak menangkapnya; daftar itu kini dibandingkan dua arah mesin. Audit menjadi **64 FIXED dari 66 temuan** (0 APPROVED, 2 OPEN). Log: `prompts/P-042-2026-09-22-skill-antislop-terpasang-dan-dipin.md`.
**Diperbarui sebelumnya:** agen (P-041) — **halaman bisnis pertama berdiri: `Projects` (`T-053`), beserta lapisan data TanStack Query.** Verifikasi menyeluruh dijalankan lebih dulu dan hijau (sembilan paket backend / 236 test, frontend 88 test, empat pemeriksa dokumen), sehingga sesi ini mengerjakan pekerjaan baru, bukan perbaikan. Yang berdiri: `services/projects.ts` (satu tempat yang tahu bentuk `42-API.md` §3), `queries/{client,projects}.ts` (kunci kueri terpusat, invalidasi sesudah mutasi), primitive **`Dialog`** aksesibel yang dipakai dua dialog, halaman daftar (penyaring dari URL, paginasi dari `meta`, keadaan memuat/kosong/gagal dibedakan), form buat project (validasi `50-FSD.md` §3.2 **dan** pemetaan galat server per-field), dan halaman detail (metadata langsung dari server, anggota, tab yang menandai dirinya **belum** dibangun). Pustaka server-state yang dijanjikan `30-ARCHITECTURE.md` §2.1 untuk "halaman data pertama" dipakai di sini — dan karena ia sudah terpasang sejak P-037, sesi ini **tidak** menambah dependency maupun menyentuh jaringan. Cakupan data **tidak** disaring ulang di klien; status kanonik selalu lewat `types/status.ts` (ADR-0012). Bukti: `tsc --noEmit` + `eslint .` bersih, **129 test / 14 berkas** hijau (naik 41 test), `vite build` 395 kB js / 21,1 kB css, dan **halaman benar-benar dibuka di dev server** — dialog membuat project sungguhan (`PREVIEW-041`), peramban berpindah ke `/projects/<uuid>` hasil server, tombol **Arsipkan** berkonfirmasi lalu status baris berubah menjadi `Archived` tanpa reload, jaringan peramban memuat `401` → `POST /auth/refresh` `200` → retry `200` (single-flight refresh di peramban nyata), dan `audit_logs` mencatat `PROJECT_CREATED` + `PROJECT_ARCHIVED`; database dev dikembalikan persis ke baseline (`audit_logs` 43, `login_attempts` 0, `projects` 0, `users` 1, versi skema 10). Sesi itu **menemukan C-063** (`OPEN`) — `50-FSD.md` §3.2 menuntut dropdown `Owner` yang wajib padahal tidak ada endpoint yang dapat menyebutkan daftar pengguna — dan pertanyaannya dicatat **Q-024**; kelas temuan ini baru: **lapisan UI menggandakan janji dokumen**, sehingga cacat yang selama ini tidak terlihat muncul begitu ada yang mencoba memenuhinya. **Log sesi sebelumnya** (P-040) — **gap anotasi izin ditutup dan mekanisme rawatnya diganti mesin.** Papan `TASKS.md` hanya menyisakan pekerjaan milik pemilik (`T-050` lisensi — `BLOCKED` menunggu Q-023; C-050 threading — keputusan produk), jadi yang dikerjakan adalah **`T-024`**: sepuluh endpoint di `42-API.md` §5/§8/§11 yang belum punya izin — `GET|POST /workflows/definitions`, tiga endpoint notifications, dan lima endpoint admin — kini beranotasi langsung dari matriks `44-SECURITY.md` §3.1.2, sehingga **55 endpoint: 48 dengan baris `Izin:` di bloknya + 7 lewat tabel izin bab §4**, tanpa satu pun endpoint tanpa keterangan. Yang lebih penting dari angkanya: angka itu **tidak lagi dirawat manual**. Selama enam sesi "45/55" ditulis ulang dari ingatan dan pernah salah (**C-055**), dan pasangan izin karangan pernah lolos ke draf §4 (`document_version:read`, Q-016) — hanya ketahuan karena ada manusia yang membandingkan dua dokumen. `scripts/check-api-contract.sh` (baru, **`T-052`**) menggantinya: ia membaca matriks §3.1.2 sebagai sumber kebenaran (**44 pasangan**) dan menahan tiga kelas cacat — endpoint tanpa izin, pasangan yang tidak ada di matriks, dan route yang memakai pasangan di luar matriks (**18 pasangan** di `router.go`) — `api-contract OK — 110 pemeriksaan, 55 endpoint`. Pemeriksa itu **langsung menemukan cacat di pekerjaan sesi ini sendiri**: kalimat penjelas "matriks tidak punya `comment:update`" terbaca sebagai klaim pasangan; kalimatnya ditulis ulang dan aturan menulisnya dicatat di `02-AGENT-PROGRESS-PROTOCOL.md` §6.3. Gigi dibuktikan dengan tiga cacat sementara (hapus satu baris `Izin:`; tambahkan `document_version:read`; ubah `task:complete` → `task:finish` di salinan `router.go`) — ketiganya tertangkap, lalu dipulihkan. Verifikasi menyeluruh dijalankan lebih dulu dan hijau: `make test` sembilan paket (236 test), frontend 88 test + `vite build`, dan **empat** pemeriksa dokumen kini berjalan di CI. `44-SECURITY.md` **tidak** diubah — tidak ada pasangan izin baru. **Log sesi sebelumnya** (P-039) — **angka & versi di `README.md` kini diperiksa mesin terhadap repo.** Pemicunya rantai temuan yang sama diulang tiga kali: **C-060** (`51-UX.md` §3/§4 menyimpang dari arah desain), **C-061** (enam klaim basi di `README.md` — `frontend/` disebut kosong, jumlah route 30 padahal 32), lalu **C-062** (baris §2 "Teknologi" masih menulis **React 18** dan "Belum diinisialisasi"). C-061 diperbaiki dengan menyapu klaim basi memakai `grep`; C-062 membuktikan cara itu **tidak cukup**, karena baris yang salah tidak memuat kata kunci yang dicari dan enam tempat lain di berkas yang sama memuat versi yang benar sehingga pembacaan sekilas terasa konsisten. Yang menutup rantai itu bukan semangat lebih teliti, melainkan **pemeriksa**: `scripts/check-readme-facts.sh` (baru, `T-051`) menghitung setiap fakta dari **sumbernya** — `backend/internal/handler/router.go` untuk jumlah route, rincian per modul, dan endpoint tiap modul; `backend/go.mod` untuk versi Go/Gin/pgx/viper/goose; `frontend/package.json` untuk versi React/Vite/Tailwind/TypeScript; `backend/internal/migration/` untuk rentang `001`-`010`; dan keberadaan path untuk klaim "belum ada". 25 fakta, `readme-facts OK` (exit 0) atau `FAIL` beserta nomor barisnya. Dua pagar menjaganya tidak bokek diam-diam: setiap **group route wajib punya ember** (group baru menggagalkan pemeriksaan dua kali: belum terklasifikasi **dan** rincian ≠ total), dan **klaim yang polanya hilang dari README dianggap gagal** — pemeriksa yang berhenti memeriksa tanpa suara lebih berbahaya daripada yang berisik. Pemeriksa itu **gagal pada percobaan pertama** terhadap README yang sudah ada, dan kegagalan itu sendiri yang menemukan C-062. Gigi dibuktikan dengan lima cacat disuntikkan sementara (route 32→31; Project 8→9 endpoint; React 19→18; migrasi `010`→`009`; kalimat "frontend/ masih kosong") — **kelimanya tertangkap** — lalu README dipulihkan; pagar ember route diuji terpisah memakai salinan sementara `router.go` (+1 group `notifications`) tanpa menyentuh berkas proyek. Tersambung ke CI (langkah ketiga job `ledger`) dan ke lima dokumen aturan, termasuk aturan baru **`02-AGENT-PROGRESS-PROTOCOL.md` §6.2**. Bukti: `readme-facts OK — 25 fakta diperiksa`, `ledger OK — 0 peringatan, 236 test di backend`, `BROKEN referensi dokumen: 0`. Tidak ada kode produksi yang berubah. **Log sesi sebelumnya** (P-038) — **`README.md` mendapat bagian Kontribusi, dan status lisensi dicatat sebagai keputusan pemilik yang masih tertunda.** Sesi ini dimulai sebagai permintaan menambah berkas `LICENSE` + bagian kontribusi "setelah pemilik proyek memutuskan lisensinya"; karena tidak ada lisensi yang pernah ditetapkan di dokumen mana pun — `README.md` §12 bahkan menyatakan statusnya belum ditetapkan dan melarang menganggap proyek ini open source — agen **bertanya lebih dulu** alih-alih memilih sendiri, dan pemilik memilih **menunda** keputusan lisensi sambil tetap menjawab pemegang hak ciptanya (**BSA**, pribadi). Konsekuensinya: **berkas `LICENSE` tidak dibuat** (bahkan ketiadaannya ditampilkan di pohon `README.md` §4 dengan penanda "belum ada"), `frontend/package.json` tetap tanpa field `license`, dan keputusannya dicatat di **Q-023** bersama butir yang masih terbuka — jenis lisensi dan apakah kontribusi pihak ketiga diterima — dengan alasan yang dapat dibaca: penundaan tidak menghalangi pengembangan sama sekali, ia menahan **distribusi dan penerimaan kontribusi luar**. Yang dikerjakan pada sesi yang sama: **bagian Kontribusi `README.md` §12** (enam subbagian: alur satu perubahan, perintah verifikasi wajib beserta keluaran yang diharapkan, tabel sepuluh aturan yang tidak boleh dilanggar **dengan sumber masing-masing**, konvensi commit, dan kanal usulan yang ditulis jujur sebagai belum ada). Sesi itu juga menutup **C-061** — enam klaim basi di README, termasuk `frontend/` yang masih disebut kosong, `DESIGN.md` yang masih disebut placeholder, dua endpoint auth yang masih disebut "Belum ada" padahal hidup sejak P-034, dan jumlah route **30** yang seharusnya **32** (dihitung ulang dari `internal/handler/router.go`). Bukti: `check-ledger.sh` → `ledger OK`, `check-doc-links.sh` → `BROKEN: 0`. **Log sesi sebelumnya** (P-037) — **`DESIGN.md` diisi dan kerangka frontend berdiri: blocker UI yang sejak P-008 menghalangi Phase 4 akhirnya dibuka.** User menjawab dua pertanyaan yang selama ini menahan UI — **Q-001** dengan mode antislop **`during`** dan **Q-002** dengan **jalur 2 ADR-0007** (agen menyusun arah desain atas instruksi eksplisit) — dan keduanya dicatat sebagai **ADR-0006**/**ADR-0007** `ACCEPTED`, sehingga temuan **C-015** (`T-006`) `FIXED`. `DESIGN.md` kini `TERISI`: identitas, palet **dengan alasan per token**, tipografi, dials resmi (**ENERGY 1 / RHYTHM 2 / MOTION 1**), motif, tema terang/gelap, dan Design Read; statusnya jujur menyebut butir *rasa* sebagai draf agen sedangkan butir *struktural* sebagai keputusan yang dipertanggungjawabkan. Yang membuatnya bukan prosa: palet dan skala dikunci di `frontend/src/styles/tokens.css` dan **dihitung ulang test** (`src/styles/tokens.contrast.test.ts` — 31 test WCAG 2.2 AA per pasangan token, dan test itu punya gigi: mengubah `--color-ink-500` membuat 3 test gagal). **Kerangka frontend (`T-048`)** berdiri di atasnya — Vite 8 + React 19 + TS 5.9 + Tailwind v4 **tanpa `tailwind.config.js`** (CSS-first, ADR-0024), shell, primitives, lapisan API dengan pemetaan galat `42-API.md` §12, halaman Login yang benar-benar login, tema terang/gelap, plus Dashboard yang **hanya** menyebut modul yang sudah/belum ada alih-alih menampilkan angka karangan. Bukti: `typecheck` + `lint` bersih, **88 test / 10 berkas** hijau, `vite build` menghasilkan `dist/` (336 kB js, 19,5 kB css), dan halaman benar-benar dibuka di dev server. Sesi itu juga menemukan dan menutup **C-060**: `51-UX.md` §3/§4 masih memuat palet biru-slate dan `H1 28px` — dokumen yang paling sering dibuka untuk kerja halaman justru menyimpang dari arah desain yang baru ditetapkan. **Log sesi sebelumnya** (P-036) — **pemeriksa konsistensi ledger otomatis (`scripts/check-ledger.sh`) + CI, dan seluruh cacat yang ditemukannya diperbaiki.** Tiga wujud cacat yang tertahan sekarang: (a) angka audit yang disalin antar dokumen — kini dikunci marker `<!-- audit-summary ... -->` yang diperiksa mesin, dan angkanya diperbarui menjadi **59 temuan / 57 FIXED / 0 APPROVED / 2 OPEN** karena tiga temuan lama yang tampak rapi ternyata salah; (b) rujukan test yang sudah tidak ada di dokumen status dan ADR (lima rujukan, dua berkas); (c) papan `TASKS.md` yang bertentangan dengan dirinya sendiri (`T-044` di `TODO` padahal `DONE`, `T-006` di dua kolom, satu ID `T-008` untuk dua pekerjaan) — dicatat sebagai **C-058** dan **C-059**. Skrip juga memeriksa hitungan test di §3 dokumen ini terhadap `grep -c '^func Test'` (sekarang **236**), sehingga kelas **C-057** tidak dapat terulang tanpa disadari. CI `.github/workflows/ci.yml` menjalankannya bersama `check-doc-links.sh` dan `make test`. Bukti: log `docs/progress/prompts/P-036-2026-09-20-pemeriksa-konsistensi-ledger.md` §6 (termasuk dua cacat yang disuntikkan sementara untuk membuktikan skrip menangkapnya). **Log sesi sebelumnya** (P-035) — **bukti alur sesi & lock dijalankan ulang pada binari terkini** (tidak ada perubahan kode): seluruh klaim `T-040`/`T-041` bertahan, dan dua perilaku yang belum pernah diuji di HTTP kini tercatat — akun terkunci masih dapat memperpanjang sesinya lewat `POST /auth/refresh` (`200`), sedangkan sesudah `logout_all` refresh token lama ditolak `401 TOKEN_REVOKED`. Ringkasan `audit_logs` rentang probe (`LOGIN=6`, `LOGOUT_ALL=1`, `USER_UNLOCKED=1`) membuktikan C-035 dengan angka: enam login berhasil menghasilkan enam entri, sembilan percobaan gagal menghasilkan nol. Bukti: `70-TESTING.md` §3.12b; log `docs/progress/prompts/P-035-2026-09-20-probe-ulang-alur-sesi-dan-lock.md`. **Log sesi sebelumnya** (P-034) — **`T-034` dan `T-045` selesai: `POST /auth/change-password` berjalan, dan `POST /auth/refresh` menyusul pada sesi yang sama sesudah user menjawab Q-020 (ADR-0023).** Refresh token adalah JWT kedua bertanda klaim `typ` (access 24 jam, refresh 7 hari) tanpa penyimpanan di server; klaim `typ` wajib, sehingga refresh token tidak dapat dipakai sebagai bearer token dan access token tidak dapat ditukar di endpoint refresh. Bukti: `gofmt`/`vet` bersih, `make test` hijau (sembilan paket, 236 test), dan satu sesi probe HTTP nyata (login menyerahkan kedua token; dekode klaim = 24 jam/7 hari; refresh token sebagai bearer → `401 UNAUTHORIZED`; penukaran sah → `200` dengan pasangan baru; `logout_all` → refresh `401 TOKEN_REVOKED`), lalu database dev dikembalikan persis seperti semula. **Log sesi sebelumnya** (P-034, bagian pertama) — **`T-034` sebagian: `POST /auth/change-password` lebih dulu, sisanya dipindahkan eksplisit ke `T-045` karena bentuk tokennya belum diputuskan (Q-020).** Route terdaftar dengan benar-benar hanya autentikasi; urutannya mengikat (`old_password` diverifikasi lebih dulu, lalu hash + `tokens_invalid_before` + audit `PASSWORD_CHANGED` dalam satu transaksi, dan token pengganti diterbitkan **sesudah commit** supaya tidak terbit di detik yang sama dengan titik pencabutan — C-053). Bukti: lima test baru (tiga service + dua handler, termasuk `TestChangePasswordEndToEnd` yang menunggu 1,1 detik supaya yang diuji memang pencabutannya), `gofmt`/`vet` bersih, `make test` hijau (sembilan paket). Ditemukan sekaligus ditutup: **C-057** (angka test per berkas di `STATE.md` §3 menyimpang dari isi repo — kelas C-044/C-055). **Log sesi sebelumnya** (P-034 juga menandai) — **Log sesi sebelumnya** (P-032) — agen (P-032) — **`T-043` selesai: `meta.total` kini seragam di seluruh endpoint daftar (project, document, task) memakai tambalan yang sama dengan modul komentar, menutup temuan C-048.** Predikat daftar diangkat jadi variabel bersama (`projectListWhere`, `documentListWhere`, `taskListWhere`) sehingga jalur cepat `COUNT(*) OVER()` dan kueri hitung tidak dapat menyimpang, dan kueri hitung hanya jalan pada halaman kosong di luar halaman pertama. Bukti: tiga test "halaman di luar rentang" (satu per modul) plus demonstrasi test gagal (`total 0`) saat tambalan dinonaktifkan sementara. **Log sesi sebelumnya** (P-030) — agen (P-030) — **`T-040` + `T-041` selesai dalam satu sesi: pencabutan seluruh sesi (ADR-0021) dan auto-lock + `login_attempts` (ADR-0022) menutup temuan C-033, C-009, dan C-035 — kolom `APPROVED` di audit kini kosong.** `logout_all: true` benar-benar mencabut seluruh sesi lewat `users.tokens_invalid_before` (dipotong ke detik) **plus** `jti` request itu, sehingga tidak ada lagi jalur `501 NOT_IMPLEMENTED`; auto-lock memakai tabel `login_attempts` (setiap percobaan, berhasil **maupun** gagal, termasuk username yang tidak ada) dan kolom `users.locked_until`, dengan ambang `auth.max_login_attempts` dari `system_settings` sementara jendela 15 menit tetap konstanta kode, dan penghitung di memori `service/login_guard.go` **dihapus**. Endpoint barunya satu: `POST /api/v1/admin/users/:id/unlock` (izin `user:update`) — membuka lock lebih awal — idempoten dan hanya mengaudit saat ada penanda lock yang benar-benar dibersihkan. Bukti: `make test` hijau (sembilan paket, dijalankan tiga kali) + **satu sesi probe HTTP nyata 30 permintaan** pada binari yang dibangun ulang (logout per-token vs `logout_all`, login ulang pada detik yang sama, empat `401` lalu `423` + `Retry-After: 900`, password benar saat terkunci → `423`, Viewer `403`, unlock `200` dua kali dengan **satu** entri `USER_UNLOCKED`) dengan database dev dikembalikan persis seperti semula. Tiga temuan baru dicatat sekaligus ditutup: **C-052** (contoh `423` di `42-API.md` §12 bertentangan dengan prosanya), **C-053** (`NOW()` mentah menolak token hasil login ulang pada detik yang sama), **C-054** (pemicu `429` yang tidak lagi cocok), lalu **C-055** (angka audit yang tidak dapat diperiksa silang) dan **C-056** (database test tidak kembali kosong: `projectHTTPFixture` login lewat HTTP tanpa membersihkan `login_attempts`). **Log sesi sebelumnya** (P-029) — agen (P-029): **`T-039` selesai: arsip dokumen (ADR-0019) menutup temuan C-004.** Migrasi `010` (`010_session_revocation_login_attempts_document_archive.sql`) memuat isi yang dijadwalkan `41-DATABASE.md` §4: `documents.archived_at`, `CHECK` dokumen bertambah nilai kanonik `archived`, dan dua trigger append-only `document_versions` — **plus** skema ADR-0021/0022 (`users.tokens_invalid_before`, `users.locked_until`, tabel `login_attempts`), dimasukkan sekaligus karena berkas migrasi tidak boleh disunting setelah diterapkan; kodenya menyusul di `T-040`/`T-041`. Kode modul dokumen: `DELETE /documents/:id` **diganti** `POST /documents/:id/archive` (izin `document:update`), arsip hanya mengubah `status`+`archived_at` sehingga baris, versi, berkas, dan auditnya tetap, terarsip keluar dari daftar default (kembali lewat `?status=archived`), dan unggahan versi baru ditolak `409` **sebelum** berkas ditulis. Test: lima service + dua HTTP + delapan migrasi (`TestDocumentVersions_*`, `TestDocumentStatusVocabularyIncludesArchived`) hijau di `make test`; bukti server nyata 13 probe + 4 query `psql` (`versi_skema 10`) dengan database dev dikembalikan seperti semula. Satu temuan baru dicatat sekaligus ditutup (**C-051**): FK `document_versions.document_id` tetap `ON DELETE CASCADE`, tetapi kaskadenya kini **ditahan** trigger append-only, sehingga pembersihan dokumen berversi wajib lewat `SET LOCAL bwdcs.audit_maintenance = 'on'` — dicatat di `41-DATABASE.md` §2.3 dan diuji `TestDocumentVersions_DeleteFromDocumentsIsRejected`. Ringkasan: `70-TESTING.md` §3.12a. **Log sesi sebelumnya** (P-028): **Q-017 dikoreksi user**: rentang `GET /tasks?due_from=&due_to=` kini **kedua batas inklusif** `[due_from, due_to]` (butir (10) diputuskan ulang; sebelumnya setengah terbuka), sedangkan butir (11) (`bindJSON` menamai field pada `422`) **diverifikasi ulang tanpa perubahan kode** karena sudah dikerjakan P-026. Konsekuensi rentang inklusif (rentang bersebelahan dapat tumpang tindih) dinyatakan di `42-API.md` §6. **Log sesi sebelumnya** (P-027): agen `T-042` — **modul Comment**: lima endpoint `42-API.md` §7 (`GET|POST /api/v1/comments`, `GET|PATCH|DELETE /api/v1/comments/:id`) dengan cakupan yang **diturunkan dari entitas** (tabel `comments` tidak menyimpan `project_id`) dan edit/hapus dibatasi **kepemilikan**, bukan izin; terbukti pada server nyata termasuk perbandingan role Viewer (boleh berkomentar) versus `POST /projects` yang baginya `403`; tiga temuan baru dicatat (**C-048** `meta.total` pada halaman di luar rentang — ditambal di modul ini, sisanya `T-043`; **C-049** rute `/:entityType/:entityId` tidak dapat dipasang — bentuk kueri; **C-050** threading dijanjikan FSD §7 tanpa kolomnya — Q-019). **Log sesi sebelumnya** (P-026): agen perbaikan gap + riset best practice (P-026): **C-045** ditutup (`bindJSON` kini menamai field yang bermasalah untuk UUID/waktu/tipe/JSON rusak di body — berlaku lintas modul, terbukti pada `PATCH /tasks/:id` dan `POST /projects`), **C-046** ditutup (penyaring rentang `?due_from=`/`?due_to=` sebagai interval **setengah terbuka** ber-batas RFC 3339, melengkapi kelima penyaring `50-FSD.md` §6.1), dan **C-047** ditemukan sekaligus ditutup (tabel status fase `80-ROADMAP.md` tertinggal jauh; label fase modul Task diselaraskan ke Phase 3). **Dan sembilan temuan yang tersisa dijawab pada sesi yang sama** dengan dasar riset best practice di `OPEN-QUESTIONS.md` §3: **C-006/C-007/C-010/C-028 `FIXED`** dan **C-004/C-009/C-033/C-035 `APPROVED`** lewat **ADR-0019/0020/0021/0022** (kode menyusul di `T-039`/`T-040`/`T-041`); hanya **C-015** masih OPEN (milik user). **Log sesi sebelumnya** (P-025): agen `T-038` — modul **Task** (5 endpoint `42-API.md` §6) dengan **cakupan baris kedua** `44-SECURITY.md` §3.1.3: baca mengikuti keanggotaan project (Contributor/Viewer) atau seluruh organisasi (Manager/Administrator), tulis hanya task milik Contributor; penanda overdue tetap turunan ADR-0012 dan kini juga penyaring `?overdue=` di database. Dua temuan baru dicatat (**C-045** pesan `422` yang menyesatkan untuk UUID tidak sah di body, **C-046** penyaring rentang tanggal di FSD tanpa kontrak) dan sebelas kontrak modul task diputuskan di **Q-017**. **Log sesi sebelumnya** (P-024): agen `T-036` — **database test terpisah `bwdcs_test`** (menutup C-038): `make test` menurunkan `TEST_DATABASE_URL` dari `.env`, menolak DSN yang menunjuk database dev, dan suite hijau sementara database dev berisi project nyata; dua temuan baru (C-043 salinan Makefile di `60-DEPLOYMENT.md` §3.1, C-044 hitungan audit 31 vs 32) ditutup di sesi yang sama. **Log sesi sebelumnya** (P-023): agen `T-037` — modul **Document** (7 endpoint `42-API.md` §4: metadata, unggah versi, daftar versi, unduh ber-audit, hapus berkaskade) dengan generator `document_number` `{PROJECT_CODE}-{NNN}` **di dalam transaksi** (ADR-0017) dan cakupan data yang sama dengan project (`44-SECURITY.md` §3.1.3), dibuktikan pada server nyata; delapan kontrak yang diputuskan agen dicatat sebagai Q-016, dan empat temuan baru (C-039..C-042) ditemukan serta ditutup di sesi yang sama; **P-024** menutup C-038 (database test terpisah) beserta C-043/C-044
**Titik masuk resume:** `CONTINUE.md` (root repo). Snapshot §0 di file itu harus selaras dengan dokumen ini; bila berbeda, dokumen ini yang berlaku.

> Agen WAJIB memperbarui file ini di akhir setiap sesi. File ini menggambarkan **kondisi sekarang**, bukan riwayat. Riwayat ada di `SESSION-LOG.md`.

---

## 1. Ringkasan Posisi

<!-- audit-summary total=82 fixed=80 approved=0 open=2 rejected=0 rinci=37 -->

| Aspek | Nilai |
|---|---|
| Fase roadmap | **Phase 0 selesai; Phase 1 berjalan; Phase 3 (Task + Comment) selesai dikerjakan lebih awal; Phase 4 (UI) dimulai P-037 dengan shell + Login + Dashboard, dan sejak P-041 halaman bisnis pertama (**Projects**) berdiri; Phase 2 (Workflow) belum.** Sumber urutan fase adalah `80-ROADMAP.md` **§1**, dan §3 dokumen itu yang melaporkan status per fase — tabelnya diperbaiki pada P-026 lewat temuan **C-047** (sebelumnya masih menulis Phase 0/1 "Belum dimulai" dan melabeli Task sebagai Phase 1). Sudah hidup: `git init` + struktur repo + skeleton backend, migrasi `001`-`009` + bootstrap admin (P-020), auth + RBAC (`T-005`, P-021), **modul Project** (`T-035`, P-022), **modul Document** (`T-037`, P-023), **modul Task** (`T-038`, P-025), dan **modul Comment** (`T-042`, P-027 — Phase 3): **25 endpoint** dengan cakupan data di kueri (task memakai **dua baris** cakupan §3.1.3 sekaligus; komentar menurunkannya dari entitas yang dikomentari). Urutan berikutnya dicatat di `OPEN-QUESTIONS.md` **Q-018** (asumsi P-026 bahwa Comment lebih dulu sudah dijalankan; sisa pilihannya **Workflow** Phase 2 atau modul admin/notification). Koreksi rujukan lama: `T-006`/`T-007` adalah task UI/`DESIGN.md`, **bukan** modul backend — nomor task modul backend adalah `T-035`/`T-037`/`T-038` |
| Kode aplikasi | **Ada dan berjalan, auth lengkap.** `backend/` (module Go `bwdcs/backend`): `internal/config`, `internal/pkg/{filestorage,response,jwt}`, `internal/model`, `internal/dto`, `internal/repository` (user, `token_revocations`, `system_settings`), `internal/service` (auth, `PermissionChecker`, `AuditService`, `UserService` — `LoginGuard` **dihapus** P-030), `internal/middleware` (Auth, RequirePermission, RateLimit, CorrelationID, Logger, CORS), `internal/handler` (health, auth, `router.go`), `internal/migration` (ADR-0018), `internal/bootstrap` (ADR-0010), `cmd/server/main.go`, `Makefile`. Endpoint hidup: `GET /health`, `POST /api/v1/auth/login`, `POST /api/v1/auth/logout`, `GET /api/v1/auth/me`, dan **delapan endpoint modul project** (`GET|POST /api/v1/projects`, `GET|PATCH /api/v1/projects/:id`, `POST /api/v1/projects/:id/archive`, `GET|POST /api/v1/projects/:id/members`, `DELETE /api/v1/projects/:id/members/:userId`) — P-022. Tambahan paket: `internal/service/project_service.go`, `internal/repository/project_repository.go`, `internal/model/project.go`, `internal/dto/project_dto.go`, `internal/handler/project_handler.go`. **Modul document hidup sejak P-023** (`T-037`), **ber-arsip sejak P-029** (`T-039`): tujuh endpoint `42-API.md` §4 (`GET|POST /api/v1/documents`, `GET /api/v1/documents/:id`, `POST /:id/archive`, `GET /:id/versions`, `POST /:id/upload`, `GET /:id/download/:versionId`) dengan paket `internal/service/{document_service,document_service_upload,scope}.go`, `internal/repository/document_repository.go`, `internal/model/document.go`, `internal/dto/document_dto.go`, `internal/handler/document_handler.go`; berkas disimpan lewat `internal/pkg/filestorage` (ADR-0005). **Modul task hidup sejak P-025** (`T-038`): lima endpoint `42-API.md` §6 (`GET|POST /api/v1/tasks`, `GET|PATCH /api/v1/tasks/:id`, `POST /api/v1/tasks/:id/complete`) dengan `internal/service/task_service.go`, `internal/repository/task_repository.go`, `internal/model/task.go`, `internal/dto/task_dto.go`, `internal/handler/task_handler.go`. **Modul komentar hidup sejak P-027** (`T-042`): lima endpoint `42-API.md` §7 (`GET|POST /api/v1/comments`, `GET|PATCH|DELETE /api/v1/comments/:id`) dengan `internal/service/comment_service.go`, `internal/repository/comment_repository.go` (pemetaan entitas → project), `internal/model/comment.go`, `internal/dto/comment_dto.go`, `internal/handler/comment_handler.go`. **Sejak P-030** logout benar-benar mencabut seluruh sesi (`logout_all` → `users.tokens_invalid_before` + `jti`) dan `/admin/users/:id/unlock` hidup (`UserHandler` + `UserService`). **Modul workflow hidup sejak P-048** (`T-064`): sembilan endpoint `/api/v1/workflows/*` (§5) dengan `internal/model/workflow.go`, `internal/dto/workflow_dto.go`, `internal/repository/workflow_repository.go`, `internal/service/workflow_service.go`, dan `internal/handler/workflow_handler.go`. Belum ada: modul notification, audit, report, dan admin (hanya `unlock` yang hidup). **`frontend/` sudah berjalan sejak P-037** dengan tiga halaman bisnis berdiri (Projects sejak P-041, Documents sejak P-044, Tasks sejak P-046); halaman Approvals, Reports, dan Administration belum dibangun — Approvals menunggu `GET /workflows/instances` dari modul Workflow (Phase 2). **Kedua kontrak auth yang tertunda kini berjalan (P-034): `POST /api/v1/auth/change-password` dan `POST /api/v1/auth/refresh`** — login juga mengembalikan `refresh_token` + `refresh_expires_at` (ADR-0023). **Tidak ada lagi** jalur hapus dokumen: arsip (`T-039`) menggantikannya, dan `document_versions` sudah append-only |
| Skema database | **Terpasang pada `bwdcs` (PostgreSQL 16.10, port 5432).** Versi skema goose **11**; **23 tabel** (22 tabel `41-DATABASE.md` §2 + `goose_db_version`), 104 baris `role_permissions` (44/30/18/12), 2 trigger append-only `audit_logs`, FK `fk_documents_workflow_instance`, 5 baris `system_settings`. Dijalankan otomatis saat startup dari berkas yang di-embed (ADR-0018) |
| Blocker utama | **Tidak ada.** Blocker UI hilang pada P-037: user menjawab **Q-001** (mode antislop **`during`**, ADR-0006 `ACCEPTED`) dan **Q-002** (agen menyusun arah desain, ADR-0007 `ACCEPTED` jalur 2), sehingga `DESIGN.md` terisi dan UI boleh dibangun. Q-013/Q-014/Q-020 sudah selesai dijalankan lewat ADR-0021/0022/0023. Sejak **P-042** tidak ada lagi pertanyaan teknis yang menggantung di sistem kerja: **Q-003** (skill antislop) `RESOLVED` — kelima skill terpasang dipin ke tag rilis, dan izin unduh agen dicatat sebagai peristiwa satu kali (ADR-0025). Blocker yang tersisa hanya **keputusan produk**: Q-019 (threading komentar, temuan C-050) dan **Q-023** (lisensi proyek — menahan distribusi/penerimaan kontribusi luar, tetapi **tidak** menghalangi pengembangan: build, test, dan migrasi tidak bergantung padanya) |
| Audit terbuka | `docs/progress/audits/AUDIT-001-2026-09-17-kontradiksi-dokumen.md` — **82 temuan: 80 FIXED, 0 APPROVED, 2 OPEN** (80 + 0 + 2 = 82). Angka ini **dikunci marker mesin** dan diperiksa `bash scripts/check-ledger.sh`, bukan dihitung dari ingatan; jalankan skrip itu alih-alih mempercayai baris ini; kesalahan jenis itu sudah **tiga** kali terjadi: C-044, C-055, dan C-057). Kolom `APPROVED` **kosong sejak P-030**: C-004 (ADR-0019 → `T-039`), C-033 (ADR-0021 → `T-040`), dan C-009 + C-035 (ADR-0022 → `T-041`) sudah punya kode **dan** test, jadi tidak ada lagi temuan "keputusan ada, kode belum". Yang masih `OPEN`: **C-050** (threading dijanjikan `50-FSD.md` §7 tanpa kolom induk di skema — Q-019, rekomendasi ditunda) dan **C-063** (`50-FSD.md` §3.2 menuntut dropdown `Owner` yang wajib padahal tidak ada endpoint yang dapat menyebutkan daftar pengguna — `GET /admin/users` belum ada dan `user:read` hanya milik Administrator; Q-024). Temuan itu **tidak terlihat dari membaca kontrak** — `42-API.md` §3 tidak pernah menjanjikan endpoint pengguna; yang menjanjikannya FSD, dan hanya terasa ketika formnya harus diisi pada P-041. **C-015 `FIXED` pada P-037** sesudah user menjawab Q-001 (mode antislop `during`) dan Q-002 (jalur 2 ADR-0007) — `DESIGN.md` terisi dan ADR-0006/0007 `ACCEPTED` — dan temuan yang ikut ketahuan, **C-060** (`51-UX.md` §3/§4 masih memuat palet biru-slate + `H1 28px` yang bertentangan dengan arah desain yang baru ditetapkan), ditutup di sesi yang sama. Diputuskan pada P-026 dengan dasar riset best practice di `OPEN-QUESTIONS.md` §3: C-006 (Reviewer = peran fungsional), C-007 (dua hierarki role, tidak digabung), C-010 (`responsible_user_id` ditunda; MVP role-based), C-028 (retensi audit = operasi pemeliharaan, ADR-0020) — keempatnya `FIXED`. FIXED lainnya: C-001..C-003, C-019 (P-008), C-017, C-008 (P-009) lewat ADR-0011..0014, C-014 (P-010), C-011, C-013 (P-011), C-012 (P-012), C-005, C-021, C-023 (P-013) lewat ADR-0015, C-022 (P-014) lewat ADR-0016, C-018 (P-015), C-016 lewat ADR-0017 dan C-024 (P-016), C-025 (P-017), C-026 + C-027 (P-018), C-020 (P-019), C-029 + C-030 + C-031 dan C-032 lewat ADR-0018 (P-020), C-034 + C-036 + C-037 (P-021), C-039 + C-040 + C-041 + C-042 (P-023), C-038 + C-043 + C-044 (P-024), C-045 + C-046 + C-047 (P-026), **C-049** (P-027), **C-048** (P-032). **C-051** ditambahkan & ditutup pada **P-029** (`T-039` — FK kaskade `document_versions` tetap ada tetapi kaskadenya tertahan trigger append-only; pembersihannya lewat jalur pemeliharaan). Ditemukan saat mengerjakan: C-021..C-023 (C-005), C-024 (C-016), C-025 (`T-028`), C-026/C-027 (`T-003`), C-028 (C-020), C-029..C-032 (`T-004`), C-033..C-037 (`T-005`), **C-038** (`T-035` — test memakai database dev yang sama, gagal begitu ada project nyata), **C-039..C-042** (`T-037` — muncul dari **menjalankan** test dan dari memeriksa sumber sebelum menerapkan aturan), **C-045**/**C-046** (`T-038` — dari uji HTTP nyata dan dari mencocokkan FSD dengan kontrak), **C-047** (P-026 — dari memeriksa ulang ledger terhadap dokumen desain), **C-048**/**C-049**/**C-050** (`T-042` — berturut-turut dari test paginasi sendiri, dari probe registrasi rute sebelum handler ditulis, dan dari mencocokkan field `50-FSD.md` §7 dengan kolom tabel `comments`) |
| Konvensi yang sudah terkunci sebelum kode pertama | ADR-0011 (audit ditulis di service, dalam satu transaksi) · ADR-0012 (kolom `status` hanya nilai kanonik; "overdue" turunan) · ADR-0013 (`40-TSD.md` §2.0 = sumber tunggal struktur backend, flat, satu folder = satu package) · ADR-0014 (`44-SECURITY.md` §3.1 = sumber tunggal matriks permission, seed `008` = 104 baris) · ADR-0015 (transisi `workflow_instances` hanya lewat conditional `UPDATE` dengan `version` + `status = 'running'` + `current_step`; `rowsAffected = 0` = rollback + `409 WORKFLOW_CONFLICT`, tanpa retry) · ADR-0016 (`request_revision` kembali ke step sebelumnya, batas bawah step 1; instance tetap `running`; re-submit melanjutkan instance yang sama) · ADR-0017 (`document_number` selalu dibangkitkan server `{PROJECT_CODE}-{NNN}`, atomik di `document_sequences`, immutable, tanpa penomoran manual; `projects.code` permanen) · append-only `audit_logs` (`44-SECURITY.md` §6, dipasang migrasi `007`): trigger menolak `UPDATE`/`DELETE`/`TRUNCATE` dengan SQLSTATE `23001`, hanya `INSERT` yang lolos, dengan satu jalur pemeliharaan eksplisit `bwdcs.audit_maintenance` (`SET LOCAL`, per transaksi). **Sejak P-029** pasangan trigger yang sama juga menutup `document_versions` (migrasi `010`, ADR-0019, `44-SECURITY.md` §6.1) — satu-satunya jalur keluar tetap `SET LOCAL bwdcs.audit_maintenance = 'on'`, dan konsekuensinya yang penting: **`DELETE FROM documents` pun tertahan** kalau dokumennya punya versi, karena kaskadenya melewati trigger itu · ADR-0018 (migrasi di-embed dan dijalankan aplikasi saat startup oleh package kecil di `internal/migration`; goose library **dan** CLI dipin v3.24.1; berkas `.sql` tetap sumber kebenaran skema) |
| Skema workflow | `workflow_instances` memuat `version INTEGER NOT NULL DEFAULT 0` dan `current_step_deadline TIMESTAMP WITH TIME ZONE` (keduanya masuk migrasi `005_create_workflows.sql`, ADR-0015). Overdue step tetap turunan dari `current_step_deadline`, tidak pernah menjadi nilai `status` |
| Skema dokumen | `documents.document_number` dibangkitkan server `{PROJECT_CODE}-{NNN}` lewat tabel `document_sequences` (keduanya masuk migrasi `004_create_documents.sql`, ADR-0017); nomor immutable, tidak pernah dipakai ulang, dan `projects.code` tidak dapat diubah setelah dibuat. **Skema ini kini dipakai penuh sejak P-023**: `documents` (status `draft` → **enam** nilai kanonik termasuk `archived` sejak migrasi `010`), `document_versions` (versi `major.minor`, checksum SHA-256 64 heksadesimal, `file_key` ADR-0005), dan `document_categories` (hanya dirujuk; belum ada endpoint kategori) |
| Atribusi error body | `bindJSON` (`internal/handler/project_handler.go`, dipakai **semua** modul) membedakan tiga sebab dan selalu menamai field: tipe salah (`json.UnmarshalTypeError`), JSON rusak (`json.SyntaxError`/`json.Valid` → `body`), JSON sah dengan nilai yang tidak dapat diurai (field dicari lewat `undecodableField`/reflection, pesan dari bentuk tipe: "harus UUID yang sah", "harus waktu RFC 3339 yang sah", "harus tanggal yang sah"). Kontrak pemetaannya di `42-API.md` §12; sebelumnya ketiganya dijawab "`body` / harus JSON objek yang sah" (temuan **C-045**, FIXED P-026) |
| Penyaring rentang tanggal | `?due_from=`/`?due_to=` pada `GET /tasks`: interval **tertutup** `[due_from, due_to]` — **kedua batas inklusif** (`P-028`, keputusan user; sebelumnya setengah terbuka) — ber-batas RFC 3339 (offset eksplisit), karena `+` sampai ke handler sebagai spasi maka `parseRFC3339Query` menerima `+07:00` maupun `%2B07:00`; rentang terbalik/berdiri di satu titik → `422` pada `due_to`; task tanpa `due_date` tidak masuk rentang (C-046, FIXED P-026) |
| Cakupan data | Dua penyusun cakupan di satu tempat, `internal/service/scope.go`. **(a)** `systemScope` → `repository.ProjectScope`, dipakai project **dan** document lewat `projectScopePredicate` di `WHERE`. **(b)** `taskScope` → `repository.TaskScope`, dipakai `taskReadPredicate`/`taskWritePredicate` — modul pertama yang memakai **dua baris** §3.1.3 sekaligus (baca ≠ tulis): Contributor/Viewer membaca task miliknya dan task pada project yang diikutinya sementara Manager/Administrator seluruh organisasi, sedangkan penulisan lebih sempit (Contributor hanya task yang ditugaskan kepadanya atau dibuatnya). Seluruh pembeda role dikirim sebagai **parameter** ke kueri, bukan dirangkai ke SQL. Pelanggaran cakupan → `404`, bukan `403` (`44-SECURITY.md` §3.1.3) — termasuk kasus "boleh dibaca karena anggota project, tetapi tidak boleh diubah". **(c)** Modul komentar memakai `systemScope` yang sama, tetapi barisnya **tidak menyimpan `project_id`**: cakupan hanya dapat ditegakkan dengan menurunkan project dari entitas yang dikomentari lewat **satu** pemetaan (`commentEntityProjectCase` di `comment_repository.go`: project itu sendiri, `documents.project_id`, `tasks.project_id`, atau `workflow_instances.document_id` → `documents.project_id`). Aturan **kedua** modul itu kepemilikan, bukan cakupan — `WHERE created_by_id = actor` pada `PATCH`/`DELETE`, sehingga anggota project lain boleh **membaca** tetapi tidak boleh mengubah (`404`). Pola inilah yang wajib dipakai modul berikutnya (notification) |
| Aturan versi dokumen | Versi berikutnya ditentukan server (`model.NextVersion`): `1.0` pada unggahan pertama, minor naik pada unggahan biasa, dan **major naik (`2.0`) bila `documents.status = revision_required`** — itulah wujud "major based on revision" `50-FSD.md` §4.2 tanpa input jenis versi dari klien (Q-016 butir 1). Versi lama tidak pernah ditimpa (FR-VER-03) |
| Aturan modul komentar | `entity_type` = kosakata **tertutup** `project`/`document`/`task`/`workflow` (sama dengan `CHECK` kolomnya; `workflow_instance` **ditolak** `422`; hanya besar-kecil huruf + spasi tepi yang dinormalkan). `workflow` berarti **workflow instance** (`workflow_instances.id`), projectnya lewat dokumen instance itu. Isi komentar wajib, ≤ **2000 karakter** per rune setelah spasi tepi dibuang, dan aturannya hidup di `validateCommentContent` supaya `POST`/`PATCH` tidak berbeda. Daftar memakai **bentuk kueri** `GET /comments?entity_type=&entity_id=&page=&limit=` (`entity_type`/`entity_id` **wajib**); bentuk `/:entityType/:entityId` tidak dapat dipasang bersama `/:id` — Gin panik saat registrasi (C-049). Izin hanya `comment:read` (`GET`, `PATCH`, `DELETE`) dan `comment:create` (`POST`), keduanya untuk **semua** role termasuk Viewer; matriks **tidak** memuat `comment:update`/`comment:delete` dan menambahkannya berarti mengubah matriks tanpa ADR. Edit/hapus = kepemilikan di `WHERE` (`404` untuk komentar orang lain). Balasan ber-thread **belum ada** (C-050/Q-019); komentar dihapus **permanen** (bukan arsip seperti dokumen). Audit: `COMMENT_CREATED`/`COMMENT_UPDATED`/`COMMENT_DELETED` ber-`entity = comment`, metadata memuat `entity_type`, `entity_id`, dan `content_size` (+`content_size_before`) — **tanpa** menyalin isi komentar |
| Aturan modul task | Status kanonik `open`/`in_progress`/`completed` dan prioritas `low`/`medium`/`high`/`urgent` (`model.TaskStatuses`/`TaskPriorities`; nilai dinormalkan sebelum diperiksa). Task **selalu lahir `open`** (`status` dari klien → `422`); `assignee_id` dan `due_date` wajib; `priority` kosong → default kolom `medium`. Transisi hanya tiga aksi `50-FSD.md` §6.3: Start (`open` → `in_progress`) dan Reopen (`completed` → `open`) lewat `PATCH` (`task:update`), Complete lewat **`POST /tasks/:id/complete`** (`task:complete`) dan hanya dari `in_progress` — `in_progress` → `completed` lewat `PATCH` sengaja `409`. `project_id` tidak dapat diubah (`409`). `?overdue=` tri-state (`true`/`false`/tidak dikirim) disaring di `WHERE` sebelum paginasi. Audit: `TASK_CREATED` (assignee awal di metadata), `TASK_UPDATED`, `TASK_ASSIGNED` (hanya bila assignee berubah), `TASK_COMPLETED` — semuanya di transaksi yang sama (ADR-0011). Rincian: `42-API.md` §6 |
| Aturan modul workflow | Instance **selalu lahir `version 0`** di step 1; setiap transisinya hanya lewat conditional `UPDATE` ber-guard **empat** kondisi (id, `version`, `status = running`, `current_step`) — `rowsAffected = 0` → rollback + `409 WORKFLOW_CONFLICT` ber-`details` objek (ADR-0015). Aksi step menuntut **dua** syarat sekaligus: `role` penanggung jawab step **dan** izin `workflow_instance:<action>` — Administrator **tidak** otomatis menjadi penanggung jawab step (C-073). Izin aksi hanya diperiksa di **service** (`POST /workflows/instances/:id/actions` memasang `workflow_instance:read` di route), satu tempat saja. `request_revision` mundur **satu** step (batas bawah step 1) dan instance **tetap `running`**, sehingga penolakan selama jeda revisi wajib membaca `documents.status = 'revision_required'`. Dua jalur masuk review tidak pernah hidup bersamaan (`/workflows/submit` hanya `draft` tanpa instance; `/workflows/instances/:id/resubmit` hanya `revision_required` ber-instance), dan re-submit **tidak** menulis `workflow_actions`, tidak membuat instance baru, dan tidak mengubah definisi. Keterlambatan step adalah **turunan** (`is_overdue` dari `current_step_deadline`, ADR-0012) — tidak ada status `overdue`. Definisi hanya di `/workflows/definitions`; `responsible_user_id` per user tetap tidak ada (MVP role-based, P-026). Bukti: `scripts/probe-workflow-module.py` (46 asersi) |
| Task aktif | Sesi P-051 selesai: **`T-067` dan `T-068` DONE** — halaman anak mendapat jalan masuk lewat `subPages`, dan aturan §2.1 kini dijaga mesin; empat temuan lahir dan ditutup di sesi yang sama (**C-079** aturan itu tidak dijaga apa pun, **C-080** penegak barunya melaporkan sebab yang salah, **C-081** total di dalam laporan audit tertinggal tiga sesi, **C-082** suite gagal berpindah antar-berkas karena jendela tunggu bawaan menguji mesin). Sesi P-050 sebelumnya: **`T-066` DONE** — pengukuran tata letak per lebar kini mencakup **setiap** halaman, dan perluasannya menutup **C-076** (kolom penyaring wajib dapat menyusut), **C-077** (ambang sentuh adalah kotak, bukan garis), dan **C-078** (lima cacat pada alat ukurnya sendiri, termasuk cakupan yang tidak pernah meluas dan argumen CLI yang diam-diam diabaikan). Sebelumnya P-049: **`T-065` DONE** — penyaring **rentang tenggat** di halaman Tasks disatukan menjadi satu kelompok berlabel, sehingga kedua batasnya tidak dapat terpisah baris pada lebar lebar dan tetap terbaca sebagai satu rentang pada lebar sempit; klaimnya **diukur** (`responsive-evidence`), gigi tiga butirnya dibuktikan dengan memasang kembali bentuk lamanya, dan satu cacat pada alat ukur itu sendiri (**kelas C-075**) ditutup. Sebelumnya P-048: **`T-064` DONE** — **modul Workflow hidup** (sembilan endpoint `42-API.md` §5: definisi + step, submit, aksi `approve`/`reject`/`request_revision`, re-submit, daftar/detail instance ber-cakupan), sehingga **Phase 2 tidak lagi punya modul yang menggantung**; 29 test, **270 test backend**, dan probe **46/46 PASS** di server nyata. Dua temuan audit ditutup di sesi yang sama: **C-073** (pseudokode §4.1 memberi Administrator jalan pintas `OR be admin` yang tidak ada di dokumen pengikat) dan **C-074** (`42-API.md` §5 menyebut "satu-satunya route" untuk himpunan yang dokumen lain sebut dua). Sebelumnya P-047: **`T-063` DONE** — sidebar kembali menjadi daftar modul (satu tautan per modul) dan baris penyaring Task kembali sebaris, keduanya **diukur di peramban** oleh bagian `navigation` `scripts/responsive-evidence.mjs` yang baru (`T-058` diperluas) dan `51-UX.md` §2.1 diselaraskan; frontend **240 test / 24 berkas**. Tidak ada temuan audit baru. Sebelumnya P-046: **`T-062` DONE** — halaman bisnis ketiga (**Tasks**): daftar ber-penyaring hidup di URL (sub-halaman view, prioritas, penanggung jawab, rentang tenggat RFC 3339 inklusif, **tri-state overdue**), dialog buat dari anggota project, dan detail dengan tabel transisi §6.3 plus tombol menurut status; seluruh klaimnya dibuktikan probe **41/41 PASS** di server nyata (`scripts/probe-task-module.py`, `versi_skema 11`, baseline pulih otomatis). Sebelumnya P-045: **`T-061` DONE** (`C-072` MIME unggahan) plus penyelesaian ledger P-044 (`T-059`/`T-060` — halaman **Documents**). Sebelumnya P-043 selesai: **`T-056`/`T-057`/`T-058` DONE** — aturan antislop diterapkan pada frontend, laci menu menjadi lapisan modal yang sungguhan, dan klaim tata letaknya kini **diukur mesin** (`scripts/responsive-evidence.mjs`), dengan **C-067/C-068/C-069** (`FIXED`) dicatat dari sesi itu. Sebelumnya P-041: **`T-053` DONE** — halaman bisnis pertama (**Projects**) beserta lapisan data TanStack Query, dan **temuan `C-063` (`OPEN`, Q-024)** dicatat dari sesi itu. Tidak ada task yang dapat dikerjakan agen tanpa keputusan pemilik; berikutnya adalah halaman bisnis berikutnya (Documents paling dekat) atau kembali ke backend (Workflow). Sebelumnya P-032: **`T-043` DONE** — `meta.total` seragam di seluruh endpoint daftar, **C-048 `FIXED`**. Sebelumnya P-030: **`T-040` + `T-041` DONE** (pencabutan seluruh sesi ADR-0021 + auto-lock & `login_attempts` ADR-0022), sehingga **C-033, C-009, dan C-035 `FIXED`** dan kolom `APPROVED` di audit kosong. Sebelumnya: sesi P-029: **`T-039` DONE** (arsip dokumen + trigger `document_versions` + migrasi `010`; C-004 `FIXED`). Sebelumnya: P-028: **Q-017 butir (10) dikoreksi** (rentang tertutup) + butir (11) diverifikasi ulang; `T-044` DONE. Sebelumnya: P-026: perbaikan gap (C-045 + C-046 + C-047) **dan** sembilan temuan lama diputuskan lewat ADR-0019/0020/0021/0022 (C-006/C-007/C-010/C-028 `FIXED`). Sebelumnya P-025: `T-038` (modul task); P-024: `T-036` (database test `bwdcs_test`); P-023: `T-037` (document); P-022: `T-035` (project); P-021: `T-005` + `T-033`; P-020: `T-004` + ADR-0018. **Tidak ada lagi tugas yang tertunda karena kode**: ketiga implementasi ADR sudah selesai. Berikutnya: halaman **Approvals** di frontend (`50-FSD.md` §5.4 — kontraknya kini hidup, jadi tidak ada lagi penunggu) dan modul **Notification**/**Audit**/**Report**/**admin** di backend (`42-API.md` §8-§11). Anotasi izin endpoint **sudah tuntas** dan dijaga mesin (`scripts/check-api-contract.sh`), jadi angkanya tidak dirawat manual. |
| Kontrak API | **`42-API.md` adalah sumber tunggal daftar endpoint** (**55** judul endpoint, 13 bab — angka dihitung langsung dari judul `### METHOD /path` pada P-027, sebelumnya tertulis 51/52; termasuk **§6 Tasks yang ditulis penuh pada P-025** dan **§7 Comments pada P-027**: izin per endpoint, tabel transisi status, penyaring, dan kode error; termasuk daftar `GET /workflows/instances` untuk halaman Approvals dan `POST /workflows/instances/:id/resubmit` untuk melanjutkan review setelah revisi). `40-TSD.md` §6 hanya contoh pemasangan route. Definisi workflow hanya di `/workflows/definitions` (izin `workflow_definition:manage`), bukan prefiks `/admin` (temuan C-011/C-013). Bab: §9 Audit, **§10 Reports**, §11 Administration, §12 Error Responses, §13 Swagger |
| Konfigurasi runtime | **Satu sumber:** kontrak di `60-DEPLOYMENT.md` §2/§2.1, cermin yang dapat dieksekusi `.env.example`, compose di `docker-compose.yml`. Tiga dokumen (`90-AGENT-GUIDE` §7, `30-ARCHITECTURE` §5.1, `12-DEVELOPMENT-WORKFLOW` §7) hanya menunjuk — tidak ada daftar variabel atau YAML compose yang disalin (temuan C-014). Sejak P-024 **tidak ada lagi salinan `Makefile` di dokumen**: `60-DEPLOYMENT.md` §3.1 menunjuk `backend/Makefile` sebagai sumber tunggal (temuan C-043 / kelas C-014) |
| Konfigurasi test | **Database test terpisah `bwdcs_test`** (owner role `bwdcs`, dibuat P-024 dengan izin user; role aplikasi tidak diberi `CREATEDB`). `TEST_DATABASE_URL` **diturunkan** `backend/Makefile` dari kredensial `.env` (`TEST_DB_NAME` mengubah nama), dijalankan `-count=1`, dan `make test` **menolak** DSN yang menunjuk database dev. Skema database test dimigrasikan otomatis oleh `TestMain` (ADR-0018). Kontrak: `70-TESTING.md` §8/§8.1 |
| Next action | **Halaman bisnis berikutnya: Approvals** (`50-FSD.md` §5.4) — penunggunya sudah hilang, karena `GET /workflows/instances`, `GET /workflows/instances/:id`, `POST /workflows/instances/:id/actions`, dan `POST /workflows/instances/:id/resubmit` hidup sejak P-048 dengan pola lapisan data yang sudah terbukti di empat halaman sebelumnya. Sesudah itu **Reports** (`GET /reports/export`, `42-API.md` §10) dan **Administration** (`42-API.md` §11) — keduanya masih menunggu backend. Jalur backend yang belum tersentuh tinggal modul pendukung: **Notification** (`42-API.md` §8 — tabelnya sudah dipakai workflow tetapi handler-nya belum), **Audit** (§9), **Report** (§10), dan **admin** selain `unlock` (§11). Anotasi izin endpoint **sudah tuntas** (`T-024` ditutup P-040) dan dijaga mesin oleh `scripts/check-api-contract.sh` — jangan menulis ulang angkanya sebagai hitungan manual. Sistem aturan antislop **juga sudah tuntas** sejak **P-042** (`T-054`): skill terpasang dipin ke tag rilis (ADR-0025), aturan hanya bersumber dari `antislop.md`, dan rujukannya dijaga `scripts/check-antislop-refs.sh` — **jangan menyalin daftar aturan atau butir Delivery Gate** ke dokumen mana pun, dan jangan mengunduh berkas antislop atas inisiatif sendiri. **Jangan buat migrasi baru untuk `tokens_invalid_before`, `locked_until`, atau `login_attempts`** — semuanya sudah di migrasi `010` dan dipakai kode sejak P-030. Test: backend `cd backend && make test` (database terpisah `bwdcs_test`, `-p 1`, `-count=1`); frontend `cd frontend && npm run typecheck && npm run lint && npm run test:run && npm run build`. Satu langkah tambahan kini bagian dari serah terima halaman UI: jalankan `node scripts/responsive-evidence.mjs` (butuh backend + dev server hidup) sebelum menutup sesi yang menyentuh tata letak — klaim "tanpa gulir mendatar", "target sentuh", dan "laci menutupi konten" sudah tidak boleh ditulis tanpa angka (R-35, `70-TESTING.md` §3.14b). Keputusan yang menunggu pemilik kini **lima**, dan tak satu pun memblokir halaman lain: **Q-023** (lisensi, `T-050` `BLOCKED`), **Q-019/C-050** (threading komentar), **Q-024/C-063** (endpoint daftar pengguna untuk pemilih `Owner` dan halaman Members), **Q-025** (sejauh mana em dash disapu ke prosa dokumen dan komentar lama), dan **Q-026** (ambang target sentuh desktop: 36px padat atau naik ke 44px) |

## 2. Environment & Tooling (hasil T-001, 2026-09-17)

| Item | Persyaratan | Hasil | Status |
|---|---|---|---|
| OS / arsitektur | — | Darwin x86_64 (Intel Mac), disk 107 GiB bebas | Cukup |
| Go | 1.22+ | 1.22.5 di `/usr/local/go/bin/go`; sejak P-018 masuk `PATH` dari `~/.zshrc` (blok `BWDCS toolchain`) | **OK** (`T-011` DONE) |
| Node.js | 20+ | v26.7.0 | OK |
| npm | — | 11.19.0 | OK |
| PostgreSQL server | 16+ | **16.10 (Postgres.app) berjalan di `localhost:5432`**, data dir `~/Library/Application Support/Postgres/var-16`. Dipakai bersama proyek lain (`finmo`, `glid_gateway`, `restaurant`, `wms`); sejak P-018 ada **role dan database `bwdcs`** di instance yang sama (0 tabel publik — menunggu migrasi `T-004`) | **OK** (`T-013` DONE). Instalasi terpisah di `5433` tidak dipakai |
| PostgreSQL client | 16+ | Client 16 dari Postgres.app masuk `PATH` lewat `~/.zshrc`; instalasi Homebrew 14.6 tidak diubah dan tidak dihapus | **OK** (`T-011` DONE) |
| Database test | terpisah dari database aplikasi | **`bwdcs_test`** di instance yang sama (owner role `bwdcs`; dibuat P-024 lewat peran superuser lokal karena role aplikasi **tidak** diberi `CREATEDB`). 23 tabel, `role_permissions` 104, versi skema goose **10**, dan kosong dari data uji di antara sesi. Klaim "kosong" itu **diperiksa** untuk pertama kali pada P-030 — dan ternyata meleset: 220 baris `login_attempts` tertinggal karena `projectHTTPFixture` tidak membersihkannya (temuan **C-056**, ditambal); kini `make test` dua kali berturut-turut meninggalkan `login_attempts = 0` | **OK** (`T-036` DONE, C-038 tertutup) |
| Perintah test kanonik | — | `cd backend && make test` (menurunkan `TEST_DATABASE_URL` → `bwdcs_test`, `-p 1`, `-count=1`, menolak DSN database dev). `make test-dsn` mencetak target database dengan sandi disamarkan. `go test` telanjang **bukan** bukti: tanpa `TEST_DATABASE_URL` test integrasi memanggil `t.Skip` | **OK** (`70-TESTING.md` §8/§8.1) |
| Bukti runtime modul (server dev) | — | Jalankan server **di dalam command yang sama** dengan probe HTTP-nya: runner perintah dapat mengambil alih grup proses sehingga server `nohup` mati begitu command selesai (terjadi pada P-025, `health` 200 lalu SIGTERM). Job `launchctl submit` juga tidak dapat membaca berkas di `~/Documents` pada mesin ini (proses berhenti di `dyld: open` dan tidak pernah mendengarkan port). **Bangun ulang binari sebelum membuktikan perubahan kode** — P-025 sempat membaca "penyaring tidak bekerja" dari binari lama; bila hasil tampak mustahil, curigai binari basi lebih dulu |
| Formulasi Homebrew PostgreSQL (tidak dipakai) | — | `postgresql` dan `postgresql@14` 14.6 terpasang di `/usr/local/Cellar`, binary server-nya rusak (`libicu4c 71` hilang, yang ada `icu4c@78`) | Tidak dipakai; **jangan diandalkan** |
| goose (migrasi) | terbaru | **v3.24.1** di `~/go/bin/goose` — **dipin** ke versi yang sama dengan library yang dipakai aplikasi saat startup (ADR-0018 butir 3), karena v3.24.2+ menuntut Go ≥ 1.23 dan v3.28.0 menuntut 1.26. Pemasangan awal (P-018) memakai v3.28.0 lalu diturunkan pada P-020. `~/go/bin` juga berisi `dlv`, `gopls`, `migrate`, `staticcheck` | **OK** (`T-012` DONE, pin P-020) |
| Docker CLI + Compose | opsional | 24.0.2 / v2.19.1, daemon tidak berjalan | **Tidak diperlukan** karena PostgreSQL 16 sudah ada (`T-014` jadi opsional) |
| git | terbaru | 2.39.5; repo **sudah diinisialisasi** di branch `main`, `.gitignore` + `.editorconfig` aktif, `.env` terabaikan | **OK** (`T-002`/`T-002a` DONE) |
| Go module & ketergantungan | sesuai ADR-0008 | Module `bwdcs/backend`, `go 1.22.5`; gin v1.10.0, `jackc/pgx/v5` **v5.7.4**, `pressly/goose/v3` **v3.24.1**, viper v1.19.0, `golang.org/x/crypto` v0.31.0 (bcrypt), **`golang-jwt/jwt/v5` v5.3.1** dan **`google/uuid` v1.6.0** (ditambahkan P-021; keduanya tidak menaikkan direktif `go`). Ketiga pin terakhir menjaga toolchain 1.22.5: pgx v5.7.5, goose v3.24.2+, dan seterusnya menuntut Go ≥ 1.23. Perintah penyiapan memakai `GOTOOLCHAIN=local` agar tidak menarik toolchain lain | **OK**. Kenaikan toolchain harus dicatat di tabel ini sebelum pin dilepas — naikkan library **dan** CLI goose bersamaan (ADR-0018) |
| make | — | GNU Make 3.81 | OK |
| bash + `scripts/check-doc-links.sh` | — | Jalan; kriteria lulus hanya jumlah `BROKEN` = 0 (jumlah `PLANNED` berubah seiring dokumen bertambah, jangan dicek manual). Dua klasifikasi yang **sengaja tidak diperiksa** dan dinyatakan terang-terangan (P-042): `guide.md` ada di `ABSENT_DOCS` (panduan upstream yang memang tidak disalin), dan isi `skills/` selain `README.md` dilewati karena berkas pihak ketiga ber-`sha256` yang rujukannya menunjuk tata letak repo upstream | OK |
| bash + `scripts/check-api-contract.sh` | — | Jalan (P-040); membandingkan anotasi izin `42-API.md` dengan matriks `44-SECURITY.md` §3.1.2 dan `RequirePermission` di `router.go` — kriteria lulus `api-contract OK` (110 pemeriksaan). Ia **tidak** mengesahkan pasangan baru; menambah pasangan izin tetap menuntut ADR (ADR-0014) | OK |
| bash + `scripts/check-antislop-refs.sh` | — | Jalan (P-042); memeriksa rujukan ke sistem aturan antislop **tanpa jaringan** — nomor `R-XX` yang dirujuk ada di `antislop.md` (38 aturan), path `skills/...` di blok penunjuk `AGENTS.md` ada di disk, `sha256` tiap berkas antislop cocok tabel provenans `skills/README.md` §1, salinan core di `skills/antislop/SKILL.md` identik dengan core di root, dan kalimat aturan upstream tidak tersalin ke dokumen proyek — kriteria lulus `antislop-refs OK`. Ia **tidak** menilai keputusan desain terhadap aturannya; itu Delivery Gate + tinjauan user (ADR-0025) | OK |
| bash + `scripts/check-readme-facts.sh` | — | Jalan (P-039; **§6 ditambahkan P-042**); menghitung angka & versi `README.md` dari `router.go`, `go.mod`, `package.json`, dan berkas migrasi, **dan** membandingkan daftar berkas di pohonnya dengan isi `scripts/` serta `skills/` (kelas cacat **C-066**) — kriteria lulus `readme-facts OK` (37 fakta). Bukan penilaian rasa: skrip ini hanya membandingkan klaim dengan sumbernya | OK |
| docker compose (validasi konfigurasi) | — | `docker compose config -q` jalan tanpa daemon; `docker compose up` **belum pernah** dijalankan | Sebagian |

### Porta yang sudah terpakai di mesin ini

| Port | Pemakai | Konsekuensi |
|---|---|---|
| `8080` | `wms-backend` (proyek lain) | Backend BWDCS dev memakai **8081** (dicatat di `12-DEVELOPMENT-WORKFLOW.md` §7.1) |
| `5432` | PostgreSQL 16.10 (Postgres.app) | BWDCS memakai port ini dengan **database sendiri (`bwdcs`)**, bukan server baru |
| `5433` | bebas | Cadangan bila kelak butuh cluster terpisah |
| `5173` | bebas | Aman untuk Vite |

## 3. Modul (Status Implementasi)

| Modul | Dokumen Desain | Implementasi | Test | Catatan |
|---|---|---|---|---|
| Migrasi & skema | `41-DATABASE.md` §2/§4 — ADR-0003/ADR-0018 | **Selesai untuk `001`-`011`**: 11 berkas `.sql` + runner embed (`internal/migration/migration.go`); `010` memuat arsip dokumen + trigger `document_versions` (ADR-0019) dan `011` menulis ulang pesan penjaga append-only agar menyebut tabel sasaran lewat `TG_TABLE_NAME` (C-071) dan skema pencabutan sesi/auto-lock (ADR-0021/0022) | `migration_test.go` (**10** test: kelengkapan tabel, FK tertunda C-029, seed 44/30/18/12, kosakata tertutup, spot check, tanpa duplikat, `system_settings`, kosakata `archived` = `CHECK` database, skema `login_attempts`, dan kesesuaian kolom telemetri login dengan ADR-0022) + `audit_append_only_test.go` (**15** test: 7 `audit_logs` + 8 `document_versions`) | Terpasang di `bwdcs` **versi 11** (P-029/P-030; **dipakai penuh sejak P-030**); `010` dan `011` diterapkan pada P-029/P-045, dan `down-to 0` + `up` terbukti bersih sampai versi 9 pada P-020 |
| **Project** | `50-FSD.md` §3, `42-API.md` §3, `44-SECURITY.md` §3.1.3 — ADR-0014/ADR-0017 | **Selesai** (`T-035`, P-022): `internal/model/project.go`, `internal/repository/project_repository.go` (cakupan di `WHERE`), `internal/service/project_service.go` (transaksi + audit), `internal/dto/project_dto.go`, `internal/handler/project_handler.go`, 8 route di `router.go` | `internal/service/project_service_test.go` (17), `internal/handler/project_handler_test.go` (13, termasuk 401 kedelapan endpoint) | Cakupan: anggota project (non-admin) / seluruh organisasi (admin) / tetap satu tenant. Invariant: owner selalu anggota & tidak dapat dihapus, `code` permanen, arsip = status. Audit 5 aksi. Tiga kontrak diputuskan agen → Q-015 |
| **Document & Version** | `50-FSD.md` §4, `42-API.md` §4, `44-SECURITY.md` §3.1.3/§4.2/§6.1 — ADR-0017/ADR-0005/**ADR-0019** | **Selesai** (`T-037` P-023; **arsip** `T-039` P-029): `internal/model/document.go` (status kanonik enam nilai + `NextVersion`), `internal/repository/document_repository.go` (cakupan di `WHERE`, `NextNumber` atomik, versi, `HasRunningWorkflow`, `Archive` menggantikan `Delete`, daftar default tanpa arsip), `internal/service/document_service.go` + `document_service_upload.go` (transaksi + audit, checksum, `limitedReader`, `Archive`, tolak unggahan pada dokumen terarsip), `internal/dto/document_dto.go`, `internal/handler/document_handler.go` (multipart + streaming unduhan + `Archive`), 7 route di `router.go` (`DELETE` diganti `POST /:id/archive`) | `document_number_test.go` (5 penomoran), `document_service_test.go` (15), `document_upload_limit_internal_test.go` (unit batas), `document_handler_test.go` (12, termasuk 401, izin arsip, dan berkas `.txt`/`.csv` sungguhan seusai C-072) | Versi: `1.0` → minor pada unggahan biasa → **major** sesudah `revision_required`. Unduhan diaudit di transaksi singkat tersendiri. **Arsip** = ubah `status`+`archived_at` (tidak menghapus apa pun), ditolak `409` bila workflow `running` atau sudah terarsip. Delapan kontrak agen → Q-016; bukti arsip → `70-TESTING.md` §3.12a. **Unggahan `.txt`/`.csv` baru benar-benar bekerja sejak P-045** (`normalizeMimeType`, temuan **C-072**): MIME hasil `http.DetectContentType` membawa parameter `; charset=utf-8` dan daftar tertutup menulis tipe medianya saja, dan `document_service_test.go` (15) kini menguji kelima golongan berkas memakai MIME **hasil deteksi byte** |
| Bootstrap data awal | `docs/adr/0010-first-run-bootstrap.md`, `41-DATABASE.md` §4.1 | **Ada** (`internal/bootstrap/bootstrap.go`) | `bootstrap_test.go` (**8** test: validasi password lemah/contoh, org+admin+role, cost 12, idempotent, pesan bila seed 008 hilang, dilewati saat sudah ada user; kekuatan password diuji juga lewat `pkg/password`) | Membuat organisasi + admin pertama hanya bila `users` kosong; password tidak pernah masuk log |
| Config & logging | `40-TSD.md` §2.1 | **Skeleton** (`internal/config`, slog JSON, `.env` opsional, pesan gagal jelas) | `config_test.go`, `envfile_test.go` (84.1%, hermetis sejak `T-033`) | `GET /health` memeriksa database + storage (`T-003`) |
| Auth & RBAC | `40-TSD.md` §5.2/§5.2.1/§5.2.2, `44-SECURITY.md` §2/§2.3/§3.1/§3.2, ADR-0009/0014/**0021/0022** | **Selesai untuk `T-005`; dilengkapi `T-040`+`T-041` (P-030)**: `internal/pkg/jwt` (HS256 + `jti` + `iat` wajib), `internal/service/auth_service.go` (login dengan pencatatan `login_attempts`, auto-lock, logout per-sesi **dan** `logout_all`), `user_service.go` (`Unlock`), `permission_checker.go` (matriks tanpa bypass), `internal/repository/{user,login_attempt,token_revocation,setting}_repository.go` (yang terakhir memutuskan **kedua** sebab pencabutan dalam satu kueri + cache TTL 30 detik), `internal/middleware/*` (Auth, RequirePermission, RateLimit, CorrelationID, Logger, CORS), `internal/handler/{auth,user}_handler.go` + `router.go`. **`login_guard.go` dihapus.** **Kedua kontrak auth yang tertunda kini berjalan (P-034):** `change-password` (`T-034` — `tokens_invalid_before` + token pengganti sesudah commit) dan `refresh` (**`T-045`** — ADR-0023: JWT bertanda `typ`, 7 hari, tanpa penyimpanan di server). | jwt **17** test (termasuk enam untuk refresh) · middleware **12** (termasuk RBAC `70-TESTING.md` §4.1) · service **auth 22 + user 4** · handler **auth 14 + user 3** (end-to-end HTTP) · **total suite 279 test** | Login admin pertama `admin`/`ADMIN_PASSWORD` → 200 + 44 izin; logout → token ditolak `401 TOKEN_REVOKED`; `logout_all` → **semua** sesi user itu ditolak (`tokens_invalid_before`) sementara sesi user lain tidak; lima kegagalan → `423 LOCKED` + `Retry-After`; `POST /admin/users/:id/unlock` membukanya kembali |
| Organization & User | `41-DATABASE.md` | Belum | Belum | |
| **Workflow engine** | `43-WORKFLOW.md`, `42-API.md` §5, `44-SECURITY.md` §3.1.2/§3.3 — ADR-0014/0015/0016 | **Selesai** (`T-064`, P-048 — menutup **Phase 2**): `internal/model/workflow.go` (kosakata kanonik + navigasi step + deadline turunan), `internal/repository/workflow_repository.go` (`ApplyTransition` ber-guard **empat** kondisi, instance ber-cakupan di `WHERE`, riwayat aksi), `internal/service/workflow_service.go` (`Submit`, `ExecuteAction`, `Resubmit`, `List`/`Get`, definisi + step), `internal/dto/workflow_dto.go`, `internal/handler/workflow_handler.go`, **9 route** di `router.go` | `internal/model/workflow_test.go` (**6**), `internal/service/workflow_service_test.go` (**18**), `internal/handler/workflow_handler_test.go` (**6**) — 30 fungsi | Aksi pada step menuntut **izin DAN penunjukan step** (bukan salah satu; temuan **C-073**), guard optimistic ADR-0015 hidup di kueri (`rowsAffected = 0` → `409 WORKFLOW_CONFLICT` ber-`details`), jeda revisi dibaca dari **status dokumen** karena instance tetap `running` (ADR-0016), re-submit tidak menulis `workflow_actions`, dan `is_overdue` tetap turunan (ADR-0012). Bukti server nyata: `scripts/probe-workflow-module.py` — **46 asersi PASS** (`70-TESTING.md` §3.15); tanpa perubahan skema |
| **Task** | `50-FSD.md` §6, `42-API.md` §6, `44-SECURITY.md` §3.1.3 — ADR-0012/ADR-0014 | **Selesai** (`T-038`, P-025; **Phase 3 dikerjakan lebih awal**): `internal/model/task.go`, `internal/repository/task_repository.go` (dua predikat cakupan di `WHERE`), `internal/service/task_service.go` + `taskScope` di `scope.go`, `internal/dto/task_dto.go`, `internal/handler/task_handler.go` (penjaga `task:assign` bergantung isi body), 5 route di `router.go` | `internal/model/task_test.go` (3), `internal/service/task_service_test.go` (14), `internal/handler/task_handler_test.go` (12 lewat HTTP) | Modul pertama dengan **baca ≠ tulis** pada `44-SECURITY.md` §3.1.3. Pindah project → `409`; `in_progress` → `completed` lewat `PATCH` → `409` (jalurnya `POST /complete`, izin terpisah). Dua temuan (C-045/C-046) ditutup P-026; sebelas kontrak → Q-017 |
| **Comment** | `50-FSD.md` §7, `42-API.md` §7, `44-SECURITY.md` §3.1.3 — ADR-0014 | **Selesai** (`T-042`, P-027; menutup **Phase 3** bersama Task): `internal/model/comment.go` (kosakata `entity_type`), `internal/repository/comment_repository.go` (`commentEntityProjectCase` — satu-satunya pemetaan entitas → project), `internal/service/comment_service.go`, `internal/dto/comment_dto.go`, `internal/handler/comment_handler.go`, 5 route di `router.go` | `internal/model/comment_test.go` (3), `internal/service/comment_service_test.go` (9), `internal/handler/comment_handler_test.go` (7 lewat HTTP) | Modul pertama yang **tidak menyimpan project** di barisnya: cakupan baca ditegakkan lewat pemetaan entitas → project; edit/hapus = **kepemilikan**, bukan izin (lima endpoint dijaga `comment:read`/`comment:create` saja). Tiga temuan: C-048 (`meta.total` halaman kosong — ditambal di sini), C-049 (rute `/:entityType/:entityId` tidak dapat dipasang → bentuk kueri), C-050 (threading belum ada) |
| Notification | `50-FSD.md` §8 | Belum | Belum | |
| Audit trail | `44-SECURITY.md` §6/§6.1, ADR-0011 | **Mulai nyata**: `internal/service/audit_service.go` menulis entri di transaksi pemanggil; dipakai login (`LOGIN`), logout (`LOGOUT`), dan seluruh aksi project/document/task/comment. **Dua tabel append-only sejak P-029**: `audit_logs` (migrasi `007`) dan `document_versions` (migrasi `010`) | Trigger append-only (7 test `audit_logs` + 7 test `document_versions`) + audit terverifikasi lewat `psql` | `audit:read` hanya Administrator. Percobaan login — berhasil maupun gagal, termasuk username yang tidak ada — masuk tabel `login_attempts`, **bukan** `audit_logs` (ADR-0022, temuan C-035: `actor_id` di sana `NOT NULL`); aksi baru dari P-030: `LOGOUT_ALL` (`T-040`) dan `USER_UNLOCKED` (`T-041`, ditulis **hanya** bila lock benar-benar dibersihkan sehingga panggilan idempoten kedua tidak menambah entri) |
| Dashboard & Report | `50-FSD.md` §9, §10 | Belum | Belum | |
| **Frontend — kerangka & halaman awal** | `DESIGN.md` (warna/tipografi/dials — sumber tunggal), `51-UX.md` §2 (tata letak, navigasi), ADR-0002/**ADR-0024** | **Berjalan** (`T-048`, P-037): Vite 8 + React 19 + TypeScript 5.9 + Tailwind v4 (plugin `@tailwindcss/vite`, **tanpa** `tailwind.config.js`), `src/styles/tokens.css` (token + `@theme` + tema terang/gelap), shell (`AppShell`/`Header`/`Sidebar`/`PageHeader`), primitives (`Button`, `Field`, `Panel`, `DataTable`, `StatusBadge`, `States`, `tones`, **`Dialog`** sejak P-041), lapisan API (`services/http.ts` dengan pemetaan galat `42-API.md` §12, `services/auth.ts`, `services/session.ts`), `store/auth.ts` + `store/theme.ts`, halaman Login + Dashboard + `ModulePending`/`NotFound`. **Halaman bisnis pertama berdiri P-041 (`T-053`): Projects** — `services/projects.ts`, `queries/client.ts` + `queries/projects.ts` (TanStack Query 5, kunci kueri terpusat), `pages/Projects/{index,CreateProjectDialog,ProjectDetail}.tsx`. **Halaman bisnis kedua berdiri P-044 (`T-059`): Documents** — `services/documents.ts` (`listDocuments`, `createDocument`, `uploadDocumentVersion` multipart tanpa menyetel `Content-Type`, `downloadDocumentVersion` sebagai blob, `archiveDocument` yang membuka `data.document` — temuan **C-070**), `queries/documents.ts`, `pages/Documents/{index,CreateDocumentDialog,UploadVersionDialog,FilePicker,DocumentDetail}.tsx`, `utils/download.ts`, **Halaman bisnis ketiga berdiri P-046 (`T-062`): Tasks** — `services/tasks.ts` (`listTasks`/`fetchTask`/`createTask`/`updateTask`/`completeTask`, kosakata prioritas + label, `validateDueRange` batas inklusif), `queries/tasks.ts` (mutasi detail **diambil ulang** karena `is_overdue`/`updated_at` dihitung server), `pages/Tasks/{index,CreateTaskDialog,TaskDetail}.tsx` (penyaring hidup di URL termasuk **tri-state overdue** `""`/`"true"`/`"false"`, dialog buat dari anggota project, detail dengan tabel transisi §6.3 dan tombol menurut status), dan `utils/format.ts` dipakai ulang untuk tanggal/prioritas. **Sejak P-049 (`T-065`), penyaring rentang tenggat adalah satu kelompok ber-peran `group` berlabel** — berdampingan pada lebar lebar, menumpuk di dalam kelompok yang sama pada lebar sempit. **Sejak P-050 (`T-066`), setiap kolom penyaring ber-`select` memasang `min-w-0`** (kolom rentang tenggat sengaja tidak) sehingga pilihan panjang dipotong browser di dalam kontrolnya alih-alih melebarkan halaman, dan `tap-target` menetapkan **kedua sisi** kotak sentuh (44px / 36px) | **25 berkas test, 247 test** (termasuk `src/styles/tokens.contrast.test.ts` — 31 test kontras WCAG AA yang **menghitung ulang** angka di `DESIGN.md` — dan `src/test/a11y.test.tsx` dengan `axe-core`) | Halaman bisnis **Projects**, **Documents**, dan **Tasks** (+detail; Documents: unggah dua langkah, unggah versi baru, unduh, arsip; Tasks: penyaring tri-state overdue dan ketiga transisi status) ada; Approvals, Reports, Administration **belum** dibangun dan masih tampil sebagai `ModulePending` yang menyebut apa yang belum ada, bukan tabel kosong — di halaman detail dokumen bagian yang belum ada menyebut **alasan**nya per bagian (Workflow/Comments/Activity/Related Tasks); Dashboard hanya menampilkan modul yang sudah/selum ada, **tanpa angka karangan** (R-18). Batas yang dinyatakan terbuka di layar: pemilih `Owner` dan tambah/cabut anggota menunggu endpoint daftar pengguna (temuan **C-063**, Q-024). Verifikasi: `npm run typecheck && npm run lint && npm run test:run && npm run build`; bukti perilaku di server nyata: `python3 scripts/probe-task-module.py` — **41 asersi PASS** (tri-state overdue, rentang inklusif, ketiga transisi + `409`, cakupan tulis, baseline pulih). **Aturan navigasi ditegaskan dan diukur sejak P-047 (`T-063`):** sidebar memuat **modul saja** (tanpa `subItems`), sub-navigasi hidup di halaman yang memilikinya (Documents dan Tasks berbaris tab; Projects tidak butuh karena List = halaman dan Create = tombol header), dan bagian `navigation` di `scripts/responsive-evidence.mjs` mengukur ketiganya di peramban — sidebar 8 tautan tanpa kueri dengan tepat satu menu aktif, **0** kontrol penyaring terangkat dari barisnya, tab Documents 5 tautan dengan `Milik saya` yang tetap dapat dibuka sehingga alasan Q-016 terbaca di layar; `responsive-evidence` karena itu menjadi bukti wajib sesudah menyentuh navigasi atau tata letak. **Sejak P-051 (`T-067`/`T-068`) aturan itu punya penegak mesin, dan halaman anak punya tempatnya:** dua daftar dipisahkan di `frontend/src/config/navigation.ts` — `navigation` (sidebar: modul saja, satu segmen, tanpa kueri) dan **`subPages`** (halaman bernavigasi yang bukan menu, dengan `parent`), dengan `subNavFamily()` merender baris sub-navigasi modul induknya (Audit di bawah Reports hidup di situ, rutenya dibangun dari kedua daftar sehingga tetap ada walau modul induknya masih halaman penjelasan). Penegaknya `bash scripts/check-navigation.sh` (`navigation OK`), dan ia **dua arah** terhadap tabel `51-UX.md` §2.1. Suite frontend berjalan dengan anggaran waktu yang tidak lagi menguji mesin (`asyncUtilTimeout: 5000`, `testTimeout: 20000` — temuan **C-082**), sehingga `npm run test:run` hijau tiga kali berturut-turut tanpa satu pun asersi dilonggarkan |

## 4. Keputusan Pending (Ringkas)

Lihat detail dan opsi di `OPEN-QUESTIONS.md`.

| Kode | Pertanyaan | Dampak jika tidak dijawab |
|---|---|---|
| ~~Q-001~~ | ~~Mode antislop: during atau after?~~ | **RESOLVED 2026-09-21 (P-037): user memilih `during` — ADR-0006 `ACCEPTED`.** Filter antislop dipakai **selama** pengerjaan UI, bukan hanya sebelum serah terima |
| ~~Q-002~~ | ~~Siapa yang mengisi arah desain di `DESIGN.md`?~~ | **RESOLVED 2026-09-21 (P-037): user memilih jalur 2 — ADR-0007 `ACCEPTED`.** Agen menyusun arah desain atas instruksi eksplisit; `DESIGN.md` terisi (dials resmi ENERGY 1 / RHYTHM 2 / MOTION 1), status "draft without direction" gugur, dan temuan **C-015** ditutup |
| Q-003 | Skill antislop (`skills/*/SKILL.md`) belum ada di repo; user yang menyediakan | Agen tidak boleh mengunduh dari jaringan; filter hanya memakai `antislop.md` |
| ~~Q-004~~ | ~~Izin `git init` + `.gitignore` untuk T-002~~ | **RESOLVED 2026-09-18 (P-018): user memberi izin; `git init -b main` + `.gitignore` + `.editorconfig` selesai** |
| ~~Q-006~~ | ~~Strategi invalidasi token saat logout (ADR-0009)~~ | **RESOLVED 2026-09-17 (P-004): ADR-0009 `ACCEPTED` — daftar revokasi `jti` di `token_revocations`; dijalankan pada `T-005`** |
| ~~Q-007~~ | ~~Bootstrap organisasi & admin pertama~~ | **RESOLVED 2026-09-17 (P-004): ADR-0010 `ACCEPTED` — `EnsureAdminFirstRun`; dijalankan pada `T-004`** |
| Q-008 | Whitelist ekstensi `.doc/.docx/.pptx` (non-blocking, Phase 1) | Document module terbatas ke pdf/xls/gambar |
| ~~Q-009~~ | ~~Izin menyiapkan toolchain (`PATH` Go, `goose`, role+database `bwdcs`)~~ | **RESOLVED 2026-09-18 (P-018): user memberi izin; `T-011`/`T-012`/`T-013` DONE tanpa menyentuh PostgreSQL 14.6 Homebrew maupun database proyek lain** |
| ~~Q-010~~ | ~~Temuan audit mana yang diperbaiki berikutnya~~ | **RESOLVED 2026-09-19 (P-026): user meminta seluruh pertanyaan terbuka dijawab memakai best practice.** Hasil saat itu: 42 FIXED, 4 APPROVED (`T-039`/`T-040`/`T-041`), 1 OPEN (C-015 — arah desain, milik user). **Status P-027: 43 FIXED, 4 APPROVED, 3 OPEN** — OPEN bertambah dua dari temuan baru saat mengerjakan modul Comment (C-048 `T-043`, C-050/Q-019). **Status P-029: 45 FIXED, 3 APPROVED, 3 OPEN** (C-004 ditutup `T-039`, C-051 baru ditutup di sesi yang sama). **Status P-032: 54 FIXED, 0 APPROVED, 2 OPEN** (C-048 ditutup `T-043`) |
| **Q-017** | Kontrak modul task (11 butir): sembilan diputuskan agen saat implementasi, dua di antaranya (**penyaring rentang tanggal**, **atribusi error body**) diminta keputusan Anda | **Butir (11) atribusi error body: diputuskan & dikerjakan P-026** (`bindJSON` menamai field; tetap berlaku). **Butir (10) rentang tanggal: dikoreksi user pada P-028** — bukan lagi setengah terbuka seperti usulan agen, melainkan **kedua batas inklusif** `[due_from, due_to]`, dengan `due_to == due_from` sah dan hanya rentang terbalik yang ditolak `422`. Konsekuensi (rentang bersebelahan dapat tumpang tindih) dinyatakan di `42-API.md` §6; perubahan ini tidak menyentuh skema dan reversibel |
| **Q-018** | Urutan fase berikutnya: **Comment** (menutup Phase 3) atau kembali ke **Workflow** (Phase 2) sesuai `80-ROADMAP.md`? | **Separuh terjawab oleh P-027:** Comment sudah dikerjakan (asumsi "Comment lebih dulu karena murah" terbukti), sehingga pertanyaan yang tersisa adalah **Workflow (Phase 2) atau modul admin/notification**. Masih tidak memblokir |
| **Q-023** | **Lisensi proyek** dan kebijakan kontribusi pihak ketiga. Pemilik memilih **menunda** jenis lisensinya pada P-038; pemegang hak cipta sudah dijawab: **BSA** | Tidak menghalangi pengembangan sama sekali; yang tertahan distribusi dan penerimaan kontribusi luar. Tanpa lisensi, pihak ketiga tidak punya hak apa pun atas kode ini — termasuk hak untuk menyumbang dengan aman. Berkas `LICENSE` **tidak** dibuat sampai diputuskan, dan ketiadaannya ditampilkan di `README.md` §4/§13 |
| **Q-019** | Apakah balasan komentar ber-thread (`Reply`, `50-FSD.md` §7) perlu dihidupkan? Skema tidak punya `parent_id` (temuan C-050) | Rekomendasi agen: **tunda** — tidak ada `FR-CMT-*` yang menuntutnya, dan bentuk responsnya (rekursif vs datar) lebih baik ditetapkan setelah ada pemakai nyata |
| ~~Q-013~~ | ~~Mekanisme pencabutan "seluruh token aktif user"~~ | **RESOLVED 2026-09-19 (P-026): `users.tokens_invalid_before` — ADR-0021 `ACCEPTED`.** Opsi `session_id` (daftar sesi) ditolak **untuk MVP** tetapi tidak dimatikan sebagai kemungkinan. Kodenya dijalankan `T-040` (P-030) dan `change-password` menutup sisanya pada P-034 (`T-034`); hanya `POST /auth/refresh` yang tersisa, dan itu bukan soal mekanisme pencabutan melainkan bentuk tokennya (**Q-020**, `T-045`) |
| ~~Q-014~~ | ~~Apakah login **gagal** harus masuk `audit_logs`~~ | **RESOLVED 2026-09-19 (P-026): tidak — tabel `login_attempts` — ADR-0022 `ACCEPTED`.** `actor_id` tetap `NOT NULL`; FR-AUDIT-01 "login" = login **berhasil**. Tabelnya sekaligus penghitung auto-lock C-009. Kode di `T-041` |
| ~~T-039/T-040/T-041~~ | ~~Implementasi tiga ADR yang sudah `ACCEPTED` (arsip dokumen, pencabutan sesi, auto-lock + `login_attempts`)~~ | **SELESAI P-029/P-030.** Ketiga titik perbedaan kode-vs-dokumen sudah hilang: `DELETE /documents/:id` diganti arsip, `logout_all` → `200`, dan auto-lock memakai `login_attempts` + `users.locked_until` (penghitung di memori dihapus). Bukti: `70-TESTING.md` §3.12a/§3.12b |
| ~~Q-020~~ | ~~Bentuk token `POST /auth/refresh`~~ | **RESOLVED 2026-09-20 (P-034): Opsi A — JWT bertanda `typ`, 7 hari, tanpa penyimpanan di server — ADR-0023 `ACCEPTED`.** Yang lebih kuat (token buram ber-rotasi, pola OWASP) tidak dibatalkan sebagai kemungkinan kelak, dan batasan statelessnya dikunci terbuka oleh `TestRefreshKeepsPreviousRefreshTokenValid`. `T-045` **DONE** |
| **Q-015** | Tiga kontrak modul project yang diputuskan agen (NON-BLOCKING): owner selalu anggota; pelanggaran cakupan → **404** bukan 403; batas `limit` 1–100 → 422 | Tidak ada yang terblokir — implementasi sudah berjalan dan terdokumentasi (`42-API.md` §3). Bila Anda memilih sebaliknya, koreksinya satu perubahan kecil per butir. Ditemukan saat `T-035` |
| **Q-016** | Delapan kontrak modul document yang diputuskan agen (NON-BLOCKING): aturan versi (major bila `revision_required`); daftar versi memakai `document:read`; berkas tidak sah → `422` bukan `413`; owner = pembuat; hapus = kaskade + ditolak saat workflow berjalan; `entity_id` audit = nomor dokumen; project arsip **belum** ditolak; filter kategori/owner/tanggal belum ada | Tidak ada yang terblokir. Satu butir (**project arsip menerima dokumen baru?**) benar-benar keputusan Anda; sisanya konfirmasi. Detail + alternatif yang ditolak di `OPEN-QUESTIONS.md`. Ditemukan saat `T-037` |

## 5. File Penting dari Sesi Terakhir

| File | Perubahan |
|---|---|
| `backend/internal/{model/workflow.go,model/workflow_test.go,dto/workflow_dto.go,repository/workflow_repository.go,service/workflow_service.go,service/workflow_service_test.go,handler/workflow_handler.go,handler/workflow_handler_test.go}` | Added (**P-048**, `T-064`): modul **Workflow** lengkap — kosakata + navigasi step, bentuk kontrak `42-API.md` §5, `ApplyTransition` ber-guard empat kondisi, aturan aksi (izin dari isi body + penunjukan step), re-submit, cakupan; 30 fungsi test |
| `backend/internal/handler/router.go`, `backend/cmd/server/main.go`, `backend/internal/handler/main_test.go`, `backend/internal/repository/db.go`, `backend/internal/pkg/response/response.go` | Changed (**P-048**): group `/workflows` + sembilan route beserta izinnya, wiring service/handler, fixture HTTP, helper transaksi, dan amplop `409` ber-`details` **objek** |
| `scripts/probe-workflow-module.py` | Added (**P-048**): probe HTTP nyata modul Workflow — **46 asersi PASS** dengan lima aktor login sungguhan dan pemulihan baseline otomatis |
| `docs/design/{43-WORKFLOW,42-API,50-FSD,40-TSD,70-TESTING}.md` | Changed (**P-048**): syarat aksi menjadi izin **DAN** penunjukan step (**C-073**), kardinalitas route izin-dari-body (**C-074**), jenis notifikasi `REVIEW_REQUIRED_AGAIN`, modul + rute workflow di TSD, dan §3.15 bukti test/mutasi/probe |
| `scripts/check-ledger.sh` | Changed (**P-048**): pola per-berkas §3 meluas ke bentuk `(N test: …)` yang sebelumnya tidak dikenali sehingga dua klaim lolos tanpa diperiksa (**C-075**); `README.md`/`scripts/check-readme-facts.sh` mendapat ember route `workflows` |
| `skills/README.md`, `skills/antislop*/SKILL.md`, `skills/antislop-human/contrast-check.py`, `skills/LICENSE-antislop` | Added (**P-042**, `T-054`): lima skill yang sejak P-037 didaftarkan `AGENTS.md` tetapi tidak ada di disk (**C-064**) kini terpasang, byte-identik dari **tag rilis `v3.2.12`**, dengan tabel `sha256`, cara memperbarui, dan lisensi MIT tercatat |
| `antislop.md` | Changed (**P-042**): diganti berkas pristine dari tag `v3.2.12`. Berkas sebelumnya **varian lama** yang berbeda dari salinan Gate di dokumen desain (**C-065**) — dua versi core beredar |
| `scripts/check-navigation.sh` | Added (**P-051**, `T-068`): pemeriksa batas sidebar (`51-UX.md` §2.1) — entri sidebar berkueri/berfragmen, entri lebih dari satu segmen, label bergaya remah, halaman anak tanpa induk/berinduk hantu/di luar induknya, path/label ganda, penyimpangan himpunan menu **dua arah**, teks aturan yang dilunakkan, dan penjaga bentuk parser (objek tanpa label/path/induk → berhenti, bukan menilai hasil kosong). Keluaran `navigation OK`. Gigi dibuktikan dengan tujuh cacat disuntikkan sementara; menutup **C-079**/**C-080** |
| `scripts/check-antislop-refs.sh` | Added (**P-042**, `T-054`): 7 pemeriksaan rujukan antislop (daftar aturan, `R-XX`, path skill dua arah, `sha256` vs tabel, salinan core identik, klaim rentang, sidik jari aturan tersalin). Gigi dibuktikan dengan enam cacat disuntikkan sementara |
| `docs/design/01-AGENT-WORKFRAME.md` §3.2/§5.3/§6 | Changed (**P-042**): tabel 23 aturan dan blok empat Gate **dihapus** (salinan yang dapat menyimpang — pola C-014), diganti penunjuk ke `antislop.md` + keputusan proyek per nomor aturan + bentuk laporan Gate; pohon folder memuat `skills/` dan lima `scripts/` yang sebenarnya |
| `docs/adr/0025-skill-antislop-terpasang-dan-dipin.md`, `docs/adr/README.md` | Added/Changed (**P-042**): keputusan pin ke tag rilis, lisensi MIT, sumber aturan tunggal, dan **amandemen butir 2 ADR-0006** (izin unduh agen satu kali, bukan aturan tetap); indeks ADR diperbarui |
| `.github/workflows/ci.yml`, `AGENTS.md`, `README.md` §12.3, `docs/design/{02-AGENT-PROGRESS-PROTOCOL,12-DEVELOPMENT-WORKFLOW,90-AGENT-GUIDE}.md` | Changed (**P-042**): pemeriksa kelima disambungkan ke CI dan ke daftar verifikasi; aturan baru **§6.4** menetapkan kelas cacat C-064/C-065 dan tiga larangan penggunaannya (jangan melunakkan skrip, jangan memperbarui `sha256` tanpa mengambil berkas dari tag, jangan menyalin aturan ke dokumen) |
| `README.md` §4/§12.3, `scripts/check-readme-facts.sh`, `scripts/check-doc-links.sh` | Changed (**P-042**, `T-055`): temuan **C-066** — pohon folder README hanya menyebut 2 dari 5 skrip yang sudah jalan di CI, sehingga daftar itu kini dibandingkan mesin **dua arah** (§6 `check-readme-facts.sh`, 25 → **37 fakta**); `check-doc-links.sh` menambahkan klasifikasi `guide.md` → `ABSENT_DOCS` dan melewati isi `skills/` selain `README.md` (berkas pihak ketiga ber-`sha256`) |
| `docs/progress/OPEN-QUESTIONS.md` | Changed (**P-042**): **Q-003 `RESOLVED`** — skill terpasang dan dipin; izin unduh agen dicatat sebagai peristiwa satu kali, bukan kebiasaan |
| `docs/progress/audits/AUDIT-001-...md` + `audits/README.md` | Changed (**P-042**): temuan **C-064** dan **C-065** ditambahkan, keduanya selesai di sesi yang sama |
| `frontend/src/services/projects.ts`, `frontend/src/queries/client.ts`, `frontend/src/queries/projects.ts` | Added (**P-041**, `T-053`): lapisan data modul project — satu tempat yang tahu bentuk `42-API.md` §3, kunci kueri terpusat, invalidasi cache sesudah mutasi. `archiveProject` memakai `POST /projects/:id/archive`, **bukan** `DELETE` |
| `frontend/src/pages/Projects/index.tsx`, `frontend/src/pages/Projects/CreateProjectDialog.tsx`, `frontend/src/pages/Projects/ProjectDetail.tsx` | Added (**P-041**, `T-053`): daftar (penyaring dari URL, paginasi dari `meta`, tiga keadaan), form buat project (validasi §3.2 + pemetaan galat server per-field), dan halaman detail (metadata dari server, anggota, tab yang menyatakan dirinya belum dibangun). Batas yang **tidak** dikarang: pemilih `Owner` dan tambah/cabut anggota menunggu endpoint daftar pengguna (temuan **C-063**) |
| `frontend/src/components/common/Dialog.tsx` | Added (**P-041**): primitive dialog aksesibel (fokus awal, jebakan fokus, Escape, `aria-modal`) — satu implementasi untuk dialog buat project dan konfirmasi arsip |
| `frontend/src/services/projects.test.ts`, `frontend/src/pages/Projects/Projects.test.tsx`, `frontend/src/pages/Projects/CreateProjectDialog.test.tsx`, `frontend/src/pages/Projects/ProjectDetail.test.tsx`, `frontend/src/test/render.tsx` | Added (**P-041**): 41 test baru (9 + 15 + 8 + 9) dan helper render ber-provider; total frontend **129 test / 14 berkas** |
| `frontend/src/App.tsx`, `frontend/src/main.tsx`, `frontend/src/config/navigation.ts`, `frontend/src/pages/Dashboard/index.tsx`, `frontend/src/components/common/DataTable.tsx`, `frontend/src/components/common/Field.tsx`, `frontend/src/utils/format.ts`, `frontend/src/App.test.tsx`, `frontend/src/test/a11y.test.tsx` | Changed (**P-041**): rute `/projects` + `/projects/:id` hidup, provider kueri dipasang, menu Projects tidak lagi menunjuk halaman yang belum ada, dan pemeriksaan `axe` diperluas ke halaman Projects |
| `docs/design/70-TESTING.md` §3.14a, `docs/progress/TRACEABILITY.md` (`FR-PROJ-01`..`FR-PROJ-07`) | Changed (**P-041**): apa yang dikunci test halaman Projects + bukti server nyata + batas jujur (suite frontend masih memakai HTTP tiruan); kolom bukti lapisan UI tanpa menaikkan status karena requirementnya sudah `DONE` |
| `docs/progress/audits/AUDIT-001-...md` + `audits/README.md`, `AGENTS.md`, `docs/progress/STATE.md`, `CONTINUE.md`, `docs/progress/OPEN-QUESTIONS.md` | Changed (**P-041**; angka **saat itu**): temuan **C-063** ditambahkan (`OPEN`), **Q-024** dicatat, dan angka audit — waktu itu — diselaraskan ke 63 temuan / 61 FIXED lewat marker `audit-summary`. Sejak **P-042** angkanya 66 / 64 (C-064..C-066) |
| `README.md` §2/§4 | Changed (**P-041**): baris status frontend menyebut halaman **Projects** yang kini berdiri, dan pohon folder frontend memuat `queries/` serta `pages/Projects/` |
| `README.md` §1/§3/§4/§12/§13 | Changed (**P-038**, `T-049`): bagian **Kontribusi** (§12, enam subbagian) ditambahkan; §13 Lisensi ditulis ulang sebagai tabel status (keputusan **belum ditetapkan**, pemegang hak cipta **BSA**, field `license` package.json belum diisi, rujukan Q-023) dengan peringatan "jangan anggap open source"; enam klaim basi diperbaiki — baris status Frontend, diagram §3 ("Frontend SPA"), pohon §4 (`DESIGN.md`, `frontend/` diperluas, baris `LICENSE` bertanda belum ada, `.github/workflows/` + `check-ledger.sh`), baris auth yang masih "Belum ada", dan jumlah route **30 → 32** (dihitung dari `router.go`) — temuan **C-061** |
| `docs/design/42-API.md` §5/§8/§11 | Changed (**P-040**, `T-024`): 10 baris `Izin:` ditambahkan dari matriks §3.1.2 — `GET|POST /workflows/definitions` (`workflow_definition:read` semua role / `:manage` Administrator), `GET /notifications` + `PATCH /notifications/:id/read` + `POST /notifications/read-all` (`notification:read`/`notification:update`, semua role dengan cakupan `user_id = user`), `GET|POST /admin/users` (`user:read`/`user:create`), `PATCH /admin/users/:id` (dari butir prosa menjadi baris `Izin:` sendiri), `GET /admin/roles` (`role:read`), `PATCH /admin/settings/:key` (`setting:manage`); kalimat penjelas `comment:update` ditulis ulang agar tidak terbaca sebagai klaim pasangan. Total **55 endpoint: 48 + 7 lewat tabel izin bab §4** |
| `scripts/check-api-contract.sh` | Added (**P-040**, `T-052`): pemeriksa 110 titik — anotasi izin per endpoint, keabsahan pasangan terhadap matriks (44 pasangan), dan keabsahan pasangan pada `RequirePermission` router (18 pasangan). Dipakai untuk menutup `T-024` sebagai hitungan manual |
| `.github/workflows/ci.yml`, `AGENTS.md`, `README.md` §12.3, `docs/design/{02-AGENT-PROGRESS-PROTOCOL,12-DEVELOPMENT-WORKFLOW}.md` | Changed (**P-040**): pemeriksa keempat disambungkan ke CI dan ke perintah verifikasi; aturan baru **§6.3** menetapkan bahwa izin endpoint berasal dari matriks, termasuk cara menulis kalimat tentang pasangan yang tidak ada |
| `scripts/check-readme-facts.sh` | Added (**P-039**, `T-051`): pemeriksa 25 fakta `README.md` terhadap repo (route + rincian + endpoint per modul, versi Go/Gin/pgx/viper/goose, versi React/Vite/Tailwind/TypeScript, rentang migrasi, klaim "belum ada"). Dipakai untuk menemukan **C-062** dan sekarang dijalankan di CI |
| `README.md` §2/§12.3 | Changed (**P-039**, temuan **C-062**): baris `Frontend (rencana) … React 18 … Belum diinisialisasi` diganti keadaan sebenarnya (`React 19 + TypeScript 5.9 + Vite 8 + Tailwind v4`, tanpa label "rencana"); blok perintah verifikasi §12.3 menambah `check-readme-facts.sh` beserta alasan angkanya dibaca dari sumber |
| `.github/workflows/ci.yml`, `AGENTS.md`, `docs/design/{02-AGENT-PROGRESS-PROTOCOL,12-DEVELOPMENT-WORKFLOW,90-AGENT-GUIDE}.md`, `docs/progress/README.md` | Changed (**P-039**): pemeriksa baru disambungkan ke CI (langkah ketiga job `ledger`) dan ke perintah/aturan verifikasi; aturan baru **§6.2** di protokol progress menjelaskan tabel fakta README → sumber kebenarannya dan melarang melunakkan skripnya |
| `scripts/check-doc-links.sh` | Changed (**P-038**): klasifikasi baru **`ABSENT`** untuk dokumen yang **sengaja tidak dibuat** (`ABSENT_DOCS`, kini hanya `CONTRIBUTING.md` — berkas itu memang tidak ada karena aturan kontribusi tinggal di `README.md` §12). Sebelumnya setiap penyebutannya membuat skrip melaporkan `BROKEN` dan keluar `1`. Klasifikasi ini **tidak** melonggarkan pemeriksaan: dokumen hilang yang tidak terdaftar tetap `BROKEN`, dibuktikan dengan menyisipkan berkas `.md` sementara di `docs/design/` yang jelas tidak ada, lalu menghapusnya kembali |
| `DESIGN.md` | Changed (**P-037**, `T-006`): **terisi penuh** — identitas & kepribadian, palet §2.1-§2.4 **dengan alasan dan angka kontras per token**, tipografi §3 (system stack, plafon 26px, aturan mono), mood & kepadatan, dials §5 (**ENERGY 1 / RHYTHM 2 / MOTION 1** + alasan), motif "punggung rekam" §6, tema terang/gelap §7, referensi & anti-referensi §8, dan Design Read §9. Peringatan status: butir *rasa* = draf agen, butir *struktural* = keputusan yang dipertanggungjawabkan |
| `frontend/**` (51 berkas baru) | Added (**P-037**, `T-048`): kerangka Vite 8 + React 19 + TS 5.9 + Tailwind v4. Inti: `src/styles/tokens.css` (token + `@theme` + lapisan semantik + tema gelap), `src/styles/base.css`, `src/components/{common,layout}/*`, `src/services/{http,auth,session}.ts`, `src/store/{auth,theme}.ts`, `src/pages/{Login,Dashboard,ModulePending,NotFound}`, `src/types/{api,status}.ts`, `src/test/*`, `vite.config.ts`, `tsconfig.json`, `eslint.config.js`, `index.html`, `.env.example`, `.gitignore`. **Tidak ada** `tailwind.config.js` |
| `frontend/src/styles/tokens.contrast.test.ts`, `src/test/a11y.test.tsx` | Added (**P-037**): 31 test kontras yang **membaca `tokens.css` dan menghitung sendiri** rasio WCAG 2.2 AA per pasangan token (teks 4,5:1; batas kontrol & focus ring 3:1; badge status), plus pemeriksaan "tidak ada komponen yang menulis warna langsung"; dan 4 test aksesibilitas dengan `axe-core` yang sekaligus **membuktikan pemeriksanya menemukan pelanggaran** (bukan lulus kosong). Ditemukan & diperbaiki di sesi yang sama: parser blok CSS di test kontras salah membaca prelude `@theme` (terbawa `@import`/`@custom-variant`), sehingga 15 test gagal meski tokennya benar |
| `docs/design/51-UX.md` §1/§3/§4 | Changed (**P-037**, temuan **C-060**): §3 Color System dan §4 Typography yang memuat palet biru-slate + `H1 28px` diganti **penunjuk** ke `DESIGN.md` §2/§3 + `frontend/src/styles/tokens.css` (tabel token semantik, bukan hex, plus empat aturan yang tidak boleh dilanggar); §1 dinaikkan ke **WCAG 2.2 AA** |
| `docs/design/30-ARCHITECTURE.md` §2.1/§2.2/§2.3, `60-DEPLOYMENT.md` §2.1/§3.2 | Changed (**P-037**): stack frontend diselaraskan ke versi yang benar-benar terpasang (React 19, Router 7, Tailwind v4 **tanpa berkas konfigurasi**, TanStack Query menyusul) lewat **ADR-0024**; teks CJK yang salah tulis dibersihkan; pohon `frontend/` di §2.2 diganti struktur nyata beserta catatan apa yang **sengaja tidak ada** (folder kosong per modul); §2.1 `60-DEPLOYMENT` mendapat tabel variabel frontend (`VITE_API_BASE_URL`/`VITE_API_PROXY_TARGET`/`VITE_DEV_PORT`/`VITE_PREVIEW_PORT`) dan §3.2 skrip build yang sebenarnya beserta alur verifikasinya |
| `docs/adr/0006-antislop-usage-mode.md`, `0007-design-direction-source.md`, `0024-versi-dan-tooling-frontend.md`, `docs/adr/README.md` | Changed (P-037): ADR-0006 dan ADR-0007 dari `PROPOSED` menjadi **`ACCEPTED`** (mode `during`; jalur 2 arah desain), **ADR-0024** baru mengunci versi & tooling frontend; baris indeksnya ditambahkan |
| `docs/design/01-AGENT-WORKFRAME.md` §3.3/§5.2/§7, `00-README.md`, `11-DESIGN-DIRECTION.md`, `12-DEVELOPMENT-WORKFLOW.md` §10, `AGENTS.md` | Changed (P-037): dial 1/2/1 dinyatakan **resmi** (bukan lagi usulan yang menunggu arah desain); `11-DESIGN-DIRECTION.md` berstatus **`SUPERSEDED`** (kuesioner lama, jangan diisi lagi); tabel gap §7 menandai Q-001/Q-002 selesai; pitfall "UI dibangun sebelum arah desain ada" disesuaikan; `AGENTS.md` mendapat status `DESIGN.md`/`frontend/` terbaru, aturan UI yang mengikat, dan perintah verifikasi frontend |
| `backend/internal/service/auth_service.go`, `internal/handler/auth_handler.go`, `internal/dto/auth_dto.go`, `internal/handler/router.go` | Changed (P-034, `T-034`): `AuthService.ChangePassword` (verifikasi `old_password` lebih dulu, lalu hash + `RevokeAllForUser` + audit `PASSWORD_CHANGED` dalam satu transaksi, token pengganti diterbitkan **sesudah commit**), `AuthHandler.ChangePassword` + `writeChangePasswordError` (`400 INVALID_CURRENT_PASSWORD` vs `422` ber-field vs `401`), `ChangePasswordRequest`/`Response`, dan route `POST /api/v1/auth/change-password` (hanya autentikasi). `POST /auth/refresh` **sengaja belum** didaftarkan — bentuk tokennya **Q-020** |
| `backend/internal/service/auth_service_test.go`, `internal/handler/auth_handler_test.go`, `internal/service/audit_service.go` | Changed (P-034, `T-034`): `TestChangePasswordKeepsCurrentSession` (lima asersi: token pengganti baru & sah, token sesi ini **dan** perangkat lain mati, user lain aman, audit tepat satu, password berganti), `TestChangePasswordRejectsWrongCurrentPassword`, `TestChangePasswordEnforcesNewPasswordRules`, `TestChangePasswordEndToEnd` (HTTP, menunggu 1,1 detik supaya pencabutannya yang diuji, bukan kebetulan detik), `TestChangePasswordErrorMapping` (5 subtest); konstanta `ActionPasswordChanged` |
| `docs/design/42-API.md` §2, `70-TESTING.md` §3.12/§3.12c, `40-TSD.md` §2.4/§6 | Diselaraskan (P-034): kontrak `change-password` ditulis penuh (response memuat token pengganti, `keepJTI` yang usang dihapus karena penanda per user tidak dapat mengecualikan satu `jti`), bagian refresh dipisah tegas menjadi "sudah diputuskan" vs "belum diputuskan", ringkasan bukti §3.12c, sketsa route + interface `Refresh` ditandai belum ada |
| `backend/internal/pkg/jwt/jwt.go`, `internal/pkg/jwt/jwt_test.go`, `internal/service/auth_service.go`, `internal/dto/auth_dto.go`, `internal/handler/auth_handler.go`, `internal/handler/router.go`, `internal/service/auth_service_test.go`, `internal/handler/auth_handler_test.go`, `internal/handler/main_test.go` | Changed (P-034, `T-045`): klaim `typ` wajib + `GenerateRefresh`/`ValidateRefresh` + `RefreshExpiry` 7 hari; `AuthService.Refresh` (tipe → pencabutan → akun aktif → pasangan baru); `AuthService.Login` kini juga menerbitkan refresh token; `RefreshRequest`/`RefreshResponse` dan `refresh_token`/`refresh_expires_at` pada response login; handler + route `POST /api/v1/auth/refresh` **tanpa** `AuthMiddleware`; 14 test baru (6 jwt + 5 service + 3 handler) |
| `docs/adr/0023-bentuk-token-refresh.md`, `docs/adr/README.md` | Added (P-034, `T-045`): ADR-0023 `ACCEPTED` — bentuk token refresh (JWT bertanda `typ`, 7 hari, tanpa penyimpanan) beserta opsi yang ditolak dan batas yang diterima sadar; barisnya ditambahkan ke indeks ADR |
| `docs/design/42-API.md` §2, `44-SECURITY.md` §2.2, `40-TSD.md` §2.4/§6, `70-TESTING.md` §3.12d | Diselaraskan (P-034, `T-045`): kontrak `login` (menyerahkan refresh token) dan `refresh` (tanpa `Authorization`, aturan `typ`, empat kode error, batasan stateless terbuka), bentuk refresh token di dokumen keamanan, sketsa interface + route, dan ringkasan bukti §3.12d |
| `AGENTS.md` | Changed (P-034, `T-045`): aturan refresh yang mengikat (klaim `typ` wajib, 7 hari, tanpa tabel, tanpa `AuthMiddleware`, tanpa audit) + daftar modul selesai memuat comment |
| `docs/progress/{TASKS,OPEN-QUESTIONS,TRACEABILITY,STATE,CHANGELOG,SESSION-LOG}.md` | Ledger diselaraskan (P-034): `T-034` → **DONE** (sebagian, sisanya dipindahkan eksplisit), **`T-045`** baru di papan `BLOCKED`, **Q-020** baru, `FR-AUTH-09` → **DONE**, dan angka test per berkas dikoreksi ke hasil hitung ulang |
| `backend/internal/migration/010_session_revocation_login_attempts_document_archive.sql` | Baru (P-029, `T-039`): `documents.archived_at` + `CHECK` enam nilai, dua trigger `document_versions` (`StatementBegin`/`StatementEnd`), sekaligus skema ADR-0021/0022 (`users.tokens_invalid_before`, `users.locked_until`, `login_attempts`) — satu berkas sesuai `41-DATABASE.md` §4 |
| `backend/internal/model/document.go`, `internal/repository/document_repository.go`, `internal/service/document_service.go`, `document_service_upload.go`, `internal/dto/document_dto.go`, `internal/handler/document_handler.go`, `internal/handler/router.go` | Changed (P-029, `T-039`): status kanonik `archived` + `ArchivedAt`; `Archive` menggantikan `Delete` (penjaga `status <> 'archived'` di `WHERE`); daftar default tanpa arsip; unggahan versi ditolak pada dokumen terarsip; route `DELETE /documents/:id` **diganti** `POST /documents/:id/archive` (izin `document:update`); audit `DOCUMENT_ARCHIVED` |
| `backend/internal/service/document_service_test.go`, `internal/handler/document_handler_test.go`, `internal/migration/{migration_test.go,audit_append_only_test.go,main_test.go}` | Changed (P-029, `T-039`): lima test arsip di service, `TestArchiveDocumentEndToEnd` + izin arsip di handler, `TestDocumentStatusVocabularyIncludesArchived` + `TestLoginAttemptsSchemaExists` di migrasi, dan tujuh `TestDocumentVersions_*` append-only (termasuk kaskade `DELETE FROM documents` yang kini tertahan) |
| `docs/design/42-API.md` §4, `44-SECURITY.md` §3.1.3/§6, `40-TSD.md` §2.4, `50-FSD.md` §11.1, `70-TESTING.md` §3.12/§3.12a/§4.3 | Diselaraskan (P-029, `T-039`): tabel izin (arsip = `document:update`), enam nilai `status` + aturan daftar default, catatan "kontrak ini berjalan" menggantikan blok "Status implementasi", ringkasan bukti §3.12a, dan imutabilitas versi tiga lapis |
| `docs/progress/audits/AUDIT-001-...md` + `audits/README.md`, `TASKS.md`, `STATE.md`, `CONTINUE.md`, `AGENTS.md`, `CHANGELOG.md`, `SESSION-LOG.md`, `TRACEABILITY.md` | Ledger diselaraskan (**historis**, P-029): **C-004 `FIXED`** dan **C-051** baru (FIXED), hitungan **saat itu** 51 / 45 FIXED / 3 APPROVED / 3 OPEN, `T-039` pindah ke DONE, dan catatan bahwa skema `010` sudah terpasang sehingga `001`-`010` tidak boleh disunting |
| `backend/Makefile` | Changed (P-024): `test` memuat `.env`, **menurunkan `TEST_DATABASE_URL`** ke `bwdcs_test` (`TEST_DB_NAME`), `-count=1`, menolak DSN database dev, mencetak database yang dipakai dengan sandi disamarkan; target baru `test-dsn` |
| `backend/internal/bootstrap/bootstrap_test.go` | Changed (P-024): pesan kegagalan menyebut penyebabnya (`TEST_DATABASE_URL` harus menunjuk database test terpisah, `70-TESTING.md` §8.1) |
| `docs/design/70-TESTING.md` §8/§8.1 | Changed (P-024): database test terpisah sebagai praktik wajib + prosedur pembuatan `bwdcs_test`; peringatan bahwa `go test` tanpa `TEST_DATABASE_URL` **bukan bukti** (test integrasi `t.Skip`) |
| `docs/design/60-DEPLOYMENT.md` §3.1 | Changed (P-024): cuplikan Makefile yang menyimpang **dihapus**, diganti tabel target + penunjuk ke `backend/Makefile` (temuan C-043) |
| `docs/design/12-DEVELOPMENT-WORKFLOW.md` §8, `90-AGENT-GUIDE.md` §7 | Changed (P-024): perintah verifikasi memakai `make test`, bukan `go test` telanjang |
| `docs/progress/audits/AUDIT-001-...md` + `audits/README.md` | Changed (P-024, **historis**): C-038 FIXED, C-043 & C-044 baru (FIXED); hitungan **saat itu** 44 temuan / 35 FIXED / 9 OPEN (angka 51/45 yang sempat ditulis di sini sebagai "yang berlaku sekarang" **sudah tidak berlaku** — hitungan yang berlaku hanya satu, yaitu marker `audit-summary`, lihat §1 dan `scripts/check-ledger.sh`) |
| `docs/adr/0019-arsip-sebagai-default-penghapusan-dokumen.md`, `0020-retensi-audit-log-sebagai-operasi-pemeliharaan.md`, `0021-pencabutan-seluruh-sesi-tokens-invalid-before.md`, `0022-login-attempts-dan-auto-lock.md` + `docs/adr/README.md` | Added (P-026): empat keputusan `ACCEPTED` yang menjawab sembilan temuan tersisa (C-004, C-009, C-028, C-033, C-035 dan pendampingnya), masing-masing dengan konteks, alternatif yang ditolak, konsekuensi, dan dasar best practice |
| `docs/design/41-DATABASE.md` §2.1/§2.3/§3/§4 | Changed (P-026): `users.tokens_invalid_before` + `users.locked_until`, tabel `login_attempts` + indeks, `documents.archived_at` dan nilai kanonik `archived`, serta migrasi **`010`** beserta alasan mengapa perubahan ini tidak disunting ke berkas `001`-`009` yang sudah terpasang |
| `docs/design/42-API.md` §2/§4/§11/§12 | Changed (P-026): `logout_all` & pencabutan sesi (ADR-0021), lockout `423 LOCKED` + `POST /admin/users/:id/unlock` (ADR-0022), arsip dokumen menggantikan `DELETE` (ADR-0019), dan tabel `TOKEN_REVOKED` yang menyebut kedua sebab |
| `docs/design/44-SECURITY.md` §2.3/§3.1/§3.3/§6/§8 | Changed (P-026): auto-lock + `login_attempts` (C-009/C-035), dua hierarki role (C-007), izin arsip vs `document:delete` (C-004), §6.1 trigger `document_versions`, §6.2 retensi (C-028), dan checklist keamanan bertambah empat butir |
| `docs/design/50-FSD.md` §4.3/§10.5/§11.1, `20-SRS.md` (FR-AUTH-06/08/09, FR-DOC-03, FR-VER-03, FR-WF-03, FR-AUDIT-01), `10-BRD.md` §4/BR-07, `51-UX.md` §2.1, `43-WORKFLOW.md` §5, `40-TSD.md` §5.2.1/§5.2.2, `70-TESTING.md` §3.12, `60-DEPLOYMENT.md` §6.4 | Changed (P-026): kontrak & requirement diselaraskan dengan empat ADR baru; halaman Settings hanya memuat kunci yang benar-benar ada; §3.12 menulis **test yang wajib ada** sebelum implementasinya; §6.4 memuat prosedur retensi bernomor |
| `backend/internal/handler/project_handler.go` | Changed (P-026): `bindJSON` membedakan tiga sebab kegagalan decode dan menamai field; helper `undecodableField` + `messageForUndecodable` (temuan C-045) |
| `backend/internal/repository/task_repository.go`, `internal/service/task_service.go`, `internal/handler/task_handler.go` | Changed (P-026, **dikoreksi P-028**): penyaring `due_from`/`due_to` + `parseRFC3339Query` yang menerima `+07:00` maupun `%2B07:00` (temuan C-046). **Semantik batasnya dikoreksi P-028 menjadi kedua batas inklusif** (`T-044`) — lihat baris "Penyaring rentang tanggal" di atas |
| `backend/internal/handler/project_handler_test.go`, `task_handler_test.go`, `internal/service/task_service_test.go` | Changed (P-026): test C-045 (4 sebab decode, lintas modul) dan C-046 (6 kasus service + 7 kasus HTTP) |
| `docs/design/42-API.md` §6/§12, `50-FSD.md` §6.1, `70-TESTING.md` §3.11, `80-ROADMAP.md` §3 | Changed (P-026): kontrak penyaring rentang + tabel atribusi error; inventaris test; tabel status fase diperbaiki (temuan C-047) |
| Database `bwdcs_test` (bukan berkas) | Added (P-024): database test terpisah, owner role `bwdcs`, termigrasi otomatis saat suite berjalan |
| `AGENTS.md` | Ditambah protokol progress wajib & routing dokumen baru |
| `DESIGN.md` | Baru, placeholder jujur (belum diisi) |
| `docs/design/02-AGENT-PROGRESS-PROTOCOL.md` | Baru |
| `docs/design/12-DEVELOPMENT-WORKFLOW.md` | Baru |
| `docs/progress/**` | Baru (ledger progress, 9 file) |
| `docs/adr/**` | Baru (index + 7 ADR) |
| `scripts/check-doc-links.sh` | Baru; jalankan sebelum menutup sesi dokumentasi (BROKEN=0) |
| `scripts/check-readme-facts.sh` | Baru (P-039, `T-051`); pemeriksa kesegaran angka & versi `README.md` terhadap repo (jumlah route + rincian per modul, versi dependensi, rentang migrasi, klaim "belum ada") — jalankan sebelum menutup sesi; `readme-facts OK` = lulus |
| `scripts/check-ledger.sh` | Baru (P-036, `T-047`); pemeriksa konsistensi ledger (hitungan audit vs marker, papan `TASKS.md`, rujukan test mati, hitungan test §3) — jalankan sebelum menutup sesi, dan otomatis di CI (`.github/workflows/ci.yml`); **P-051:** aturan baru membandingkan ringkasan jumlah temuan **di dalam** laporan audit dengan markernya (**C-081**) |
| `scripts/check-navigation.sh` | Baru (P-051, `T-068`); pemeriksa batas sidebar terhadap model navigasi dan `51-UX.md` §2.1 (dua arah) — jalankan sebelum menutup sesi dan saat menyentuh navigasi; `navigation OK` = lulus |
| `scripts/check-api-contract.sh` | Baru (P-040, `T-052`); pemeriksa anotasi izin endpoint terhadap matriks RBAC dan router — jalankan sebelum menutup sesi; `api-contract OK` = lulus |
| `.github/workflows/ci.yml` | Baru (P-036); job `ledger` (**empat** skrip dokumen sejak P-040) + job `backend` (`go build`/`vet`/`gofmt` + `make test` di atas PostgreSQL 16 `bwdcs_test`) |
| `CONTINUE.md` | Baru (P-002); titik masuk resume untuk agen/model apa pun |
| `.env.example` | Baru (P-006); 17 variabel, cermin `60-DEPLOYMENT.md` §2.1 |
| `docker-compose.yml` | Baru (P-006); tervalidasi `docker compose config -q`, app 8081->8080, postgres 5433->5432 |
| `docs/design/60-DEPLOYMENT.md` | §2/§2.1 menjadi tabel kontrak (duplikasi YAML dihapus) |
| `docs/adr/0008-backend-library-lockin.md` | Baru (P-003); mengunci stack backend, menghapus "Chi/Gin" dan "SQLX/pgx" |
| `docs/adr/0009-logout-token-invalidation.md` | Baru (P-003); PROPOSED, menunggu Q-006 |
| `docs/adr/0011-audit-log-layer.md` | Baru (P-008); ACCEPTED — audit hanya di service, satu transaksi |
| `docs/adr/0012-status-kanonik-dan-overdue-turunan.md` | Baru (P-008); ACCEPTED — kanonik vs label, overdue turunan |
| `docs/adr/0013-struktur-paket-backend.md` | Baru (P-008); ACCEPTED — struktur backend flat, satu sumber |
| `docs/design/40-TSD.md` | §2.0 pohon struktur kanonik (baru); §2.4 aturan transaksi & audit; §2.6 handler tanpa audit (P-008) |
| `docs/design/50-FSD.md` | §11 tabel status kanonik vs label + tabel nilai turunan (baru) (P-008) |
| `docs/design/30-ARCHITECTURE.md`, `01-AGENT-WORKFRAME.md`, `90-AGENT-GUIDE.md` | Pohon struktur duplikat diganti tautan ke `40-TSD.md` §2.0; contoh audit di service (P-008) |
| `docs/design/51-UX.md`, `43-WORKFLOW.md`, `20-SRS.md`, `41-DATABASE.md`, `80-ROADMAP.md`, `70-TESTING.md`, `12-DEVELOPMENT-WORKFLOW.md`, `02-AGENT-PROGRESS-PROTOCOL.md`, `IDEA.md` | Diselaraskan dengan ADR-0011/0012/0013 (P-008) |
| `docs/adr/0014-matriks-permission-rbac.md` | Baru (P-009); ACCEPTED — matriks permission = sumber migrasi `008`, 104 baris |
| `docs/design/44-SECURITY.md` | §3.1 menjadi sumber tunggal matriks permission (17 resource × 15 action, scoping §3.1.3); §3.3 hierarki role dipisah (P-009) |
| `docs/design/40-TSD.md`, `41-DATABASE.md`, `70-TESTING.md`, `51-UX.md`, `50-FSD.md`, `20-SRS.md`, `12-DEVELOPMENT-WORKFLOW.md`, `AGENTS.md` | Diselaraskan dengan ADR-0014: `RequirePermission`, prosedur migrasi `008`, test matriks, rute routing (P-009) |
| `docs/design/90-AGENT-GUIDE.md` | §7 env: salinan usang dihapus, diganti penunjuk (P-010, C-014) |
| `docs/design/30-ARCHITECTURE.md` | §5.1: blok YAML compose kedua dihapus, diganti penunjuk (P-010) |
| `docs/design/70-TESTING.md` | §5.1: kredensial E2E dari env, bukan `admin123` (P-010) |
| `docs/design/42-API.md` | Header sumber tunggal endpoint; §5 +2 endpoint definisi workflow; §10 endpoint kembar dihapus; 9 fence markdown liar dibersihkan (P-011) |
| `docs/design/40-TSD.md` | §6 daftar route → contoh wiring + aturan pemetaan izin (P-011) |
| `docs/design/43-WORKFLOW.md`, `60-DEPLOYMENT.md`, `70-TESTING.md` | Satu fence markdown liar di akhir berkas dihapus (P-011) |
| `docs/design/42-API.md` | §2 `change-password`, §9 filter audit, **§10 Reports baru** (export), §11 `reset-password`/`roles`/`organizations`, `PATCH /admin/users/:id` dipersempit; bab Error→§12, Swagger→§13 (P-012) |
| `docs/design/50-FSD.md` | §2.2, §10.1, §10.3 diberi penunjuk endpoint (P-012) |
| `docs/adr/0015-optimistic-locking-workflow-instance.md` | Baru (P-013); ACCEPTED — guard `version` di database, tanpa retry, `409 WORKFLOW_CONFLICT` |
| `docs/design/41-DATABASE.md` | §2.4 `workflow_instances.version` + `current_step_deadline` + indeks parsial + blok aturan transisi; §4 isi migrasi `005` (P-013) |
| `docs/design/43-WORKFLOW.md` | §4.1/§4.2/§4.5/§6/§7: satu pola guard, deadline dari kolom (P-013) |
| `docs/design/42-API.md` | §5 `version`/`current_step_deadline`/`409 WORKFLOW_CONFLICT`; §12 beda `CONFLICT` vs `WORKFLOW_CONFLICT` (P-013) |
| `docs/design/40-TSD.md` | §2.3 model `Version`/`CurrentStepDeadline`; §2.4 kontrak `ExecuteAction`; §6 route aksi memakai izin baca (P-013) |
| `docs/adr/0016-request-revision-rollback-step-sebelumnya.md` | Baru (P-014); ACCEPTED — `request_revision` kembali ke step sebelumnya sesuai FR-WF-09; "reset ke step 1" ditolak |
| `docs/design/43-WORKFLOW.md` | §4.5 ditulis ulang (satu perilaku rollback + kasus tepi step 1); §7 catatan salah rujuk C-021 diperbaiki (P-014) |
| `docs/design/42-API.md` | §5: `/workflows/submit` hanya untuk draft, aksi `request_revision` rollback satu step, re-submit lanjut instance yang sama (P-014) |
| `docs/design/20-SRS.md`, `41-DATABASE.md`, `50-FSD.md`, `70-TESTING.md` | Diselaraskan dengan ADR-0016: FR-WF-09 + penunjuk ADR, §2.4, §8.1, test §3.4 (P-014) |
| `docs/design/70-TESTING.md` | §3.3 test konkurensi; §5.1 test E2E konflik; §8.1 env test (P-013) |
| `docs/design/50-FSD.md` | §5.4 Approvals, §10.6 Reports, §10.7 Workflow Definition Management (baru); §5.1 diberi penunjuk ke §10.7 (P-015, C-018) |
| `docs/design/42-API.md` | §5 endpoint daftar `GET /workflows/instances` (`?status=&scope=assigned_to_me`); rujukan T-027 → T-028; §11 catatan → §10.7 (P-015) |
| `docs/design/51-UX.md` | §2.1 catatan penunjuk ke tiga spec FSD baru (P-015) |
| `AGENTS.md` | Baris routing "Halaman Approvals"; status audit 15 FIXED / 8 OPEN (P-015) |
| `docs/adr/0017-format-nomor-dokumen.md` | Baru (P-016); ACCEPTED — `{PROJECT_CODE}-{NNN}` dibangkitkan server, atomik, immutable, tanpa penomoran manual |
| `docs/design/41-DATABASE.md` | §2.2 komentar `projects.code` (UPPERCASE, pola, permanen); §2.3 `document_sequences` + komentar nomor; §3 index; §4 isi migrasi `004` berikut kueri pembangkit (P-016) |
| `docs/design/42-API.md` | §3 `code` project (POST + PATCH menolak perubahan); §4 `POST /documents` tanpa `document_number` dari klien; §9 contoh; §12 `409` dirapikan (P-016) |
| `docs/design/50-FSD.md`, `20-SRS.md`, `51-UX.md`, `IDEA.md` | §3.2/§4.2 (nomor read-only), FR-DOC-02 menunjuk ADR-0017, contoh nomor seragam `WEB-001`/`API-001` (P-016) |
| `docs/design/40-TSD.md`, `44-SECURITY.md`, `90-AGENT-GUIDE.md` | `document_number` dihapus dari input DTO; tag `alphanum` yang salah diganti catatan regex; contoh service membangkitkan nomor di dalam transaksi (P-016) |
| `docs/design/70-TESTING.md` | §3.1 assert `TEST-001`; §3.5 test penomoran (berurutan, rollback, konkurensi, tolak nomor dari klien); §5.1 E2E tidak mengisi nomor (P-016) |
| `docs/design/12-DEVELOPMENT-WORKFLOW.md` | Contoh commit: ID hantu `FR-DOC-08` → `[FR-VER-01, FR-VER-04]` (C-024) (P-016) |
| `docs/design/42-API.md` | §5 endpoint baru `POST /workflows/instances/:id/resubmit` (prasyarat, dua guard, efek, izin `workflow_instance:submit`); jeda revisi pada `/actions`; `document_status` pada endpoint daftar (P-017) |
| `docs/design/43-WORKFLOW.md` | §4.6 baru (re-submit: instance sama, deadline dihitung ulang, tanpa baris `workflow_actions`, jeda, siklus); §4.2 langkah 4 jadi per siklus; §4.5 butir 3 diperbaiki (P-017) |
| `docs/design/40-TSD.md` | §2.4 `WorkflowService.Resubmit`; §6 route resubmit dengan `RequirePermission("workflow_instance", "submit")` (P-017) |
| `docs/design/50-FSD.md` | §4.3 aksi "Resubmit for Review"; §5.2 tombol berubah setelah revisi; §5.4 tab Pending menyaring jeda revisi (P-017) |
| `docs/design/70-TESTING.md` | §3.6 tujuh test re-submit (instance sama, deadline, wajib versi baru, status salah, stale version, jeda, siklus) (P-017) |
| `~/.zshrc` | Blok `BWDCS toolchain` (idempoten): `/usr/local/go/bin`, `$HOME/go/bin`, client psql 16 Postgres.app (T-011) |
| `.env` | Berkas lokal **tidak di-commit** (mode 600): kredensial DB `bwdcs` di `localhost:5432`, `JWT_SECRET` acak, `APP_PORT=8081`, nilai `ADMIN_*` untuk bootstrap; nilai berspasi dikutip agar aman di-source shell |
| `.gitignore`, `.editorconfig` | Baru (T-002/T-002a): `.env`/`storage/`/`bin/`/`node_modules/` terabaikan; tab untuk Go, 2 spasi untuk sisanya |
| `backend/go.mod`, `go.sum` | Module `bwdcs/backend`, `go 1.22.5`; gin v1.10.0, pgx v5.7.4, viper v1.19.0 (T-003) |
| `backend/internal/config/config.go` | Loader viper: `.env` opsional, environment menang, validasi wajib (DB_PASSWORD, JWT_SECRET ≥ 32) yang melaporkan semua masalah sekaligus (T-003) |
| `backend/internal/pkg/filestorage/` | `FileStorage` + `Prober` + `LocalStorage` (skema key ADR-0005, sanitasi nama, tolak path traversal, tolak penimpaan versi) + 9 test (T-003/T-031) |
| `backend/internal/handler/health_handler.go` | `GET /health`: 200 `healthy` / 503 `unhealthy` dengan nama pemeriksaan yang gagal (T-003/C-027) |
| `backend/cmd/server/main.go` | Urutan startup config → storage → pool pgx (lazy) → HTTP; `gin.SetMode(release)` di produksi; shutdown rapi; TODO `T-004`/`T-005` (T-003) |
| `backend/Makefile` | Target `60-DEPLOYMENT.md` §3.1 + `vet`/`fmt`/`migrate-status`; memuat `../.env` bila ada tanpa menimpa environment (T-003) |
| `frontend/README.md` | Placeholder blokir `DESIGN.md` (Phase 4), bukan skeleton aplikasi (T-002) |
| `docs/design/40-TSD.md` | §2.0 nama module + catatan toolchain (pgx v5.7.4); §2.1 field `Bootstrap` + perilaku loader; §2.4 `FileStorage.Save` dengan `originalName` (C-026) |
| `docs/design/60-DEPLOYMENT.md` | §5: cuplikan health check memakai `storage.Ping` dan dapat dikompilasi (C-027) |
| `docs/design/44-SECURITY.md` | §6 ditulis ulang: fungsi PL/pgSQL `prevent_audit_modification()` + `trg_audit_logs_append_only`/`trg_audit_logs_no_truncate` (tolak `23001`), jalur pemeliharaan `bwdcs.audit_maintenance`, down migration, dan alasan `REVOKE` tidak dipakai; §8 checklist ditambah test append-only (P-019, C-020) |
| `docs/design/41-DATABASE.md` | §2.5 penunjuk penegakan append-only ke `44-SECURITY.md` §6; §4 isi migrasi `007` kini memuat kedua trigger (sebelumnya tidak ada migrasi yang memasangnya) (P-019) |
| `docs/design/70-TESTING.md` | §4.3 baru: enam test append-only audit log (UPDATE/DELETE/TRUNCATE ditolak, INSERT lolos, dua trigger terpasang, GUC harus opt-in); §8 catatan `teardownTestDB` memakai GUC pemeliharaan (P-019) |
| `docs/adr/0018-migrasi-di-embed-dan-dijalankan-saat-startup.md` | Baru (P-020); ACCEPTED — migrasi di-embed & dijalankan saat startup, `internal/migration` menjadi package (memperbarui ADR-0013 butir 1), goose dipin v3.24.1 library+CLI |
| `backend/internal/migration/**` | Baru (T-004): 9 berkas migrasi `001`-`009` + `migration.go` (embed + goose) + `main_test.go`, `migration_test.go`, `audit_append_only_test.go` (13 test) |
| `backend/internal/bootstrap/**` | Baru (T-004): `bootstrap.go` (`EnsureAdminFirstRun`, `ValidatePassword`, `Querier`) + `bootstrap_test.go` (10 test) |
| `backend/cmd/server/main.go` | Urutan startup diisi: migrasi (embed) → bootstrap (ADR-0010) → HTTP; TODO `T-004` dihapus (P-020) |
| `backend/go.mod`, `backend/go.sum` | Tambah `pressly/goose/v3` v3.24.1 dan `golang.org/x/crypto` v0.31.0 sebagai dependensi langsung (bcrypt) (P-020) |
| `docs/design/41-DATABASE.md` | §2.3 komentar FK tertunda; §2.6 penunjuk rumah migrasi; §4 pemetaan isi sembilan berkas + catatan C-029/C-030/C-031 (P-020) |
| `docs/design/44-SECURITY.md` | §6 butir baru: badan fungsi wajib dibungkus `StatementBegin`/`StatementEnd` (C-031) (P-020) |
| `docs/design/40-TSD.md` | §2.0: `migration/` kini memuat package kecil (ADR-0018); catatan pin pgx **dan** goose v3.24.1 (P-020) |
| `docs/design/60-DEPLOYMENT.md` | §4.2: berkas migrasi tidak perlu ikut di-deploy karena di-embed (ADR-0018) (P-020) |
| `backend/internal/pkg/jwt/jwt.go` | Baru (T-005): penerbit/validator token HS256, `jti` wajib, `iss=bwdcs`, `exp` wajib, tolak alg none/issuer lain (ADR-0009) |
| `backend/internal/pkg/response/response.go` | Baru (T-005): amplop response + kode error kanonik `42-API.md` §12 (termasuk `TOKEN_REVOKED`, `INVALID_CREDENTIALS`, `ACCOUNT_INACTIVE`, `TOO_MANY_REQUESTS`). `NOT_IMPLEMENTED` **dihapus pada P-030** ketika `logout_all` benar-benar jalan |
| `backend/internal/repository/{user,token_revocation,setting}_repository.go` | Baru (T-005): akses `users`/`role_permissions` (termasuk `HasPermission`), `token_revocations` dengan cache TTL 30 detik + `CleanupExpired`, dan kebijakan login dari `system_settings` |
| `backend/internal/service/{auth_service,permission_checker,audit_service}.go`, `user_service.go` | Baru (T-005; `user_service.go` P-030): login/logout/profil, pemeriksa izin tanpa bypass Administrator (C-037), penulisan audit di transaksi pemanggil (ADR-0011), `Unlock` akun (`T-041`). `login_guard.go` (batas percobaan di memori) **dihapus pada P-030** — digantikan auto-lock ADR-0022 |
| `backend/internal/middleware/*` | Baru (T-005): `AuthMiddleware` (+ cek revokasi → `401 TOKEN_REVOKED`), `RequirePermission`, `RateLimitMiddleware`, `CorrelationID`, `Logger`, `CORS` (hanya origin loopback di dev) |
| `backend/internal/handler/{auth_handler,router}.go`, `internal/dto/auth_dto.go` | Baru (T-005): endpoint `42-API.md` §2 dan pemasangan route (`handler.Setup`) |
| `backend/cmd/server/main.go` | Changed (T-005): rakitan modul auth + pembersihan `token_revocations` saat startup dan setiap 1 jam |
| `backend/Makefile` | Changed (T-005): `test` memakai `-p 1` — paket test integrasi berbagi satu database (C-036) |
| `docs/design/42-API.md` | §2: anotasi `Izin:` kelima endpoint auth + catatan `logout_all` (501), `refresh`/`change-password` belum dijalankan; §12: kode `401`/`403`/`429`/`501` diperjelas (P-021). **P-030:** `501` dihapus, `423 LOCKED` ditambahkan, dan `logout_all` → `200`. **P-034:** §2 `change-password` menjadi kontrak yang berjalan (response memuat token pengganti; butir `keepJTI` yang usang dihapus), dan bagian `refresh` dipisah tegas antara "sudah diputuskan" (pemeriksaan pencabutan) dan "belum" (bentuk tokennya, Q-020) |
| `docs/design/40-TSD.md` | §2.2 interface middleware nyata (package `auth` hantu dihapus — C-034) + pembagian rate limit; §2.4 `AuditService.Log(ctx, tx, …)`; §5.2.2 batas percobaan login & batas auto-lock (C-009); §6 lokasi `Setup` (P-021) |
| `docs/design/44-SECURITY.md` | §2.3 batas auto-lock + audit login gagal (C-035); §3.2 tanpa bypass Administrator (C-037) (P-021) |
| `docs/design/70-TESTING.md` | §8: test integrasi berbagi satu database → jalankan serial (`-p 1`) + catatan pembersihan data yang sudah commit (C-036) (P-021) |
| `backend/internal/model/document.go` | Baru (T-037): status kanonik dokumen + `DocumentStatuses`/`IsDocumentStatus`; aturan versi `ParseVersion`/`FormatVersion`/`NextVersion` (major bila `revision_required`) |
| `backend/internal/repository/document_repository.go` | Baru (T-037): cakupan di `WHERE` (JOIN `projects p`, memakai `projectScopePredicate` yang sama), `NextNumber` atomik, `CategoryExists`, versi (`AddVersion`/`Versions`/`LatestVersion`/`FindVersion`), `VersionKeys`, `Delete` berkaskade, `HasRunningWorkflow` |
| `backend/internal/service/document_service.go`, `document_service_upload.go`, `scope.go` | Baru (T-037): `DocumentService` (create dengan nomor di dalam transaksi, list/get/delete, upload versi + checksum + `limitedReader`, versi, unduh ber-audit). `scope.go` mengekstrak `Actor` + `systemScope` supaya project dan document memakai satu aturan cakupan |
| `backend/internal/service/document_service_test.go`, `document_number_test.go`, `document_upload_limit_internal_test.go` | Baru (T-037): 9 test integrasi service, 5 test penomoran ADR-0017 (`70-TESTING.md` §3.5), dan unit `limitedReader` |
| `backend/internal/handler/document_handler.go`, `document_handler_test.go`, `internal/dto/document_dto.go` | Baru (T-037): tujuh handler (multipart unggah dengan magic bytes, streaming unduh ber-`Content-Disposition`, pemetaan error terpusat) + 11 test HTTP + bentuk request/response `42-API.md` §4 |
| `backend/internal/handler/router.go`, `main.go`, `internal/handler/main_test.go` | Changed (T-037): tujuh route `/documents` dengan `RequirePermission` matriks ADR-0014; wiring `DocumentService` + `FileStorage`; harness test dipecah (`newEngineParts`) supaya test dokumen memakai storage nyata |
| `backend/internal/service/project_service.go`, `internal/model/project.go` | Changed (T-037): normalisasi `projects.code` juga di service (C-041); komentar `IsArchived()` dikoreksi — aturan "project arsip tidak menerima dokumen baru" tidak punya sumber desain (C-042) |
| `backend/internal/pkg/jwt/jwt_test.go` | Changed (T-037): `tamperSignature` mengubah **byte** tanda tangan, bukan karakter terakhir base64url — test tidak lagi gagal acak (C-039) |
| `docs/design/42-API.md` §4 | Ditulis ulang (T-037): tabel izin 7 endpoint + aturan penomoran/versi/berkas/unduhan/hapus, contoh `current_version` dan `checksum` 64 heksadesimal (C-040) |
| `docs/design/40-TSD.md` §2.3-§2.6/§6 | Diselaraskan (T-037): model document/version nyata, `DocumentService`/`DocumentRepository` sesuai implementasi, pola handler multipart + streaming, tujuh route dokumen |
| `docs/design/44-SECURITY.md` §3.1.3/§4.2 | Diselaraskan (T-037): cakupan satu fungsi (`systemScope`) untuk project+document; daftar kebijakan unggahan menjadi bentuk yang benar-benar berjalan (termasuk `limitedReader` dan alasan `422` bukan `413`) |
| `docs/design/50-FSD.md` §4.2/§4.3, `41-DATABASE.md` §2.3, `70-TESTING.md` §3.5/§3.8/§3.9 | Diselaraskan (T-037): tabel versi minor/major + owner = pembuat + syarat hapus; komentar kolom `checksum`; status §3.5 dijalankan, daftar test modul dokumen §3.8, aturan test deterministik §3.9 (C-039) |
| `docs/progress/TRACEABILITY.md` | FR-DOC-01..07 → DONE (FR-DOC-07 PARTIAL); **enam baris FR-VER-01..06 ditambahkan**; FR-AUDIT-01 diperluas (T-037) |
| `docs/progress/OPEN-QUESTIONS.md` | **Q-016** baru: delapan kontrak modul document (T-037) |
| `docs/progress/audits/AUDIT-001-...md` + `audits/README.md` | C-039..C-042 ditambahkan (FIXED di sesi yang sama); hitungan waktu itu ditulis 42 temuan / 31 FIXED / 10 OPEN — angka FIXED itu **salah** dan dikoreksi di P-024 (C-044). **P-034:** C-057 ditambahkan dan langsung `FIXED` (angka test per berkas di §3 menyimpang dari repo, dan status `C-015` dicetak tebal supaya hitungan status bertemu); hitungan menjadi **57 / 55 FIXED / 0 APPROVED / 2 OPEN** |
| `docs/progress/TASKS.md`, `STATE.md`, `SESSION-LOG.md`, `CHANGELOG.md`, `CONTINUE.md`, `AGENTS.md` | Ledger diselaraskan dengan modul document (T-037) |
